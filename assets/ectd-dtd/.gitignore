# Vendored agency DTDs ARE committed (see README.md "Vendoring policy"):
# committed verbatim with their license headers, pinned by checksums.txt,
# upgraded only via reviewed PR. Nothing in this directory is ignored.
#
# Guard: never let scratch/derived files ride along with an acquisition drop.
*.tmp
*.bak
*.orig
