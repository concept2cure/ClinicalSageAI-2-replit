# === PDF Extraction Pipeline ===
# Multi-strategy PDF text extraction with OCR fallback
# System requirements: tesseract-ocr, mupdf (see replit.nix)

PyMuPDF>=1.27.0               # Strategy 1: best layout preservation for native PDFs
PyPDF2>=3.0.0                 # Strategy 2: native PDF fallback
pdfminer.six>=20260107        # Strategy 3: layout-aware extraction with LAParams
pytesseract>=0.3.13           # Strategy 4 & 5: OCR for scanned PDFs
opencv-python-headless>=4.13.0.92 # Strategy 5: image preprocessing for enhanced OCR
Pillow>=10.0.0                # Image handling for OCR
pdf2image>=1.17.0             # Strategy 6: pdf->image via poppler (OCR fallback)

# === Document Extraction - DOCX / XLSX / TXT ===
python-docx>=0.8.11           # Read/write DOCX (extraction + generation)
openpyxl>=3.1.0               # Read XLSX spreadsheets
mammoth>=1.12.0                # DOCX plain-text extraction
docx2txt>=0.8                 # DOCX fallback extractor
tiktoken>=0.5.0               # Token counting for context window trimming

# === Analytics Engine / API Bridge ===
fastapi>=0.110.0              # HTTP API bridge for analytics engine
uvicorn>=0.27.0               # ASGI server for FastAPI
httpx>=0.27.0                 # Async HTTP client
pydantic>=2.13.3               # Data validation
python-multipart>=0.0.27       # FastAPI multipart/form-data uploads

# === Data / AI ===
openai>=1.0.0                 # LLM API client (section generation)
numpy>=1.24.0                 # Numerical processing
pandas>=2.0.0                 # Data manipulation
