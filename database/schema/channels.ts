// Roadmap Step 1.5: Communication channels schema
// Re-export canonical definitions to match roadmap pathing without duplication.

export { communicationChannels, insertCommunicationChannelSchema } from '../../shared/schema';
export type { CommunicationChannel, InsertCommunicationChannel } from '../../shared/schema';
