// Roadmap Step 1.7: Document versions schema
// Re-export canonical definitions to match roadmap pathing without duplication.

export { documentVersions, insertDocumentVersionSchema } from '../../shared/schema';
export type { DocumentVersion, InsertDocumentVersion } from '../../shared/schema';
