// Roadmap Step 1.7: Documents schema
// Re-export canonical definitions to match roadmap pathing without duplication.

export { documents, insertDocumentSchema } from '../../shared/schema';
export type { Document, InsertDocument } from '../../shared/schema';
