// Roadmap Step 1.3: PM settings schema
// Re-export canonical definitions to match roadmap pathing without duplication.

export { pmSettings, insertPmSettingsSchema } from '../../shared/schema';
export type { PmSettings, InsertPmSettings } from '../../shared/schema';
