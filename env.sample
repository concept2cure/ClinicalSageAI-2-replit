BASE_URL=http://localhost:3000
# Neon connection string (include sslmode=require) - set once and reuse
NEON_CONNECTION_STRING=
# Set DATABASE_URL to reference NEON_CONNECTION_STRING so every service shares the same URL
DATABASE_URL=${NEON_CONNECTION_STRING}
REDIS_URL=redis://redis:6379
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
TEAMS_WEBHOOK_URL=
