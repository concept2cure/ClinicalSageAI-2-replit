# Risk assessment — launch catalog

**Document ID:** RA-LAUNCH-001
**Generated by:** `scripts/ops/generate-validation-package.mjs` from `docs/validation/launch/urs-registry.json`.
**Do not edit by hand — regenerate.** A hand edit is invisible to
`npm run ci:validation-traceability`, which is the only thing keeping the
citations below honest.

> **What this document is.** Qualification *evidence*, not a qualification. A
> qualification is a protocol executed and signed by a competent person; no
> script can be that. What this removes is the part a quality reviewer should
> never have to establish by hand — which requirement is implemented by which
> code, checked by which test, and whether that test actually ran and passed.
> Review starts from observed fact rather than from a claim.
>
> Every result below was observed at generation time. A verification that was
> not run reads NOT EXECUTED. A suite that could not start reads ERROR. Neither
> is ever rendered as a pass.

## Method

Risk is assessed per requirement, against its intended use, in the manner FDA's
Computer Software Assurance guidance describes: decide what the failure of this
specific function would do to a regulated record or to a decision made from one,
then spend the assurance effort that failure justifies and no more.

Two honesty notes a reviewer should have in front of them:

- That guidance is scoped to device **production and quality-system** software
  and does not modify 21 CFR Part 11. This package borrows its method; it does
  not claim the guidance governs this system. The binding requirements here are
  **21 CFR Part 11** and **EU GMP Annex 11**, with **GAMP 5 (2nd ed.)** as the
  framework.
- Risk here is the risk of the *software* failing. It is not a clinical or
  patient-safety risk assessment, and it does not replace one.

The assurance rule is mechanically enforced, not merely stated:
`scripts/ci/check-validation-traceability.mjs` refuses a registry in which a
high-assurance requirement is discharged by inspection or a unit test alone, or
in which a direct-impact requirement carries low assurance.

**Reading the impact column.** `direct` means the software itself affects the
integrity, authenticity or content of a regulated record, or decides who may act
on one. `indirect` means it supports that work without producing the record.
The distinction sets the assurance effort: a direct-impact requirement cannot be
discharged by inspection, and the gate refuses a registry that tries.

## Distribution

| Assurance level | Requirements |
|---|---|
| high | 16 |
| medium | 42 |
| low | 1 |

## Assessment

| ID | App | Failure mode | Impact | Achieved | Deserves | Verification | Result |
|---|---|---|---|---|---|---|---|
| URS-AUTHORING-01 | authoring | A client that asserts its own roles, email or tenant in HTTP headers edits, approves or exports another organization's governed document, and the audit trail names whoever the request claimed to be. A regulator would then believe a named individual authored or approved content they never touched. | direct | high | — | integration, unit | PASS |
| URS-AUTHORING-02 | authoring | A VIEWER or an unrelated organization member edits or approves a governed document because the decision was made on a global role rather than on the object's grants, or a permission change is lost silently so the authority history cannot be reconstructed. | direct | high | — | integration | PASS |
| URS-AUTHORING-03 | authoring | A revision is edited or deleted in place, or a new revision is written unchained, and the history still reads as complete. An inspector would believe the displayed history is the whole record of how the filed text came to say what it says. | direct | high | — | integration, unit | PASS |
| URS-AUTHORING-08 | authoring | Text generated by a model is persisted attributed to the human whose session saved it, or content commits while its provenance write fails, so a filing contains claims whose origin the organization cannot reconstruct or, worse, attributes to a person who never asserted them. | direct | high | — | integration, unit | PASS |
| URS-AUTHORING-90 | authoring | The surface renders for a visitor with no session, exposing a regulated record to an unidentified person and breaking the attribution every audit entry depends on. | direct | high | — | e2e | PASS |
| URS-PROJECTS-90 | projects | The surface renders for a visitor with no session, exposing a regulated record to an unidentified person and breaking the attribution every audit entry depends on. | direct | high | — | e2e | PASS |
| URS-QMS-04 | qms | The audit write fails (auditService.logAction resolves rather than rejecting on persistence failure) and the outcome is discarded. The document is approved, the change is transitioned, and the response is byte-identical to the audited case — so the only trace of the missing §11.10(e) record is a server log line, and the Part 11 export is silently incomplete. | direct | high | — | integration, unit | PASS |
| URS-QMS-05 | qms | A change is moved from 'closed' back to 'approved', or from 'proposed' straight to 'in_implementation'. The change-control log then shows an implemented change that was never impact-assessed or independently approved, and the stamps (approved_by, implemented_at, verified_at) no longer describe what happened. | direct | high | — | integration, unit | PASS |
| URS-QMS-06 | qms | A proposer approves their own change. The change-control record then carries approved_by equal to proposed_by, and an inspection finds a governed change that passed no independent review while the record asserts that it did. | direct | high | — | integration, unit | PASS |
| URS-QMS-90 | qms | The surface renders for a visitor with no session, exposing a regulated record to an unidentified person and breaking the attribution every audit entry depends on. | direct | high | — | e2e | PASS |
| URS-SUBMISSION-CENTER-03 | submission-center | A caller (or the UI) posts status='frozen' to the generic transition endpoint and the sequence becomes frozen or dispatched with no electronic signature; the submission record then carries an irreversible state change with no attributable human authorization, which a Part 11 inspection would read as an unsigned approval of a regulatory submission. | direct | high | — | integration, unit | PASS |
| URS-SUBMISSION-CENTER-04 | submission-center | One signature is replayed to freeze, dispatch and transmit, or a leaf is swapped after signing and the altered dossier is frozen under a signature applied to different bytes; the platform would then attest that a named human approved content they never saw, and the signature manifest in the record would be false. | direct | high | — | integration | PASS |
| URS-SUBMISSIONCENTER-90 | submission-center | The surface renders for a visitor with no session, exposing a regulated record to an unidentified person and breaking the attribution every audit entry depends on. | direct | high | — | e2e | PASS |
| URS-SUBMISSIONREADINESS-90 | submission-readiness | The surface renders for a visitor with no session, exposing a regulated record to an unidentified person and breaking the attribution every audit entry depends on. | direct | high | — | e2e | PASS |
| URS-VAULT-04 | vault | Uploading a revised file under the same free-text version (client default '1.0', document_code defaulting to the raw filename) replaces s3_key, content_hash, file_size and extracted_text on the existing row, destroying the record of the previously admitted document while the audit trail still cites its hash. | direct | high | — | integration | PASS |
| URS-VAULT-90 | vault | The surface renders for a visitor with no session, exposing a regulated record to an unidentified person and breaking the attribution every audit entry depends on. | direct | high | — | e2e | PASS |
| URS-AUTHORING-04 | authoring | Text is typed into and saved onto a document the toolbar shows as Frozen, so the freeze snapshot's content hash and any signature applied over it certify content that has since changed — the seal becomes a claim the system cannot support. | direct | medium | high | unit | PASS |
| URS-AUTHORING-05 | authoring | An authenticated member without signing authority applies an APPROVER signature, or a signature is stored while the workflow step or the audit row it depends on was never written, leaving a signed record with no traceable approval act or a meaning outside the governed vocabulary. | direct | medium | high | unit | PASS |
| URS-AUTHORING-06 | authoring | A DRAFT or IN_REVIEW document exports byte-for-byte like an approved one with reviewer signatures appended, so unapproved, still-editable content circulates as a certified record, attributed to whoever the request body named. | direct | medium | high | unit | PASS |
| URS-AUTHORING-09 | authoring | The later save overwrites the whole section, the overwrite enters the hash-chained revision ledger as an ordinary authored revision, and nothing downstream shows that a collision occurred — the chain verifies and the content is wrong. | direct | medium | high | unit | PASS |
| URS-AUTHORING-10 | authoring | The audit rail shows 'No audit events yet' and the history rail shows 'No prior revisions' because the GET returned 500, and a reviewer or inspector concludes that no governed acts or revisions exist for a record whose Part 11 trail was in fact written correctly and merely unreadable. | indirect | medium | — | unit | PASS |
| URS-AUTHORING-12 | authoring | Saves reach the governed store with no reason, so the audit trail records only that a change happened and not why, or the client invents a reason and the record attributes a justification to an author who never gave one. | direct | medium | — | unit | PASS |
| URS-PROJECTS-01 | projects | If the organisation id were taken from a client-supplied value or defaulted when absent, another sponsor's programme names, codes, filing types and target submission dates would render inside this tenant's workspace, and the reader would believe those programmes are theirs. An unbounded ?limit would additionally let one request drain the whole table. | indirect | medium | — | unit | PASS |
| URS-PROJECTS-04 | projects | Without the shared transaction a failure between the insert and the audit write leaves a regulated programme with no creation entry in the governed ledger — or, in the other direction, an audit entry asserting a programme that does not exist. Either way the activity feed and an inspector's reconstruction of the record disagree with the database. | direct | medium | high | unit | PASS |
| URS-PROJECTS-05 | projects | Guessing a pathway would file a Canadian clinical trial application or an EU MDR Class IIb technical file against the 71-section US NDA outline. Staying silent about the skip instead drops the customer into a permanently empty Vault with no indication anything was declined — they conclude the product is broken, or worse, that the dossier is simply not started yet. | direct | medium | — | unit | PASS |
| URS-PROJECTS-06 | projects | regulatory_programs.progress_percent is written exactly once, as the literal 0 in the creation INSERT, and no UPDATE of it exists in the server. Reporting it would show 0% for a fully drafted and approved IND forever — a figure that looks measured and can never move, read by its audience as 'no progress' rather than 'not computed'. | indirect | medium | — | unit | PASS |
| URS-PROJECTS-07 | projects | The row array is empty both when the organisation genuinely has no programmes and when the query threw. Computing the header from it yields a settled '0 active programmes, 0 blocked' and, with the \|\| 1 divisor, a clean '0% mean readiness' over an outage. The same substitution on Project Home's data room would present a failed source read as a project with no evidence, and on the task board as a team with no work. | indirect | medium | — | unit | PASS |
| URS-PROJECTS-08 | projects | Any member of the organisation silently repins another team's evidence; the next generated section is grounded in a document the programme's owners never approved, and the drafted regulated text carries provenance nobody authorised. A 403 for an out-of-org programme would additionally confirm that programme's existence to a stranger. | direct | medium | high | unit | PASS |
| URS-PROJECTS-10 | projects | Without the gate any caller PATCHes an approval-gated task to completed with no identity check and no record, and the 'approval checkpoint' chip on the board has nothing behind it. If the PIN were echoed into the manifestation or the audit payload, the sole signing component would be readable by anyone with ledger access, invalidating every signature in the tenant. | direct | medium | high | unit | PASS |
| URS-PROJECTS-12 | projects | An unvalidated stage id or status would persist a lifecycle state no reader can interpret and no overlay can render, while an unaudited advance would move a programme's recorded stage with no trace of who moved it. An unprovisioned store returning 500 would take the surface down rather than showing an honest empty spine. | indirect | medium | — | integration, unit | PASS |
| URS-QMS-01 | qms | An unscoped or mis-scoped request returns another organization's qms_documents / qms_change_controls rows. The register then shows effective SOPs, approvers and training counts that belong to a different company, and an inspector or a QA decision treats them as this tenant's quality system. | direct | medium | high | unit | PASS |
| URS-QMS-07 | qms | A link is dropped or an expanded change renders no cross-references although rows exist. A reviewer concludes the change touches no validated process, and an implemented change ships with its revalidation obligation invisible. | direct | medium | — | integration, unit | PASS |
| URS-QMS-09 | qms | A 500, an expired token or a body the client cannot adapt renders as a row of zeroes and an empty register. The lead reports "nothing overdue, nothing awaiting approval" to management, and AnA repeats it, while the real register is unread. | indirect | medium | — | unit | PASS |
| URS-QMS-10 | qms | An empty tenant, an expired token or a failed fetch causes fixture rows asserting status 'effective' with effective dates, and training counts such as "47 of 47", to render unmarked. A filing or inspection-readiness decision is taken against example data presented as the company's own. | indirect | medium | — | unit | PASS |
| URS-QMS-12 | qms | A partial dashboard body renders as a populated panel, so a freshly created plan with no sections shows "0 hard gates" as though it enforced nothing, and a lead activates it believing the gates are configured. | direct | medium | — | unit | PASS |
| URS-SUBMISSION-CENTER-02 | submission-center | A 401/500/malformed response collapses to an empty array and the surface renders "No submissions yet", "No compilations yet" or a computed "0% eCTD readiness"; the user concludes that a filing, a compilation history or an assembled dossier does not exist when the system simply could not read it, and proceeds (or reports to an agency) on that basis. | indirect | medium | — | unit | PASS |
| URS-SUBMISSION-CENTER-05 | submission-center | A client passes validationErrors: 0, or the surface offers the freeze button while the server gate blocks, and a sequence with unresolved structural errors or open critical findings is frozen and dispatched — a defective dossier reaches the agency and the platform's own record shows it as cleared. | direct | medium | high | unit | PASS |
| URS-SUBMISSION-CENTER-06 | submission-center | A mistyped section code becomes a literal folder name in the package, or a misspelled document table is stored, audited as a placement and reported dispatch-clear, and the leaf only fails to resolve at assembly — at the end of a filing window; or a leaf is edited after freeze, so the frozen dossier is not the one that was signed. | direct | medium | high | unit | PASS |
| URS-SUBMISSION-CENTER-08 | submission-center | A transmit runs without verified re-authentication or without a recorded reason, or a refusal is rendered as a generic failure; the platform then holds no attributable authorization for a filing that reached an agency, or an operator re-sends a package that is already in flight because the 409 lock was reported as an ordinary error. | direct | medium | high | unit | PASS |
| URS-SUBMISSION-CENTER-09 | submission-center | A text file composed by the platform from its own transmittal row is downloaded as "ack-<id>.txt" and archived as agency correspondence; in a filing-date dispute or an inspection the sponsor is shown to have represented a self-authored document as an agency receipt, and holds no agency receipt at all. | direct | medium | high | unit | PASS |
| URS-SUBMISSION-CENTER-10 | submission-center | An omitted environment defaults to production and a test package is filed to the live agency endpoint; or a sequence transmits with an "UNASSIGNED-SEQ-<id>" application number in the regional backbone; or a leaf whose bytes live in external storage is silently dropped from the ZIP and the agency receives a dossier missing a document its own index.xml lists. | direct | medium | high | unit | PASS |
| URS-SUBMISSION-CENTER-11 | submission-center | The draft backbone declares <ectd:file-path> entries for files that were never written and checksums that were never computed, and the response says "Submission-ready" beside a download button; a publisher downloads and forwards a backbone that references non-existent leaf files as if it were an assembled package. | direct | medium | high | unit | PASS |
| URS-SUBRDY-01 | submission-readiness | The surface recomputes the verdict from raw inputs using fewer gates than the server composes (it previously merged only two of four), so a sequence the server blocks renders as 'Cleared to dispatch' and publishes facts.cleared=true to the assistant. A regulator or sponsor would then believe a dossier had passed every pre-transmit control when the authoritative gate refused it. | indirect | medium | — | unit | PASS |
| URS-SUBRDY-02 | submission-readiness | Zero open critical findings is read as 'clean' when it actually means 'nothing ever looked'. A sequence with no completed Shadow Review run clears the gate, and a dossier that was never adversarially reviewed is transmitted to the agency carrying a cleared verdict. | direct | medium | high | unit | PASS |
| URS-SUBRDY-05 | submission-readiness | A misconfigured or unset NODE_ENV is treated as non-production, so the fail-closed rule silently switches off and a package the agency validator would have rejected is cleared and dispatched. | direct | medium | high | unit | PASS |
| URS-SUBRDY-07 | submission-readiness | The checkpoints read fails, the mapped value stays an empty array, and the Awaiting-approval tile settles on 0. A reviewer who trusts it walks away from approval gates that are actually pending, and the surface's error copy tells them to retry with nothing on screen to retry with. | indirect | medium | — | unit | PASS |
| URS-SUBRDY-09 | submission-readiness | A program the engine never scanned renders 'Submission gate — CLEAR' and 'no contradictions', because the empty-list branch and the nothing-found branch are the same expression. That is the most expensive sentence the surface can say to a regulatory director. | indirect | medium | — | unit | PASS |
| URS-SUBRDY-10 | submission-readiness | The two writes are issued separately and the second fails, leaving the old and new assumptions both live; or the client reports a change that was refused, so the user believes the dossier moved while the governed record still holds the previous value and nothing downstream was invalidated. | direct | medium | high | unit | PASS |
| URS-SUBRDY-11 | submission-readiness | A detector catches its own read error, logs a warning and resolves with a partial array, so the response lists every detector as having run and returns a short total. A preflight verdict then reads 'zero blocking contradictions' for a dossier four detectors never examined. | direct | medium | — | unit | PASS |
| URS-SUBRDY-12 | submission-readiness | Any authenticated user who knows an execution id cancels another sponsor's in-flight governed workflow, or learns from a 403 that the run exists. The other tenant's orchestration run is terminated with no trace of who did it in their organization's records. | direct | medium | high | unit | PASS |
| URS-VAULT-02 | vault | The vault read (or the uploads / Module 3 / data-room branch) errors and the surface shows 'No documents in this project's vault yet' or an empty folder, so a reviewer concludes a required document was never filed when it exists but could not be read. | indirect | medium | — | unit | PASS |
| URS-VAULT-05 | vault | A low-confidence guess is stored as a settled filing, or a re-upload silently moves a document a reviewer had already confirmed into the classifier's folder, so the dossier shows a placement nobody decided. | direct | medium | — | unit | PASS |
| URS-VAULT-08 | vault | Bytes that were altered in place (or the wrong object returned by object storage) are served as the recorded document — a same-length tamper is invisible to every check except the hash — and the recipient treats a corrupted file as the governed record. | direct | medium | high | unit | PASS |
| URS-VAULT-09 | vault | The audit write fails under load and the file is served anyway, so a controlled document leaves the platform with no record of who took it or which edition they took. | direct | medium | high | unit | PASS |
| URS-VAULT-10 | vault | The search query errors (or a stray character makes tsquery raise) and the screen shows zero results, so the reviewer records that a required document is not in the dossier when it is. | indirect | medium | — | unit | PASS |
| URS-VAULT-11 | vault | Past the page cap the unfiled review queue shrinks as the backlog grows and filed data-room sources report as unfiled, so a user believes the filing backlog is cleared and that captured evidence never reached the vault. | indirect | medium | — | unit | PASS |
| URS-VAULT-12 | vault | An artifact signed at v3 and edited to v7 shows an unqualified 'signed' badge, so a reviewer treats unapproved content as approved and carries it into a submission. | indirect | medium | — | unit | PASS |
| URS-VAULT-13 | vault | An unreviewed AI-drafted artifact is downloaded and circulated without the draft notice, or a caller-asserted approval flag substitutes for a persisted decision, so a document nobody approved is read as an approved regulatory record. | direct | medium | high | unit | PASS |
| URS-SUBMISSION-CENTER-12 | submission-center | A proxy or version skew returns a plausible but wrong body and the panel renders "No spec versions" or "No codes"; the publisher concludes the package is qualified against nothing, or that a controlled vocabulary has no codes, and selects an attribute value the agency's validator will reject. | indirect | low | — | unit | PASS |
