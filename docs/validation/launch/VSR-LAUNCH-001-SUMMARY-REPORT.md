# Validation summary report — launch catalog

**Document ID:** VSR-LAUNCH-001
**Generated by:** `scripts/ops/generate-validation-package.mjs` from `docs/validation/launch/urs-registry.json`.
**Do not edit by hand — regenerate.** A hand edit is invisible to
`npm run ci:validation-traceability`, which is the only thing keeping the
citations below honest.

> **What this document is.** Qualification *evidence*, not a qualification. A
> qualification is a protocol executed and signed by a competent person; no
> script can be that. What this removes is the part a quality reviewer should
> never have to establish by hand — which requirement is implemented by which
> code, checked by which test, and whether that test actually ran and passed.
> Review starts from observed fact rather than from a claim.
>
> Every result below was observed at generation time. A verification that was
> not run reads NOT EXECUTED. A suite that could not start reads ERROR. Neither
> is ever rendered as a pass.

## Verdict

**INCOMPLETE**

The package is not complete. Either a requirement has no executed, passing verification, or a requirement is not yet qualified to the assurance level its risk deserves. Both lists are below. This verdict is the point of the document: a summary report that could only ever say COMPLETE would evidence nothing.

| | Count |
|---|---|
| Launch apps | 6 |
| Requirements | 59 |
| Executed and passed | 59 |
| Executed and failed | 0 |
| Could not be executed (ERROR) | 0 |
| Not executed in this run | 0 |
| **Not yet qualified to the level the risk deserves** | 21 |
| **Declared with no verification at all** | 20 |

Verification was executed in this run.

## Requirements whose verification did not pass

This section answers one question only — did the cited verification execute and
pass. It is **not** the list of what is outstanding for the package; the two
sections below it are, and they can be long while this one is empty.

None. Every requirement in the registry has an executed, passing verification.

## Not yet qualified to the level the risk deserves

These requirements are implemented and partially evidenced. The evidence that
exists supports a lower assurance level than the requirement's risk justifies,
so they are **declared, not qualified**, and the package cannot read COMPLETE
while any of them stands. Each names the work that would close it.

| ID | App | Achieved | Deserves | Evidence owed |
|---|---|---|---|---|
| URS-AUTHORING-04 | authoring | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. The reviewer named these as likely candidates: server/routes/__tests__/authoringSignFreezeAndExportGate.test.ts. |
| URS-AUTHORING-05 | authoring | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-AUTHORING-06 | authoring | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. The reviewer named these as likely candidates: server/routes/__tests__/authoringExportDocx.test.ts, server/routes/__tests__/authoringExportHistory.test.ts. |
| URS-AUTHORING-09 | authoring | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-PROJECTS-04 | projects | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-PROJECTS-08 | projects | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-PROJECTS-10 | projects | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-QMS-01 | qms | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-SUBMISSION-CENTER-05 | submission-center | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-SUBMISSION-CENTER-06 | submission-center | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. The reviewer named these as likely candidates: tests/golden-journeys/drug-nda-ectd.journey.test.ts. |
| URS-SUBMISSION-CENTER-08 | submission-center | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-SUBMISSION-CENTER-09 | submission-center | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. The reviewer named these as likely candidates: tests/mdx-submission-gateway-routes.test.ts. |
| URS-SUBMISSION-CENTER-10 | submission-center | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-SUBMISSION-CENTER-11 | submission-center | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-SUBRDY-02 | submission-readiness | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. The reviewer named these as likely candidates: server/services/ectd/__tests__/release-signature-gate.test.ts. |
| URS-SUBRDY-05 | submission-readiness | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-SUBRDY-10 | submission-readiness | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-SUBRDY-12 | submission-readiness | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-VAULT-08 | vault | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-VAULT-09 | vault | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |
| URS-VAULT-13 | vault | medium | high | an integration or end-to-end test that exercises this behaviour against the real route; the evidence cited today supports only medium assurance. |

## Declared, with no verification at all

These requirements describe behaviour that is in the product — the implementing
code is cited and resolves — but no test in the repository was found to exercise
them. They are listed rather than omitted: the behaviour exists, a reviewer will
ask about it, and a package that quietly left it out would read as though the
surface had nothing further to validate. A declared gap is evidence; a silent one
is a misrepresentation.

They are not counted as requirements above, and the package cannot read COMPLETE
while any of them stands.

| ID | App | Requirement | Why nothing counts yet | Candidate tests to assess |
|---|---|---|---|---|
| URS-AUTHORING-07 | authoring | The system shall refuse to freeze a document that still carries open reviewer comments or pending tracked-change insertions/deletions unless the caller explicitly acknowledges the outstanding work, shall count those using the same parser the export uses, shall record the acknowledgement in the freeze reason and the audit trail, and shall reject a second freeze of an already sealed document regardless of status casing. | no cited test exercises this behaviour | `tests/schema-contract/authoring-section-commits-to-filing.contract.test.ts`, `client/src/concept2cure/v2/__tests__/authoringFilingBar.test.tsx` |
| URS-AUTHORING-11 | authoring | The system shall permit an approve, reject, delegate or request-changes act on a review workflow only by a caller the workflow currently has a pending step assigned to within their organization, shall require a substantive reason for a rejection, a delegation and a change request, shall refuse self-delegation, and shall persist each act to the workflow history, to the caller's pending approval step for approve, reject and delegate (a change request leaves the approval row untouched), and to the document comment thread whenever a reason was stated. | no cited test exercises this behaviour | — none identified |
| URS-PROJECTS-02 | projects | The system shall refuse programme creation, before any row is written, for a caller with no resolved user identity and for a caller whose organisation role is explicitly read-only (viewer, readonly, read_only, guest). | no cited test exercises this behaviour | — none identified |
| URS-PROJECTS-03 | projects | The system shall derive the persisted product class from the chosen filing type when the client omits it, and shall reject with 400 any creation request that would record a device-family filing (510(k), De Novo, PMA, HDE, IDE, MDR, CER, IVDR) as a non-device product class, whatever product class the client submitted. | no cited test exercises this behaviour | — none identified |
| URS-PROJECTS-09 | projects | The system shall build the programme activity feed from the tenant-scoped audit_logs table, matching entries by record_id or by new_values->>'project_id', verify programme membership of the caller's organisation first, and answer with an error status when the read fails rather than an empty activity array. | no cited test exercises this behaviour | — none identified |
| URS-PROJECTS-11 | projects | The system shall provide archiving as the only removal verb for a task, stamping deleted_at (and deleted_by when the actor's user id is resolvable) on the row within the caller's organisation, retaining the row and writing an audited task.delete entry with the supplied reason, and shall exclude archived rows from every task read model. | no cited test exercises this behaviour | — none identified |
| URS-QMS-02 | qms | The system shall make a controlled document effective only from draft or under-review state, recording the approver identity and approval timestamp on the document row, and shall refuse approval from any other state with HTTP 409. | no cited test exercises this behaviour | — none identified |
| URS-QMS-03 | qms | The system shall refuse to open a controlled revision of a document without a reason for change, and on acceptance shall bump the version, return the document to draft and clear the prior approval so it must be re-reviewed and re-approved. | no cited test exercises this behaviour | — none identified |
| URS-QMS-08 | qms | The system shall perform no governed QMS mutation from the Quality and Assurance screens; approving, revising, retiring, recording training and raising or advancing a change shall be handed as a grounded prompt to the AnA conversation, whose tools run the transition and commit the Part 11 audit row in the same transaction, requiring a caller-supplied reason for change on a document revision and a change-control transition only, substituting a generated reason on create, approve and retire, and capturing no electronic signature. | no cited test exercises this behaviour | — none identified |
| URS-QMS-11 | qms | The system shall present the controlled-document register with each document's number, title, type, version, lifecycle status, effective date and next review date, shall filter it by status from both the register's own chips and an AnA directive driving the same state, and shall flag documents past their next review date. | no cited test exercises this behaviour | — none identified |
| URS-SUBMISSION-CENTER-01 | submission-center | The system shall resolve the organization and user for every Submission Center request from the authenticated session only, never from the request body or parameters, reject with 401 any /api/submissions request whose session carries no valid positive-integer organization, restrict submission and sequence reads and writes to holders of the AUTHOR role, and require an explicit projectId for the Dossier Map (refusing PROJECT_REQUIRED without one) so a readiness view is never an org-wide roll-up; the Dossier Map read scopes to whatever organization claim it can parse as a finite number, a weaker check than the submissions router's. | no cited test exercises this behaviour | — none identified |
| URS-SUBMISSION-CENTER-07 | submission-center | The system shall apply every sequence status change — governed and non-governed alike — only while the row still holds the status the transition was evaluated against, and report a lost race as INVALID_STATE naming the state it expected; for the governed transitions (freeze, dispatch and the transmit resolution) it shall commit the state change and its hash-chained audit row in one transaction or neither, while the reversible non-governed transition commits its state change first and reports the outcome of its audit write to the caller. | no cited test exercises this behaviour | — none identified |
| URS-SUBRDY-03 | submission-readiness | The system shall distinguish, on the dispatch readiness surface, a failed readiness read from an organization that has no sequence to gate, and shall render neither as a verdict. | no cited test exercises this behaviour | — none identified |
| URS-SUBRDY-04 | submission-readiness | The system shall permit a governed freeze or dispatch of an eCTD sequence only when a valid, unspent Part 11 electronic signature bound to that sequence's current leaf manifest is presented together with a clear step-specific gate, and shall apply the state change and its hash-chained audit entry in a single transaction predicated on the status that was authorised. | no cited test exercises this behaviour | — none identified |
| URS-SUBRDY-06 | submission-readiness | The system shall record a human-in-the-loop approval decision only from an approver holding one of the gate's required roles, shall refuse a second decision from the same approver, shall close the gate only when the distinct approving count reaches the required quorum, shall stamp the decision time from the database clock, shall write the decision and its audit entry in one transaction, and shall state in both that the decision is not a 21 CFR 11.50 electronic signature. | no cited test exercises this behaviour | — none identified |
| URS-SUBRDY-08 | submission-readiness | The system shall refuse to serve the cross-document inconsistency board with an empty findings set when the contradiction findings store could not be read, answering with an explicit service-unavailable error, and the surface shall render that failure as an error state rather than a clean board. | no cited test exercises this behaviour | — none identified |
| URS-VAULT-01 | vault | The system shall serve a project's vault tree, search results and document downloads only after re-proving that the requested regulatory program belongs to the acting user's organization, reporting a program owned by another organization as absent rather than as forbidden. | no cited test exercises this behaviour | `server/routes/__tests__/vault-search.test.ts`, `server/routes/__tests__/vault-download.test.ts` |
| URS-VAULT-03 | vault | The system shall admit an uploaded document into the vault only after verifying its magic bytes against the declared MIME type and completing an antivirus scan that is fail-closed in production, computing the SHA-256 of the bytes, and successfully persisting those bytes through the canonical storage provider; if the byte write fails, no vault record is created. | no cited test exercises this behaviour | — none identified |
| URS-VAULT-06 | vault | The system shall permit a filing decision (POST /api/c2c/project-vault/:id/file) only to a caller holding a governed-write organization role, shall take the document row FOR UPDATE, and shall commit the placement change and its hash-chained 21 CFR Part 11 audit row — recording actor, from/to folder and status, and rationale — in a single transaction, while leaving vault reads open to viewers. | no cited test exercises this behaviour | — none identified |
| URS-VAULT-07 | vault | The system shall refuse a filing decision naming a folder that does not exist in the program's own vault taxonomy view (INVALID_FOLDER), shall accept an explicit unfile (folderId null) as a legitimate recorded decision, and shall refuse a request that names neither a target nor a confirmation (NO_TARGET). | no cited test exercises this behaviour | — none identified |

## What this report does not cover

- **The truth of a requirement.** That the cited code implements the cited
  statement is a reviewer's judgement. Nothing mechanical establishes it, and
  this document does not imply that anything did.
- **Installation.** `docs/validation/IQ_OQ_EVIDENCE_PACK.md` records what is
  installed, and `node scripts/ops/ga-readiness-report.mjs` records the
  licensed artefacts and credentials a deployment still needs.
- **Performance qualification.** Qualification under production load with real
  data is a separate exercise against a real deployment.
- **The signature.** Quality signs the qualification. This report is its input.

## Approval

| Role | Name | Signature | Date |
|---|---|---|---|
| System owner | | | |
| Qualified reviewer (independent) | | | |

A signature on this page attests to the review of the evidence above, not to
its generation.
