# Test Matrix

## Implemented automated coverage
- Retrieval chunk + scoped lookup (pass)
- Tool gate off-mode blocking (pass)
- Hard-task classifier + plan/execute + quality loop invocation (pass)

## Manual/phase-2 pending
- End-to-end UI artifact split rendering
- Tenant-aware policy inheritance
- Persisted DB-backed conversation state
- Cross-client workflow scenario packs
