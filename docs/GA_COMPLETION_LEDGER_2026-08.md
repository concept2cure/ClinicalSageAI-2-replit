# GA completion ledger — 2026-08

**One register for every open item across every space.** This file exists because work
has been dispatched, lost to a container restart, and silently forgotten more than once
in this effort. A ledger that lives in a session's memory is not a ledger.

## What this file is — and what it deliberately is not

The doctrine for this codebase is *there is never more than one of anything*. That
applies to tracking documents too, so this file **restates nothing**. It is an index:
one row per open item, each pointing at the document that actually owns the detail.

| Document | Owns |
|---|---|
| `docs/GA_OPS_PROCUREMENT_RUNBOOK_2026-08.md` | Blockers B1–B21 — the licensed assets, credentials and flags. The prose. |
| `scripts/ops/ga-readiness-report.mjs` | The *probe* for those blockers. Observation, not intention. |
| `docs/COMPETITIVE_LANDSCAPE_2026-08.md` | The 2026 market benchmark and the four-silo finding. |
| `docs/COMPETITIVE_POSITION_DELTA_2026-08.md` | Table stakes closed vs. open, per journey. |
| `docs/AUDIT_STORE_INVENTORY_2026-08.md` | All 81 audit-ish tables, per-table verdicts, the delete lists. |
| `docs/DOCUMENT_IDENTITY_CONTRACT_2026-08.md` | The approved identity contract (Option C), slices C1/C2. |
| **This file** | Which of all of the above is still open, who owns it, and how it proves itself done. |

Rows are checked mechanically by `node scripts/ops/ledger-check.mjs`, which fails when a
row cites a file that does not exist, claims `done` without evidence, or duplicates an ID.
A ledger that can rot silently is the problem it was written to solve.

## How to read a row

- **Owner** — `eng` work I do; `proc` purchase/credential, no code will conjure it;
  `qual` quality-system sign-off; `prod` a product/pricing decision that is not mine.
- **State** — `open`, `in-flight` (an agent or a person is on it *now*), `done`
  (evidence cited and passing), `blocked` (named on another row).
- **Evidence** — for `done`, the test or command that proves it. For everything else,
  the file where the work lands or the document that specifies it.

---

## 1. In flight

Work with a live owner right now. If a row here has no live agent and no recent commit,
it has been lost — re-dispatch it.

| ID | Item | Owner | State | Evidence |
|---|---|---|---|---|
| L1 | ESG transport gap (B16): re-point `mdx-command-handlers.ts` off the not-implemented `ESGSubmissionService.transmitToESG` onto the real AS2 in `server/services/submission-gateways/fda-esg.ts`, through the canonical governed transmit so preconditions travel with it | eng | in-flight | `server/services/submission-gateways/fda-esg.ts` |
| L2 | AnA performance assessment — latency path, context assembly, tool parallelism, model routing, pgvector index + pre-filter tenancy, pool sizing | eng | in-flight | `docs/ANA_PERFORMANCE_ASSESSMENT_2026-08.md` |
| L3 | E2E golden journeys for 510(k), CER, NDA — asserting honest refusals as first-class outcomes, not as failures | eng | in-flight | `tests/golden-journeys/` |

## 2. Engineering backlog — mine

Ranked. Every row here was at some point described as somebody else's problem; it is not.

| ID | Item | Owner | State | Evidence |
|---|---|---|---|---|
| L4 | Literature screening state — screening/appraisal decisions now survive the session. `literature_screening_decisions` (`migrations/20260814b`, on the deploy set above the RLS sweep) holds one current decision per (org, entry, program, appraisal stage) with reviewer + timestamp, and a MEDDEV §8 exclusion rationale enforced by CHECK; stages are the CER flow's own `title_abstract`/`full_text`, not new vocabulary. One path only — `literature-screening.service.ts` behind `POST /literature/screen` + `GET /literature/screening`, read back into `LiteratureTab`. Old→new chained through `auditService`. Refuses 422 (never fakes) when the article is not in the corpus or the migration is unapplied | eng | done | `server/services/__tests__/literature-screening.service.test.ts`, `tests/routes/cerv2-literature-screening.test.ts`, `client/src/concept2cure/mdx/hooks/__tests__/useCerLiterature.test.ts` |
| L5 | PMS complaint / PMCF enrolment backends — the generators and documentation status are live; the feeds behind them have no backend | eng | open | `docs/COMPETITIVE_POSITION_DELTA_2026-08.md` §CER |
| L6 | Standards mapping per product code — vendored FDA recognition-list drop-point, whole-file-validating loader, org-scoped `GET /api/510k/device/standards`, and the intake panel rendering "dataset not held" / "list holds nothing for this code" / "here is FDA's list" as three distinct states. Nothing is seeded or inferred; the acquisition itself is procurement (runbook B21) | eng | done | `server/services/fda-recognized-standards/__tests__/recognized-standards.test.ts`, `tests/routes/510k-device-routes.test.ts`, `client/src/concept2cure/mdx/surfaces/__tests__/DeviceProfilePanel.render.test.tsx` |
| L7 | IQ/OQ validation pack — **engineering side delivered**: 11 Part 11 controls mapped to the tests that exercise them, all 11 executed and passing, installation observed, verdict fails closed on any control lacking evidence. What remains is not engineering — a qualification is a protocol executed and signed by a competent person, and no script can be that. The pack is its input | qual | open | `node scripts/ops/generate-iq-oq-pack.mjs`, `docs/validation/IQ_OQ_EVIDENCE_PACK.md` |
| L8 | Outcome-data capture — (submission content → agency response) pairs. The strongest moat in the benchmark and the only one no competitor can buy. Nothing captures it today | eng | open | `docs/COMPETITIVE_LANDSCAPE_2026-08.md` §moat |
| L9 | Template-chase ingestion pipeline — the substrate is right (versioned catalog, fail-closed registry); the ingestion is unbuilt | eng | open | `docs/COMPETITIVE_POSITION_DELTA_2026-08.md` §4 |
| L98 | **~1,830 of ~4,190 measured text elements were below WCAG 2.2 AA (43.7%), and it was three tokens, not 1,830 decisions.** CLOSED 2026-09-03 by measurement, not by argument: `visual-qa:contrast` over the same 126 surfaces now reports **0 below AA out of 3,948 text elements**. The decision this row was holding open — darkening a base text token reaches every surface, so take it deliberately — was taken: `--text-400` is `#75736d` (4.50:1 on `--bg-000`, was `#8a8880`/3.37) and `--accent-main-200` is `#ad5132` (4.98:1, was 3.70), both now enforced by `ci:token-contrast` rather than excused by it. The last two elements standing were not token decisions at all and are recorded in L151. Read the ratio, not the integer, on the old counts: they moved by a handful between runs | eng + prod | done | `npm run visual-qa:contrast` |
| L99 | **Two token values caused 90% of every WCAG contrast failure in the product, and the gate was green over both by design.** The analysis stands and the finding is now closed: grouping by cause turned one unactionable number (1,819 of 4,151 below AA) into two token decisions, `--text-400` at 68.5% and `--accent-main-200` at 21.1%, and `ci:token-contrast` was passing over both because it carried them as exceptions SCOPED TO LARGE TEXT while exactly ONE failing element in the product — 0.1% — was large text. Both tokens are darkened and both are now ENFORCED pairs in that gate. Re-measured 2026-09-03: **0 below AA of 3,948 elements**, and `contrast-why`'s large-text line reads 0. **The pattern worth keeping is unchanged and was reused twice more the day this closed** (L151): an aggregate that cannot be acted on is not yet a finding — group by cause before deciding anything is too big to fix | eng | done | `npm run visual-qa:contrast-why` |
| L101 | **The largest single accessibility defect in the product, fixed in two lines — and the obvious fix was the wrong one.** L99 measured `--text-400` (#8a8880) causing 1,202 of 1,756 WCAG failures (68.5%). The instinct is to darken the token until it passes; that is **not possible**. To clear 4.5:1 on every light background the value must reach **#696761, which is DARKER than `--text-300` (#6b6963)** — the ramp has no room, and the fix would invert its own hierarchy. Establishing that changed the answer: `--text-400` scoped to `bg-000`, the background most muted text actually sits on, reaches AA at **#75736d (4.50:1)** while remaining a real step below `--text-300` (5.21:1); dark side **#8e8c84 (4.50:1 from 4.27)**. **Measured, not assumed: 1,756 → 1,001 failures, −755 (−43%) from two token values**, with this token's own share falling 1,202 → 447 and every one of its `bg-000` failures gone. The remainder sits on secondary backgrounds at 4.01–4.30:1 and cannot be closed by this token at all — that needs either those usages moved to `--text-300` or the backgrounds lightened, which is now a bounded decision rather than an open one. The gate's recorded floors were RAISED to 4.50 so the improvement ratchets, and its justification corrected: it read "passes AA for large text only", a premise the same measurement falsifies — exactly **one** failing element in the product is large text. Also fixed the same hex hardcoded 9× at 11–12px in `emailService.ts`, where tokens cannot reach and the defect therefore ships to inboxes (3.23–3.55:1 → 4.32–4.74:1 across every background those templates use). **NOT touched: `--accent-200` (#c96442), now the largest remaining cause at 371 failures (37.1%).** That is the brand orange as text; the gate already records changing it as a design decision rather than a lint fix, and owning the work does not make a brand hue mine to reassign. **The pattern worth keeping: when a token cannot be made to pass without swallowing the step above it, the token is not the thing that is wrong — its use as body text is.** | eng | done | `npm run ci:token-contrast`, `npm run visual-qa:contrast-why` |
| L102 | **The last 447 contrast failures cannot be fixed by any token value, and proving that is the finding.** After L101 took `--text-400` to #75736d, 447 of its failures remain, all on secondary surfaces (#f5f4ee / #eeece4 / #f0eee6) at 4.01–4.30:1 — tantalisingly close to 4.5. Both levers were computed rather than argued. **Darkening further:** #6d6b65 would clear 4 of 5 backgrounds and take 348 of the 447 — and sits **1.030:1 from `--text-300`**, which is imperceptible. That does not fix a ramp, it makes two tokens the same colour while keeping both names: a token that lies about being a distinct step, the same two-things-claiming-to-differ defect as the duplicate registries and generators already on this ledger. The shipped #75736d holds **1.158:1** from `--text-300` — slim, but a real step. **Lightening the backgrounds:** all three would have to become **#faf9f6**, i.e. collapse into `bg-000`, erasing the surface hierarchy entirely. So neither token has room, and the remainder is not a colour problem: those specific usages are body text set in a MUTED token on a tinted surface, and the only honest fix is moving them to `--text-300` — accepting that `--text-400` is not a body-text colour off `bg-000`. Left open deliberately: that is ~447 call-site decisions across 45 stylesheets, each needing a look at whether the element is text (4.5:1), an icon (3:1) or decoration (exempt), and a sweeping find-and-replace would silently promote every icon and rule alongside the text. **Also named and not taken: `--accent-200` (#c96442), 371 failures (37.1%), the largest single remaining cause** — the brand orange as text, which the gate already records as a design decision rather than a lint fix. **The pattern worth keeping: knowing a thing cannot be fixed the cheap way is worth more than a cheap fix, and the way to find out is to compute both options instead of picking one.** **CLOSED, and the premise was wrong.** The analysis above rests on a hidden assumption: that the token must take ONE value globally. Custom properties inherit, so it can be re-based on the element that ESTABLISHES the surface. Measured per surface the ramp has room everywhere — bg-050 t400 #71706a (4.51) / t300 #686660 (5.21), step 1.156; bg-100 #6e6c66 (4.52) / #64625c (5.25), step 1.162; bg-200 #686661 (4.58) / #5f5d58 (5.26), step 1.147 — against a global step of 1.158. The step is PRESERVED; evaluating one value against every background at once is what forced the collapse this row documented. Two facts made it safe rather than clever: `--text-400` is a foreground token in practice (1,437 of ~1,490 stylesheet uses are `color:`), and `--text-300` itself measures 4.39 on bg-200 — so the proposed 447-call-site migration would not have closed that surface either. Zero call sites changed. The sheet is GENERATED from a real postcss parse of every stylesheet that sets one of these backgrounds (1,042 selectors today), because a hand-maintained list is wrong the first time someone adds a card and wrong silently; `ci:surface-text-ramp` fails on drift, proven by adding a tinted rule (exit 1). Verified MEASURED in Chromium, not computed — and the first probe invented three class names, failed, and was itself the thing that was wrong. | eng | done | `client/src/concept2cure/v2/styles/surface-text-ramp.css`, `scripts/design/generate-surface-text-ramp.mjs`, `scripts/visual-qa/probe-surface-text-ramp.mjs` |
| L104 | **AnA could be steered mid-run, by a server that has supported it all along and a rail that offered no way to say so.** `POST /api/ana-ri/stream/:runId/control` takes pause / resume / cancel / interject; the agentic loop applies each at its round-boundary checkpoint; an accepted steer is spliced into the NEXT round and written into the decision lineage; `runId` is an unguessable bearer capability only the owning client holds. `useAnaChat` exposes all four as `pause`, `resume`, `stop`, `interject`, tracks `runStatus`, and handles the `paused` and `interjected` events. **The v2 rail referenced none of them — zero occurrences of any of the four.** So a reviewer watching AnA work a question the wrong way — six rounds into 510(k) precedent when the question is EU MDR — could only wait for her to finish. Capability complete at both ends, wiring nobody's job: the same gap that hid the tool calls (L85), the reasoning (L87) and the answer's caveats (L87). SHIPPED: a control row above the composer while a run is live — pause/resume, stop, and a steer field — plus the accepted steers rendered under the answer they shaped, `interjections` having been captured since run control landed and rendered nowhere. **Honesty is the load-bearing part**: control takes effect at a ROUND BOUNDARY, so the state reads "Paused after this step" rather than "Paused" — a label claiming the tool in flight stopped dead would be the interface overstating the server, and a test fails on exactly that word. The steer buffer is separate from the composer draft, because one buffer makes a half-typed sentence ambiguous between steering this turn and starting the next. Every contract verified by making it fail: removing the row (7 fail), showing it when nothing runs, promising an instant stop, not clearing the field so a stray Enter resends a steer, dropping the accepted-steer rendering, and dropping `interjections` from the adapter. Rendered at the true 356px rail width: no overflow, rail does not scroll. **The pattern worth keeping: the most valuable thing in a mature codebase is rarely unbuilt — it is built at both ends and connected at neither.** | eng | done | `client/src/concept2cure/v2/__tests__/anaRunControl.test.tsx` |
| L105 | **The server graded every answer's evidence and no surface showed it.** `validateEvidence` counts the sources, splits an answer's claims into grounded and weak, flags each weak one by kind, and writes a reviewer risk summary; post-processing ships it as `grounding_strip`; `useAnaChat` stores it under a comment reading "for the grounding chip on the reply". **There was no chip** — nothing in the product read `groundedClaims`, `weakClaims` or `flaggedClaims`, so a reviewer could not tell whether a sentence rested on six sources or on nothing. On a surface people draft submissions from, that is the most useful thing an assistant can say about its own output. SHIPPED as rows in the work record's own vocabulary — how many claims are grounded, how many weak and of what kind, and the first flagged claim QUOTED, because "2 weak" says a problem exists without saying which sentence has it. **The load-bearing rule is the unassessed state.** `useAnaChat` fills every count with 0 when the field is absent, so a turn the pipeline never graded arrives as `{validated:false, sourceCount:0, groundedClaims:0, weakClaims:0}` — byte-identical to one graded and found clean. Rendered naively that becomes **"0 weak claims": the strongest reassurance the surface can give, produced by having checked nothing.** `validated` therefore gates every number, counts arriving alongside `validated:false` are ignored on purpose, and the state is dressed as neither a pass nor a failure — plenty of answers legitimately go ungraded. Verified by making each fail: trusting the counts over the flag breaks 3 tests; dropping the kind names, the quoted claim, or the adapter carriage each break their own; substituting an invented risk summary for the server's breaks another. **Scope was decided by audit, not instinct** — `suggestedActions` was left alone because the rail already has `AI_ACTIONS` chips, and `CrlPremortemPanel` was left alone because it already EXISTS, fully tested, with zero mount sites: that one is wiring, not a surface to design. Rendered at the true 380px rail: no overflow. **The pattern worth keeping: before building a surface, check whether the product already has one — twice here it did.** | eng | done | `client/src/concept2cure/v2/__tests__/anaGrounding.test.tsx` |
| L106 | **A board-ready decision artifact was built, tested, and mounted nowhere.** `CrlPremortemPanel` — the CRL/RTF pre-mortem: an approval-probability ESTIMATE carrying its denominator and confidence, the ranked risks each bound to the precedent that grounds it, and a prioritised fix list — shipped with E14, has its own test suite, and had **zero call sites outside itself**. `useAnaChat` assembled `crlPremortem` onto the message the whole time. A committee-grade artifact the product could not show anyone, which is the same shape as the tool calls (L85), the reasoning (L87), the caveats (L87), the steers (L104) and the grounding verdict (L105): built at both ends, connected at neither. MOUNTED on the rail, carried through the adapter, pinned by a carriage test. **Mounting it exposed a defect in the panel first.** Its export button keyed `disabled` off `artifact.exportable` alone, so a host with no export handler — this rail, which has no DOCX route for the artifact — rendered an ENABLED button whose click ran `onExport?.(…)` on undefined and did nothing. A control that looks available and silently does nothing is precisely what this codebase keeps being bitten by, so `!onExport` now disables it too and says where export actually lives. The panel already had the disabled-with-a-reason pattern; it simply did not cover the case. **An existing test had to change, and the distinction matters**: `exportable artifact has no dangling aria-describedby` asserted `null`, which held only because such an artifact rendered no reason at all. Its NAME is the real invariant — describedby points at a real element or is absent — so it now asserts that directly, in both configurations, which is stronger than what it replaced rather than bent to fit. Verified by making each fail: unmounting the panel breaks 3, restoring the old disabled rule breaks 2, dropping the adapter line breaks the carriage test. **The pattern worth keeping: wiring an orphaned component is not free — the reason it was never mounted is often a question it was never asked.** | eng | done | `client/src/concept2cure/v2/__tests__/anaPremortemMount.test.tsx` |
| L107 | **The composer's mode picker changed nothing, and it was the most consequential inert control in the product.** `prefs.anaMode` was stored, passed to the rail, and rendered beside the send button as "Ask · Maximum" — and never reached the request. `V2App` called `useAnaChat({screenName, projectId, moduleContext})` with no `effortLevel`, and nothing mapped the mode onto one. **`effort_level` is not cosmetic**: the server maps it to the agentic loop's round ceiling — `fast` 4, `balanced` 6 (+2 earned), `thorough` 10 (+4) — plus the output budget and the model tier, and `resolveEffortLevel` falls back to `DEFAULT_EFFORT = 'balanced'` for anything absent. So a reviewer choosing **"Deep research — drafting, multi-step analysis, long-form" got 8 rounds where the label promised 14**, and "Quick ask" cost twice what it advertised; only "Standard" was right, and only by coinciding with the default. A control that presents a deliberate choice and discards it is worse than no control — it does not merely fail to help, it tells the user something untrue about the work that was done. FIXED at the source: each `ANA_MODES` entry now declares the `effort` it buys, `effortForMode()` resolves it, and `V2App` passes it. An unrecognised mode returns **null** so the field is OMITTED and the server's own default applies — inventing a level here would let this layer decide how hard AnA works on the strength of a string it does not recognise. **Pinned at the wire, not just in the map**: a correct mapping nobody sends is worth nothing, and that WAS the defect, so a test asserts `effort_level` in the request body the way the attachment suite asserts `file_ids`, plus a carriage assertion on `V2App` because every rail suite passes props in directly and none can see the mapping stop happening. Verified by making each fail: disconnecting the picker, quietly demoting Deep research to balanced, and guessing an effort for an unknown mode each break their own case. **The pattern worth keeping: an inert control is a lie with a UI, and the cheapest place to find one is any setting the request body never mentions.** | eng | done | `client/src/concept2cure/components/ana/__tests__/useAnaChat-effort.test.ts` |
| L108 | **The gate counting AnA's screen-awareness counted mentions, not publishing — and two more surfaces now publish.** L45's ratchet reported "N surfaces publish screen state" from `src.includes('usePublishSurfaceContext')`, **a plain substring check**: a file that merely IMPORTED the publisher, or called it with a typo'd id, counted as wired. The id matters absolutely — the publisher keys context by surface id and the reader returns NOTHING unless it matches the active surface, so a wrong id is a silent no-op where the surface publishes, the shell reads past it, and AnA is still looking at a blank screen. The gate now requires a real CALL with a literal id **checked against the registry** (read from `surfaceViews`, not an allowlist that could go stale), and every surface wired to date passes a literal, so it costs nothing. Verified by making it fail: renaming a published id to `'vualt'` now FAILS naming the surface and the reason, where before it passed silently; removing a call while keeping the import correctly stops counting. **Baseline raised 13 → 16** and confirmed to bite. **Two surfaces wired**, both chosen because their questions are unanswerable without screen state: `vault` (tree size, open folder, active search, selected document with status/version/owner/blocker) and `safety-narrative` (SAE case count, how many meet a seriousness criterion, the soonest expedited-reporting clock, and which ICH E3 §16 fields the open case is still missing). **Both publish a FAILED read as a failure**: `allDocs` and `live.rows` are empty when the store is genuinely clear AND when the read threw, and "0 cases" over an outage would tell a safety reviewer their queue is clear when it is unknown. 92 surfaces remain. **Also fixed: a THIRD file carrying a literal NUL byte** — `surfaceContext.ts`, which `grep -n` prints nothing from, so every search-driven read of the module AnA's screen-awareness depends on silently returned no match. Escaped to `\u0000`: same byte, same behaviour, greppable file (L51 records the other two). **The pattern worth keeping: a ratchet is only as honest as its predicate, and `includes()` is not a predicate about behaviour.** | eng | in-flight | `npm run ci:ana-surface-context` |
| L113 | **The ledger's own integrity check was a log line, not a gate — and I pushed past it twice.** Duplicate row ids kept reaching `concept2cure-v2`: two sessions each append the next free id, both are correct in isolation, and the merge keeps both. **The offending state does not exist until the histories join**, so a pre-COMMIT check structurally cannot see it, and the author who merges is not the author of either row. Both times the pushing session HAD run `ledger-check` — and read its output after the push had already gone, because the check and the push sat in one command sequence with nothing gating one on the other. A result that does not decide whether the push happens is not enforcement. FIXED in `.husky/pre-push`, beside the eCTD-bundle and risk-code gates that are already unconditional there, so it protects every session rather than whichever one remembers. It costs 0.16s. Verified by making it fail: injecting a duplicate row makes the hook refuse the push and print the offending id, its line number, and the reason a merge produces one; restoring passes. **The pattern worth keeping: when the same mistake survives being noticed twice, the fix is not more attention — it is moving the check to where it can refuse.** | eng | done | `.husky/pre-push` |
| L114 | **`risk` publishes the ISO 14971 file, and an attempt to wire `review` was caught adding a SECOND publisher for one id.** The risk surface now tells AnA what a quality lead actually asks: hazards total, how many open or mitigating, how many accepted, **how many carry a residual score of 15 or more**, the mean initial and residual scores, whether the matrix is showing initial or residual risk, and the selected hazard's severity, probability and status. A failed read publishes the failure — `rows` is `EMPTY_ROWS` when the governed risk file is genuinely empty AND when the read threw, and **"0 open risks" over an outage is the most dangerous sentence this surface could produce**: it reads as a clean risk file to the person accountable for one. **The `review` attempt was wrong and a concurrent session's test proved it.** `ReviewThreads.tsx` — a CHILD rendered inside `Review.tsx`, not a registered surface — already publishes under `'review'`, so adding a publisher to the parent put two behind one id. The store is keyed by id: two publishers fight, last render wins, and either unmounting clears the other's context. `surfaceContextIds.test.ts` failed on exactly that (`at most one publisher per surface id`) and the change was reverted rather than argued with — the id was already covered, and improving it means editing the child, not adding a rival. Baseline 19 → 20. 88 remain. **The pattern worth keeping: "this surface is unwired" and "this surface id is unpublished" are different claims, and the file-level count answers only the first.** | eng | in-flight | `npm run ci:ana-surface-context` |
| L115 | **The screen-awareness gate was counting files, and files are not what AnA sees by.** It reported "20 surfaces publish, 88 still to wire" — a FILE count, while the context store is keyed by surface ID and the reader returns nothing unless the published key matches the ACTIVE surface. The two diverge in both directions: one file can serve several registered ids, so a "wired" file leaves the other ids it renders under blind; and `review` counted as UNWIRED while its child `ReviewThreads.tsx` publishes that id, so the id was covered and the file was not (L114 records the duplicate publisher that discovery cost). Measured properly: **122 registered surface ids, 20 publishing, 102 blind** — against a file number of 88, so the honest gap is 16% larger than the gate was reporting. The gate now reports and RATCHETS on both, with `ID_BASELINE` separate from `BASELINE` because they count different things, and test-file publishers excluded — a fixture publishing an id proves nothing about the product. Verified by making it fail: removing `risk`'s call now trips BOTH ratchets with distinct messages. **This is the third time in this effort a ratchet has been found weaker than its own sentence** — `includes()` counting mentions (L108), a contrast exception scoped to a size the product never uses (L99/L101), and now a file count standing in for an id count. **The pattern worth keeping: when a gate reports a number, check that the noun in its sentence is the noun it counted.** | eng | in-flight | `npm run ci:ana-surface-context` |
| L116 | **The NDA cockpit tells AnA that Refuse-to-File risk is NOT ASSESSED, and why — publishing screen state was a new way to lose that distinction.** This surface fought for it once already: no completed shadow-review signal exists anywhere for it to read, so an empty RTF log cannot mean "the day-60 review found nothing", and the comment above `filingState` records that deriving clearance from `rtf.length === 0` would reinstate the exact defect. **A context summary reading "0 Refuse-to-File risks" is true, reassuring, and wrong in the way that matters** — it hands the model a clean bill of health nobody issued, and a model repeats it in prose to a filing team. So `filingState` is published **verbatim** rather than re-derived: its vocabulary IS the finding (`not-assessed` is not `assessed-clear`, and `unreadable` is neither), and a second derivation here could drift from the one on screen. The summary also carries the REASON — "no completed shadow review is recorded" — because bare "not assessed" invites a model to treat it as a formality it can reason past. Also published: CTD readiness, module count, open Module 1 admin items, and `mayReassure`. Tested through the real store with a `Probe` reading `useActiveSurfaceContext`, and verified by making it fail: swapping the caution for the reassuring zero breaks 2, re-deriving the verdict from `rtf.length` breaks 1. **The harness needed fixing first** — a `waitFor` on truthiness is satisfied by the LOADING summary on first paint, so three assertions ran against "still loading" and meant nothing; they now wait for the state to leave loading. Baselines 20 → 21, both ratchets. 101 ids blind. **The pattern worth keeping: every new channel out of a surface is a new place to launder a caution the surface earned.** | eng | in-flight | `client/src/concept2cure/v2/__tests__/ndaCockpitSurfaceContext.test.tsx` |
| L117 | **The §11.50 printed name is now resolved by org MEMBERSHIP, closing L17.** `persistGovernedActionSignature` read the signer with a bare `SELECT name, email, title FROM users WHERE id = $1` and no tenant predicate. Safe as CALLED — both callers pass the authenticated actor's own id — and safe by convention, not construction: nothing stopped a future caller passing any user id, and the function would then print that person's name onto a signature in another tenant's filing. **§11.50 requires the printed name OF THE SIGNER, and no later check would catch a wrong one** — the §11.200 manifest hash would bind the misattributed name perfectly faithfully. FIXED with a join to `organization_users`, bound to the signing org, so the name resolves only for a signer this org can attribute. **The obvious fix was the wrong one and L17 said so**: `users.default_organization_id` names a PREFERENCE, not a membership, so scoping there would refuse a signer legitimately acting outside their default org — trading a rare misattribution for a routine outage on a governed action. `organization_users` is the authorization relation, and the one `server/auth.ts` selects a session's tenant from. Non-membership and non-existence share ONE refusal deliberately: distinguishing them would disclose that a user id exists in another tenant. Verified by making it fail: restoring the bare read breaks 1 test, scoping on `default_organization_id` breaks 2. 138 server files / 1,066 tests pass, check-security-patterns and ci:tenant-isolation both green. **The pattern worth keeping: "safe as called" is a property of today's callers, and a filing an agency reads outlives them.** | eng | done | `server/services/part11/__tests__/signer-org-scope.test.ts` |
| L118 | **Two more writers turned an unstated author into a person.** Same defect as L32, found by sweeping for its shape rather than waiting to trip over it. `q-sub.ts` resolved an omitted `draft_source` to `'human'` on the body of a Pre-Sub briefing document bound for FDA — beside the `accepted_at`/`accepted_by` columns that exist to record a human ACCEPTING an AnA draft, so an unaccepted machine draft became indistinguishable from prose a person wrote; now NULL, which that column's own schema comment names as a value. And `write_q_sub_section` wrote clause-level author lineage as `String(ctx.userId ?? 'system')` into `document_span_lineage.asserted_by`, whose CHECK requires that column for an author_assertion — satisfying an attribution constraint with a placeholder defeats the thing the constraint is for. It now refuses without an identified actor, matching `create_qms_document`/`approve_qms_document`. Both guards were shown failing before being reported working | eng | done | `server/__tests__/routes/q-sub.test.ts` |
| L119 | **The authoring loop's JWT subjects could not hold a signing role, so e-signature was unreachable and a golden journey was red on it.** `ind-authoring.journey.test.ts` failed at `e-sign-wrong-pin`, which expects 401 and got 403: the §11.10(g) authority gate refused before the PIN was ever checked, so the two real signing steps, the §11.70 binding and the whole export section had not run since. The 403 was CORRECT and fail-closed — `getActorId` returned the JWT subject and `resolveSignerOrgRole` casts it with `Number()`, which is NaN for a UUID. THE FIXTURE WAS WRONG, NOT THE PRODUCT, and my first reading of this row said the opposite. `users.id` is a `serial` (shared/schema.ts:2575, scripts/db-verify/00_bootstrap_base.sql:3) and `organization_users.user_id` is the integer referencing it. The journey seeded UUID subjects sharing a `3f1c2a10…` prefix under ONE membership row derived by `Number.parseInt(AUTHOR.id, 10)` — the prefix-parse identity confusion C-21 removed, where a UUID subject silently became a different real integer-keyed user. Its stated justification, that the history handler joins `u.id = created_by::uuid`, was stale: that cast had already been replaced by `u.id::text = r.created_by` after it was found to raise 42883 at parse time on every call. Subjects are integers now, matching the product, and the journey passes end to end | eng | done | `tests/golden-journeys/ind-authoring.journey.test.ts` |
| L120 | **AnA went blind to the Report Engine as collateral damage, and the gate that should have said so was the thing that broke.** The honest-state sweep (`6866d4fb1`) deleted `ReportEngine.tsx`'s `surfaceContext` import, its `anaContext` memo and its `usePublishSurfaceContext('report-engine', …)` call while fixing an unrelated empty-vs-unassessed problem. Its message does not mention them; nothing in the change wanted them gone. Screen-id coverage fell 114 → 113 while `ID_BASELINE` stayed at 114, so `check-ana-surface-context` failed — a third thing keeping CI red, on top of L109's blackout. Restored verbatim, because the removed context was already honest: it distinguishes a server-side analysis from the local text parse this surface falls back to, and says plainly when nothing has been analysed. Gate now reports 114 of 120 publishing, 0 blind | eng | done | `client/src/concept2cure/v2/surfaces/ReportEngine.tsx` |
| L121 | **The 510(k) forms were fixed in one file and left intact in the other — and the untouched one is the one that ships.** `2a8e9b7be` removed the fabricated device class, clinical-studies certification and fee posture from `FDAFormGenerator.ts`. `CrossReferenceMapping.ts` is a second implementation of the same four forms, wired into `POST /api/510k/:projectId/generate-documents` via `DocumentOrchestrationService.generateFDAForms`, and it kept every one of them plus worse. Form 3654 carried three LITERAL `☑` boxes — "I have reviewed the information", "complete and accurate", "all required documentation is included" — certifying on the signer's behalf, with no input feeding them, directly above the debarment warning about penalties for false information; `certification_statement: true, // Always true when submitting`; an execution date stamped from the render clock for a signature never applied; and `|| false` on the clinical-studies and Part 54 financial-interest disclosures, so "nobody answered" printed as "No financial interests to disclose". Form 3881's `prescriptionUse !== false` made an UNSET field TRUE, ticking the box that declares the device's labeling regime — the only default found that fabricated in the affirmative from pure absence. Form 3601 hardcoded fee category, payment method and `small_business: false`. All of it persisted to `fda510k_documents.content` and then SHA-256 sealed by `lockDocument()`, so the guesses became part of an immutable 510(k) record. Now tri-state throughout: unanswered prints an empty box or "Not stated", a stated answer still prints verbatim. Four of the seven new guards were shown failing against the restored fabrications | eng | done | `server/services/__tests__/CrossReferenceMapping.no-fabricated-content.test.ts` |
| L122 | **The integrity check claimed more than it checked, in the one place an inspector reads.** `verifyIntegrityChain` recomputes SHA-256 over `artifact.content` and each version's content — both read from the DATABASE — and compares each to the hash stored beside it. That is a real check and it stays: it catches a stored row altered without its hash being updated. What it is not is verification of the originating document; nothing in this path reads source bytes. The problem was the framing around it. `/verify-integrity` returned a bare `verified: true` next to `algorithm: 'SHA-256'`, naming no subject, and the audit report emits the same verdict under `standard: '21 CFR Part 11 · ICH M8 eCTD v4.0'` DIRECTLY ABOVE its `sourceLineage` section — then persists that as a governed artifact. A reviewer reading "verified, SHA-256" beside a list of source documents concludes those documents were checked. The verdict now travels with an explicit `scope` naming what was compared AND what was not, plus `sourceDocumentBytesVerified: false` — present rather than omitted, so the absence is visible instead of inferred — and the bare field is renamed `storedRecordSelfConsistent`. Nothing in the client or the suite read the old name. Four of the six guards were shown failing against the restored wording. STILL OPEN: the underlying gap, that no filed artifact's stored hash is ever re-derived from its source bytes except the vault download route | eng | done | `server/routes/__tests__/integrity-claim-scope.test.ts` |
| L123 | **The error-leak gate fired on correct code, which is how a real leak gets in.** `check-server-error-leaks` cut the response "body" it scans at the first BLANK LINE — its own comment claimed "stop at the end of this statement", and a blank line is not that. The standard fail-closed handler has no blank line in it: a static 503, then a `console.error(..., err.message)` on the next line. So the LOGGER's error text was attributed to the RESPONSE and a correct handler was reported as a leak. Five files were over their counts on this alone, blocking CI, none of them leaking anything. That is worse than a missed finding — a gate that fires on correct code is one people silence by widening the baseline, and the widened baseline is where a real leak lands. The body is now delimited by walking bracket depth from the `res.status(...)` call, skipping string and template literals so a `;` inside a message cannot end it early, and stopping at the first `;` at depth 0. Verified by probe: `err.message` in the body and `String(err)` in the body are both still caught; a static body followed immediately by a logger is not. 63 false sites disappeared, so the baseline shrank 843 → 780 across 150 files, and the ratchet was re-checked to still fail on a newly added leak | eng | done | `scripts/ci/check-server-error-leaks.mjs` |
| L124 | **The traceability matrix reported full coverage of nothing, and filed it as a governed export.** `evidence_claims` / `evidence_sources` / `evidence_claim_links` have ZERO writers repo-wide and their only migration (`db/migrations/20260319_evidence_fabric.sql`) is on no durable apply path — not in `C2C_MIGRATION_FILES`, not in the drizzle journal — so the RTM route either raises 42P01 or reads an empty table. Both were rendered as an answer. Zero claims produced `untracedClaims: 0`, which a regulatory reviewer reads as "nothing is untraced" — i.e. everything is traced — beside `coverageScore: 0`, a MEASURED nought asserted where nothing was measured; a missing relation produced a bare 500 indistinguishable from a transient fault; and the CSV route hashed a header-only file and registered it as a governed regulated export titled "RTM Export: Program N" with nothing on the artifact saying it traces nothing. The three states are now distinct: an unprovisioned store fails closed with `CLAIM_STORE_UNPROVISIONED` on both routes and registers NO governed export, zero claims reports `coverageScore: null` / `untracedClaims: null` with `state: 'no-claims-recorded'`, and a real matrix still measures — 0%% coverage on a real claim is still reported as 0. The exported CSV states its own emptiness on the artifact. Three of five guards shown failing against the restored behaviour. STILL OPEN: the store has no writers at all, which is the underlying decision (converge onto the live spine or delete the surface) | eng | done | `server/routes/__tests__/rtm-export-honesty.test.ts` |
| L125 | **The NDA journey froze and dispatched a dossier nothing had ever reviewed.** `assess-dispatch-readiness` now blocks dispatch until at least one completed Shadow Review exists — zero runs is UNASSESSED, not clean, for the same reason an unread document has no findings. The golden journey asserted the opposite: `gate.cleared === true` ALONGSIDE `shadowReviewMissing === true`, a contradiction that encoded the behaviour the fix removed, and it then froze and transmitted without a review ever running. The step now asserts the gate does NOT clear and names the Shadow Review blocker specifically, and the journey performs the review it was skipping before freezing. That run is SEEDED rather than driven through `POST /sequences/:id/shadow-review`, because that endpoint calls a model and returns 502 with no provider reachable — what this step proves is the GATE's response to a completed run, which is a database fact, and saying so keeps the journey honest about which half it exercises | eng | done | `tests/golden-journeys/drug-nda-ectd.journey.test.ts` |
| L126 | **The eCTD v4.0 validator failed every valid submission, and named the wrong cause.** A context of use is governed by ONE OF TWO code systems: Module 1 is regional (FDA CL2 list), Modules 2–5 are ICH-governed and carry the ICH CoU code system with an `ich_<section>` code. `sectionToCou` in `forward-compat.ts` emits exactly that split, deliberately and documented. `validateRpsMessage` checked EVERY context against the FDA list and the FDA OID alone, so every Module 2–5 context — most of a real dossier — was reported as both `RPS-COU-CODE-VALID` (invalid code) and `RPS-COU-CODESYSTEM-OID` (invalid code system). A validator that fails every valid submission is not strict, it is broken, and both findings pointed at the wrong thing. Now split by code system, with strictness preserved in all three directions: an unrecognised code system is still refused, an FDA-governed code must still be catalogued in CL2, and an ICH-governed code must still match the governed shape rather than being waved past on its code system alone. `ICH_COU_OID` is imported rather than re-derived so the validator and the converter cannot drift | eng | done | `server/services/ectd/ectd4/__tests__/rps-cou-code-systems.test.ts` |
| L127 | **The ESLint warning ratchet was the sixth thing holding CI red, and one of the new warnings was mine.** The gate had risen to 6788 against a frozen 6783 — `complexity` +3, `max-lines-per-function` +3 — and it refuses on principle to be rebaselined, correctly: 6.7k warnings is already a green light bolted over a wall of noise, and a new one is invisible without the freeze. Because the baseline holds only per-rule totals, the offenders were found by linting the tree at the commit that wrote it in a worktree and diffing per file. Mine was `rtm-export.ts`, pushed to 104 lines by the empty-matrix statement I had added inline; it moved to a module constant. The rest came in with fail-closed fixes that legitimately grew their functions: `assessSequenceDispatchReadiness` (shadow-run count extracted), `assembleSequence` (leaf read and the path-safety guard extracted, the latter keeping its ECTD_ASSEMBLE_BLOCKED audit row), and `validateRpsMessage` (Submission Unit checks extracted verbatim — every code and message unchanged, since those codes appear in reports a reviewer reads). Also corrects a stale test asserting `toPackagerRegion('cn')` throws: `cn` was deliberately ADDED to the region map by the fix that let the spine build CN/BR/IN/KR/SG submissions at all, so the test now uses a code that really is unsupported and additionally asserts `cn` resolves. Ratchet back to 6783 exactly | eng | done | `scripts/ci/eslint-warning-baseline.json` |
| L128 | **The GA readiness report told Ops to start DR work that was already done — and would have called it finished for a file existing.** The backup/restore row searched three paths for a markdown document; the runbook, the drill and its CI proof all landed at names that list did not contain, so the report read "no DR runbook at any expected path" while `docs/operations/database-disaster-recovery.md`, `scripts/ops/dr-restore-drill.sh` (real `pg_dump --format=custom` → `pg_restore`) and `.github/workflows/database-dr-restore-proof.yml` all existed. On a report whose whole job is to say what is left before GA, a blocker that misnames done work costs exactly the time it exists to save. Worse was the status rule: `found.length > 0 ? 'ready' : 'blocked'` — a GA BLOCKER satisfied by a markdown file EXISTING, so an empty `docs/runbooks/disaster-recovery.md` would have reported the rehearsal complete. A restore is proven by restoring. The row now observes the three components separately and stays BLOCKED regardless, because what is established is that the mechanism works against a disposable CI fixture, and what remains is a rehearsal against a production-shaped database with its RPO/RTO observations committed — which the runbook itself is careful to say the drill does not provide. The unblock line names that one step instead of a from-scratch project | eng | done | `scripts/ops/ga-readiness-report.mjs` |
| L129 | **The full test suite had not run in CI for days, and it was hiding 35 failures across 20 files.** `npm test` is one of the jobs reported `skipped` behind the proof-tier blackout (L109), so nothing observed it. Run against the current head it was 35 red out of 22,326. NONE was a product bug: every one was a fixture or assertion that had not kept up with a fail-closed guard someone added deliberately — which is the blackout's real cost, since each of those guards landed unverified. The pattern repeated. A Part 11 export refused a still-editable document while fixtures seeded `status: 'draft'` and asked for a filing artifact. The eCTD packager refused a backbone reference to an unpackaged leaf — the golden fixture declared Form 356h under `fda.forms` and left it out of `leaves`, and a duplicate object does NOT fix that, because `resolve()` keys on leaf IDENTITY. Production config demanded a real AI sensitive-data placement posture, where an empty approvals map is refused as the "nobody decided" state it is. An access token had to say `type: 'access'` rather than become one by staying silent. The tenant lifecycle guard refused an unreadable org posture. A CMC stability narrative stopped asserting "is stable" from data that does not establish it, and the test had been asserting the fabrication. An eCTD history filter moved to a word-boundary match after the substring form leaked OTHER same-org projects' compilations. Two route suites 500'd on a `server/db` mock missing `pool` entirely. All 20 files green, 273 tests | eng | done | `tests/field-sync-auth.contract.test.ts` |
| L130 | **One failing step could blind the whole pipeline, and did — for three days.** The root cause of L109 was never the failing check itself; it was that a failure STOPS everything after it. Of the lint job's 79 steps only 7 carried an `if` condition, so when Proof tier (step 25) went red the 54 independent guardrail steps after it were SKIPPED — each one a gate on tenant isolation, Part 11 audit coverage, RLS adoption, design-system rules — and none of them reported anything. Worse, seven downstream JOBS declare `needs: lint`, so the `Test` job carrying the full 22k-test suite was skipped too. A skipped job reports neither pass nor fail. Those independent checks now carry `!cancelled() && steps.install.conclusion == 'success'`: they all report in one run, so a broken gate no longer hides the state of every other gate, while a genuine SETUP failure still fails fast instead of turning one real error into 70 confusing ones. Checkout, Setup Node and Install stay unguarded for that reason, and the one pre-existing `if: failure()` step is untouched. `Test` additionally gets a job-level `!cancelled()`. It keeps `needs: lint` so ordering is unchanged; it simply stops being skipped. The marginal cost lands exactly where it is wanted — the job already runs on every push where lint passes, so the only ADDITIONAL runs are those where lint is broken, which is precisely when it matters whether the tests still pass. That is not hypothetical: when this suite finally ran it was 35 failures across 20 files, every one a fixture that had not kept up with a fail-closed guard, meaning each of those guards had shipped unverified ALSO APPLIED TO `pr-checks.yml`, which had the same shape and not one `if` across 18 steps: after setup it runs ten INDEPENDENT checks — security patterns, compliance claims, microcopy, the GA ledger, surface discoverability, error leaks, internals-in-copy, large files — so the first failure hid the rest and a contributor learned about them one push at a time. Fourteen checks there now report together; its four setup steps still fail fast. | eng | done | `.github/workflows/ci.yml` |
| L131 | **The Source Tracer told regulatory leads their citations were still current, and had no mechanism that could ever say otherwise.** It rendered `content unchanged since cited` with the hint "the checksum recorded when this section cited the source still matches the source today", and the banner read "Every recorded citation still matches its source's current content". The `changed` branch is UNREACHABLE in production. `authoring_citations.payload_sha256` is set to the source's checksum at cite time, and nothing in the product ever revises `cre_evidence_sources.checksum` — the only `UPDATE ... SET checksum` statements in the repo are in tests. A revised document is ingested as a NEW source row (different checksum, so `findSourceByChecksum` misses), which the old citation does not point at. So `src.checksum <> c.payload_sha256` can never be true, every citation shows the reassuring branch forever, and a genuinely revised source is exactly the case it cannot see. Both surfaces now say what was actually established — the citation matches the source RECORD as stored — and say plainly that a revised document becomes a new source and is not detected here. Changed in the authoring Sources rail too, because the vocabularies are deliberately mirrored so one fact never gets two wordings. STILL OPEN: L21, the underlying gap — sources are never versioned, so no `previous_version_id`/`is_current` links a revision to what it replaced | eng | done | `client/src/concept2cure/v2/surfaces/SourceTracer.tsx` |
| L132 | **A revised source document is now linked to the one it replaces, so a citation can stop calling itself current.** Closes the gap behind L131. `cre_evidence_sources.checksum` is written once and never rewritten, so a revision was ingested as a wholly NEW row with nothing pointing back — and because `authoring_citations.payload_sha256` holds the checksum AT CITE TIME, the freshness comparison compared an immutable value with itself and could never be false. Adds `previous_version_id` + `is_current` (additive, no backfill: nothing in the existing data says which row superseded which, and inferring it would manufacture a lineage never observed). `createSupersedingSource` writes the successor and retires the predecessor in ONE transaction — apart, a failure leaves either two current versions or a retired source with no successor, both worse than no linkage because both read as complete. A unique partial index refuses a second successor, so a lineage cannot fork. The ingest path decides supersession CONSERVATIVELY: same tenant, project scope, source type and title, still current — and returns null when two documents in the project share a filename, because that is where "this is a revision" becomes a guess and a wrong one retires a live source. How the link was reached is recorded in the successor's provenance. `usageState` gains `superseded`, which OUTRANKS the checksum comparison precisely because the checksums still match; `is_current` is read through `to_jsonb` so a database mid-deploy yields NULL rather than 500ing every Source Tracer read. Both surfaces render it. Falsified: removing the check makes the superseded citation report 'current' again | eng | done | `server/services/clinical-regulatory-evidence/__tests__/source-versioning.pglite.integration.test.ts` |
| L133 | **"No orphan claims, no contradicted claims" was a clean bill of health from a store with nothing in it — and it fed an approval verdict.** Partly addresses L22 without pre-empting its architectural half. Since that row was written the evidence-fabric migration HAS been put on the apply path and the readers have grown to six, so the tables now exist and are provisioned; what has not changed is that nothing anywhere in the product INSERTs into them. So the counts are not stale — they are permanently zero. `programClaimsReport` returned `orphanClaimCount: 0, contradictedClaimCount: 0` with no denominator, which reads as "this program's evidence is fully linked and internally consistent" and reads identically when there are no claims at all. It now reports `claimsRecorded` and `assessed` alongside the counts. That mattered downstream: `evidence-sufficiency` takes those counts as `claimHealth`, its claim checks only ever ADD findings, and so an empty store contributed nothing and the verdict came out as though claim health had been examined and passed — a verdict that decides whether a submission is cleared for approval. An unassessed claim store is now a `warning` finding naming itself, not a silent pass. Warning rather than critical deliberately: a missing assessment is a gap in the evidence FOR the verdict, not a defect found in the submission, and blocking approval on an absence overcorrects. STILL OPEN — the L22 decision itself: the store has no writer, and whether to converge these readers onto the live evidence spine or delete the subsystem is an architectural call, not a defect fix. I have deliberately not made it while another session is actively wiring this path | eng | done | `server/services/evidence-sufficiency/__tests__/evidence-sufficiency.test.ts` |
| L134 | **A store nothing writes may no longer quietly gain readers.** The evidence-fabric trio reached SIX readers and never once acquired a writer, and none of that was visible from any single file — it was only visible by asking a question nobody was asking: does anything write here? A new gate asks it. For every drizzle table read by `server/` outside tests, it looks for any write (drizzle insert/update/delete, or raw INSERT/UPDATE) and reports the ones that have none. First run found **20**, including `evidenceSources` and `evidenceClaimLinks` — and `fda510kProjects`, read by NINE files and written by nothing, which nobody had noticed. It is a RATCHET, not a purge: the inherited 20 are recorded and only a NEW one fails the build. Failing on the existing set would have invited twenty invented reasons, which is the same dishonesty relocated. The two evidence entries carry real reasons; the other eighteen say plainly they are NOT ADJUDICATED. The first draft of this gate reported 189 "stores" because `.from(` matches `Array.from` — a gate whose findings are mostly noise is one people learn to skip, which is the exact failure it exists to prevent, so it now resolves identifiers against the real `pgTable` exports. Falsified by removing an entry from the baseline | eng | done | `scripts/ci/check-writerless-stores.mjs` |
| L110 | **A third surface AnA can see, and the branch unblocked on the way.** `reg-change` now publishes what is on screen — changes tracked, how many affect this portfolio, how many require action, how many are in force or imminent, and the open change with its source, severity, owner, due date and impact count — so "does this affect us?" no longer needs the user to retype their own worklist. As with `vault` and `safety-narrative`, a FAILED read publishes the failure: `live.rows` is `[]` both when the horizon scan is genuinely clear and when the read threw, and "0 changes tracked" over an outage would tell a regulatory lead nothing is coming when in fact nothing is known. Baseline 16 → 17; 91 remain. **Found on the way: `concept2cure-v2` was red on typecheck.** `dossierStore.dataIntegrity.test.ts` called `listDir('k510', 11, 'Performance testing')` and `listDir` takes ONE argument — one TS2554 against a gate whose baseline is 0, from another session's commit (58fd1a9). The assertion was doubly broken: even compiled, `toBeDefined()` on a function that always returns an array could never fail. Repaired to what its own comment claims — the attachment is listed, by name — and proved non-vacuous by pointing it at a file that was never attached. **Also recorded, because it shaped the session: this container repeatedly restored the working tree to an old snapshot (`c8235bd`), silently undoing fast-forwards that reported success**; the reflog held no trace of twelve commits that were verifiably on the remote. Nothing was lost — every commit had been pushed — but the work moved to a fresh clone once the pattern was clear. **The pattern worth keeping: when a fast-forward reports success and the files do not change, believe the files.** | eng | in-flight | `npm run ci:ana-surface-context` |
| L112 | **Two more surfaces AnA can see, both chosen because their question is a filing decision.** `labeling-smpc` publishes QRD readiness — completeness percentage, how many required sections are final out of the total, whether the set reports as ready, and which sections are outstanding — so "are we ready to file the SmPC?" is answerable without the user reading their own screen back. `registrations` publishes the market position — registrations, countries, products, how many approved versus submitted or under review, and the SOONEST renewal with its product, country and days remaining, because a lapsing registration is not a small event. **Both publish a failed read as a failure**, and `registrations` has already been bitten by exactly that: the comment above its `GridResponse` guard records the surface confidently reporting "No market registrations yet" for a grid it had never actually read. `regs` is `[]` when an org has none AND when the read threw, so "0 markets approved" over an outage would misreport a company's entire market position; `readiness` is absent in both states too, and a completeness figure drawn from an outage would be a filing judgement made from nothing. Baseline 17 → 19, confirmed to bite. **89 remain** — this is a long tail and the count is the honest measure of it. | eng | in-flight | `npm run ci:ana-surface-context` |

## 3. Consolidation debt — the D-items still open

| ID | Item | Owner | State | Evidence |
|---|---|---|---|---|
| L10 | **D8 C2** — attribute-free alias map, plus the CI gate that enforces the no-attributes invariant. C1 (the program anchor) shipped | eng | open | `docs/DOCUMENT_IDENTITY_CONTRACT_2026-08.md` |
| L11 | **Audit substrate decision** — decided: `audit_events` is the reference, because its chain is enforced by a BEFORE INSERT trigger and cannot be bypassed, while the `audit_logs` chain skips NULL links and so loses rows silently (it already did, in production). Stage 0 shipped: the Part 11 audit surface no longer asserts integrity it never checked. Stage 1 (bridge) is next; Stage 2 (flip the readers) needs approval | eng | in-flight | `docs/AUDIT_SUBSTRATE_DECISION_2026-08.md` §4 |
| L12 | **Chain-linkage of the 23 domain-history tables** — `linkDomainHistory` exists with **zero call sites**; every (b) table is chain-orphaned. Blocked on L11: linking them into `audit_logs` would wire 23 tables into the substrate being retired | eng | blocked | `docs/AUDIT_STORE_INVENTORY_2026-08.md` §1.3 |
| L13 | **43 dead audit tables** — DONE, and **the delete list was wrong: 24 of the 43 are alive.** Executing it as written would have dropped live 21 CFR Part 11 audit stores. One methodological gap explains almost all of it — §5.1's evidence grep covered `server client shared scripts tests` and never opened `db/migrations/`, and in this codebase the Part 11 stores are written by **PL/pgSQL**: SECURITY DEFINER functions and BEFORE INSERT hash-chain triggers defined in the migration that creates the table. No grep of TypeScript can see one. The sharpest case is the row that argued hardest for deletion: `compliance.audit_trail` was called dead because `grep log_audit_event` returned nothing — **the function is `compliance.write_audit_entry`**, called from `cortexComplianceService.ts:166` on every governed write, and again by `compliance.create_electronic_signature`. A grep for the wrong identifier is not evidence of absence. Others had live app readers (`audit_trail` in two places, `regulatory_harmonization.audit_log`, `ana_kernel_decision_log`), a seed writer, or would fail a readiness gate on drop (`authoring_audit_events` flips `/readyz` to PARTIAL). One stale citation went the other way and was corrected: `program_activity_log`'s note pointed at a file that does not exist. **19 survived; 13 dropped, 6 withheld.** The six are dead in every code path but are `pgTable`s on the drizzle-push surface, so a SQL-only DROP gives a table that comes back — the deletion that does not delete, which is the failure this row exists to end; removing them is a typed `shared/schema.ts` edit and is its own change, carried on the guard as an explicit burndown list. The migration settles retention at run time rather than trusting the repo: per table it drops only if it exists, is a table, **`count(*) = 0`**, has no inbound FK, and `DROP TABLE … RESTRICT` succeeds — otherwise KEPT with a NOTICE naming the reason, exit 0. Verified against real Postgres (PGlite self-test): refuses a table with one row, one with an inbound FK, and one with a dependent view, and is idempotent | eng | done | `db/migrations/20260901_drop_dead_audit_tables.sql` |
| L143 | **Six dead audit tables are `pgTable`s on the drizzle-push surface, so SQL alone cannot delete them.** FIXED, both halves. All six re-verified dead from scratch — grepping the SQL name, the Drizzle constant, and every inferred type across `server/ client/ shared/ scripts/ db/ migrations/ database/ prisma/`, reading PL/pgSQL bodies in the migrations and the seed scripts, which is the method L13 established after the previous list turned out 24 tables wrong. Zero writers, zero readers, and every FK on all six has the dead table as the referencing CHILD — zero inbound. Two traps cleared: `document_audit_logs` (PLURAL, `shared/schema/unified_workflow.ts`) is a different and LIVE table, and the `qmp_audit_trail` entries in `db/migrations/_consolidated/` are in a directory every applier excludes. Removed: five `pgTable`s from `shared/schema.ts` plus `csrExtractionLog` from `shared/schema/csr-knowledge-db.ts`, their `index()` blocks, six insert-schema/type triples, and two `many()` relation entries. **The schema half alone would have been another deletion that does not delete**: `install-fresh` step 3/8 replays `migrations/`, and `0000_sweet_joseph.sql` still CREATEs five of the six while `0005_csr_knowledge_database.sql` creates the sixth and enables RLS on it by name — history, not editable — so all six are now in the drop migration's list too, which runs last. The guard gained two checks: it parses `schema: [...]` out of `drizzle.config.ts`, follows each entrypoint's `export *` one level (11 files), and fails on a `pgTable` for a dead table there; and it cross-checks its own DROPPED set against the migration's array in both directions, so it can neither claim a drop the SQL does not perform nor carry a stale pending marker. Both run BEFORE `--write-baseline`, so a resurrection cannot be regenerated past. 19/19 dropped, 0 withheld, baseline 86→80. Verified by making each check fail: a re-added `pgTable` is named with its file and line, a stale pending marker is named, and a claimed-but-absent drop is named | eng | done | `scripts/ci/check-dead-audit-tables.mjs` |
| L144 | **The audit-store inventory's evidence method could not see PL/pgSQL writers, and it produced a delete list 24 tables too long.** §5.1's greps covered `server client shared scripts tests` and never opened `db/migrations/`, where this codebase's Part 11 stores are actually written — SECURITY DEFINER functions and BEFORE INSERT hash-chain triggers defined alongside the CREATE TABLE. Every store whose only writer is a database function read as dead. §8 is corrected to require scanning `db/migrations/` and to search the function NAME rather than a remembered one. Worth generalising: any liveness claim in this repo made by grepping TypeScript alone is unsound for the governed stores, and several other rows rest on exactly that method CLOSED: the generalisation is now both enforced and applied. Writer side: `ci:writerless-stores` scans `migrations/` and `db/migrations/` SQL for `INSERT INTO`/`UPDATE … SET`, which reaches PL/pgSQL function bodies, so a store whose only writer is a database function is credited (falsified with a probe store when the gate was built). Reader side: the one other row resting on the TypeScript-only method, L14/§5.2, was re-verified over migration SQL on 2026-09-02 — all eleven "no reader" verdicts hold, and the three database-side hits are recorded per row (two immutability triggers, one one-off `RAISE NOTICE`, and a view plus two functions over `ai_provider_audit_log` that nothing in the application references) | eng | done | `docs/AUDIT_STORE_INVENTORY_2026-08.md` §5.2, §8 |
| L14 | **11 write-only audit tables** — rows land, nothing reads them. Delete only with owner sign-off. Re-verified over migration SQL 2026-09-02 under the corrected method (L144): all 11 hold; `ai_provider_audit_log` has a view and two functions over it that nothing references, so its reader chain ends inside the database. Still an owner decision, now on a sound list | eng + prod | open | `docs/AUDIT_STORE_INVENTORY_2026-08.md` §5.2 |
| L17 | **Signer lookup in the §11.50 path is not org-scoped.** `persistGovernedActionSignature` resolves the printed name with `SELECT … FROM users WHERE id = $1` and no tenant predicate, though every caller already passes `orgId`. Safe as called today — both callers pass the authenticated actor's own id — but safe by convention, not construction. The obvious fix is wrong: `users` carries `default_organization_id`, not `organization_id`, so scoping on it would fail to resolve a signer acting outside their default org and break a legitimate signature. Correct fix goes through the org-membership table | eng | done | `server/services/part11/signature-persistence.ts` |

## 4. Not mine — but must not fall off the edge

These are real and they block a first real filing. They are tracked here so that "not
engineering" never becomes "not tracked".

| ID | Item | Owner | State | Evidence |
|---|---|---|---|---|
| L40 | **Eight built surfaces a user could not find.** The product renders 118 surfaces; the rail exposes 14 and the rest are found through the Apps catalog, which nothing kept in step with the renderer. Design controls, human factors, eTMF, IND lifecycle, investigator brochure, SmPC labeling, publishing centre and report engine were all mounted and API-backed — IND lifecycle across 17 endpoints — and in no catalog. Added, with a CI gate requiring every renderable surface to be catalogued or declared contextual WITH A REASON. The seven `pdev-*` ids stay out deliberately: they are tabs of the `pdev` surface, which is catalogued | eng | done | `npm run ci:surface-discoverability` |
| L15 | Blockers B1–B21 — eSTAR templates + field maps, eCTD DTDs, LORENZ licence, gateway credentials, MedDRA, the enforcement flags, the FDA recognition list (B21, the procurement half of L6) | proc | open | `node scripts/ops/ga-readiness-report.mjs` |
| L16 | Consultant / CRO channel — multi-client workspaces and per-submission pricing. A product decision, unstarted | prod | open | `docs/COMPETITIVE_LANDSCAPE_2026-08.md` §moat |

## 5. Data lineage — findings from the 2026-08-14 traceability audit

The question asked: *as source documents come in, get processed, and are deployed by AnA
or the editor into an IND filing, can we trace with certainty what changed, when, why, who
approved it, and get back to the source document?*

Answer: **strong from draft to approval, broken at both ends.** The editor cannot save
content without clause-level provenance (`lineage-gate.ts`, enforced in-transaction and
structurally gated in CI), and a user can select text and ask where it came from
(`/api/data-origins/selection`, wired into `EditorCanvas.tsx`). What does not hold is the
ingest end and the filing end. Each row below was verified by reading the code, not from a
report.

| ID | Item | Owner | State | Evidence |
|---|---|---|---|---|
| L18 | **Extraction fabricated content when it could not read the file.** FIXED with L19 in one change: `extractText` now reads the one source that exists and throws `ExtractionSourceUnavailableError` when there is nothing to read — a failed job is recoverable, a governed artifact containing invented text is not. The query against the nonexistent `uploads` table is gone. `extractText` returns `` `[Extracted content from …]` `` when no artifact row matches, and `` `[Pending extraction from …]` `` from a bare catch — then the caller hashes that string and stores it as a governed `category:'extracted'` artifact. The catch fires reliably: the second query reads a table named `uploads`, which no migration creates. **Latent, not live** — the pipeline's only entry point 500s (L19), so it cannot run today. Fixing that route without fixing this would begin writing invented content as governed evidence; they must land together | eng | done | `server/services/__tests__/extraction-no-fabrication.test.ts` |
| L19 | **The extraction pipeline's only HTTP entry point returned 500 unconditionally.** FIXED: called positionally, takes `fileId` (the pipeline resolves source text by artifact id and never had a `fileContent` parameter), and refuses without organization context rather than attributing extraction to org 0. `audit-services.ts:388` passes one object to `queueExtraction(fileId, fileName, fileSize, …)`, which is positional, so `fileName` is undefined and `detectFileType` throws. No other caller exists anywhere | eng | done | `server/services/__tests__/extraction-no-fabrication.test.ts` |
| L20 | **Two `content_hash` values could never verify.** FIXED: each artifact now digests the bytes it persists. Truncation is recorded on the row (`contentTruncated`, `sourceCharCount`, `storedCharCount`) rather than being silent. The main artifact stores `content` truncated to 100k but hashes the untruncated text, so any document over 100k chars is permanently reported tampered. The table artifact stores an **MD5 of a different object** in a column the audit report verifies as SHA-256, so it never matches. Both surface as false tamper alarms in `verifyIntegrityChain` | eng | done | `server/services/__tests__/extraction-no-fabrication.test.ts` |
| L21 | **Source documents are not versioned.** FIXED. The row was partly stale — `previous_version_id`, `is_current` and a superseding path all exist now (`20260829_cre_source_versioning.sql`, `createSupersedingSource`) — but its headline survived: `cre_evidence_sources.version` was bound by `createSource` and passed by NONE of its three call sites, so every row in the column was NULL. The ingest path now determines it from the document. **What makes this closed rather than merely written-to** is that the determination refuses more than it accepts: it requires an explicit keyword (a bare dotted number is never a version, so section numbers, dates and dose strings cannot be captured), reads only the first 4,000 chars so body cross-references cannot poison it, and DECLINES when a document declares two different labels rather than rescuing itself from the weaker evidence. The unknown case is recorded as three distinguishable states — no key at all (nothing looked), `no_declaration_found` (looked, none declared), `ambiguous_declarations` with the candidates (refused to pick) — because a bare NULL conflates the first two. CSR and CRL ingests are deliberately NOT wired: they adapt an already-structured record and are handed no text or filename, so `declared: false` would overstate what happened and NULL is the truthful state. Found and fixed the READ side of the same defect: `toSourceRef` did `parseInt(String(src.version))`, silently rendering a "3.2" document as version 3 — latent only because the column was empty, and writing it would have made it live. **No backfill**, deliberately: inferring versions from existing titles would manufacture exactly the provenance this change exists to avoid. Verified three ways, including defaulting the unknown to `'1'` — the shape "make the writer write" degenerates into — which fails the two tests that matter | eng | done | `server/services/clinical-regulatory-evidence/source-version.ts` |
| L22 | **The correctly-modelled provenance tables have zero writers.** RE-VERIFIED AND SHARPER; NOT closed, and deliberately so. Zero INSERTs into `evidence_sources` / `evidence_claims` / `evidence_claim_links` anywhere outside node_modules. Sharper than the row states: `evidence_claims` has **five UPDATE writers** (`fact-change-orchestrator.ts:289`, `reconciliation-engine.ts:71,242,278`, `fact-propagation.ts:86`) that can never match a row — dead code that reads as live — and that is why `evidenceClaims` is ABSENT from `writerless-stores-baseline.json` while its two siblings are in it: **the gate under-reports this table.** Populating these is the WRONG move and the row should not be read as asking for it: `cre_evidence_sources` is the live canonical spine with a sole writer and six readers, so writing the fabric too would put the same facts in two places — strictly worse than today. The right move is the zero-duplication one: delete the trio and migrate their 9 readers (~3,800 lines across `living-record/*`, `regulatory-graph/*`, `evidence-sufficiency`, plus 2 route files) onto the spine. Not attempted in a shared tree as a rider on L21 — it is its own change, and half-doing it would leave two half-populated substrates, which is the worst of the three states SINCE THEN: the harm is closed by L133 (an unassessed claim store no longer reads as a clean bill of health in the approval verdict) and the class is closed by L134 (`ci:writerless-stores` — a store nothing writes can no longer quietly gain readers; the trio is declared with its reason). What remains open is only the architectural choice: converge the six readers onto the live spine or delete the subsystem. GATE UNDER-REPORT FIXED 2026-09-02: `ci:writerless-stores` now credits a table only with a statement that can CREATE a row (drizzle `.insert`, raw or migration `INSERT INTO`); an UPDATE/DELETE against a table nothing inserts into is reported and flagged update-only. `evidenceClaims` is now in the baseline with this row as its reason, and the same rule surfaced six more update-only stores (`c2cDigests`, `defensePackets`, `fda510kStageProgress`, `protocols`, `standardsApplicability`, and `qSubCommitments` — whose only INSERT is a beta seed script), recorded NOT ADJUDICATED. Falsified: a probe `.insert(evidenceClaims)` removes it from the list. The converge-or-delete decision itself is unchanged | eng | open | `shared/schema.ts` (evidence_sources / evidence_claims / evidence_claim_links) |
| L23 | **A filed eCTD leaf did not pin what it shipped.** FIXED: `submission_leaves` now records `document_content_sha256` — SHA-256 of the SOURCE document's content at filing — plus when the pin was taken, distinct from `checksum` (MD5 of RENDERED bytes, an eCTD index value). Taken from the same org-scoped read that proves ownership, so a concurrent edit cannot be pinned. No backfill, deliberately: a pin computed today would manufacture agreement for every existing leaf. Still open on this row — the IND path supplies no document pointer, so its leaves pin nothing (truthfully) | eng | done | `server/services/submission-service/__tests__/leaf-source-pin.test.ts` |
| L24 | ~~The end-to-end lineage dossier is unreachable.~~ **FINDING WITHDRAWN — it was wrong.** `buildDocumentLineageDossier` is mounted and live at `GET /api/ana-ri/documents/:artifactId/lineage-dossier` (+ `.xml`), via `routes/ana-ri/lineage.ts`, and has been since 2026-08-07 — before the audit that reported it missing. The search that produced the finding guessed at an export name (`assembleLineageDossier`) the module does not use, and I relayed it without checking the mount. Recorded rather than deleted: a register that quietly drops its own false entries teaches nothing about how much to trust the rest | eng | done | `server/routes/ana-ri/lineage.ts` |
| L25 | **No stored hash is ever checked against the source bytes.** Recorded as "the byte reader never loads the checksum"; it was a level worse — `file_uploads` had NO digest column at all (id, user_id, original_name, mime_type, file_size, storage_path, status, created_at, organization_id), so nothing about an uploaded document was verifiable after the fact and keeping `file_size` consistent was all that was needed to hide an alteration. Now: `checksum_sha256` is recorded by both writers (chat upload and `saveDerivedUpload`) over the bytes as received, and `loadUploadedFile` — the single module that resolves an upload id to bytes, so no caller can opt out — re-derives SHA-256 and REFUSES a mismatch by throwing rather than returning flagged bytes, because a caller holding a Buffer will use it. Three states kept distinct: verified, unverifiable (pre-checksum rows, left NULL **on purpose** — hashing today's bytes would record existing corruption as authentic, which is worse than an unverifiable record), and mismatch. The tamper test uses a SAME-LENGTH substitution so `file_size` cannot be what catches it. **STILL OPEN**: this covers `file_uploads`; `verifyIntegrityChain` still verifies the extracted-text artifact rather than the originating document (L122's open half), and no background job sweeps stored files for drift — verification happens on read | eng | done | `tests/db/upload-integrity-check.dbtest.ts` |

### 5.1 The approval half — signatures and post-signing change

The audit's second segment asked whether an approval can be trusted: is a signature bound
to the *content* it approved, and does a later edit invalidate it? The binding is written
well on two paths and **never checked anywhere**. Verified by reading the code.

| ID | Item | Owner | State | Evidence |
|---|---|---|---|---|
| L26 | **A live route mints signatures with no content binding and no tenant.** FIXED by deletion, not repair — repairing it would have left a second live signing surface beside `/api/esignature/sign`. The writer is gone, the route answers 410 naming the canonical one, and the verification endpoint survives. One signing entry point per substrate, one INSERT | eng | done | `server/services/__tests__/signature-write-path-single.test.ts` |
| L27 | **The one live signature-verification endpoint cannot see a content change.** FIXED: it now re-derives the §11.70 binding through the same shared evaluator the canonical validator uses, reports `attestsToContent` separately from `valid`, and distinguishes BROKEN from NOT_RECORDED. A rewritten document can no longer report valid | eng | done | `server/services/__tests__/signature-content-verification.test.ts` |
| L28 | **The real tamper check is never asked — CLOSED, and the row had the two verifiers the wrong way round.** It asked for the service-only `validateElectronicSignature` (no production caller) and the LIVE `verifySignatureIntegrity` behind `GET /electronic-signature/:id/verify` to be consolidated. Reading the writer settled which one was right: `persistElectronicSignature` — the ONLY writer of `electronic_signatures.signature_hash` — stores `sha256(JSON.stringify(signatureManifest))`. The dead method re-hashed the manifest and its real-writer round-trip passed. The live endpoint re-hashed an identifier payload `{documentId, versionId, signerId, signerEmail, signatureType, signatureMeaning, timestamp}` that NO writer has ever produced, so `hashValid` was false on every real row: the one production verification endpoint reported `hashIntegrity: COMPROMISED, valid: false` for every genuine signature. Its suite was green because its fixture built rows in the identifier shape — a verifier proved against a row that does not exist. Consolidated onto ONE recipe: `manifestSignatureHash` is exported from the writer and called by the writer AND the verifier, so they cannot drift again. The duplicate method and its private helpers (`checkSignatureExpiry` — signatures do not expire under Part 11) are deleted; nothing else referenced them. The round-trip contract now goes real writer → LIVE endpoint. Falsified: restoring the identifier recipe makes the real writer's signature verify as COMPROMISED, the exact bug, on demand | eng | done | `tests/schema-contract/esignature-verify-roundtrip.contract.test.ts` |
| L29 | **A stale approval read as a current one.** RE-FRAMED on inspection: version content is immutable (zero UPDATEs to `document_versions`), so an edit mints a new version and the old signature correctly covers the bytes it covered — invalidating it on edit would be wrong. The real defect was that nothing could SAY so: the Artifacts Center derived `is_signed` as EXISTS-any, so an artifact signed at v3 and edited to v7 showed the signed badge for content nobody approved. Now reports the signed version and a staleness verdict, null when unresolvable | eng | done | `server/routes/__tests__/artifacts-center-signature-staleness.test.ts` |
| L30 | **`POST /api/ana/submission-chat/apply-rewrite` could not succeed.** FIXED: the snapshot INSERT now names only columns both DDL lineages create, and a version row is written for the NEW content too — previously the ledger held only superseded states, so the e-signature's `artifact_version_id` pointed at the predecessor while its hash covered the new text. Proven against the real DDL in PGlite, including a regression asserting the old statement still fails | eng | done | `server/services/ana/__tests__/apply-rewrite-version-columns.test.ts` |
| L38 | **`artifactVersionStore` writes `updated_at`, which no migration creates.** The drizzle model declares it, so a drizzle-push (install-fresh) database has it and a migration-provisioned one does not — the same 42703 class as L30, latent on exactly the deployments that matter. The column joins the migrations, because shared/schema.ts is the canonical source install-fresh pushes; removing the writer would leave pushed databases carrying a column nothing maintains. Reproduced first — dropping it from a freshly provisioned database and replaying the writer's own column list gives `column "updated_at" ... does not exist`. Narrower than recorded here: all three creating migrations DO create updated_at on the PARENT table `concept2cure_artifacts`, which is why it survived — the neighbour looks right. **Searching for the rest of the class found five more**, each written by a module and created by no migration: `concept2cure_signatures.created_at/.updated_at` and `regulatory_audit_logs.created_at/.updated_at` (verifiedSealService, submission-chat-apply-rewrite — a Part 11 signature and the audit row beside it) and `concept2cure_submission_snapshots.updated_at` (exportGovernance). The same search flagged `knowledge_graph_nodes/edges.organization_id` and was WRONG: `20260813_knowledge_graph_tenant_keys.sql` adds them inside a DO block via `format('ALTER TABLE public.%I ADD COLUMN organization_id INTEGER', t)`, so the column name is in a format string and no textual scan can see it — a migration set that builds DDL dynamically cannot be audited by grep, and anything such a scan flags needs hand-confirmation before it is acted on. Now guarded by a post-deploy invariant asserting all 8 written columns exist, shown failing (exit 1, naming the column) with one dropped | eng | done | `node scripts/db/deploy-smoke-assert.mjs` |
| L31 | **AnA could destroy section history on the 510(k) surface.** FIXED: `write_kit_section` now snapshots the prior content and writes a `cerv2_section_versions` row through the shared `recordCerv2SectionVersion`, inside a transaction with the content UPDATE and behind a `FOR UPDATE` lock — so content and history commit together, and two writers cannot claim the same version number | eng | done | `server/services/cerv2/__tests__/section-version.test.ts` |
| L44 | **RETRACTED — the fabrication path was already retired, and my number was a prefix grep.** This row said 72 surfaces call `useLive` and 4 render `SampleTag`, and I repeated it in the design brief, the surface-review canvas and the white-paper assessment. It is wrong. `useLive` as a SUBSTRING matches `useLiveData`/`useLiveRows`, and `liveGet` matches `liveGetOrNull` — the fixture-free successors. At token granularity (`\b(useLive|useLiveList|liveGet)\s*[<(]`): **1 of 118 surfaces** uses a fixture-backed helper (`DispatchReadiness.tsx`), and **72 use the fixture-free ones**. The denominator migrated; the ratio never moved because it was never that ratio. Nor can the one remaining site fabricate: both its `liveGet` calls pass `null` as the fixture and bail on `.sample`, so a failed discovery yields an honest empty state. The successors already draw the distinction this row asked for — `DataState` carries `error` and `empty` separately — so the three-way split I started building was a second answer to a solved question, on a layer slated for deletion; it is reverted. What remains is retirement, not adoption: two one-line swaps, after which `useLive`, `useLiveList`, `liveGet`, `matchesShape` and the sample-true `SampleTag` branch are dead and the fabrication path can be deleted outright. **The lesson is the one this session kept relearning and I still got caught by: a prefix grep gives confident wrong answers here.** Superseded by L72 | eng | done | `client/src/concept2cure/v2/dataConnect.tsx` |
| L46 | **Fifteen deterministic JSON serializers feed hashes and signatures, and they disagree.** The sorted-key serializer whose entire purpose is that equal content hashes equally is written fifteen times. A hand count found nine; the gate written to hold the number found six more, in `audit/audit-archive.service.ts`, `audit/signedAuditExport.ts`, `part11/signature-persistence.ts`, `part11ComplianceService.ts`, `resolution/receipt-store.ts`, `tenant-export/attestation-report.service.ts` and `stats/compute-cache.ts` — that is, in the Part 11 and audit-export paths whose whole value is that a hash means something. They do not agree: `routes/chat/provenance.ts` maps `null` to the empty string and emits output that is not valid JSON (`{"a":1,"b":}`); `stats/rng.ts` writes an explicit `null` for a key others drop; `part11ComplianceService.canonicalize` sorts only the top level, so the same signature payload canonicalizes two ways depending on insertion order. Nothing is mismatched today only because each write path and its verify path use the same copy — agreement by coincidence, one edit from a chain that stops verifying. PARTIAL: `shared/canonical-json.ts` is now the one canonicalizer (promoted from `shared/ivdr/`, where it sat with zero importers), pinned by 20 golden vectors on exactly the cases the copies disagreed on, and `ci:canonicalizers` holds the count at 15 and refuses a sixteenth. The 6 Class A sites — where the digest is written and never recomputed, or is a cache/dedup key — are re-pointed, taking the gate from 15 to 9; `provenance.ts`, the copy emitting invalid JSON, is among them. The remaining 9 are NOT mechanical: each recomputes a digest from stored content and compares it to a stored one, so re-pointing invalidates every record it has written. `docs/CANONICALIZATION_MIGRATION_2026-08.md` owns the per-site disposition, and the mechanism it prescribed now exists: `shared/versioned-digest.ts` (11 tests) after L55 proved the shape at the smallest scale it could be demonstrated. Steps 1–4 are under way: Class A re-pointed, L55 fixed, the mechanism extracted to `shared/versioned-digest.ts` (12 tests), and the first Class B site — `report-os/sealing/seal.ts` — migrated with 9 tests covering the boundary. That migration surfaced a trap now written down as rule 3: passing a stored marker into a default parameter silently verifies every legacy record with the current serializer, because a JS default fires on `undefined`. The IVDR pair was then re-pointed together and, on tracing its verify path, turned out to be Class A rather than Class B — a pack manifest hash is displayed, never recomputed and compared — so no tag was needed and the byte-identical duplicate is gone. Gate 15 → 7. Correcting that classification is the method worth keeping: classify by tracing the verify path, not by how important the artifact sounds. Four sites remain | eng | in-flight | `docs/CANONICALIZATION_MIGRATION_2026-08.md`, `scripts/ci/check-canonicalizers.mjs` |
| L55 | **The §11.10(e) tamper-evident audit export signs a manifest whose nested fields are blank.** `audit/signedAuditExport.ts` canonicalizes with `JSON.stringify(manifest, Object.keys(manifest).sort())`. The second argument to `JSON.stringify` is not a sort — it is a key allow-list applied at every depth, and its order is ignored. Because no nested key name also appears at the top level, `queryFilters`, `chainIntegrity` and `compliance` all serialize to `{}`. Demonstrated: two exports, one for January 2026 with a valid chain and one for all time with the chain broken at row 4, produce byte-identical signed manifests. `dataHash` is top-level and IS covered, so modification of the exported rows is still detectable — what the signature does not cover is the manifest's own claims about which records were exported and whether the chain verified, so a chain-integrity failure can be swapped for a pass without invalidating the signature. The verify path at line 352 repeats the same expression, so signatures do verify and the defect is invisible from inside. Found by `ci:canonicalizers` within an hour of that gate existing. FIXED: `canonicalizeManifest` now dispatches on a `manifestVersion` field the manifest carries itself. New exports are stamped version 2 and signed with `shared/canonical-json.ts`, so the signature covers every field at every depth. Exports issued before the fix carry no marker, resolve to version 1, and are canonicalized by the original expression reproduced byte for byte — so their signatures still verify. The v1 branch is frozen, reachable only from the verify path, and deliberately still carries the defect, because reproducing what was signed is its entire job; deleting it would not repair those signatures, it would make them unverifiable. `ci:canonicalizers` correctly still counts this module at 9 — v1 is a second serializer until the last pre-fix export ages out. 8 tests covering both halves: a broken chain no longer signs identically to an intact one, and the legacy string is pinned | eng | done | `server/services/audit/__tests__/signed-audit-export-canonicalization.test.ts` |
| L56 | **The platform's regulatory classification vocabularies disagree with each other.** 507 exported type/interface names are declared in more than one module, and 170 of those are declared in `shared/` AND redeclared outside it — `shared` exists to be the one definition. The redeclarations are not synonyms: `Severity` has **8 different member sets** across 13 declarations (`critical|high|low|medium` vs `critical|informational|major|minor` vs `error|info|warning`); `FindingSeverity` has 5 across 9; `RiskLevel` has 6 across 9, splitting `medium` from `moderate` from `extreme` from `very_high`; `Agency` has 5 across 5, including `FDA` beside `fda` and one list of 10 agencies beside one of 3; `Jurisdiction` has 7 across 7, one of them just `eu|us`; `Region` has 6 across 7. Consequences are user-visible rather than internal: a finding marked `major` by one engine and `high` by another cannot be sorted or rolled up together, two dashboards counting `moderate` and `medium` risks report different totals for the same risks, and an agency recorded as `FDA` in one service will not join a row recording `fda`. This is the domain's own vocabulary, fragmented. PARTIAL: `shared/regulatory/vocabulary.ts` lands the fix for both halves, which are different problems. The identity vocabularies — `Agency`, `Jurisdiction` — get one canonical spelling plus `normalizeAgency`/`normalizeJurisdiction` accepting every historical variant that was live, so a call site can adopt the type without migrating a column holding `fda`. The ordinal scales are deliberately NOT merged: `risk-4`, `deficiency-4` and `diagnostic-3` are separate named registries, because a three-point validator output and a four-point inspection classification are different regulatory instruments that share the word severity, and flattening them destroys the distinction an inspector relies on. `rankIn` gives each member an explicit ordinal — which string comparison does not, and is why `major` currently sorts above `high` — and `translateScale` makes crossing scales a visible, greppable, deliberately lossy call rather than an accidental comparison. 14 tests, every alias in them a spelling that was actually in the tree. Remaining: re-point the 170 local declarations, each a decision about stored data, and a gate on redeclaring a `shared/` type name | eng | in-flight | `shared/regulatory/vocabulary.ts` |
| L57 | **50 circular import cycles.** `madge --circular` over `server/services` and `shared`. 14 are one pattern — `shared/constants/domain/index.ts` is a barrel that imports its members while several members import the barrel back, which works under the bundler and makes module init order load-bearing. The rest are pairwise service cycles: `memory-context-assembler ↔ memory-orchestrator`, `ind-form-fill-service ↔ ind-form-reconstruct`, `design-gates ↔ schedule-of-activities`, `ui-surface-registry ↔ ui-surface-registry.ui-v2`, and a three-hop `deep-investigation → AnaToolDefinitions → agentic-workflow-tools`. None is failing today; all of them make a module's exports depend on who imported first, which is the failure mode that appears once under a different entry point and never reproduces. PARTIAL: the barrel cycle is broken — `DomainEntry` moved out of `index.ts` into `shared/constants/domain/types.ts`, a leaf that imports nothing from the directory, and the barrel now re-exports it from there. **50 cycles → 36.** A type belongs below the barrel that re-exports it, never inside it. Remaining: the pairwise service cycles, then `madge --circular` as a ratcheting gate | eng | in-flight | `shared/constants/domain/types.ts` |
| L58 | **194 modules answer "which organization is this request for" from the raw request, in their own way.** A hand count of the exact name `resolveOrgId` found 57 definitions in 17 distinct precedence orders; the gate, matching on shape rather than name, found **194 modules** — the rest are called `getOrgId`, `resolveOrganizationId`, `resolveOrgIdForSlug`. Return types differ too (`number`, `number | null`, `number | undefined`), so the *absent* case is handled three ways as well. Alongside them `resolveUserId` (47) and `fail` (51): a 14-line block of tenant resolution plus error mapping is the most-copied code in the repository. The implementations genuinely differ in precedence: 35 files read `tenantId ?? organizationId ?? user.organizationId ?? user.tenantId`; the canonical one in `server/types/auth-request.ts` — documented as "safe to call from any route handler" — reads `organizationId ?? tenantContext.organizationId ?? user.organizationId ?? tenantId`, **the reverse order**; `ectd-export.ts` reads `user.organizationId` before any tenant-scope field; `dossier-readiness.ts` ignores `req.organizationId` entirely. **This is latent, not a live leak, and the distinction is the finding.** They converge today only because `establishRequestTenantScope` (mounted at `middleware/auth.ts:180`) sets `req.tenantId` and `req.tenantContext.organizationId` from one value, so every precedence order lands on the same answer. That is agreement by coincidence on the control whose whole job is tenant isolation: any new middleware that populates these fields from different sources, or any change to which fields get set, silently changes which tenant 194 modules serve, and no test covers the divergence. Fix in this order — (1) `ci:tenant-resolvers`, landed here, refusing a 195th, (2) integration tests that assert every mounted route resolves the same org for a request where the fields deliberately disagree, (3) only then re-point the 57 onto `auth-request.ts`. Do not reverse 2 and 3: re-pointing without the tests is the change most likely to cause the leak it is meant to prevent | eng | in-flight | `scripts/ci/check-tenant-resolvers.mjs` |
| L60 | **The gate against internal identifiers in customer copy saw almost none of the copy.** `ci:internals-in-copy` exists, passes, and reported 0 findings across the whole client — while a live `/api/ind-checklist` sat in the IND Lifecycle eyebrow, which is exactly the class it was written to catch (UAT report BP-07). Its JSX-text rule was `/>([^<>{}\n]{3,400})</`: copy had to sit between `>` and `<` on ONE line with no JSX expression between. Measured against the four shapes real copy takes, it caught one — `<p>text</p>`. It missed text following an expression (`{I.icon} label`, the dominant kicker idiom), text spanning lines (how nearly all JSX here is formatted), and both. FIXED: the run now spans lines and tolerates embedded expressions, which a `jsxProse` helper strips before the patterns apply; `.ts` files are excluded from the JSX rule. **The corrected gate found 12 real leaks across 10 surfaces where it had reported zero** — routes in the Admin, Biostatistics, DesignControls, eCTD co-author, ReportEngine, ReportGovernance, TemplateLibrary and Vault eyebrows, and source filenames in PyramidAnalytics and UsageBilling. All 12 fixed; the gate is at 0 and was verified by re-introducing the original line and watching it fail. Two wrong turns are recorded in the source: anchoring on `[>}]…[<{]` matched plain TypeScript, and an alternation under a lazy quantifier backtracked catastrophically and hung the gate — a CI gate that hangs is worse than one that misses | eng | done | `scripts/ci/check-internals-in-copy.mjs` |
| L61 | **A tenant gate meant for two routers was gating twenty, and that is most of BP-02.** `register-inline-routes.ts` stacked 20+ routers on `/api/mdx` and wrote `app.use('/api/mdx', requireTenantContext, mdxAnaDraftsRoutes)` for the two that query the global pool. That reads as "gate this router" and is not: path-mounted middleware runs for every request matching the prefix, whichever router serves it. So `/api/mdx/notifications/unread-count` — mounted below with no auth **by design** — answered 401 whenever tenant context was unresolved, which is exactly the 500-then-401 alternation the UAT report recorded. Proven with a real Express app before and after, not inferred. FIXED: the gate is bound to the two URL sub-trees those routers own (`/api/mdx/ana-drafts`, `/api/mdx/vault`), so it still refuses them without tenant context and no longer touches the other eighteen. 5 regression tests, including one asserting the OLD behaviour so the difference is visible, and a source scan that strips comments first — the explanatory comment quotes the defective call, and an unstripped scan failed on its own explanation. Swept for the same shape elsewhere: two other prefixes stack gated routers (`/api/concept2cure`, `/api/ai`) but apply the SAME middleware to every mount, so the over-application is intended there and harmless | eng | done | `server/bootstrap/__tests__/mdx-prefix-gate.test.ts` |
| L62 | **The BioPharma UAT report's seven P0 blockers, re-measured: five closed, one root-caused today, one open.** Re-verified each against trunk rather than against the report, because it was written 2026-08-14 and trunk has moved. **BP-01** raw `PENDING_STORE` token: fixed, with a regression test asserting the banner never shows it; the 503 itself is deliberate fail-soft on an unprovisioned store, so what remains is provisioning, not code. **BP-02** "half the specialist services are down": five of six are mounted and route correctly; the one real cause was the `/api/mdx` prefix gate, found and fixed today (L61). **BP-03** "the editor is a plain textarea": fixed — `v2/editor/RichSectionEditor` (TipTap, with `TableKit`, which is the CTD requirement) is imported and rendered by `DocumentAuthoring`, and its test drives a real editor instance. **BP-05** Word export 500: fixed — the cause was `require('docx')` in an ESM package, now `await import`, and the silent-failure half is closed too by an explicit failure toast. **BP-06** frozen document unreachable: fixed — `frozen` is in the status filter. **BP-07** internal identifiers in copy: closed today, including the gate that was blind to them. **BP-04 remains open in substance**: the compile → validate → dispatch chain has unit coverage but has never been exercised end to end against a real sequence, which needs a provisioned store; its copy-disclosure half is closed. **The pattern worth keeping: the report's observations were accurate and its diagnoses frequently were not** — a stale install, an eyebrow string, a middleware scope. Re-measure before scheduling work off it | eng | done | `docs/GA_COMPLETION_LEDGER_2026-08.md` |
| L63 | **Two regulatory filing taxonomies, 113 items and 158, both rendered.** FIXED, and the row was stale in a way worth recording: BP-W1-2 had already done the hard half. `shared/regulatory/filing-catalog.ts` is the one view over `GLOBAL_REGISTRY`; `RegistryBridge` was migrated onto it, and `v2/fixtures/filings-catalog-data.ts` — which the row cites as a second taxonomy — already DERIVES `FILINGS_TAXONOMY` from it and authors only the workflow-code tables. `FilingsCatalog` computes and renders its own total. So the content decision the row said blocked this (subset or divergence) had already been made. **Both counts in the row are also wrong now**: the canon holds **234** entries across 4 segments and 21 categories, 19 agencies, 13 regions — measured, not quoted. What was genuinely still broken: one written-down count, `'Canonical taxonomy of 113 filing types'`, in the surface registry's `notes` — which the client renders verbatim as the scaffold page's subtitle. So the product still answered "how many filing types" two ways, 113 on one screen and 234 on the other, exactly as the row described, just from a file the row never named. Removed rather than corrected to 234: counts are computed, never written down. Pulling that thread found L135 | eng | done | `shared/regulatory/filing-catalog.ts` |
| L135 | **25 of 110 rendered surface notes carried API routes, source paths, a table name and an agent branch name — and the gate for exactly this was green over all of them.** `check-internals-in-copy.mjs` enforces "no routes, tables, env vars or file paths in client UI"; its walk is `git ls-files 'client/src/**'`. The surface registry declares its copy in `shared/constants/ui-surface-registry{,.ui-v2}.ts`, and the client renders each `notes` value verbatim in three places: the scaffold page's subtitle under the `<h1>` (V2App's fallback for any surface with no view — a customer-reachable page), the Coverage card note, and the nav card `title=` tooltip. A gate that reads only where copy is USED cannot see copy DECLARED one directory over, so it had never once looked at the file. Worst single case: the eTMF note told the reader that endpoints "are on the feature branch `claude/doc-mgmt-structure-alignment-4p91eu`" — an internal branch name, on a customer page, naming a branch CLAUDE.md RULE 0 says must not exist. Fixed both halves: every rendered note rewritten as product copy, and the engineering detail MOVED to a new non-rendered `engineering` field rather than deleted — it was useful to whoever maintains the surface, it was only ever wrong on screen. The gate now reads the registry's rendered fields and skips `engineering` deliberately. It caught two more on its first run that my own sweep had missed (`dispatch-gate.ts`, `SubmissionPyramidEngine.ts` in prose). Verified by re-injecting the branch-name note: caught | eng | done | `scripts/ci/check-internals-in-copy.mjs` |
| L64 | **The em-dash convention held in 2,287 places and not in 399.** UAT BP-P1-7 reported literal double-hyphens standing in for em-dashes across the CMC, Nonclinical, IND-lifecycle, eCTD co-author and shadow-review surfaces. Measured across all v2 surfaces: 2,287 real em-dashes against 399 literal ` -- ` in rendered copy — 15% of dashes off-convention, in strings a customer reads (`21 CFR §11.50 -- e-signature`, `CMC -- Module 3`, `Enter a reason of at least 3 characters -- it is written to the audit record`). FIXED: all 399 converted across 41 surfaces, in two passes — string literals first, then JSX text nodes — with every changed line diffed to confirm it differs only in the dash. ` -- ` between alphanumerics is never valid TypeScript, since decrement takes no surrounding spaces, so the substitution cannot reach code. Deliberately NOT converted: occurrences adjacent to a template-literal expression inside generated SAP and TLF Markdown, because that is document content rather than UI copy and changing it edits a regulatory artifact | eng | done | `client/src/concept2cure/v2/surfaces` |
| L65 | **The UAT report's P1 list, re-measured: five closed, one open, one half-open.** Same exercise as L62, same result. **BP-P1-1** "Biotech and Pharma are the same product with different chips" — closed, and the report was right about the cause: the lanes held the same 57 surfaces differing only by an accidental ordering swap, so they were MERGED into `biopharma`, on the reasoning that company label was the wrong axis and review centre, pathway, fee programme and CMC core are functions of MODALITY. **BP-P1-5** modality — closed by the same work: `shared/regulatory/modality.ts`, schema modality codes, read at project creation. **BP-P1-2** the false "You're close" at 0% readiness — closed, with an `assessmentState` module and 23 tests distinguishing assessed-and-clear from nothing-assessed. **BP-P1-3** the assurance calculator showing placeholders as values and fusing label to value — closed by ONE CSS rule: 47 controls across 11 surfaces had no border, no background and `display:inline-block`, so a field was indistinguishable from the text beside it; `display:block` plus real chrome fixed both symptoms at once. **BP-P1-7** typography — was real, fixed today (L64); its portfolio-filter half is closed structurally, `CLIENT_CATEGORIES` is now DERIVED from the canonical `SEGMENTS` so it cannot omit a category. **BP-P1-4** two filing taxonomies — genuinely open (L63). **BP-P1-6** half-open: the 401 on `/api/mdx/notifications/unread-count` was the prefix gate and is fixed (L61), but the naming stands — `TaskTray` is rendered from the Shell, so a device-namespaced endpoint really is on every lane's critical path | eng | done | `client/src/concept2cure/v2/registryModel.ts` |
| L66 | **AnA did not work at all, and the platform reported itself healthy the whole time.** A tester opened the product and every question came back "AnA is unreachable". Root cause, reproduced on a real boot: no AI provider key was configured, so the gateway initialized with `providers: ` empty, so `POST /api/ana-ri/stream` returned 503 `GATEWAY_UNAVAILABLE` on every turn (`stream.ts:173`) and `useAnaChat` rendered the unreachable message. **The defect worth fixing was not the missing key — it was that nothing anywhere said so.** The boot log carried ~200 green checkmarks, every one of them reporting that a ROUTE HAD MOUNTED and not one establishing that anything behind a route could do work; the single line naming the fault was pid-stamped JSON at debug level in the middle of them. `/readyz` — the probe an orchestrator uses to decide whether to send this process live traffic — checked database, schema, Redis and the Bull worker, and never asked whether the platform's headline capability had a model behind it, so the process answered `ready: true` and then failed every AnA request it was given. That is the same shape as the incident already recorded in `readiness-state.ts` one layer down, where a schema check that had never run reported ready; the lesson did not generalise on its own, so it is written out again in `ana-readiness-state.ts`. FIXED in four places, so the failure cannot be reached silently from any direction: a critical startup invariant (`ana_ai_provider`), a fail-closed `/readyz` dependency where `'unknown'` is a failure and `'deterministic'` passes but is never collapsed into `'ready'`, an unskimmable boot banner, and the setup script that generated the keyless `.env` in the first place now writes the placeholder and warns on an existing file. Verified by making it fail: with the AnA check neutralised the load-bearing case returns **`expected 200 to be 503`** — the original defect, reproduced — and a live boot with no key now prints the banner, lists `ana_ai_provider` among the invariant failures, and serves `503 {"failed":["ana"],"anaState":"no_provider"}`. **The pattern worth keeping: mounting is not readiness.** A checkmark that a route registered says nothing about whether the capability behind it can answer, and a probe that only aggregates such checkmarks will report green through a total outage of the product | eng | done | `server/startup/__tests__/ana-readiness.test.ts` |
| L67 | **AnA had no personality on the surfaces that emit structured output, and "intellectual" was missing from the ones that did.** `ANA_PERSONALITY_CORE` is composed into the RI persona and the cortex prompts, but both submission-chat builders (`buildSystemPrompt`, `buildStreamingSystemPrompt`) opened straight into "You are AnA in submission-chat mode" with none of it — so she read as a warm colleague in one panel and a terse extraction engine in the next, on prose a human still reads out of the `answer` field. Of the four traits the product specifies — warm, intellectual, kind, lightly funny — the first, third and fourth were defined and **intellectual was absent entirely**. FIXED: an `Intellectually alive` trait (mechanism over rule, precision about the edge of what she knows, interest in why a wrong model was reasonable, depth ≠ length) and an explicit `Warm, never performative` restraint clause; plus `ANA_PERSONALITY_BRIEF`, a compression of the same character — not a second definition of it — for surfaces under a strict schema, wired into both submission-chat builders and subordinated in its own text to the output contract. The restraint clause was **found by the test, not by reading**: `no exclamation marks` lived only in `persona.ts`, so every cortex surface inherited the warmth without the guardrail that keeps warmth from becoming cheerleading. The gate now pins all five traits, both guardrails, brief-vs-core drift, and that every AnA prompt builder carries one block or the other | eng | done | `server/services/ana-ri/__tests__/personality-coverage.test.ts` |
| L68 | **A Dependabot bump killed typechecking for a week and the gate reported it as five ordinary type errors.** `chore(deps-dev): Bump typescript from 5.6.3 to 7.0.2` (1b3c154, 2026-08-13) is a MAJOR bump: TS 7 removed `baseUrl` and `moduleResolution: node10`, both of which `tsconfig.json` used. From that commit `tsc --noEmit -p tsconfig.json` rejected the CONFIG and typechecked **zero files** — while exiting 1 and printing five well-formed `error TS` lines. **Every guard in `typecheck-no-regression.mjs` passed.** It already hardens against a compiler that DIES (the OOM incident: a V8 fatal error contains no `error TS`, so the count came out 0 and the gate printed OK). It had nothing for a compiler that runs perfectly and checks nothing. So it reported "error count 5 exceeds baseline 0" and printed its standard remedy — `npm run ci:typecheck:write-baseline` — which would have written `errorCount: 5` and left the gate **permanently green over a dead typechecker**. Nobody ran it, so the repo simply had no typechecking for seven days. FIXED in three parts. (1) The config: `moduleResolution: bundler`, `baseUrl` removed with `paths` made relative, and an explicit `shared/*` mapping — ~33 files import `shared/schema` bare and had been resolving on `baseUrl` alone. `lib` moves to ES2022 (node >= 20 per engines) but **`target` deliberately does NOT**: raising it flips `useDefineForClassFields` to true, changing class-field semantics across ~2,500 files, and repairing a typecheck config must not smuggle in a runtime behaviour change. That restraint was load-bearing — under ES2022 the compiler reported `circuitBreaker.ts` using `name` before initialization, and that error **disappears** at ES2020, so the ES2022 run would have had me 'fix' a non-bug. (2) The 13 real errors the restored config surfaced, invisible since the bump: twelve are one class — a Node `Buffer` handed to `fetch`/`Blob`/`FormData`, which stopped type-checking because @types/node 26 makes `Buffer` generic over `ArrayBufferLike` and `SharedArrayBuffer` is genuinely not a `BlobPart` — now one canonical `toBinaryBody` helper rather than twelve casts, with `byteOffset`/`byteLength` honoured because a pooled `Buffer` is a WINDOW onto a larger arena and passing `.buffer` through would transmit other requests' bytes; the thirteenth is exceljs resolving `Buffer` through the older @types/node bundled under its `@fast-csv/*` deps, cast at that one boundary. (3) The gate now treats a config diagnostic as categorically distinct from a type error, fails with a message naming the real problem, and **refuses `--write-baseline`** so the trap cannot be sprung. Verified by making it fail: reinstating the pre-fix config makes the new guard print "tsc rejected the CONFIG, so NOTHING was typechecked" and `--write-baseline` exits 1 leaving the baseline at 0; with the fix, `tsc` exits 0 with 0 errors. **The pattern worth keeping: a gate is only as good as its assumption that it ran.** This one had already learned that a crashed compiler is not a clean one, and still had to learn separately that a compiler which checks nothing is not one that found nothing. **`npm run lint` (ci.yml:465) remains broken from the same bump and cannot be fixed by a version bump — no `typescript-eslint` release supports TS 7 (latest peer range is `<6.1.0`). It fails loudly rather than silently, so it is visible, but it needs a TypeScript-version decision** | eng | done | `server/utils/__tests__/binary-body.test.ts` |
| L69 | **Nothing AnA learned in conversation could ever become durable memory, on three severed links at once.** The nightly consolidation job — the ONLY path from conversation to durable, cross-thread memory — selected with `JOIN concept2cure_conversations ON cwm.conversation_id = cc.id`, and the live AnA chat path (stream → post-processing → `storeWorkingMemoryForThread`) writes every row with `conversation_id` NULL: 100% of what the real UI produced was structurally invisible to promotion and expired at the 7-day staleness threshold. Whatever the one legacy conversation-keyed endpoint did promote was inserted WITHOUT an embedding, and both recall paths filter `embedding IS NOT NULL` — durable but invisible to semantic search, forever. And the memory-acceptance gate (provenance, external confidence, dedupe, contradiction — built, tested, exported) had zero production callers, its own header still reading "today, memory acceptance is essentially 'write everything'". FIXED as one repair because each fix alone is misleading: thread rows now carry `project_id` recorded at write time (20260820 migration; the writer probes the column and degrades with one loud warning, never loses the write), the job resolves `COALESCE(cc.project_id, cwm.project_id)` over a LEFT JOIN, every promotion runs the gate unmodified — junk and duplicates refused, contradictions stored quarantined as `pending_review` where recall cannot see them until a human decides, honest sub-0.7 confidence stored instead of the hardcoded 0.75 — and promoted entries are embedded. Also closed while here: `ana_relational_profiles` (AnA's own notes about each user and project) and the working-memory embedding column existed on NO durable apply path — fresh installs only — with both consumers swallowing the resulting 42P01 silently; all three migrations are now in `C2C_MIGRATION_FILES` ahead of the tenant-isolation sweep, and the 42P01 swallow warns once, naming the migration. Verified by making it fail: the integration suite against the pre-fix job fails 6 of 8 (the thread-keyed promotion, the embedding, the gate, the quarantine — each on the exact defect it pins), and the migration contract test fails 2 of 7 against the pre-fix set | eng | done | `server/services/__tests__/memory-consolidation.pglite.integration.test.ts`, `tests/schema-contract/ana-memory-migrations-reachable.contract.test.ts`, `server/services/__tests__/working-memory-project-attribution.test.ts` |
| L70 | **The RIM — "the layer that accumulates judgment over time" — had a complete read path and a dead write link, and what little could persist went to a container-local gitignored directory.** `recall_rim_patterns`, `query_rim_patterns_by_domain` and `summarize_rim_intelligence` are registered, tenant-scoped, honest about emptiness — and structurally, eternally empty: `interceptLearnedPattern`, the ONLY caller of `recordPattern`, was itself called by NOTHING, so every signal the four live interceptors captured died in process RAM. The pattern store's "durability" was JSON files under `data/rim-patterns/` — per-container disk that had never held one file — with a hydration race (`getPatterns` fired its persistence load and read the map in the same tick, so the first read of EVERY process returned `[]` even with data on disk) and a write-before-hydrate clobber (`persistOrg` overwrites the org's full set, so a fresh process's first write destroyed all prior patterns). FIXED as one activation: persistence is now a tenant-scoped `project_memory_entries` row per org under `rim_learned_pattern` (the same substrate and org→profile bridge the pattern-registry export already uses; a dedicated Part 11-auditable table is the recorded follow-up), the store contract went async so BOTH operations await hydration (killing the race and the clobber, with per-org write chaining so update-else-insert cannot race into duplicate rows), and the write link is finally connected where judgment already existed in hand: every compliance-scan ERROR records a rule-keyed `deficiency` pattern (rule, not message, so identity aggregates instead of fragmenting into single-occurrence noise), and every `rejected`/`regenerated` feedback records a section-aggregated `rejection` pattern. Verified by making it fail: the learning-loop suite against the pre-fix RIM fails **5 of 5** — nothing recorded, nothing persisted, nothing surviving restart — and passes 5/5 with the fix, including learning CONTINUING across a simulated process restart (the new process increments the pattern the old one learned). Tenant isolation holds through persistence and back; the 30-test ana-tool tenant-scope contract still passes | eng | done | `server/services/intelligence/__tests__/rim-learning-loop.pglite.integration.test.ts`, `server/services/intelligence/__tests__/rim-pattern-store.test.ts`, `server/services/intelligence/__tests__/rim-pattern-persistence.test.ts` |
| L71 | **Contradiction findings that BLOCK PROMOTION were invisible in chat — the block had a builder, a docstring promising the chat context, and zero callers.** `contradiction-watch.ts` renders the contradiction engine's open findings severity-first, tested, with its own module docstring stating it exists so AnA "can fold them into the proactive digest and (optionally) the chat context". The digest half was wired; the chat half was a docstring — `buildContradictionWatchBlock` had no production caller, so a persisted, unresolved finding with `blocks_promotion` authority surfaced nowhere a user actually talks to AnA unless they explicitly invoked the scan/search API. FIXED by carrying the block through the same spine the deadline radar already rides: a fourth org-scoped, fail-soft, 1500 ms-capped entry in the route-layer prefetch, a `_contradictionWatchBlock` orchestrator input injected into the system prompt, and pass-through on both live routes (`stream`, `chat`). Deliberately NOT part of the briefing/deadline alternation: a finding that blocks promotion is live on EVERY turn until resolved, not until mentioned once; the builder returns '' when nothing is open, so quiet orgs pay one capped query and no tokens. The test pins the CARRIAGE (what was missing), not the rendering (already covered): prefetch fetches org-scoped and carries the block, no-org skips the query, and source-contract assertions pin each hand-off line in the orchestrator, both routes, and the builder. Verified by making it fail: all 6 assertions fail against the pre-fix sources; 504 ana-ri tests pass with the fix | eng | done | `server/services/ana-ri/__tests__/contradiction-watch-wiring.test.ts` |
| L72 | **Retire the fixture-backed data helpers; delete the fabrication path.** Successor to the retracted L44. `dataConnect.tsx`'s own note says the legacy helpers stay until the last consumer is gone; that point has arrived. Two consumers remain, both already using the helper as a did-the-read-succeed check with a `null` fixture: `surfaces/DispatchReadiness.tsx:64,69` (`liveGet` → `liveGetOrNull`) and `V2App.tsx:194` (`useLive` → `useLiveData`). After those swaps `useLive`, `useLiveList`, `liveGet`, `matchesShape` and the sample-true `SampleTag` branch have no callers and can be deleted, which removes the ability to render a fixture as live rather than merely labelling it — the strongest available form of the honesty rule. Do the swaps and the deletion together, and check the `*NoFixtures` tests still pass. DONE: both swaps had already landed (`DispatchReadiness` on `liveGetOrNull`, `V2App` on `useLiveData`), so the only remaining callers of `liveGet` / `useLive` / `useLiveList` / `matchesShape` / `SampleTag` were their own tests. All five are deleted along with `LiveResult` / `UseLiveState`; `unwrapList` and `connected()` stay because the fixture-free hooks use them. `liveBranchRender.test.tsx` — the gate that forced the legacy hooks into their "Live" branch — is deleted with them: with no fixture fallback there is no second provenance branch to render, and its every-surface render is what `surfaceRender` already proves. `AdminSurfaces` rendered a hard-coded `<SampleTag sample={false} />` — a "Live" pill with no other branch left to contrast against — removed; nine test mocks still stubbed the deleted names and are cleaned. The seven affected suites (dataConnect, the five `*NoFixtures`, surfaceRender), the six admin suites and the nine cleaned suites pass. The 30 modules under `fixtures/` are NOT touched: 41 files import them for types and label tables, not for stand-in data, and that is a separate inventory MERGE NOTE: origin closed this row in parallel (a2c51d888) keeping a prop-less `SampleTag` that can only ever say "Live"; at merge the deletion won — a pill with one branch is a claim, not information — and origin's reframed `liveBranchRender.test.tsx` (every surface renders its no-data-yet state) is kept as reframed, since it no longer stubs anything | eng | done | `client/src/concept2cure/v2/dataConnect.tsx` |
| L73 | **RIM learned patterns graduate from a JSON blob to their own audited tables — the follow-up L70 recorded, closed same-day.** The stopgap (one JSON blob per org in `project_memory_entries`) had no per-pattern identity, no queryable history, and no provenance a reviewer can inspect — "accumulated judgment" that cannot show its work. Now: `rim_learned_patterns` holds one row per pattern, upserted on its deterministic identity via a real unique index (`ON CONFLICT DO UPDATE` — no more update-else-insert to race), and `rim_pattern_observations` is the append-only trail: one row per observation event, never updated, never deleted. `created_by` is a text actor label, not a users(id) FK — patterns are written by system interceptors, and inventing a user for a machine observation would falsify the record. The persistence module now writes the TOUCHED pattern per event instead of serializing the org's full set, and its loader lazily imports any stopgap blob when the real table is empty for an org, so nothing learned in the interim window is lost — imported once, through the same upsert, blob left inert. Migration is in `C2C_MIGRATION_FILES` before the tenant-isolation sweep (both tables carry integer organization_id, so the sweep policies them in the deploy that creates them), applies twice cleanly in the batch gate, and the contract test pins presence, ordering, both tables, and the upsert index — failing 1 of 8 against the pre-fix set. 25 RIM suite tests including the migration DDL applied for real in PGlite; 163 intelligence tests; typecheck clean | eng | done | `server/services/intelligence/__tests__/rim-pattern-persistence.test.ts`, `tests/schema-contract/ana-memory-migrations-reachable.contract.test.ts` |
| L74 | **The TypeScript 7 bump is reverted, and Dependabot is no longer allowed to make that call unattended.** L68 restored typechecking under TS 7, but the same bump also killed `npm run lint` (ci.yml:465) and that half **cannot be fixed at all** on TS 7: no `typescript-eslint` release supports it — the latest peer range is `typescript: >=4.8.4 <6.1.0`, and there is no v9. The choice was therefore between a faster compiler with no linting, and both gates working. REVERTED to 5.6.3 (the exact pin the bump replaced), verified rather than assumed: `npm run lint` returns **exit 0, 0 errors** (6,531 pre-existing warnings, which do not fail the gate) and the typecheck gate reports 0 against a baseline of 0. The lockfile delta is -375 lines, all of it TS 7's `@typescript/typescript-<platform>` native binaries from the Go port; **no other package moved**, which was checked rather than trusted. **The tsconfig repair from L68 is deliberately KEPT** — `moduleResolution: bundler`, relative `paths`, explicit `shared/*` — because it is valid on both 5.6 and 7, so the config is no longer a blocker for the eventual re-upgrade. Cost accepted: TS 5.6 is markedly slower than the native port. **The durable half is the Dependabot ignore.** Without it the same PR reopens weekly and the incident repeats on whoever merges it next. `typescript` majors are now held with a comment naming both failure modes and the exact condition for lifting the hold — typescript-eslint declaring TS 7 in its peerDependencies. **The pattern worth keeping: a dev-dependency major that disables the checks protecting every other dependency bump is not a routine update.** Two CI gates went dark on one automated PR, and the reason nobody caught it is that neither gate could report its own absence — which is the same failure as L66 (a platform reporting ready with its core feature dead) and L68 (a gate reporting five type errors while checking zero files), three times in one week at three different layers | eng | done | `.github/dependabot.yml` |
| L75 | **AnA's only unprompted surface required Redis, silently.** Every proactive signal except one is computed inside a user's request — session briefing on the first turn, deadline radar per turn, contradiction watch per turn, MDX alerts per MDX turn: a user who never opens chat is never told anything. The one exception, the 7 AM proactive digest (deadlines + open risks + contradictions → notification tray), fired from a Bull cron, and Bull needs Redis — on a Redis-less deploy `initScheduledJobs` logged one warn and no-opped, so the platform's ENTIRE unprompted surface was zero, and nothing said so (the same shape as L66: a capability off with no probe naming it). FIXED with a second TRIGGER, never a second implementation: `digest-heartbeat.ts` runs the same `runProactiveDigest` on a plain unref'd interval (the pattern the submission-chat sweep proves works Redis-free), started at boot ONLY when the Bull queue did not come up — one trigger per deploy shape — behind a loud boot warning that names the consequence. Cadence mirrors the cron (weekdays from 07:00 UTC); delivery is at most once per org per day via DB idempotency on the notification row itself — restart-safe, and correct even if Redis returns mid-day and the cron fires first, because the guard is the row, not the trigger. An org inside its own quiet hours retries on a later tick (its evening is not the day's end); an org with nothing material is not re-queried all day; one org's failure never starves the rest and retries. Registered in background-job metrics so ops can distinguish wedged-since-boot from never-scheduled. 13 tests against real PGlite (the once-per-day guarantee IS a database question), including the restart case and the cron-got-there-first case, plus source-contract pins on the boot hand-off lines | eng | done | `server/services/digest/__tests__/digest-heartbeat.pglite.integration.test.ts` |
| L76 | **A semantic working-memory pilot could not be measured: telemetry reported 'ok' for a semantic hit and a recency fallback identically.** `ENABLE_SEMANTIC_WORKING_MEMORY` is complete end to end (write-side embedding, similarity read, ranking-policy swap, migration, safe-to-flip gates) — and `layerOutcomes.workingMemory` was set to `'ok'` on BOTH branches of the recall, so flipping the flag on in an environment produced no evidence it was actually recalling semantically. The worst state was fully silent: flag on, every semantic search clearing nothing (missing embeddings, un-migrated column), recency quietly answering every turn — indistinguishable from the feature working. The repo's own rule is benchmark before enabling any experimental path by default; a mode you cannot observe cannot be benchmarked. FIXED with a `workingMemoryMode` diagnostic (`semantic` / `recency_fallback` / `recency` / `none`) computed where the branch decision is made, carried through `recordAnaTurn`, and rendered as `ana_ri_working_memory_mode_total{mode}` — `recency_fallback` is the counter that did not exist and is the number a pilot watches. The flag stays off by default: this change makes the benchmark POSSIBLE, it does not pre-empt it. Verified by making it fail: all 6 assertions fail against the pre-fix assembler/metrics/route; typecheck clean | eng | done | `server/services/__tests__/memory-context-assembler-mode.test.ts` |
| L77 | **Session bootstrap read "AnA's past lessons" from a table written by nothing, on a database that mostly did not have it.** `logOutcome()` (ana-capability-registry) is a complete writer — outcome row plus aggregate success/failure counters — with ZERO callers; `loadRecentOutcomeLessons` filtered `lessons_learned IS NOT NULL` over rows that could never exist; and the whole 0013 ana-intelligence family sat on no durable apply path (fresh-install overlay only). FIXED at both ends. Writer: the feedback interceptor — the one place user ground truth already flows — now logs every verdict with the honest mapping (accepted → success, edited → partial, rejected/regenerated → failure) under the deliberately generic `authoring-output` key, because feedback does not know which capability produced the artifact and guessing one would corrupt that capability's counters; a lesson is written ONLY for a rejection/regeneration and states what happened and where, never an invented why. Reachability: 0013 joins `C2C_MIGRATION_FILES` before the sweep; its trailing DO block ALTERed `project_intelligence_profiles` unguarded — a push-surface table a set-only database never has, which would have aborted the whole file — and is now guarded on the TABLE existing (behavior identical on every fresh install, where it exists). The contract test applies 0013 twice against a fixture WITHOUT that table and pins both the guard holding and no phantom table appearing. Along the way the same concurrent-dynamic-import race fixed in L70's persistence was found and fixed here (memoized import). Fail-first: the outcome-mapping test fails against the pre-fix interceptor; 192 intelligence+contract tests pass; typecheck clean | eng | done | `server/services/intelligence/__tests__/rim-learning-loop.pglite.integration.test.ts`, `tests/schema-contract/ana-memory-migrations-reachable.contract.test.ts` |
| L78 | **The restored typecheck gate had still never run in CI, because it was step 72 of 73.** L74 verified `npm run lint` and the typecheck gate locally and pushed. Checking the actual workflow run afterwards showed the whole branch red — and the reason was not either gate. The `lint:` job runs ~70 guardrail steps and only then reads the TypeScript source (`Run ESLint`, `Run TypeScript check`, `Run beta-slice`). GitHub skips every remaining step once one fails, so on b3b2005 the **tenant-isolation guardrail at step 29 failed and all three verification steps were SKIPPED** — along with every downstream job, all of which `needs: lint` (Test, Coverage, Integration, Build, AnA Readiness Tests). **The typecheck restoration that had just landed was invisible in CI, and a skipped step is not red — it is simply absent from the summary.** That is the same defect as L66, L68 and L74 a fourth time, now at the workflow layer: a check that cannot report its own absence. FIXED: the three source-reading steps carry `if: '!cancelled()'` — not `always()`, so a deliberately cancelled run still stops. The job still fails when a guardrail fails; the difference is that a green typecheck can no longer be indistinguishable from one that never executed. The blocking guardrail itself was **upstream code from the same day and a true false positive**: `memory-consolidation-job.ts` inserts into `project_memory_entries` with `cols` = [project_profile_id, project_id, organization_id, …] and binds `memory.organization_id` — genuinely org-scoped, but invisible to a static scanner because the column list is interpolated rather than written literally. Given a `// tenant-isolation-safe:` marker with that rationale; the first attempt did NOT work because the marker sat 7 lines above the SQL literal and `DEFAULT_LOOKBACK` is 6, which the re-run caught. **Still open, and a judgement call for the team rather than a unilateral change: the same short-circuit hides guardrails from each other.** A failure at step 5 conceals steps 6-70. Making all ~70 unskippable would surface every finding per run instead of one at a time, but it changes how CI reads and is a large diff | eng | done | `.github/workflows/ci.yml` |
| L79 | **The AnA readiness gate did exactly what it was built to do, and the first thing it caught was our own CI.** With CI finally executing end to end (L75), `Production Boot Smoke (RLS on, non-superuser role)` failed: the shipped build bound its port (`Server running on http://0.0.0.0:5199`) and then sat at `/readyz` 503 for the full 300s budget. **The cause was the `ana` dependency added in L66.** That job sets DATABASE_URL, JWT/session/audit/MFA secrets — and no AI provider key and no `AI_GATEWAY_DETERMINISTIC`, so `evaluateAnaReadiness()` returned `no_provider` and readiness refused, by construction and by design. FIXED by declaring the posture rather than weakening the gate: `AI_GATEWAY_DETERMINISTIC: 'true'` on that step. Readiness then passes and `/readyz` reports `anaState: "deterministic"` — **never `"ready"`**, which is precisely why the two states were kept distinct in L66 rather than collapsed. **A throwaway `ANTHROPIC_API_KEY` would also have turned the probe green and would have been strictly worse**: it would assert an enabled provider that does not exist, which is the exact class of false green this gate exists to prevent. Verified the ordering holds first: the gateway checks `deterministicMode` BEFORE the production no-provider throw, and no startup guard refuses it in production. **The other two failures in that run are NOT from this work, established rather than assumed.** `Test`: 1 file failed of 1785 (1,781 passed) — `rls-scoped-route-mounts-tranche2.contract.test.ts` asserts `mdxAnaDraftsRoutes`/`mdxVaultRoutes` are mounted behind `requireTenantContext` and gets `false`. Its subject `register-inline-routes.ts` was last changed by `8b432ee` — the L61 fix that deliberately re-bound that gate to the two URL sub-trees it was for — and the contract test was never updated to match. **It has been failing since L61 landed and nothing reported it, because the pipeline was dark.** `Integration Tests` also failed and is NOT diagnosed here; it is recorded as open rather than guessed at. **The pattern worth keeping: the first thing a newly-honest gate catches is usually your own infrastructure lying to itself.** A week of green-looking, entirely dark CI hid a stale security contract test; the moment the pipeline ran, it surfaced | eng | done | `.github/workflows/ci.yml` |
| L80 | **Two contract tests in this repo asserted mutually exclusive things, so CI could not go green whatever the source said.** `rls-scoped-route-mounts-tranche2` required `app.use('/api/mdx', requireTenantContext, <router>)` to be PRESENT. `bootstrap/__tests__/mdx-prefix-gate` — added by L61, which deliberately removed that shape — asserts it is **NEVER** used. Literal inverses. The tranche-2 pair had been failing since L61 landed and nothing reported it, because the pipeline was dark (L75); it was 2 of the 3 tests still red once CI could speak. **The security property was verified before the test was touched, not assumed.** The gate is bound to the two sub-trees (`/api/mdx/ana-drafts`, `/api/mdx/vault`) at register-inline-routes.ts:1169-1170, and every route either router actually serves — `/ana-drafts`, `/ana-drafts/rollup`, `/vault`, `/vault/:artifactId`, `/vault/:artifactId/versions` — was enumerated and sits under one of them. Tenant scoping is intact; the stale test was checking a syntactic FORM, not the property. **The replacement is stronger than what it replaced, and stronger than either existing test.** It asserts the sub-tree gate exists AND that no route escapes it — the step nobody was verifying and that had to be done by hand to resolve this. A sub-tree gate is only sufficient if nothing escapes it, so a future route at `/api/mdx/rollup` would silently bypass tenant scoping while the old form-check and mdx-prefix-gate both stayed green. Verified by making it fail: adding such a route produces `mdxVaultRoutes serves /rollup outside /vault, which the sub-tree gate does not cover`. Behavioural proof (refuses without tenant context, admits with it) stays in mdx-prefix-gate, which drives a real Express app. **The pattern worth keeping: when a fix changes an invariant, retiring the old assertion is part of the fix.** L61 codified its new invariant and left the contradicting one standing; a dark pipeline meant the collision went unobserved for the whole window | eng | done | `tests/rls-scoped-route-mounts-tranche2.contract.test.ts` |
| L81 | **The last red test was a race, and the predicate that lost it was guarding the wrong distinction.** `biopharmaSpecialtyNoFixtures` → "Lifecycle does not claim no post-approval records" failed in CI and **passed 18/18 in isolation**. CI named the mechanism exactly: `expected 'Reading the post-approval records on …' to contain 'could not be read'` — that is the LOADING headline. `UnresolvedLead` renders a headline in BOTH unresolved states, so `waitFor(() => expect(headline()).not.toBe(''))` is satisfied on the FIRST render while the reads are still in flight, and the assertion then runs against the loading sentence. **The surface was correct and was not touched.** Lifecycle's lead combines THREE reads (supplements, CMC changes, renewals) and may only speak once all three answer — deliberately, so a supplement list arriving while the renewal read is pending cannot be narrated as "your portfolio is steady". That correct design is exactly why Lifecycle lost the race first: its loading window is the widest. FIXED in the test with a `settled()` helper that waits for the lead to LEAVE loading rather than merely to be non-empty. It waits on "not loading", never on the asserted text, so the assertions stay non-vacuous. **Orphan carried the identical predicate and had simply not lost yet** — fixed in the same pass rather than left to flake later. Verified by making it fail: delaying the mocked reads 25ms to simulate CI scheduling makes the OLD predicate fail BOTH cases with the exact CI message, and the new one passes 18/18 under that same delay. A sweep for the pattern elsewhere returns only this file (the sole other match is the comment quoting the defective predicate — the same read-my-own-explanation trap L61 and the internals-in-copy gate both hit). **The pattern worth keeping: "it passes locally" is a diagnosis, not a dismissal.** A test that is green alone and red in the suite is reporting a race, and the race here was in the test's notion of "resolved" — non-empty is not settled when the loading state also renders content | eng | done | `client/src/concept2cure/v2/__tests__/biopharmaSpecialtyNoFixtures.test.tsx` |
| L82 | **The restored typecheck gate caught a live bug in a governed authoring path, hours after coming back online.** `BiostatWorkbench.tsx` read `calc.label` at two call sites; `Calculator` declares `title`, not `label`. **This is not merely a type error** — `calc.label` is `undefined` at runtime, so `insertIntoDocument` filed a governed authoring section titled **"undefined — engine result"**, in both the rendered HTML and the saved section. Line 283 of the same file already used `calc.title`, so the intent was never ambiguous; every other `.label` in the file is on a `CalculatorField` or a table row, which legitimately has one. **A week ago this was unreachable twice over**: the typechecker was rejecting its config and checking zero files (L68/L74), and after that the step was being skipped whole (L75). It surfaced on the first runs after both were closed. **The `!cancelled()` change earned its keep in the same run**: step 72 (typecheck) failed and step 73 (beta-slice) still executed and passed, where before it would have been silently skipped. **Recorded because the reasoning was wrong before it was right:** the failure landed on the commit immediately after a test-file edit and was called "an error in my own change" before the log was read. It was not — the errors named a file that edit never touched. **A failure adjacent to your change is not evidence it is your change**, and the log says which; assuming provenance from timing cost a wrong statement that had to be retracted | eng | done | `client/src/concept2cure/v2/surfaces/BiostatWorkbench.tsx` |
| L83 | **AnA answered with silence, and the transport called it a completed turn.** First end-to-end drive of the chat path in this whole sequence — provisioned DB, seeded org, dev-login, real POST to `/api/ana-ri/stream`. The stream emitted `run_started`, three `status` events, `thread_id`, `orchestration`, `done`, `post_done` — and **not one `text` event**. `done` carried `outputTokens: 71` and `turn_status: "completed"`. **The gateway produced the content and the transport never forwarded it**, so `useAnaChat` renders an empty assistant bubble with no error: not a failure state, just nothing. CAUSE: a streaming caller passes `stream: true` with an `onStream` callback and renders ONLY what that callback delivers; `buildDeterministicResponse` returned the content and never invoked it. FIXED at the gateway rather than per-route, so every streaming caller is covered at once, with the sink guarded — a client that disconnected mid-turn must not turn a fixture response into a thrown request. Re-verified live: the `text` event now arrives, and a `grounding_strip` event appears that previously could not, because there was nothing to ground. **This directly undermined L79.** That fix set `AI_GATEWAY_DETERMINISTIC` on CI's production boot smoke and asserted deterministic mode "serves fixed responses". On the streaming path it served none — `/readyz` reported `anaState: "deterministic"` and passed readiness over a chat surface that produced no words. The premise is true now; it was not when it was written. Verified by making it fail: disabling the forward returns `expected 0 to be greater than 0` — zero chunks delivered while the response is fully populated. **Getting here also required diagnosing why local provisioning never worked**: `install-fresh` fails on a NON-blank database because drizzle-kit's schema pull hits `Expected string, received null` on an index expression, which is why CI's blank-DB job passes and every local attempt had not. On a genuinely blank database it installs 774 tables; only pgvector-dependent objects skip, that extension being unavailable here. **The pattern worth keeping: a readiness signal is only as honest as the narrowest thing it claims.** `ana: ok` meant "a provider is configured", and was read as "AnA can answer" | eng | done | `server/services/ai-gateway/__tests__/gateway.test.ts` |
| L84 | **There was no way to ask "does AnA actually answer?", which is why finding out took a whole session.** `/readyz` answers "is a provider configured" — strictly narrower than "AnA can answer", and every defect in this sequence lived in that gap: `ready: true` while every turn 503'd (L66), then `ana: ok` while the transport delivered no words (L83). `npm run verify:ana` closes it by driving the real path — dev-login, POST `/api/ana-ri/stream` — and classifying what comes back into the four states this work actually encountered: **LIVE** (a provider model answered), **FIXTURES** (deterministic; honest for CI, not evidence the provider path works, and refused under `--require-live`), **SILENT** (stream completed carrying nothing), **UNREACHABLE**. Verified against three of the four on a live server, not asserted: UNREACHABLE against a dead port; FIXTURES against a deterministic instance (574 chars, `demo-mode`), with `--require-live` correctly exiting 1 on it; and **SILENT by reintroducing the L83 bug**, where the probe reported `chars: 0` and `OUTCOME: SILENT` **while `/readyz` on that same instance, at that same moment, reported `ana: ok`**. That side-by-side is the whole argument for the probe existing. LIVE is the one state that could not be exercised here — no provider credential exists in this environment, and Claude Code's own session credentials are provisioned to run the agent, not to be spent by the product under test; borrowing them would also not have tested the deployment's own configuration. **That step remains unproven and is named as such rather than approximated.** **The pattern worth keeping: a probe should distinguish the states you were actually confused between.** LIVE-vs-FIXTURES and ANSWERED-vs-SILENT are precisely the two distinctions that cost this sequence the most time, so they are the two the exit code turns on | eng | done | `scripts/ops/verify-ana-answers.mjs` |
| L85 | **A three-engine, two-round investigation presented to the user as the word "Thinking…".** `useAnaChat` was already capturing the intent lens AnA read the question through, every deterministic tool she invoked with its server label, agentic-loop round and status, her extended reasoning, and the deliverable she produced. The rail rendered one line of it — `m.text || (m.streaming ? m.statusPhase : '')` — so every signal needed to show the work was arriving over the wire and being discarded at the render layer. `AnaActivity` renders the record: real tool rows under their real labels, grouped by real round so going back for more reads as exactly that, open while streaming and collapsed to a summary once the answer lands. **No progress bar and no percentage** — the loop runs until AnA decides she has enough, so any bar would be a fiction with a number on it. **The honesty half is load-bearing:** a turn that ran nothing renders nothing rather than an empty frame implying otherwise, a failed step is shown failed rather than dropped, and only SETTLED calls count toward "n steps completed" — the one in flight is not work done. Failures speak in the sentence the server wrote, never `c.result`: that is the raw capped tool payload, and a test pins that `ECONNREFUSED` and `internal-svc` cannot reach the rail, this repo having already shipped internals in copy once. Outcomes (a failure, a deliverable) sit on the COLLAPSED line, not inside the disclosure — an outcome you must expand a twisty to discover is one the product is hiding. **An a11y pass then found two defects the green gates could not see.** `.ana-body` was `aria-live="polite"`, making the entire growing transcript a live region: every streamed token re-read the whole subtree, and the new narrow region nested inside it was undefined behaviour on top. And four rules used `--text-400`, which measures 3.37:1 light / 4.27:1 dark and fails SC 1.4.3 at 10-11px — **`ci:token-contrast` stayed green throughout**, because it carries `--text-400` as a documented exception scoped to LARGE text, so the gate was structurally incapable of catching small text using it. Now `--text-300` (5.21:1 / 6.82:1); only the glyphs keep `--text-400`, needing 3:1 not 4.5:1. Status is announced by one always-mounted SR-only region carrying phase plus outcomes — always mounted because a live region that appears in the same paint as its first content is the documented case AT misses. All four a11y contracts were **verified by making each fail on the exact regression it exists to catch**: nesting the live region back gives `expected 2 to be 1`; mounting it conditionally gives `expected null to be truthy`; dropping `aria-controls` gives `expected null to be truthy`; announcing only the phase gives `expected '' to contain '1 step did not complete'`. **The pattern worth keeping: a gate's documented exception is a hole in its coverage, and the hole is shaped exactly like the thing the exception permits.** | eng | done | `client/src/concept2cure/v2/__tests__/anaActivity.test.tsx`, `npm run ci:token-contrast` |
| L86 | **The tool loop ran with no live line at all, and the first diagnosis of why was wrong.** With the work record landed (L85), the remaining silent window was the loop itself: `tool_use` sets `statusPhase: undefined`, so from the first tool call until the first answer token the user watched a column of settled rows with nothing indicating she was still working — a window that runs many seconds while the model composes from a full round of results. **The initial claim written into the code was that the phase went STALE, reading "Generating response…" over the whole loop. That was false and the test caught it** (`expected undefined to be 'Generating response…'`), which is the entire reason the negative case was written; the comment and the docstring asserting it were corrected rather than quietly dropped. The clear was CORRECT when it was added — the rail rendered `statusPhase` as its single body line, so a phase and a tool row competed for one slot and the row had to win. The work record renders them as different things (the round, and the steps within it), so they now coexist. FIXED as one change in two halves: the client stops blanking the phase on `tool_use`, and `executeTools` announces each round (`Running N steps…`, `Round 2 — running N more steps…`) and its completion (`Reading the results…`). **Both halves are required and the test pins the dependency**: dropping the clear is what gives the round a line to speak on and is ALSO what would let a stale line stand if the round said nothing, so deleting the server emit makes the third test fail with the exact stale string. Counts are read off the real round (`calls.length`, `round`) and a test forbids a hardcoded number; the step LABELS are deliberately not repeated in the phase because they are already the rows beneath it. **Two harness bugs were caught and recorded before any assertion could be trusted**, both producing `undefined` — indistinguishable from a broken fix: reading the phase after the turn completed (`post_done` clears it unconditionally), and draining with a `Promise.resolve()` chain, which never schedules a reader loop awaiting a stream fed from another task. The stream is now held open at the instant being asserted. **The pattern worth keeping: a passing test whose subject has already been torn down is not evidence, and `undefined` is the shape both "correctly cleared" and "never set" take.** | eng | done | `client/src/concept2cure/components/ana/__tests__/useAnaChat-round-status.test.ts` |
| L87 | **A turn cut short by a timeout rendered its partial answer as a finished one, and the carriage that caused it was nobody's test.** Auditing the message the hook builds against what the rail consumes — the same sweep that found L85 — showed `warnings` captured and rendered nowhere in v2. The timeout path is the sharp case: when tokens have already streamed it KEEPS them (`text: m.text.length > 0 ? m.text : …`) and records `'Response timed out'` in `warnings` alone. So on a surface people draft submission content with, an answer that died mid-sentence showed its truncated text with nothing marking it truncated — an incomplete result presented as a complete one, which is the working agreement's "an error is never rendered as an empty result" in its worst direction. Server degraded-mode `warning` events were dropped the same way. FIXED: caveats render directly under the answer they qualify, above the work record and never inside it — the record answers "how did she get here" and collapses; a caveat answers "how much should I trust this" and must not need a twisty opened. Colour is never the only carrier (triangle glyph + sentence, WCAG 1.4.1). **The more valuable half is the gap this exposed.** Deleting `warnings: m.warnings` from `adaptChatMessage` left **1748 tests passing** — measured, not assumed. Every rail suite passes props in directly, so none of them can see the adapter stop handing something over, and that seam is where BOTH regressions lived (the tools, rounds, lens and reasoning of L85 were dropped in the same function). `anaMessageCarriage.test.ts` now asserts the seam field by field, and dropping any of `warnings` / `thinking` / `toolCalls` / `lens` each fails it. **Same shape as L45**, where publisher and consumer were each built and tested and surfaces actually calling it was nobody's test. Also landed here: AnA's reasoning, captured and streamed and rendered nowhere on the rail (`ConversationThread` had it; the primary surface did not) — shown live as the newest stretch while she thinks, in full behind the record afterwards, always **her text and never a summary of it**, since a paraphrase would be inventing a thought she did not have. Its scroll container is a tab stop with a name, because `overflow-y:auto` with no tab stop gives a mouse user the whole deliberation and a keyboard user the first 240px (SC 2.1.1). The same `--text-400` contrast defect from L85 was fixed on `ConversationThread`'s existing reasoning disclosure. **The pattern worth keeping: when every test for a component passes its props in directly, no test can see the wiring stop delivering them.** | eng | done | `client/src/concept2cure/v2/__tests__/anaMessageCarriage.test.ts`, `client/src/concept2cure/v2/__tests__/anaAnswerCaveats.test.tsx` |
| L88 | **A draft whose governed version failed to save looked exactly like one that saved.** AnA announces the deliverable — the rail renders "Drafted <title>" — and `persistCollectedDrafts` writes it to the artifact version history afterwards. When that write threw, the entire response was `console.warn(...)`: a server-side log line, nothing to the client. The turn closed with the deliverable on screen and no version row behind it, so someone could leave the session believing a draft was durably recorded when it was not. In a Part 11 context that is the product overstating what it did. FIXED by emitting a `warning`, which lands in the caveat row added in L87 — the two halves compose, and the server change would have been invisible without it. **The contract is deliberately asymmetric.** Three different situations produce no `artifact_version_saved` event: missing org/project/thread context (skipped by design — `project_id` is NOT NULL), an unchanged content hash (`saved.created === false`, nothing new to record), and a failed write. Only the third is a problem, so only the third speaks; **absence of a save event is never rendered as "not saved"**, because that would state one of three possibilities as fact. The caveat carries no `e.message` — that is an internal database string — while the operator log keeps it, and a test pins that split in both directions. Verified by making it fail: reverting to log-only, leaking the error into the sentence, and warning on an unchanged draft each break their own case. **Two docstrings were also asserting renderings that do not exist**, the same shape as the contradiction-watch defect: `interjections` claimed "Shown as small 'you steered AnA' notes" with ZERO consumers anywhere, and `generatedDraft` claimed "The UI offers an 'Open in editor' affordance" when the rail reads `title` only and nothing routes `content` anywhere (ConversationThread's button belongs to `turn.proposal`). Both corrected to state what is true. A docstring claiming a renderer that does not exist is worse than none — a reader greps for it and finds a claim. **Recorded but NOT built, needing a product decision rather than a rendering fix:** the deliverable AnA announces cannot be opened from the rail. Honouring it means CREATING a governed document, since the payload is content rather than an identifier — `Biostatistics.tsx#openEditor` documents that handoff and is the shape to follow — and a governed write from a chat rail is not something to slip into a legibility change. **Also open: `useAnaChat.ts` sits at 1498 of its 1500-line budget** and has now been trimmed twice to stay under it; the next edit needs the extraction, not another shortened comment | eng | done | `server/routes/ana-ri/__tests__/draft-persistence-honesty.test.ts` |
| L89 | **Closes the file-budget item L88 left open, without touching behaviour.** `useAnaChat.ts` sat at 1,498 of the 1,500-line repo-health ratchet and had been kept under it TWICE by shortening comments — which is the ratchet doing its job and the response ignoring what it was saying. The 420 lines of pure type declarations (`AnaChatMessage` alone is 196) moved to `useAnaChat.types.ts`; the file is now 1,113, so the next edit has 387 lines of room instead of 2. **Types only, deliberately**: declarations are erased at compile time, so the typechecker either accepts the move or names exactly what broke — it named 10 things, all missing imports, and nothing else. The runtime half (the hook, its mappers, the tool-label table) was left alone, because moving code that executes is a different change with a different risk. Every one of the 13 moved names is re-exported from the original path, so all 16 importers are untouched — the seam is internal, not a migration. **Proved rather than asserted, on the two things that could have gone wrong.** That it cannot change behaviour: the types module compiled with esbuild emits **0 bytes** of JavaScript. That nothing was silently dropped: all 1,443 non-blank lines of the original are present across the two files, checked line by line — the same audit that caught a rebuilt file silently losing an endpoint earlier in this effort. 151 client files / 2,153 tests pass unchanged. **The pattern worth keeping: when a ratchet fires twice for the same file, the second fix is the file, not the comment.** | eng | done | `client/src/concept2cure/components/ana/useAnaChat.types.ts` |
| L90 | **Two layout defects in the work record that 2,153 passing tests could not see.** The rail had been built, reviewed and shipped across L85-L88 without anyone ever LOOKING at it — every assertion asked whether text was present, never where it landed. Rendered in Chromium against the real tokens: (1) the failure sentence is a flex SIBLING of the step label in one row, so a short label like "Literature search" was squeezed into a two-line column beside a wide message — the most important row in the record, the one saying something went wrong, was the worst-looking thing on screen; (2) `.ana-activity-read` sat flush left while the phase and step rows begin after a 12px glyph plus a 6px gap, so the line naming how AnA read the question appeared to be a PARENT of the phase above it. Both are layout, and **jsdom has no layout engine** — `getByText` passes identically either way, so this class is invisible to the entire suite and is recorded here rather than pinned by a test that cannot exist. FIXED with `flex-wrap` plus `flex:1 0 100%` on the note and a matching 18px indent on the read line. **Getting to a trustworthy screenshot took three corrections, each of which would have produced a confident wrong answer**: the harness first resolved `--text-300` to EMPTY (tokens live in `design-system/colors_and_type.css`, not `app-v2.css`) so every colour on screen was a browser default; a grep for `=>` and `return` "proving" the extracted types module had no runtime code matched seven function TYPE signatures inside an interface and proved nothing, where compiling it to 0 bytes of JavaScript did; and the dark-mode capture returned the light palette because this app's dark theme is class-driven rather than `prefers-color-scheme`, so a dark screenshot from `colorScheme: 'dark'` would have been the light one relabelled. **The pattern worth keeping: a test suite that only asks whether text is PRESENT cannot tell you the interface is usable, and a green suite is not a substitute for opening the thing.** | eng | done | `client/src/concept2cure/v2/styles/app-v2.css` |
| L91 | **The L90 fix introduced a horizontal overflow, and the L90 screenshot could not have shown it.** That render used a 460px harness; the real rail is `--ana: 380px` with `.ana-body` padding of 12px a side, so the true content box is 356px — **48px narrower than what was inspected**, and looking at a component wider than it ever renders is its own way of not looking at it. Re-rendered at true width, `.ana-activity-step.is-error` measured 361px inside a 343px box: `flex-basis:100%` plus `margin-left:18px` is 100% PLUS 18px, so the note the L90 fix had just moved onto its own line pushed the rail into a sideways scroll. FIXED with `box-sizing:border-box` and `padding-left`, which puts the indent inside the basis; the rail now measures `scrollWidth === clientWidth` (378 = 378) with the record expanded. **Two things were confirmed working in the same pass, by measurement rather than by eye**: the reasoning block scroll cap (`scrollHeight` 776 vs `clientHeight` 240) and its keyboard reachability (`tabindex="0"`), which until now existed only as a jsdom assertion that no layout engine had ever exercised. Three residual overflow hits were checked before being dismissed and are the SR-only live regions, which `SR_ONLY_STYLE` clips to 1px — text wider than a 1px clip is the point of that technique, not a defect. **The pattern worth keeping: a layout fix verified at the wrong viewport is unverified, and the width to check is the one the component actually gets.** | eng | done | `client/src/concept2cure/v2/styles/app-v2.css` |
| L92 | **The visual-QA pipeline could not run at all, and the guard for exactly that reported it clean.** `npm run visual-qa:capture` named two spec files directly, and vitest matches a filter against its `include` globs first — which cover `tests|server|client|shared`, not `scripts`. Both were filtered out, the command exited **"No test files found"**, and the documented `npm run visual-qa` entry point died at step one; `.visual-qa/markup` was empty and all three downstream checks had nothing to read. **`ci:check-unrun-tests` reported "every test file is reachable" throughout, because `ROOTS` never included `scripts/`** — a guard cannot report on ground it does not walk, and a clean result over unexamined ground is the exact failure that file exists to prevent. FIXED: `vitest.visual-qa.config.ts` makes the capture specs runnable (kept separate because they WRITE artifacts and do not belong in `npm test`), and the guard now walks `scripts/` and reads the new config. Verified by making it fail: dropping the config from `VITEST_CONFIGS` makes the guard name all three specs, **including the two that had been dead all along**. **Then the gap that let L91 ship: `visual-qa:overflow`**, which measures every captured fragment at the width its component actually gets and fails on an element wider than its own box. The rail is captured for the first time — `dump-v2-surfaces` walks SURFACE_VIEWS and the rail is shell furniture, so the narrowest column in the product had never been captured by anything. Each capture declares its own width (`_viewports.json`), because L91's defect WAS inspected before shipping — at 460px, where it fits — and only appears at the 356px the rail really gets. Proved on the real defect: reintroducing the `margin-left` variant and rebuilding makes the gate report 5 overflows naming `div.ana-activity-step.is-error 339 > 321` at 356px, and exit 1; the fixed CSS reports 0. Across all 119 captured fragments it found **one pre-existing overflow** (`label.pe-f` on precedent-intelligence, a `select` wider than its flex container at 1440px), recorded in a ratcheted baseline rather than fixed — guessing at another surface's layout intent is not this change's business. **Also surfaced, not fixed: 5 surface captures fail to settle** (`global-ri`, `part11-console`, `registrations`, `source-tracer`, `submission-center`), invisible until the pipeline ran for the first time. **The pattern worth keeping: a tool that cannot run and a guard that does not look are the same defect wearing different clothes — both report nothing wrong.** | eng | done | `npm run visual-qa:overflow`, `scripts/visual-qa/check-overflow.mjs` |
| L93 | **Five surfaces reported as "the surface THREW" and none of them had.** The capture spec replaced the whole of `@/lib/queryClient` with one export — `vi.mock('@/lib/queryClient', () => ({ apiRequest }))` — which holds only while every surface captures cleanly. The moment a read fails, `ErrorState` renders and reaches for `redactInternals`, absent from the stand-in, so the component threw, React unmounted the tree, and the capture saw an empty container and blamed the surface. **The component meant to show an error was the thing that crashed**, and the harness then attributed its own gap to five innocent surfaces (`global-ri`, `part11-console`, `registrations`, `source-tracer`, `submission-center`). **The repo had already diagnosed this exact failure**: `zeroStateNarrative.test.tsx` carries a comment describing it precisely and fixes it with a partial mock over `importOriginal`; this file simply had the older form. Knowledge existing in one file does not propagate to another, which is the argument for the partial-mock shape being the default rather than the remedy. FIXED the same way — 127 of 127 capture tests now pass, up from 122. **Correcting my own report:** these five were described in L92 as surfaces that fail to settle, implying a product defect. They are not; the harness was wrong and the surfaces were fine. **A second effect worth recording:** with the real module partially restored the capture became more faithful, and `crl-library` — which had captured "successfully" before — now renders its real filter row and shows the SAME `label.pe-f` overflow already known on `precedent-intelligence` (a `select` whose intrinsic width exceeds its flex allocation, 207 > 110 at 1440px). Both are recorded in the ratcheted baseline rather than fixed: one shared rule, two surfaces, neither this change's to redesign. **The pattern worth keeping: a total mock is a promise to track the module by hand forever, and it breaks first on the error paths nobody exercises.** | eng | done | `scripts/visual-qa/dump-v2-surfaces.spec.tsx` |
| L94 | **The overflow gate measured a page the product never serves, and the defect it "found" did not exist.** `check-overflow.mjs` wrapped each fragment in a bare `<div id="box">`. **Every product rule is scoped `.c2c-v2 …`** and the surface captures do not carry that class themselves, so 124 fragments were measured essentially UNSTYLED — and the two `label.pe-f` hits reported in L92/L93 as a pre-existing defect on two live surfaces were artifacts of the missing scope. They do not reproduce. **Two further corrections were each confidently wrong before this landed.** Adding `shell` to match `check-contrast.mjs` swung it the other way: `.c2c-v2.shell` is `grid-template-columns: var(--rail) 1fr var(--ana)`, so a surface fragment landed in the **264px left-rail column** and 525 elements "overflowed" a box nothing puts them in — contrast can wrap in `shell` harmlessly because colour does not depend on width, and overflow is the one check for which the container IS the measurement. Then the element rule skipped only `overflow-x: auto|scroll`, reporting 27 hits that were `white-space:nowrap; overflow:hidden; text-overflow:ellipsis` elements — where `scrollWidth > clientWidth` is BY DESIGN, that being what makes an ellipsis appear; anything but `visible` means the author is containing it. **The default width was wrong in the same direction as the bug the gate exists to catch**: 1440 is the viewport, but a surface never gets the viewport — the shell spends 264px on nav and 380px on the AnA rail, so the real column is 796px, and measuring at 1440 grants 644px of room that does not exist. Corrected, 11 occurrences across 6 selectors are real, the sharpest being `mdx__device-cer`: a tab bar of 863px in a 732px pane pushing `page-inner` to 895px in a 796px column (the geometry checks out — `page-inner` pads 32px a side, 796 − 64 = 732). Re-proved on the real defect after the rewrite: reintroducing the `margin-left` variant makes the gate report 4 new overflows naming `div.ana-activity-step.is-error 339 > 321` at 356px, and green on revert. **The pattern worth keeping: a measurement harness is a claim about the environment, and the first thing to verify is that it reproduces the world — three plausible wrappers gave 2, 525 and 27, and only one of them was measuring the product.** | eng | done | `scripts/visual-qa/check-overflow.mjs` |
| L95 | **The CER tab bar is contained, and the fix was proved by the gate refusing to move.** Six tabs at natural width measure 863px in a 732px pane (a 796px surface column less `page-inner`'s 32px sides), so the extra 131px pushed `.page-inner` to 895px and the whole surface scrolled sideways. FIXED on `.cer-tabbar` alone — `overflow-x:auto` with `overflow-y:hidden` explicit, because with only the x axis set the y axis computes to `auto` and `.pwt-btn`'s `margin-bottom:-1px` (which lays the active indicator over the bar's own border) can raise a stray vertical scrollbar. Scoped to the CER modifier rather than `.pwt-bar`, since the outer pathway bar's five tabs fit and a systemic change is not this defect's to make. Every tab keeps its sub-line, which is the point: those name the article each tab answers to, and dropping them — the option that buys the most room — is what separates Equivalence (61(5)) from PMS/PMCF (83 · Annex XIV B) for a reviewer. **The fix appeared not to work, and that was the third reproduction gap in this harness.** After rebuilding, the numbers did not move at all — because `check-overflow.mjs` loaded only the entry and V2 stylesheets, so every `.mdx-shell …` rule was absent from the measurement and the new rule was never applied. `check-contrast.mjs` already had a `sheetsFor()` making exactly this split; this file did not. A fix that produces no change reads identically to a fix that does nothing, and only the gate's stubbornness separated them. Serving each fragment its real sheets confirms CER at **zero** occurrences, and surfaces **22 further genuine overflows** on mdx surfaces that were invisible while their stylesheet was missing — `device-tasks` `div.kanban 1108 > 748`, `device-vault` `div.page-inner 941 > 796`, `stage-node` at `153 > 102` across three device surfaces — recorded in the baseline (30 occurrences, 19 selectors) rather than fixed, being neither regressions nor this change's to redesign. **The pattern worth keeping: three separate reproduction bugs in one harness — missing scope, wrong container, missing stylesheet — each produced a confident number, and every one of them was found by asking why a result did not match something already known.** | eng | done | `client/src/concept2cure/mdx/app.css`, `npm run visual-qa:overflow` |
| L96 | **The sheet fix in L95 was three of twenty, and the one instrument that could say so was already refusing to run.** L95 gave `mdx__`/`pdev__` fragments their route sheet; the build emits **twenty** CSS chunks and fourteen of the remaining sixteen style **239 classes present in the captured markup**. `check-surface-styling.mjs` had been exiting **2** for this the whole time — its self-check asserts `.rc-ana` computes to `display:flex`, `.rc-ana` had moved into the `Insights-*` chunk, and so `npm run visual-qa` died at step two. The self-check was right and the sheet list was wrong. FIXED with `built-css.mjs`, one module answering "which stylesheets" for all three CSS-consuming checks — which also retires the third copy of `sheetsFor()` that L95 had to add because `check-contrast.mjs` already owned one. The trade-off is stated in the module: all-chunks is a superset of one route, the opposite error from a missing sheet and a visible one. With it, `visual-qa:styling` applies 8,903 rules rather than 6,010 and passes, and `v2__ectd-coauthor` and `v2__quality` overflows appear that no run had ever measured. **Two more gaps in the same file:** it never called `assertCaptureIsFresh`, which the README says every check calls, so it would happily measure a stale capture; and it counted **out-of-flow decoration** as overflow — `.ae-pstage` and `.stage-node` are `::after` connector arrows in the cards' own right margins, and taking the pseudo-element away leaves 128>128 and 102>102 exactly, so all seven baselined occurrences were the arrow and nothing else. Candidates are now re-measured with absolutely positioned boxes and pseudo-elements removed. Both that and L94's `overflow-x !== visible` are SUPPRESSIONS, the dangerous direction, so the script seeds one shape of each — a spill it must find, a margin-placed `::after` it must leave alone — and exits 2 if it gets either wrong. **Five product defects fixed rather than baselined:** `.bs-fields` was `1fr 1fr`, whose `auto` floor is each track's min-content, so a `<select>` as wide as "sensitivity specificity" made two tracks 366px inside the 300px `.bs-side` column; `.section-head`/`.seg` did not wrap, so an eight-chip filter row hung over the vault drawer; the SE matrix's verdict column was 32px holding a 47px heading; `.kanban` compressed instead of scrolling, four 260px minimums needing 1,108 in 748; and `.ec-art-head` did not wrap at 392 in 361. **Also: `playwright.mjs` could not load Playwright by the command printed in its own help text** — `playwright-core`'s package main is CJS, so `await import()` returns a namespace whose `chromium` is undefined, and every visual-QA script said "Playwright is not installed" over an installed package. Baseline **13, down from 30**: seven closed as decoration, ten fixed, and thirteen genuinely open (L97). `npm run visual-qa` completes green end to end for the first time. | eng | done | `npm run visual-qa`, `scripts/visual-qa/built-css.mjs` |
| L97 | **Two surfaces rendered their own content at zero width, which is worse than overflowing — both fixed.** `v2__quality`'s `.qms-row` is a grid of eight tracks whose seven fixed columns and gaps total 848px against the 738 a surface pane gives, so `minmax(0, 1fr)` did as it was told and resolved the **title track to 0px** on every row — and `.qms-table`'s `overflow:hidden`, there for the corner radius, clipped the last two columns off the end. A register showing neither the document's name nor its next review date is not a register. `v2__document-authoring`'s `.ed-crumbs` did the same to itself: `min-width:0` is what let it shrink, and against actions at `flex:0 0 auto; max-width:100%` it shrank to **0px**, so neither module, section code nor title appeared. FIXED by containment, not by dropping columns: the register table scrolls and each row carries a minimum **derived from its own grid string** (`registerGrid.ts` — a hand-kept second number goes stale the first time a column is resized), and the breadcrumb has a floor that the action row's `max-width` is computed to leave it, one constant used twice. The title column now measures 160px and reads "Quality manual"; the crumbs measure 180 and read "eCTD › No document". Also closed: `.cv-note` ran 4px over on an unbreakable identifier (`sourceLinks.ts:`) and now breaks the word. **Which columns a governed register sheds on a narrow pane is still a design question and is still not answered** — this only stops the table from answering it by deleting the title. Verified by making it fail: `registerGrid.test.ts` catches the naive tokenizer that splits `minmax(0, 1fr)` on its inner space, and asserts both real registers still ask for more than a pane gives — if that stops being true the minimum is doing nothing and should go | eng | done | `client/src/concept2cure/quality/__tests__/registerGrid.test.ts`, `npm run visual-qa:overflow` |
| L100 | **`DocumentsPanel` ships with no stylesheet at all, and the styling gate cannot see it.** GATE GAP CLOSED; the stylesheet half was already closed by `9bb8b11d6` (2026-08-21) and corrected by `26346b3ef`, so the row was stale on its own headline. Verified mechanically rather than on the commit message: the kit reference holds exactly 48 declaration blocks, and de-scoping the shipped `DOCUMENTS PANEL` section and diffing gives two substantive differences and nothing else — `.docs-row:hover` dropped `--shadow-xs` because that token is `hsl(0 0% 0% / 0)`, alpha zero, i.e. no hover feedback at all; and `.docs-esig-na` moved `--text-400`→`--text-300` for AA on `--bg-100`. Both documented at the rule. The kit file is NOT deleted as a duplicate: `.github/workflows/sync-design-system.yml` makes `design-system/` a read-only mirror of the external design system, so deleting there is undone by the next sync and destroys the designer's reference — the one implementation the build consumes is `app.css`. **The real deliverable is the gate the row said no gate here walks.** `check-component-class-coverage.mjs` asks the weakest useful question — for every literal class a client component renders, does any selector in the built chunks mention it — reads the 20 built chunks through the canonical `built-css.mjs`, and refuses to report against a stale build (exit 2) rather than answering about a product that is not the one under test. **First honest run: 396 class names across 597 sites with no rule anywhere**, 110 of them defined only in the un-imported kit — the exact L100 shape, markup ported and stylesheet left behind — and 282 defined nowhere at all (L139). Wired into the CI Build job after `npm run build`, because a gate nothing runs is documentation with a warning label. Verified by making it fail three ways: deleting the stylesheet section (all ten families reported), rendering a class nothing defines (caught, and correctly parsed out of a template interpolation), and against a stale build (refused). The agent's first attempt used `.docs-panel` itself as the positive control, so removing the subject switched the instrument off — the controls are now the two shell roots. The `.ctable-head` half stays open (L140) | eng | done | `scripts/ci/check-component-class-coverage.mjs` |
| L139 | **396 class names are rendered by client components with no rule in any shipped CSS chunk, across 597 sites.** Measured by `ci:component-class-coverage` on its first run and baselined at 452 entries so it can only shrink. 110 are defined ONLY in the un-imported `design-system/ui_kits/` mirror — markup ported, stylesheet left behind, which is L100's shape repeated — and 282 are defined nowhere at all. Worst offenders: `PreSubManager.tsx` (70), `AnalyticsSurface.tsx` (49), `EngineeringSurface.tsx` (37), `PostmarketSurface.tsx` (31), `RichSectionEditor.tsx` (30). A class no stylesheet defines renders as nothing, and neither jsdom nor `check-surface-styling.mjs` can see it, which is why this went unmeasured until there was a gate for it. Burn the baseline down; each fix is a port from the kit or a deletion of dead markup. SHARPENED 2026-09-02 on the worst offender: `PreSubManager.tsx`'s 70 `ps-*` classes are defined NOWHERE — not in any client stylesheet, not in `ui_kits/`, not in the `design-system/` mirror — so "port from the kit" does not apply; the surface simply never had a stylesheet. And it is not dead markup: `MdxSurfaceHost` routes it as `device-presub`, a Pre-Sub manager customers reach. For this one the stylesheet is now written (`app.css`, the `ps-*` family, same rhythm and tokens as the `.sub-*` two-pane; small text on `--text-300`, since `--text-400` measures below AA for text) — the shipped build defines all 70, and the baseline shrank 452 → 382 entries with nothing added. The next four were checked the same way and were three different things. `AnalyticsSurface`, `EngineeringSurface`, `PostmarketSurface` (117 classes, all routed by `MdxSurfaceHost`) were genuine ports: every class exists in `ui_kits/mdx/surfaces.css`, identical in both kit copies, so those sections are merged into `mdx/app.css` under `.mdx-shell` with the kit's hard-coded status hex mapped onto the semantic tokens. `RichSectionEditor` (30) was neither a port nor a write: it styled itself with a 79-line inline `<style>` — 66 rules, every value carrying a hex fallback beside its token — which the class-coverage, token-cascade and contrast gates cannot see; `RedlineText` and `AuthoringRevisionDiff` did the same. All three are moved into stylesheets (`styles/rich-section-editor.css`, `styles/redline.css`), fallbacks dropped, and `ci:design-system` gained a third rule forbidding a `<style>` element in a component (falsified with a probe). `CroPortfolio` (28, registered) was a write like the Pre-Sub manager. Two `TaskBoard` classes with no rule anywhere and no reader (`tb-views`, `tb-an`) were dead markup and are deleted. Baseline: 452 → 182 entries, nothing added. MERGE NOTE: origin ported the same three surfaces in parallel (06869f5f7, 6255de5a9, 4eb69f182) and its port covered 16 classes this slice had not; at merge origin's `app.css` is kept whole and this slice's surface sections are dropped — the coverage gate on the merged build finds nothing this slice styled that origin's port does not, and the only two entries origin's baseline still carried that this slice's lacked were the dead `TaskBoard` classes deleted here. The inline-`<style>` removals, the two new stylesheets and the `ci:design-system` rule are this slice's and survive. Merged baseline regenerated from the build: 166 entries; 146 class names across 204 sites remain | eng | open | `scripts/ci/component-class-coverage-baseline.json` |
| L140 | **`.ctable-head` overflows on `mdx__device-validation`, and the remedy means touching a shared primitive.** Split out of L100. The shared MDX compact table is `88px 110px 110px 140px 1fr 100px` in 618px, so the Message column collapses to 57.7px and the row runs 680. L97's scroll-plus-derived-minimum is the proven remedy, but `.ctable` has 35 call sites across ~18 distinct grid templates, seventeen of which fit — applying it here means changing a shared primitive everywhere to fix one table. Recorded rather than rushed | eng | open | `scripts/visual-qa/overflow-baseline.json` |
| L141 | **`ui_kits/` and `design-system/ui_kits/` are two divergent full copies of nine kits.** `diff -rq` shows files present in one and absent from the other, plus content differences in `ana_ri`, `ectd_coauthor` and others. `design-system/` is a read-only mirror synced by `.github/workflows/sync-design-system.yml`, so the divergence means the mirror and the working copy disagree about what the design system says — and L139 shows 110 class names whose only definition lives in a kit copy nothing imports. Decide which is canonical and make the other a derivation or delete it. Found while closing L100. MEASURED 2026-09-02, decision still owed: NEITHER copy is imported by product code (no `from` path into either; the three CSS mentions are port comments), so the divergence is between two reference trees, not two shipped ones. `design-system/ui_kits/` was last touched 2026-08-17 and the sync workflow that is supposed to refresh it is manual-dispatch only, requires a `DESIGN_SYSTEM_SOURCE` the repository does not have wired, and has no evidence of ever having run; `ui_kits/` is edited routinely (most recently today). `diff -rq`: 26 files exist on one side only, 12 differ. The honest shape of the fix is a union — move the mirror-only files into `ui_kits/`, resolve the 12 differing files with the designer, delete the mirror and the workflow, and point `check-component-class-coverage.mjs` and `docs/design-system-sync.md` at one tree. Not done here because the 12 conflicts are design content, and because the 110 L139 class definitions that live only in the mirror must survive the move | eng | open | `.github/workflows/sync-design-system.yml` |
| L103 | **My own L97 fix was verified in a fragment and was wrong in the product — found by running the app.** The register row minimum did make the columns align, but a flex item's automatic minimum size is its min-content width, so that minimum propagated up the ancestor chain: `.qms-shell` refused to shrink below **1,094px** inside the 796px surface pane, `.page` grew a horizontal scrollbar, and **the whole quality page scrolled sideways instead of just the table**. The capture harness cannot see this and never could — a captured fragment has no `.page` or `.qms-shell` ancestor, so it showed the intended `738 / 1036 / overflow-x:auto` either way. This is the L91 lesson one level up: measuring the right component in the wrong container is still not measuring it. FIXED with `min-width:0` on `.qms-shell` and `.qms-page-inner`; the live chain is now `.page` 796/796 and `.qms-table` 738/1036/auto — only the table scrolls. **A second live-only finding:** the biostatistics document pane renders engine-produced markdown, and an assumptions table of Parameter/Value/Source/Sensitivity measured 387 in the 376px column. Column count is not known at author time, so the table now scrolls in its own box (`display:block; overflow-x:auto`) rather than pushing the prose. **What running it proved beyond the fixes:** the shell resolves to `264px 796px 380px` with the rail expanded and AnA open, so the 796px the overflow gate measures at is the real figure, confirmed rather than assumed — and the gate's defaults (`railCollapsed:true`, `anaOpen:false`) hand a surface 1352px, which exercises none of this, so a live check has to set the prefs first. Eleven surfaces driven under real auth against the migrated database: no surface scrolls sideways, zero overflowing elements, `.ed-crumbs` 180px reading "eCTD › No document", `.bs-fields` 246px, `.kanban` 748px, `.docs-row` 446px with the ported stylesheet live | eng | done | `npm run visual-qa:overflow`, `client/src/concept2cure/quality/app.css` |
| L109 | **A second overflow gate, because the first one cannot see containers — and it shares the rule rather than copying it.** L103 was found by running the app, not by any gate: a fragment has no ancestors, so the register's `min-width` measured exactly right in the capture and, in the shell, propagated up a flex ancestor whose automatic minimum size is its min-content width until the whole page scrolled. `check-live-overflow.mjs` drives the running app under `/api/auth/dev-login` and runs the SAME function over the real shell. The rule now lives in `overflow-rule.mjs` and is injected into the page by both checks — the argument `a11y-rules.mjs` already makes in this directory, that two copies of a judgement call is two places for it to drift. **The trap it self-checks for is the one I fell into:** the stored shell defaults are `railCollapsed:true, anaOpen:false`, which hand a surface **1,352px** rather than the 796 it really gets, and at that width essentially nothing overflows. My first live pass ran that way, reported every surface clean, and was worthless. So the check sets `c2c-v2-prefs` and then ASSERTS the pane width before printing a single finding — wrong width, exit 2, nothing reported. Proved by making it fail: reverting the prefs to the defaults produces `pane is 1352px, expected ~796` and exit 2. It also refuses to write a baseline from a partial sweep and names every surface it could not drive, because Chromium dies on long sweeps and a dead browser must not read as a clean product — the first attempt at this crashed 40 surfaces in and the remaining 78 came back as "clean". Surface ids are read from `surfaceViews.ts` itself rather than a list kept beside it, and the check exits 2 if it parses fewer than 50 | eng | done | `npm run visual-qa:live`, `scripts/visual-qa/overflow-rule.mjs` |
| L111 | **The live gate's first real sweep: 120 surfaces, nine findings, and seven of them one rule.** `.ana-greet-s` is the AnA rail's opening line and it renders each surface's own `here` description — several of which quote an endpoint, e.g. *"the nonclinical slice of the PDEV → IND activity registry, drilled from `GET /api/pdev/programs/:id/workstreams`"*. A URL path offers no break opportunity, so in a fixed 240px box it ran out of the rail on `maa-cockpit`, all four `pdev-*` surfaces and `source-tracer` (331 > 240 at the worst), taking `.ana-greet` 389 > 355 with it. **The fragment gate reports zero on all of them and is not wrong** — the rail's greeting is shell state, not surface markup, so no capture contains it. FIXED with `overflow-wrap:break-word`: a prose block with a fixed max-width that renders content-derived strings has to be able to break them. The other two are `.sp-row-t`/`.sp-row-b` on `nonclinical`, same remedy. **Two things fixed here that are NOT the wrap, and are recorded rather than guessed:** (1) the greeting quotes a raw API path at a user, which is a microcopy decision, not a layout one; (2) in that `.sp-row` the status chip takes **231px of 298** and the label it annotates gets **56** — unreadable at that width, and the same row on `maa-cockpit` is 425/69, so the proportions are wrong on this surface specifically rather than in the rule. Breaking the word stops the spill; it does not make a 56px label legible | eng + prod | open | `npm run visual-qa:live`, `client/src/concept2cure/v2/styles/journey-v2.css` |
| L47 | **Two `openai-service` modules export the same function names; one is governed and one is not.** Closed, and it was three, not two. `server/services/openai-service.ts` (its own provider client) and `server/test/services/openai-service.ts` (a 3-line stub returning the literal `AI: ${instruction}` as if it were model output) both shadowed the governed root `server/openai-service.ts`. Whether an import was audited was decided by directory depth — from `server/services/`, `./openai-service` reached the ungoverned sibling and `../openai-service` the governed root, one character apart, with `csr-extractor-service.ts` and `csr-search-service.ts` sitting next to each other on opposite sides of it. Neither shadow had a live caller: all three importers were a dead `any`-aliased import that called nothing, and two barrels (`services/index.ts`, `services/ai/index.ts`) that nothing imports — so the burndown was deletion, not a rename. The bypass gate now also refuses any `server/**/ <name>.ts` that shadows a governed root AI entry point, because it matches per file and a file that merely IMPORTS a colliding name constructs no client and looks clean | eng | done | `scripts/ci/check-gateway-bypass.mjs` (collision guard; baseline 21→20 entries) |
| L48 | **The eCTD fallback template catalog exists twice, 1,134 lines each, and both are served.** server/data/fallback-templates.ts (deleted) and `server/services/templates/ectd-fallback-templates.ts` are structurally identical and differ only in their docstring. `routes/templates.ts` imports the first; `routes/misc-inline-routes.ts` and the `ich-headings` test import the second. This is static FDA/ICH template data, so a correction applied to one copy leaves the other serving the superseded structure, and which skeleton a caller receives depends on which endpoint it hit. Fix: one copy, one importer set, and delete the other **CLOSED.** Verified byte-identical once comments are stripped (0 differing lines). Converged on `server/services/templates/ectd-fallback-templates.ts` — the services-layer, domain-named home; `server/data/` was a generic bucket under a generic name, which is part of why a second copy was written instead of the first being found. `routes/templates.ts` re-pointed (its re-export had no consumers) and the other copy deleted. Both existing docstrings already claimed to be "extracted from server/index.ts" — two files can each be an honest extraction of one source and still be two copies, which is why the fix carries a gate: `ci:reg-reference-single-copy` counts declaring modules per guarded regulatory export and refuses a second, proven by reintroducing one (exit 1) and removing it again (exit 0). | eng | done | `server/services/templates/ectd-fallback-templates.ts`, `scripts/ci/check-regulatory-reference-data-single-copy.mjs`, `server/services/submission-gateways/ectd-packager/__tests__/ich-headings.test.ts` |
| L49 | **Three Define-XML generators in one directory; one is reachable.** FIXED. `services/cdisc/` held `define-xml.ts` (v2.0), `define-xml-generator.ts` (v2.1) and `cdisc-conformance-service.generateDefineXml` (v2.1, zero callers, and it asserted `Mandatory="Yes"` on every variable — a claim about the dataset it had no basis for). Now one generator, `define-xml-generator`, taking `defineVersion: '2.0' | '2.1'` (default 2.1) and returning the version it emitted, so the namespace URI and `def:DefineVersion` can no longer disagree. `define-xml.ts` kept only its conformance half and is renamed `define-spec-conformance.ts`; the third generator is deleted with its two dead types (which also shrank the duplicate-exported-types baseline 247→246). `def:Origin` was the one thing the deleted 2.0 generator could do that the survivor could not — carried over, not dropped. **The worse half was on the tool surface:** `generate_define_xml` had two definitions, and `ALL_ANA_TOOLS` keeps the first spread — so the schema AnA saw came from `extendedRegulatoryTools` and said "define.xml v2.0 … returns the conformance findings", while the handler emitted 2.1 and returned gaps. The shadowed `cdiscTools` definition described `domain` / `type: 'Char' | 'Num'`, which the handler maps to `dataType` — had it ever won, it would have produced `IG.undefined` and `DataType="Char"`, an invalid define.xml, silently. Both shadowed definitions (`generate_define_xml`, `run_cdisc_pipeline`) are deleted and `KNOWN_DUPLICATE_NAMES` shrank 3→1. Behaviour change stated, not silent: `runCdiscPipeline` now defaults to 2.1, where it used to emit 2.0 only because it called the 2.0-only generator. `POST /api/cdisc-validation/define-xml` takes `defineVersion` and 400s on an unrecognised value rather than falling back. Verified by re-injecting the hardcoded version (2 tests fail) and by re-introducing a shadowed definition (the ratchet refuses it) | eng | done | `server/services/cdisc/define-xml-generator.ts`, `server/services/ana/__tests__/tool-registry-consistency.test.ts` |
| L50 | **Two document-template registries, one of which calls itself canonical.** Closed, and it was FOUR, not two — `DocumentTemplate` was exported by `intelligence/template-registry.ts` (DB-backed resolution: WHICH template applies), `docx/templateRegistry.ts` (static DOCX blueprints: what it LOOKS LIKE), `regulatory/templateCatalog.ts` (metadata: which are OFFERED) and `ana-ri/document-actions.ts` (a quick-action heading outline). Four shapes, one name. Not folded: they are genuinely different concerns and merging async DB resolution with a static blueprint catalog would make a worse module. Instead each type is renamed to what it is (`DocxTemplateBlueprint` / `CatalogTemplateEntry` / `ActionArtifactOutline`; all four were file-local, so the renames are compile-verified) and all three docstrings name the others and state the boundary — the canonical claim is now checkable in one hop instead of being an assertion the file next door falsifies. New shrink-only ratchet `ci:duplicate-exported-types` holds the wider finding: 247 exported type names in `server/services` mean more than one thing across 609 declarations (`Citation` in 15 modules, `Severity` in 11) | eng | done | `scripts/ci/check-duplicate-exported-types.mjs` (baseline 247, shrink-only) |
| L51 | **Two source files contain literal NUL bytes, so `grep -n` prints nothing from them.** Closed — this row had gone stale. Both files are clean UTF-8 today: `services/ana/citation-engine.ts` carries 6 `\u0000`/`\u0001` escapes and `services/rbm/metric-ingestion.ts` 1, exactly the prescribed fix, and `file(1)` now reports both as UTF-8 text with `grep -n` printing normally. Verified by byte count (0 NUL, 0 SOH in each) rather than by inspection | eng | done | `server/services/ana/citation-engine.ts`, `server/services/rbm/metric-ingestion.ts` |
| L52 | **`toQuestionOptions` is copy-pasted fourteen times across the domain constants.** Every `shared/constants/domain/*.ts` module ends with the same one-line body mapping its own list to `{value,label,description}`. Adding a fifteenth domain means copying it again, and a change to the option shape means fourteen edits. Not a correctness defect — a shape that makes the codebase grow by duplication. FIXED: one generic `toQuestionOptions(entries)` in `shared/constants/domain/types.ts`; all 15 modules delegate to it, so a sixteenth domain is a call rather than a copy. Landed with L57 because the same leaf module solves both — the helper had nowhere cycle-free to live while the shape it depends on was inside the barrel | eng | done | `shared/constants/domain/types.ts` |
| L53 | **Three modules carry a fifth of their layer each, and one of them is a route file.** `shared/schema.ts` is 19,956 lines / 417 `pgTable` definitions / 1,322 exports; `services/ana/AnaToolExecutor.ts` is 18,792 lines / 708 `registerToolHandler` calls / 903 dynamic imports; `routes/concept2cure.ts` is 18,410 lines / 158 route handlers / 40 inline SQL sites. The first two are registries where size is a consequence of breadth and the seams are already visible (one handler per tool, one table per domain). `concept2cure.ts` is different: it is a route file holding both HTTP handling and hand-written SQL, so it is the one that has to be split rather than merely indexed. Separately, 126 top-level functions exceed 250 lines; the eleven largest are `intelligence-questions/flows/*` factories that contain zero control-flow lines — declarative question data inside a function body, awkward but not tangled. `mountStreamRoute()` at 1,294 lines is the genuine one | eng | open | `server/routes/concept2cure.ts` |
| L54 | **What is actually true about the "unreferenced" modules — now reviewed per module.** I claimed 103 were dead, ~22,300 lines, "verified twice". That was wrong by roughly 4x and the second verification was a broken script whose output I reported as corroboration. The numbers I then recorded here (26/51/30 over 107) are also stale: the baseline has been regenerated to **101** candidates. Current split, after fixing the checker (§4 of the report): **23** with no reference of any kind, **44** named only in docs or comments, **34** held by something real (a rerun now prints 21/46/34 — writing this row named two of the 23, which is the circularity hazard the tool warns about, and both were already DOC-ONLY verdicts; §1 of the report). All 23 have now been given a per-module verdict against twelve surfaces — path, basename, every exported symbol, PL/pgSQL in `db/migrations`+`migrations`, npm scripts, `.husky/*`, `.github/workflows/*`, `scripts/**`, dynamic `await import()`, test-runner globs, registries/CI baselines, and `dist/`. Result: **0 LIVE, 4 DOC-ONLY, 19 NO REFERENCE (4,015 lines)**. Three false positives found and fixed in `prove-module-unused.mjs`, all confined to that file: (a) prose in a comment scored as a path dependency — worse than the backtick case recorded here, plain JSDoc was enough, and it was holding 4 modules including `server/middleware/csrf.ts`, the one module this row cites as genuinely dead; (b) a `.ts`-suffixed literal scored as an import edge, which is why the checker reported `import 2` for `authAdapter.ts` in flat contradiction of its own header, and why its `minFspath` control was being satisfied by an unrelated sentence in `server/utils/pdfParse.ts` — the fspath control was passing on prose; (c) the corpus omitted `.husky/pre-commit`, `.husky/pre-push`, all `.py`, `.mts`, `.tf` and `CODEOWNERS`, i.e. two of the three surfaces L13 was burned by were not being read at all. Added `maxImport`/`maxFspath` ceilings to the controls — floors alone can only fail on undercounting — and **both new controls were run against the pre-fix logic and fail there** ("2 control(s) DISAGREE"). **Still no safe-to-delete list, and this is now a considered position rather than a gap:** `server/utils/docushareHealthCheck.js` was the one candidate whose blocker could be cleared in this change, and it has been — deleted with `server/config/docushareConfig.js`, the allowlist entry that protected it, and the baseline entry (L150); `server/middleware/requireLicenseAcceptance.ts` is a working license gate wired to the live EULA service and mounted on no route, which is a product decision, not a cleanup; and the baseline is a ratchet that must be regenerated in the same change. What exists is a reviewed 19-module candidate set where every checked surface is named. Full evidence with file:line: `docs/reports/L54_UNREFERENCED_MODULE_VERDICTS_2026-09-03.md` | eng | open | `node scripts/ci/prove-module-unused.mjs --controls` |
| L59 | **Audit immutability was believed to exist and was never installed; e-signatures were not atomic with their audit row.** Both fixes sat in a stranded agent PR (#1319) for four days and are now landed on `concept2cure-v2`. `20260222_audit_events_immutability.sql` and `20260617_audit_logs_immutability.sql` were authored, reviewed, and reachable by NO apply path — no `_gcc_` infix, absent from `C2C_MIGRATION_FILES` — so `audit_logs` was mutable on every deployed database while the runtime role held the default UPDATE/DELETE grant on `public`. The reachability CI check missed it because it only flags migrations whose sole-created TABLE is unreachable, and these create only triggers. Separately, `POST /api/esignature/sign` inserted the signature on an autocommit `pool.query` and then called `auditService.logAction`, which swallows its own persistence errors — so its `catch` was dead code for the failure it was written for, and a signature could be permanently recorded with no audit row. The route now runs the shared `persistElectronicSignature` writer and `writeChainedAuditRow` on ONE transactional client. Merge note: the PR predated the single-write-path consolidation, so its inline INSERT was dropped in favour of the shared writer handed the transaction — both invariants kept, not one traded for the other | eng | done | `server/routes/__tests__/esignature-audit-atomicity.contract.test.ts`, `server/routes/__tests__/esignature-sign.test.ts` |
| L41 | **`authoring-engine` was a fixture screen sold as a subscribable capability.** It renders inline fixture data, calls no endpoint, and carried no label saying so, while sitting in the Apps catalog as a purchasable module asserting "no hallucinations, every required element" and validation that "runs as intended 100% of the time" — beside `document-authoring`, the real editor, whose own description was an internal handoff note claiming it had "no UI". FIXED: the surface states what it is, every absolute is replaced by the control it overstated, and both catalog descriptions are corrected by migration (descriptions only — no module added, removed or re-priced). Two gaps closed with it: `check:compliance-claims` had no AI-absolute rules (and its first draft of one was inert — `\b` after `100%` can never match), and no file-scanner can read the catalog at all, since that copy is an `available_modules` row assembled across migrations. A contract test now asserts the catalog the migrations produce, sharing one rule definition with the gate, and was confirmed to fail on the pre-fix state | prod | done | `tests/schema-contract/catalog-module-claims.contract.test.ts`, `npm run check:compliance-claims` |
| L43 | **AnA could resolve a screen and never take you there.** `shared/navigation` locks the contract, `list_app_screens` and `navigate_to` are registered and return a validated `NavigationDirective`, and the shell navigates by surface id — and nothing carried the directive from the tool result to the client, so every resolution ended in the model saying it had moved someone it had not. CLOSED: `navigation-actions.ts` is the one carrier, `stream.ts` collects directives beside the provenance envelopes, and post-processing emits them on `post_done` as `actionType:'navigate'` chips the rail renders as the only interactive executed action. Tool-driven, never signal-driven: `parseNavigationSignals` reads fenced prose, and prose reaches the context window from ingested documents, so wiring it that way would let any uploaded PDF move the operator's screen. Offered rather than performed — the server does not navigate anyone; a refused target (unknown id, missing param) yields null and never becomes a chip. The tool's instruction text was asserting movement and now offers it | eng | done | `server/services/ana-ri/__tests__/navigation-actions.test.ts`, `client/src/concept2cure/v2/__tests__/anaRailActions.test.tsx` |
| L42 | **Surface count has never had a consolidation pass.** 118 registry ids over 86 modules; the never-3-of-anything doctrine reached services, routes and tables and never surfaces. Per-cluster survivor recommendations are now written against measured evidence (lines, endpoint families, AnA presence) rather than names: 8 clusters to consolidate, 1 to keep intact (`labeling` — a USPI and an EU SmPC are different artifacts and merging them would be a regression dressed as consolidation), 1 needing its own pass. Net would be 118 → ~108 with no capability lost. **Awaiting a per-cluster decision; nothing merged or deleted** | prod | open | `docs/SURFACE_CONSOLIDATION_2026-08.md` |
| L45 | **AnA was asked about screens she could not see.** The surface-context pipeline is complete end to end — a surface publishes via `usePublishSurfaceContext`, the shell forwards it as `module_context`, the server renders it into the system prompt — and three surfaces out of 118 called the publisher. On the other 115 AnA answered from the surface id alone, which is worse than no assistant because it looks like one. Publisher and consumer were each built and tested; surfaces actually calling it was nobody's test. Thirteen publish now (`pv-cockpit`, `ectd-compile`, `decision-lineage`, `source-tracer`, `dispatch-readiness`, `haq-manager`, `qmp`, `gateway-transmittals` added here — the last two also had no affordance to ask with, having discarded `SurfaceViewProps` entirely), with a ratchet gate that lets adoption rise and never fall, verified to fail below the baseline rather than merely to pass above it. 93 still to wire; MDX device surfaces are excluded because they already have a richer database-backed resolver server-side | eng | in-flight | `npm run ci:ana-surface-context` |
| L39 | **Four writers of `cerv2_section_versions`.** FIXED. The three inline Drizzle inserts in `routes/cerv2-sections.ts` (create, PATCH, accept-ana-draft) now call `recordCerv2SectionVersion`, and the route file inserts into that table nowhere. Three things the row did not know were wrong: the create handler **hardcoded `version_number: 1`** rather than deriving it, so a section re-created over an existing id would collide or restart its history; PATCH and accept-ana-draft ran their own `MAX+1` **outside any transaction**, so two concurrent editors could read the same maximum; and none of the three recorded `completion_percentage`. All three now run the content write and the version write on **one** transaction — an edit that cannot be recorded does not happen, which is a behaviour that did not exist before and has its own test. `field_data` was the one column the inline inserts wrote that the shared writer had no parameter for; rather than let the consolidation drop it, the parameter was added to `section-version.ts` and all three call sites pass it — a shared writer that silently drops a column the writers it replaces were filling is not a migration. The existing writer test asserted INSERT params **positionally**, so adding a column silently re-targeted `params[9]` from `previous_values` to the new column; those assertions are now by column name and throw a diagnostic naming the missing column. Rider fixed while here: `db/ensureCoreTables.ts` boot-created this table with **10 columns** where the writer inserts 17, so every section edit 500s in any environment that DDL (not drizzle push) provisioned — the transaction makes that fail closed rather than silent, but the surface was dead. Aligned. Verified by making the checks fail: restoring one inline insert fails 4 of 14, and removing `field_data` from the writer fails 3 more | eng | done | `server/routes/__tests__/cerv2-section-versions.pglite.integration.test.ts` |
| L32 | **On the governed c2c store, accepted AnA text is recorded as human-authored.** PARTLY FIXED — the half where the store INVENTED the author is closed. Three writers turned an unstated origin into a positive claim of human authorship: the section PATCH resolved an omitted `draftSource` to `'human'`, `commit-section-to-filing.ts` hardcoded `'human'` from a layer (`authoring_sections`) that carries no provenance column and cannot know, and the version trigger's `CASE` fell through to `'human'` for NULL — so the guess reached `author_kind`, the immutable column an inspector reads, indistinguishable from a real human assertion. All three now record that the origin was not stated (`NULL` / `'unspecified'`); a stated origin still survives verbatim. Same fix as L34 one column over, and built on L34's function body so its mandatory-reason RAISE is carried forward. Existing rows are left as written — rewriting an audit trail after the fact is the worse falsification. STILL OPEN, and why this stays open: the span half. The PATCH calls `replaceAuthorSpans` only, so the Data Room sources retrieved for a draft never become `cre_evidence_source` spans; the route receives no `draftId`, so it structurally cannot `consumeDraftCandidate` the way `authoring.router.ts` does. Closing it needs the client, the c2c SSE draft path and the accept route changed together. RESOLVED, by finding the flow does not exist: nothing in the client calls the c2c SSE `/ai-draft` stream (the only channel that would have, `window.C2C_AUTHORING.streamDraft`, is one this repository never provides), it had no tests, and it retrieved Data Room chunks it never parked — so even a wired accept could not have carried source lineage. The stream is deleted as a second, weaker draft path beside the canonical one (`/api/authoring/sections/:id/ai/draft` + `/accept`, which parks the sources server-side and records verified source + author spans in one transaction); the PATCH keeps its author-span write. What AnA-authored text still lacks is recorded as L154 | eng | done | `server/routes/c2c/documents.ts` |
| L33 | **What produced a draft was never persisted.** FIXED on the authoring path: model, provider and a SHA-256 of the prompt actually sent are parked with the draft candidate (server-side, like the source chunks — a generator round-tripped through the client would be a forgeable claim) and written into the accept audit entry, the last moment they exist anywhere. A prompt DIGEST rather than a version, because that route composes its prompt inline and a version number nobody maintains only looks like a reference. Still open: the c2c SSE draft path reports the generator to the client but does not persist it at accept | eng | done | `server/services/clinical-regulatory-evidence/__tests__/draft-candidate-store.pglite.integration.test.ts` |
| L34 | **Reason-for-change was enforced by route convention, not by the database.** FIXED for the governed store: the version trigger now RAISES on a missing or blank reason exactly as it already did on a missing actor, instead of `COALESCE`-ing to the literal `'content change'`. Both writers already guarantee a reason, so nothing in the product trips it — it exists for the next writer. Existing placeholder rows are left alone: they truthfully record that nothing was captured, and rewriting them would invent history. Still open elsewhere: `cerv2-sections.ts` hardcodes `'Section updated'` and `doc_revisions` has no reason column at all | eng | done | `tests/schema-contract/c2c-section-reason-required.contract.test.ts` |
| L35 | **RAG retrieval failure was silent.** FIXED on both draft paths: `retrievalStatus` separates `ok` / `empty` (retrieval ran, corpus held nothing — a fact about the Data Room) / `failed` (no fact at all, and a draft nothing checked), with a bounded reason, reported to the caller. The authoring response no longer says "generated successfully" for a draft produced by an outage. Labelled, not blocked — a draft is not a governed write, and the accept path already refuses content whose lineage cannot be covered | eng | done | `server/routes/__tests__/ai-draft-retrieval-honesty.test.ts` |
| L37 | **Two conforming `electronic_signatures` INSERT sites remain.** FIXED. `electronic_signatures` has 26 writable columns; `persistElectronicSignature` wrote all 26, the Drizzle insert in `part11ComplianceService` wrote 20. The row called both conforming and it was right about five of the six gaps — `signed_target`, `legal_disclaimer`, `ip_address`, `device_info` are N/A on a release, `is_valid` equalled its default. **The sixth was not cosmetic: `binding_basis` was NEVER WRITTEN, so the same `bound_payload_digest` column carried two different things on this path** — the orchestrator's assembled-release-package digest when the caller supplied one, and `buildVersionBindingDigest` over the version's bytes when it did not — and the row said which. An inspector re-deriving the §11.70 binding on a release row would have re-derived version content against a package digest and read the mismatch as tampering. A new `BINDING_BASIS.SUBMISSION_RELEASE_PAYLOAD` makes the two distinguishable. The release path also had NONE of the canonical writer's five fail-closed guards (anchorless row, missing signer identity, missing attribution hash, empty §11.70 digest, missing org) and now has all of them. Both sites were already transactional and both stay so: `drizzleSignatureClient(tx)` adapts the Drizzle transaction to the writer's pg-style client, because handing it a fresh pool client would have silently split the signature from its `device_audit_trail` §11.10(e) row across two connections. Verified by re-injecting the second INSERT: 4 tests fail, three of them on `binding_basis` being null. 62 further signature tests green | eng | done | `server/services/__tests__/part11-release-signature-single-write-path.pglite.integration.test.ts` |
| L136 | **`concept2cure_signatures` fabricates §11.50 manifestation fields its caller did not supply.** FIXED, and it was four writers, not two. `submission-chat-apply-rewrite` defaulted `signerName` to `user-${id}` and `signerEmail` to `user-${id}@unknown.local`; `verifiedSealService` passed `''` into a NOT NULL `signer_email` and wrote a CALLER-SUPPLIED `manifestation.printedName` as the signer's own; `DocumentOrchestrationService` wrote `user-${id}` / `''` into `document_audit_trail` off a BARE PRIMARY-KEY read of `users` (unscoped, so an id from another tenant resolves a name onto this org's audit trail); and `/api/esignature/sign` seeded identity from client-controlled session fields, filled gaps from the same unscoped read, checked only that an EMAIL came out — so the printed NAME could be written empty — and swallowed a failed lookup with a `console.warn`, degrading to session-asserted identity exactly when the server could not confirm it. **Two of these were worse than a wrong string: the §11.200 attribution hash is computed over the signer's email (rewrite) and printed name (seal), so a signature hashed over an invented identity cannot be re-derived from the real signer — verification reports a mismatch, and a mismatch reads as tampering.** Root cause is the schema: `signer_name` / `signer_email` / `user_name` are NOT NULL, so a writer with no real name invented one to make the INSERT succeed. A column that forbids null is not a reason to write something in its place. All four now call `resolveSignerIdentity` — the org-scoped membership lookup extracted from `persistGovernedActionSignature`, so the two substrates cannot drift on who signed — and REFUSE when the signer is not attributable. The route-level `user-${userId}` fallback in `ana-features.ts` is gone too, so nothing upstream can manufacture an identity either. Gate: `ci:fabricated-identity`, wired into pre-push, which found six occurrences my own sweep had missed (L142). Verified by re-injecting the fallback: the gate names both lines, the source test fails, and the route's new refusal test turns a 403 back into a 201 | eng | done | `server/services/part11/resolve-signer-identity.ts` |
| L137 | **A dead fourth `electronic_signatures` writer that defaults every identity column it touches.** FIXED — deleted. `server/prisma/client.js` `signature.create` inserted with `document_id` defaulting to `0` and `signer_name`, `signer_email` and `signature_hash` to `''`: a §11.50 printed name and a §11.200 attribution hash written as empty strings, indistinguishable from real values once stored. Nothing called it (only `signature.findMany` is referenced, in a tenant-guard test), which is why this was a deletion rather than an incident — but it also bypassed `persistElectronicSignature`, so any future caller would have reopened the single-writer property L37 established. `grep` now finds exactly ONE `INSERT INTO electronic_signatures` in the repo. `findMany` and its tenant guard are untouched; its 7 tests pass | eng | done | `server/prisma/client.js` |
| L138 | **The route-level release-signature audit event can be missing from a committed signature.** FIXED. `release_signature_created` is now handed to `createElectronicSignature` as a `transactionalAuditEvent` and written with `writeChainedAuditRow` on the SAME transaction as the signature — the function whose own docstring says a Part 11 signing path "needs the write inside its own transaction, which is this", and which the `/api/esignature/sign` route already used. The route's honest `auditWriteFailed` / `auditWarning` response is deleted, not because the warning was wrong but because the state it described can no longer exist: the transaction rolls back and there is no signature to warn about. **Two things fell out of doing this properly.** First, my initial tests PASSED with the defect re-injected — the unit test mocks the whole service, so removing the in-transaction write changed nothing it could see; the rule caught my own test, and the real assertion is now a PGlite integration test that renames a column `audit_logs` needs and asserts ZERO rows land in `electronic_signatures` and `device_audit_trail`. Re-injected, it reports `resolved { success: true, signatureId: 1 } instead of rejecting` — L138 stated as a test. Second, **the golden journey for this gate had no `audit_logs` table at all** (L145): it asserted the §11.10(e) claim with nothing for the audit half to land in, and passed. It now provisions the table and its chain columns and exercises the real write | eng | done | `server/services/__tests__/part11-release-signature-single-write-path.pglite.integration.test.ts` |
| L145 | **A golden journey asserted a §11.10(e) claim against a database with no audit table.** FIXED, and it was not one journey — **seven of the nine** were running against databases missing tables their own subject writes to, and all nine passed. Rather than audit nine extract-lists by hand, the harness now RECORDS the failure: every statement a journey issues goes through one shim, so any 42P01 (undefined_table) or 42703 (undefined_column) is captured and rethrown unchanged, and `assertNoSchemaGaps` fails the journey in `afterAll`. The recurring gap was **`audit.tamper_proof_log` in four journeys** — the Part 11 tamper-evident hash chain — while those journeys asserted Part 11 claims; its own migration header documents why nothing noticed (the caller logs `Failed to initialize tamper-proof audit table (non-fatal)` and falls back to a console writer, which is correct in production and fatal to the evidence here). Also missing: `audit_logs` (drug-NDA), and `governed_dependencies` + `impact_propagation_log` in the HAQ-correction journey — whose entire subject is propagating a correction across dependent documents, with neither table present. The tamper-proof store and the dependency layer are now in `CANONICAL_JOURNEY_MIGRATIONS` (and in the six journeys that override that list). The request-scoped RLS client is instrumented too — three journeys put their real traffic through it, so leaving it out would have made the check quietly weaker on the paths that matter most. A source gate asserts every `createJourneyDb` journey calls the assertion, and requires a journey on a different harness to SAY so. Verified by removing a table (caught, and note the shape: the TEST passes while the FILE fails, which is the gap itself) and by removing the assertion (source gate names the file). Two residuals recorded, not hidden: L146, L147. CORRECTION (with L147): "every statement a journey issues goes through one shim" was not true — Drizzle was handed the raw PGlite and bypassed the shim, so ORM-issued writes were unobserved; recording now sits at the PGlite seam (`recordSchemaGaps`). The source gate is `tests/golden-journeys/__tests__/journey-schema-gaps.test.ts` | eng | done | `tests/golden-journeys/harness.ts` |
| L146 | **`/api/c2c/projects` diagnoses a 42P01 as a missing store without checking whether the relation is missing.** PARTLY FIXED — the diagnostic is; the underlying disappearance is not. The route treated ANY 42P01 as "store not provisioned", took the relation name straight out of the error message, and told the operator to provision it. On the one code path whose entire job is to say what is wrong, that can name a table that is already there, and the operator's next step dead-ends too. It now VERIFIES with `to_regclass` on a fresh connection before making the claim, and returns a plain server error when the relation resolves. The log is three-valued (`storeCheck: absent TIGHTENED: the probe used to drop every gap recorded inside its window; it now removes only the `audit_logs` 42P01 it causes and puts any other back for afterAll — verified by injecting an unrelated missing relation inside the window, which afterAll then names | unverified`) rather than boolean, because an aborted transaction fails the check itself and "could not verify" must not read as "confirmed missing". **STILL OPEN: why `audit_logs` disappears.** In the drug-NDA journey it resolves in one test and not during the request that follows, and the verification confirms it genuinely does not resolve at that point (`storeCheck: absent`). Ruled out by measurement, so the next person need not re-do it: not `FOR UPDATE` (works under PGlite), not parameterised `to_regclass` (literal, `$1` and `$1::text` all resolve), not DDL through the pool (traced, none), not `search_path` (never set anywhere in the request path — the RLS middleware sets only `app.current_*`), not an unbalanced transaction in the journey's twelve migrations (BEGIN/COMMIT counts match), and not a mis-resolved `pool` import (the journey mocks the exact specifier the route imports, and the failing query's stack goes through the journey's own shim). The real path is `scaffoldProjectDocuments` → `recordGovernedAction` → `computeAuditChainSealed` at `projects.ts:627` CLOSED: the seam recorder (L147) plus a statement-order trace answers it — `audit_logs` does not disappear; the journey renames it away itself. Its rollback-probe step (`ALTER TABLE audit_logs RENAME TO audit_logs_journey_hidden`, one request, rename back) is the destruction that proves intake atomicity, and the 42P01 on the chain-head read is that step working. Every measurement above was correct and beside the point. The probe now captures the gap it provokes, asserts the hidden store was actually written to (otherwise the probe proves nothing), and removes it from the journey's gap list, so afterAll's `assertNoSchemaGaps` runs with no allowlist | eng | done | `tests/golden-journeys/drug-nda-ectd.journey.test.ts` |
| L147 | **Two journeys run on a harness with no query chokepoint, so the schema-gap check cannot see them.** `submission-export-package` and `submission-export-validation` use `createIndPgliteDb` (`server/db/pglite-harness`), which hands services a Drizzle instance directly rather than the pool shim `createJourneyDb` provides — so there is no single place to observe a failed statement, and L145's check does not cover them. They now say so at the call site, and the source gate requires that sentence, so this is visible rather than an omission. Closing it means giving that harness the same recording. CLOSED: `recordSchemaGaps` (server/db/pglite-harness) wraps `pglite.query`, `pglite.exec` and the client `pglite.transaction` hands out — the one seam Drizzle, a pool shim, a request client and a raw exec all share — and `createIndPgliteDb` attaches it. Both export journeys now call `assertNoSchemaGaps` (no gaps found), and the source gate requires the call on either instrumented harness. Doing it at the PGlite seam also closed a hole in L145: `createJourneyDb` handed Drizzle the raw PGlite, so every statement the ORM issued bypassed the pool shim's recorder; that shim-level recording is deleted and the seam recorder is the only one. Verified by making it fail three ways: removing `coauthor_documents` from an export journey is named by the assertion (42P01); removing the call from an export journey is named by the gate; and `server/db/__tests__/pglite-harness-schema-gaps.test.ts` shows a Drizzle-issued statement, one inside a Drizzle transaction, and one whose caller swallowed the error each recorded exactly once, with the error rethrown unchanged | eng | done | `server/db/pglite-harness.ts` |
| L148 | **The flagship authoring journey ran every HTTP request on the org-membership middleware's degraded fallback.** First catch of the seam recorder (L147). `ind-authoring` builds its own `organizations (id, name)` stub, which predates `db/migrations/20260129_add_org_uuid_alignment.sql`; the middleware's enriched LEFT JOIN on `organizations.uuid` failed with 42703 on every request, was caught, logged as a warning (eight times per run) and answered by the membership-only fallback with `orgUuid = null` — so `tenantSessionVars` set `app.current_org_id` to the empty string for the whole journey, and its tenant-isolation and honest-failure steps were proven with the org session variable empty. The membership decision itself was correct (organization_users is the sole authority), which is exactly why nothing failed; and the JOIN is Drizzle-issued, which is exactly why the pool shim never saw it. FIXED by adding the column the migration adds to the stub in `ind-authoring`, in `marketing-application` (same stub, same drift, not yet read there) and in the shared `JOURNEY_PREREQUISITES`. Verified by the failure first: before the fix the recorder names `organizations.uuid` and the run logs the fallback warning eight times; after it, zero warnings and no gaps. Residual now CLOSED. `enrichmentDegraded` already existed but was used only to skip caching and never left the module, so a degraded run was observable nowhere but a log line. The middleware now records a count plus a bounded sample (`degradedEnrichmentCount`, `degradedEnrichmentSample`), and every journey calls `assertNoDegradedTenantEnrichment()` in `afterAll`. Ordered BEFORE `assertNoSchemaGaps` deliberately: the missing column is a symptom, the degraded membership is the cause, and whichever throws first hides the other — my first attempt put it second and the gap check masked it, so the new assertion was never exercised. The source gate now requires both calls and that order. Verified by restoring the drift: the check names **27 degraded requests** in one `ind-authoring` run, where the row's original estimate of "eight" was warning lines rather than requests | eng | done | `tests/golden-journeys/ind-authoring.journey.test.ts` |
| L149 | **A known-bad journey step that crashed was filed as the block it existed to prove.** Found while closing L146: a guard added inside the drug-NDA rollback probe could never fire, because `JourneyRecorder.expectBlocked` records ANY thrown error as `blocked-as-expected` — correct for a service-level block that manifests as a throw, and wrong for a failed `expect`, a broken setup statement, or a guard, all of which read as the subject blocking. The recorder now files an `AssertionError` from inside a known-bad step as `failed` and rethrows it; an ordinary throw keeps its service-level meaning; the probe itself expresses "the hidden audit store was actually reached" as part of its `blocked` verdict rather than a throw. Verified by the failure first: hiding `regulatory_programs` instead of `audit_logs` (blocked for the wrong reason) passed before and now fails with "expected a block; got success"; the recorder test's assertion case fails without the fix (`tests/golden-journeys/__tests__/journey-recorder-expect-blocked.test.ts`). All nine journeys re-run under the stricter recorder pass, so none had been passing on a swallowed assertion | eng | done | `tests/golden-journeys/harness.ts` |
| L152 | **An authoring workflow step with no approver was given an invented approver, `qa@company.com`, and approval matches on that address.** Found by the widened fabricated-identity gate (L142). `/docs/:docId/submit` filled a missing `approver_email` from the role; the approve route matches `approver_email = <acting user>`, so the step was one nobody could approve — or one an unrelated account holding that address could. The submit now refuses (400, naming the step) unless every step carries its approver; the column was already nullable, so nothing forced the invention. The three suites exercising the route pass unchanged | eng | done | `server/routes/authoring.router.ts` |
| L153 | **Registration invented an account email from the username.** `/api/register` turned a missing email into `${username}@trialsage.ai` — an address nobody owns, on the identity every later attribution hangs from. No client calls the endpoint that way; it now requires an email and refuses otherwise | eng | done | `server/routes/users.ts` |
| L154 | **AnA-authored section text carries author lineage but never source lineage.** Successor to L32's span half. AnA writes drafted bodies directly from the tool executor — `q_sub_section_bodies` (AnaToolExecutor ~7958, through `enforceAuthorLineage`), `cerv2_510k_sections` (~14464), and c2c sections via PATCH — and in every case the tool call carries only `content`: the retrieval that grounded the text is not part of the tool contract, so no `cre_evidence_sources` chunk can be parked and no verified quote span recorded. The human authoring path proves the shape (draft candidate parked with its sources, consumed inside the accept transaction, `enforceSourceAndAuthorLineage`). Closing this means the executor's drafting tools receiving the retrieved chunks with resolved source ids and parking them exactly as the authoring route does, then accepting through the same gate. A design change to the tool contract, not a rename | eng | open | `server/services/ana/AnaToolExecutor.ts` |
| L155 | **Accepting an AnA draft in the 510(k)/PMA/CER surfaces erases its machine origin from the live row.** `POST /api/cerv2-sections/:id/accept-ana-draft` sets `draft_source = NULL` and stamps `accepted_by` = the human, so `cerv2_510k_sections` afterwards reads as a section with no stated origin that a person accepted — the version row keeps `previous_values.draft_source = 'ana'`, so the fact survives in history but not on the record a surface reads. The client relies on the clearing to hide its accept affordance (`useK510.ts`: `draftSource === 'ana' && draftedAt`), and `mdx-analytics` counts pending drafts as `draft_source = 'ana' AND accepted_at IS NULL`, which already assumes the origin is kept. The handler also carries `Deprecation` / `Sunset: 2026-08-01` headers pointing at `/api/c2c/actions/accept-ai-suggestion` as canonical, yet `useAcceptAnaDraft.ts` still calls only the deprecated one and the canonical action records a `c2c_ana_actions` row without touching the section. Fix together: keep `draft_source` on accept, key the affordance and the analytics on `accepted_at`, and retire one of the two accept paths. FIXED: `draft_source` stays `'ana'` on accept — acceptance is a fact added (`accepted_at`, `accepted_by`), not one erased — and a second acceptance is refused (409). The 510(k) affordance and the pending-draft feed key on `accepted_at`; `k510-document-preview` now emits it; `mdx-analytics`'s "accepted" count, which had been permanently zero because the origin was cleared, becomes meaningful. The Deprecation/Sunset headers are removed: they named the ledger action as canonical although it never touched the section and this route remained the only one the surfaces call. Verified against real Postgres (PGlite): the live row keeps the origin with `accepted_at` set, and a second accept returns 409 | eng | done | `server/routes/cerv2-sections.ts` |
| L142 | **Eight more manufactured identities, baselined for burn-down.** `ci:fabricated-identity` found them on its first run, six of which a hand sweep for the L136 pattern had missed. `artifact-tagger` and `autoExtractionPipeline` (×3) write `user-${id}@system.local` into audit `userEmail`; `figureGenerationService` (×2) writes `generatedBy: user-${id}`; `ai-actions` and `orchestration` synthesise a display identity from the request. These are arguably a different case from L136 — several are machine-run jobs with genuinely no human actor — but `user-41@system.local` still asserts a person-shaped identity a reader cannot distinguish from a real one. The honest form is a named system actor, not a synthesised user. Baselined shrink-only rather than fixed blind, because deciding what a machine actor should be called is a decision, not a rename DECIDED AND CLOSED: a machine actor is named, `system+<component>@concept2cure.local`, produced only by `governedActor(userId, component)` (server/services/part11/governed-actor.ts) — the real user id when there is one and no email at all (the governed context derives its actor as `userId || userEmail`, so the invented emails for real users never even landed), the named component when there is not. Applied at all six governed-context sites, including the ad-hoc `system@concept2cure.local` precedent and two manufactures the gate could not see (`${userName}@concept2cure.local`, `${userName}@ai-actions.local`). The two route fallbacks now refuse (`Authentication required`) instead of inventing a name, and the orchestration audit no longer files an authenticated-but-nameless caller as the `system` sentinel. Figure metadata records `generatedByUserId` as a number. The gate learned the `${x}@domain` shape (falsified with a probe in a tracked file) and found two identity roots outside the audit paths — L152, L153. Baseline is empty. Carried: machine jobs still assert `userRole: 'regulatory'` to pass the persona gate; that is a role claim, not an identity, and is a separate decision MERGE NOTE: origin closed this row in parallel (baseline 8 → 0 as well) by dropping the invented emails and returning `'unknown'` at the two route helpers; at merge the named machine actor won — `governedActor` is the one recipe, the route helpers refuse rather than file an authenticated caller as `'unknown'`, and `generatedByUserId` is identical on both sides | eng | done | `server/services/part11/governed-actor.ts` |
| L36 | **`ana_action_id` in the immutable version ledger was never written.** FIXED: the version trigger now reads a transaction-local `app.ana_action_id` GUC — the same mechanism the actor and reason already use — and the c2c section save sets it. The trigger resolves the id against `c2c_ana_actions` before using it and writes NULL otherwise, so a stale or bogus value can never turn a section save into an FK violation. `author_kind` (which already worked) is untouched | eng | done | `tests/schema-contract/c2c-section-ana-backlink.contract.test.ts` |
| L150 | **A live CI gate was carrying an exemption whose written reason was false, and the audit doc repeated it.** `scripts/ci/check-js-ts-shadows.mjs` refuses any `server/**/*.js` that shadows a same-basename `.ts` module — dev (tsx), prod (esbuild) and vitest resolve such a pair differently, so the same import can execute different code on each. Thirteen pairs were allowlisted, each with a `why:` note; ten are pure re-export shims, three are content-diverged. The thirteenth read *"diverged config actually used in prod via `docushareHealthCheck.js`"*. It is not used in prod. `server/utils/docushareHealthCheck.js:6` was the ONLY importer of `server/config/docushareConfig.js`, and **nothing imported `docushareHealthCheck.js`** — it sat in `unreferenced-modules-baseline.json` as its own gate's evidence that it is unreachable. So one CI gate's exemption was justified by a file another CI gate had already recorded as dead, and `docs/audits/DUPLICATE_BASENAMES_AND_CANONICAL_MAP.md:46` recorded the same inference ("ALIVE — imported by `docushareHealthCheck.js`"), which is a true statement about the import graph and a false conclusion about liveness: reachable-from-dead is dead. This is the shape the gateway-bypass rule already names — an entry without a REASON fails the gate — with the failure one step further in: the reason was present, well-formed, and wrong, so nothing could catch it. **FIXED by deleting rather than by rewording:** both files removed (256 lines), the allowlist entry removed with them (13 pairs -> 12), `unreferenced-modules-baseline.json` regenerated (101 -> 100), and the audit doc corrected — where it also turned out to list four "remaining ALIVE" duplicates that no longer exist, so its 7 is now a verified 3. `server/config/docushareConfig.ts` stays and is what `server/config/index.ts` resolves; `tsc --noEmit` clean. Proved the exemption was load-bearing before removing it: with the `.js` still present and the entry deleted, the gate fails naming that exact pair — so the allowlist was genuinely suppressing a real finding, not decorating a pass | eng | done | `node scripts/ci/check-js-ts-shadows.mjs && npm run ci:unreferenced-modules && npm run typecheck` |
| L151 | **Every separator glyph in the product was drawn at 2.11:1, and the contrast gate could not see them because its decorative exemption was written around the glyphs it happened to meet.** With L98/L99's two token decisions taken, the product measured ONE element below AA — a `›` breadcrumb separator on `document-authoring` at 2.11:1. It was one of **18** separator declarations across 11 stylesheets all taking `--text-500`, the faintest ramp step, whose own gate exception said "verify each use is disabled/decorative" and had never been verified. Seventeen of the eighteen never reported: `decorativeText` excludes a lone `·` or `/` under SC 1.4.3, so the exemption's shape decided which siblings were visible, and `›` simply is not in its character class. `pdev` had already moved its separator to `--text-400` alone, so the correct value was in the codebase, applied once, in one of eighteen places. FIXED: all 18 take `--text-400` (4.50:1) — subordinate to the `--text-300` crumbs beside them at 5.21:1, so still quieter than what they separate, and now perceivable. The 31 separator spans that carry a glyph get `aria-hidden="true"` (one of 32 already had it), so a breadcrumb no longer reads as "eCTD chevron Module 3". Two more findings fell out. (a) The last element below AA was `<option>No RBM studies</option>` inside a **disabled** `<select>`, painted in the UA's own `#808080` that no stylesheet in this repo chooses — SC 1.4.3 exempts "text that is part of an inactive user interface component", so the gate now excludes and COUNTS it the way it already does decorative glyphs, rather than carrying a floor of 1 that no fix could clear. (b) `visual-qa:a11y`'s one remaining finding was a second copy of the Vault file picker: the v2 one carries `aria-label` and a comment explaining why a `display:none` input still needs one; the mdx twin did not. Separately, `ci:token-contrast` asserted every text pair against `--bg-000` alone — never against `--bg-100` (sidebar) or `--bg-200` (hover / sunken fill), the two other grounds text lands on. Three of the six unlooked-at pairs fall short (light `--text-400` 4.08 / 3.79, `--text-300` 4.39); all six are now pinned so they can only improve. **Result: 0 below AA of 3,948 elements over 126 surfaces, and 0 a11y findings.** Proved rather than asserted: the new inactive-control exclusion was run against three injected probes — outside a disabled control, inside `fieldset[disabled]`, and `aria-disabled` — and reported exactly the one outside (1.92:1); and reverting `--text-400` to its old `#8a8880` fails the gate on all three light pairs including the two this change added | eng | done | `npm run visual-qa:contrast`, `npm run visual-qa:a11y`, `node scripts/ci/check-token-contrast.mjs` |

---

## 6. The rule that keeps this file honest

A row moves to `done` when a command proves it, and the command goes in the Evidence
column. Not when an agent reports success — agents have reported success for work that
wrote nothing at all, and that is exactly how `ana-platform-controller`'s audit call came
to write nothing through nonexistent columns behind an empty catch for as long as it did.

Run before trusting this file:

```bash
node scripts/ops/ledger-check.mjs      # rows parse, paths exist, done rows cite evidence
node scripts/ops/ga-readiness-report.mjs   # the procurement half, observed not assumed
```
