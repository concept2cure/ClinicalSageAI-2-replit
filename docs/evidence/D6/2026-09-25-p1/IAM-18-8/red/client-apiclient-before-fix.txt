434:            code: errorData.code || `HTTP_${response.status}`,
435:            message: errorData.message || response.statusText,
HEAD read `errorData.code` / `errorData.message` at the top level only; `/mfa/resend` nests them under `error`, so the 429 reached the page as the status text (verifier finding, 2026-09-26).
