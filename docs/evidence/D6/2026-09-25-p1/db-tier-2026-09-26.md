# Database test tier, executed 2026-09-26 on a provisioned local database

Database: PostgreSQL 16 with pgvector, provisioned by `npm run db:provision-test` from the working tree at trunk head fcf4906b (the whole migration set, the app_service role). Run: `TEST_DATABASE_URL=… APP_DATABASE_URL=<app_service> RLS_ENFORCE=on npx vitest run --config vitest.db.config.ts`.

 Test Files  69 passed (69)
      Tests  748 passed (748)
   Duration  169.15s (transform 15.21s, setup 2.23s, import 27.20s, tests 128.43s, environment 7ms)

Per file:

Error-level log lines inside passing negative cases (RLS refusals, fail-closed pool, an ended session) are expected output of the cases that exercise them; no test failed.
