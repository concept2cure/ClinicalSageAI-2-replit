 × server/services/__tests__/session-registry-redis.test.ts > the registry when Redis answers > decides in one atomic script: no read on one round trip and an eviction on the next 9ms
   → expected [] to have a length of 1 but got +0
 × server/services/__tests__/session-registry-redis.test.ts > the registry when Redis answers > Redis's decision is the only one: a session signed out on another task does not make this task end a live one 7ms
   → the memory tier ended a session Redis kept: expected [ Array(1) ] to deeply equal []
 ✓ server/services/__tests__/session-registry-redis.test.ts > the registry when Redis answers > a session superseded on one task is refused on another: the marker lives in Redis 2ms
 ✓ server/services/__tests__/session-registry-redis.test.ts > the registry when Redis answers > when Redis fails, the memory tier decides for this task 2ms
 ✓ server/middleware/__tests__/auth-inactivity-logoff.test.ts > authenticateToken — inactivity logoff > answers 401 SESSION_IDLE for a session with no activity for longer than its idle window, and the handler never runs 343ms
 ✓ server/middleware/__tests__/auth-inactivity-logoff.test.ts > authenticateToken — inactivity logoff > a request is activity: a session used every 14 minutes stays open, and one left alone for 16 is over 6ms
 ✓ server/middleware/__tests__/auth-inactivity-logoff.test.ts > authenticateToken — inactivity logoff > honours the window the tenant set at sign-in (idl), not the default 2ms
 ✓ server/middleware/__tests__/auth-inactivity-logoff.test.ts > authenticateToken — inactivity logoff > answers 401 SESSION_LIFETIME for a session that began more than 12 hours ago, however active 2ms
 ✓ server/middleware/__tests__/auth-inactivity-logoff.test.ts > authenticateToken — inactivity logoff > a token minted before sessions had ids is measured from its own issue 3ms
 ✓ server/middleware/__tests__/auth-inactivity-logoff.test.ts > authenticateToken — inactivity logoff > answers 401 SESSION_SUPERSEDED for a session ended by a later sign-in beyond the account's limit 7ms
 × server/middleware/__tests__/auth-inactivity-logoff.test.ts > authenticateToken — inactivity logoff > optionalAuth attaches no user from a session that is over, and the request continues anonymous 8ms
   → an idle session attached a user: expected { id: '42', userId: '42', …(5) } to be undefined
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SSO helper routes > GET /api/auth/sso/:provider/initiate should redirect to callback 18ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SSO helper routes > GET /api/auth/sso/:provider/callback hands the session to the sign-in page in the URL fragment, never the query string 55ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SSO helper routes > mints NO token when ALLOW_DEV_AUTH is not explicitly set, even in development 9ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > initiate: an org slug configured nowhere is refused (404), not sent to the default IdP 6ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > initiate: the default and the per-tenant slugs still reach their IdPs (control) 7ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: RelayState naming a slug configured nowhere is refused (404) before the response is validated 21ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: an existing user with no membership in the config org is refused (403), with a failure event in that tenant 10ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: a member signs in to the config org with THAT membership, never the first membership found 6ms
 × server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: the session carries the config organisation's idle window and session limit, not the platform defaults 11ms
   → the SSO session ignored the tenant's idle window: expected 900 to be 1800 // Object.is equality
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: a return path rides in the fragment with the session, never in a query string (IAM-18 item 6) 6ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: the membership lookup is constrained to the config org in the statement itself 5ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: a new user is provisioned with a membership in the config org only 8ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: an existing user's name is not overwritten from the assertion 4ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: an account that is not active is refused (403), with a failure event 7ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: a validation failure is refused (401) and written as a failure event in the config org's tenant 5ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: a configured slug with no organisation row is refused (403) and written as a failure event 5ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > logout: the session token is not accepted from the query string 13ms
 × server/routes/__tests__/authEnterprise-select-organization-rotation.test.ts > POST /select-organization rotates > mints a token for the selected organisation, keeps the session, and revokes the presented token 53ms
   → the presented token stayed live beside its successor: expected "vi.fn()" to be called with arguments: [ …(2) ]
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > logout: a bearer token in the Authorization header still starts SP-initiated SLO 7ms
 ✓ server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > logout: a token POSTed in the body still starts SP-initiated SLO 4ms
⎯⎯⎯⎯⎯⎯⎯ Failed Tests 5 ⎯⎯⎯⎯⎯⎯⎯
 FAIL  server/middleware/__tests__/auth-inactivity-logoff.test.ts > authenticateToken — inactivity logoff > optionalAuth attaches no user from a session that is over, and the request continues anonymous
AssertionError: an idle session attached a user: expected { id: '42', userId: '42', …(5) } to be undefined
 FAIL  server/routes/__tests__/authEnterprise-select-organization-rotation.test.ts > POST /select-organization rotates > mints a token for the selected organisation, keeps the session, and revokes the presented token
AssertionError: the presented token stayed live beside its successor: expected "vi.fn()" to be called with arguments: [ …(2) ]
 FAIL  server/routes/__tests__/ssoRoutes.test.ts > SAML SSO — the organisation is the one that owns the matched IdP config (IAM-03 / P0-3) > callback: the session carries the config organisation's idle window and session limit, not the platform defaults
AssertionError: the SSO session ignored the tenant's idle window: expected 900 to be 1800 // Object.is equality
 FAIL  server/services/__tests__/session-registry-redis.test.ts > the registry when Redis answers > decides in one atomic script: no read on one round trip and an eviction on the next
AssertionError: expected [] to have a length of 1 but got +0
 FAIL  server/services/__tests__/session-registry-redis.test.ts > the registry when Redis answers > Redis's decision is the only one: a session signed out on another task does not make this task end a live one
AssertionError: the memory tier ended a session Redis kept: expected [ Array(1) ] to deeply equal []
 Test Files  4 failed (4)
      Tests  5 failed | 26 passed (31)
exit=1
