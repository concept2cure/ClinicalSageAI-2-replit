# D6 — P1 closures from the 2026-09-24 security audit (Phase B, second tranche, 2026-09-25)

**Rows:** D6 (D5 for the Part 11 items). **Source:** `docs/security/SECURITY_AUDIT_2026-09-24.md` (findings) and
`docs/security/REMEDIATION_AND_ENHANCEMENT_PLAN_2026-09-24.md` §2 (items). **Rule:** as in the P0 tranche
(`../2026-09-24-p0/README.md`): every item shown failing first against the unchanged code (`red/`), fixed with the
smallest change that closes the finding, shown passing (`green/`), committed on its own with the item id in the subject.

| Item | Finding | Commit | Closed | Folder |
|---|---|---|---|---|
| P1-14 | INF-32, claims register §7.1/§7.2: dead `security.txt` policy link, Host-built canonical, questionnaire and trust statement overstating controls | `ee35804a` | yes (this session's parts; the one sub-processor list is INF-21's) | `P1-14/` |
| P1-6 | IAM-13 webhook delivery through a bare `fetch` following redirects, error bodies echoed | `eeaa8267` (+ `49995fa0` lint) | yes | `P1-6/` |
| P1-9 | IAM-12 main socket namespace: query-string token, no membership re-check | `901f9c9e` | yes | `P1-9/` |
| P1-4 | IAM-10 role from a stale JWT on guarded routes | `414f203e` | yes | `P1-4/` |
| P1-21 | DP-17 governed sign accepted a null or free-text meaning | `3d09bf2a` | vocabulary half; the first-signing acknowledgement and identity-proofing record remain | `P1-21-meaning/` |
| P1-3 | IAM-09 lockout counter read-then-write and fail-open; resend refilled guesses; `/api/v1/auth` unlimited | `1219a144` | this session's parts; the per-challenge resend cap closed 2026-09-26 (`IAM-18-8/`); Redis in production remains | `P1-3/` |
| P1-2 | IAM-08 recovery codes issued and never redeemable | `fdc1e53e` | recovery-code half (service + enterprise challenge); `routes/auth.ts` wiring and the emailed-code fallback wait on its window | `P1-2-recovery/` |
| P1-17 | IAM-18 (2)(3) `/api/metrics` readable by any user; public AI-gateway health with provider detail and exception echo | see `git log -- server/startup/operator-endpoints.ts` | yes; leak baseline 146 → 145 | `P1-17/` |
| P1-5 | IAM-14 unbounded memory upload in the stability router; no count of unguarded upload sites | see `git log -- scripts/ci/check-upload-guards.mjs` | the router and the gate; wiring and the 12-file sweep remain | `P1-5/` |
| P1-27 (first part) | DP-24 `ci:dead-audit-catch` red on trunk | `3625a205` (another lane) | closed on trunk; recorded in the register | — |
| IAM-18 residuals (4)(5)(9)(10) | detector mounted before auth; enterprise sign-in admitted inactive accounts; AI prefixes metered as API; AI-assistance mount ungated | see `git log -- server/services/audit/audit-api-authority.ts` (neighbouring commits) | yes; (6)(7)(8) written up in the folder, (8) closed at the main door with P1-2 part 2 and at the enterprise door with the IAM-18-8 commit (`IAM-18-8/`), (7) with the IAM-18-7 commit (`IAM-18-7/`: the 50 MB Concept2Cure parser behind the boundary), (6) with the IAM-18-6 commit (`IAM-18-6/`: the SSO session in the URL fragment; SAML replay mode `always`) | `P1-17-residuals/` |
| IAM-18 (4) re-check | the detector's public-path skip list was dead under the `/api` mount (mount-relative `req.path`); the impersonation audit row lost its `/api` prefix; no end-to-end proof through the boundary; an unmounted second detector (`tenantIsolation.ts`) that only logged | commit of 2026-09-26 | skip list deleted (the boundary's `PUBLIC_API_ALLOWLIST` is the one list); the audit row names the full path through the new shared `requestFullPath` (detector, both guards, the request tenant scope and the platform-route gate; `authBoundary.ts` joins when its window closes); a real-token end-to-end test through `applyAuthBoundary`: 403 `TENANT_MISMATCH` and the audit row for a foreign header, pass for the session's own, the boundary's 401 with no token; the dead second detector deleted | `P1-17-residuals/` §2026-09-26 (red: the audit row's path before; green: e2e after, gates) |
| P1-5 sweeps 1 and 2 | IAM-14 the nine remaining upload routes outside `authoring.router.ts` | two commits after the gate | 13 unguarded sites → 4 in 3 files (one hot, two gate limits) | `P1-5/sweep/` |
| P1-20 | DP-18 audit reads and exports for any member; free-text event types written | commits of 2026-09-26 | yes, the ledger list route included | `P1-20/` |
| P1-2 part 2 | IAM-08 recovery codes at the main challenge; emailed-code fallback for authenticator accounts; IAM-18 (8) timing oracle | commit of 2026-09-26 | yes (routes half) | `P1-2-recovery/` (red-routes, green-routes) |
| P0-12 part 2 (P0 item) | DP-08 model output ran 36 writes unaided | commits of 2026-09-26 | yes: server half (every write a proposal, confirm tier) and client half (the confirm-only step in the sign-off dialog) | `../2026-09-24-p0/P0-12/` |
| P1-27 (second part) | DP-26 browser Sentry events unscrubbed; DP-27 `Math.random` organisation API key | commit of 2026-09-26 | yes for both; DP-28 (`tamper_proof_log` tenant column) and the logger key list open | `P1-27/` |
| P1-27 (third part) | DP-26 server logger: e-mail and IP addresses written in clear to log lines | commit of 2026-09-26 | `redactContext` masks an e-mail address to its first character and domain and an IP address to its network part in every string value, at any depth; sensitive keys stay `[REDACTED]` whole | `P1-27/` (logger section) |
| P1-1 | IAM-06 no inactivity logoff, no session lifetime; a refresh renewed any session | three commits of 2026-09-26 | server: idle window (tenant-set, 15 min default) and 12-hour lifetime at both authenticators, the verifier and the refresh; client: idle warning, sign-out with the reason on the sign-in page; OQ-PROJ-19 written (not yet executed); the concurrent-session limit follows | `P1-1/` |
| P1-1 (concurrent-session limit) | IAM-06: nothing bounded the sessions one account could hold | fourth commit of 2026-09-26 | every sign-in registers its session through `openSession`; beyond `settings.security.maxConcurrentSessions` (five by default) the oldest is superseded and refused as `SESSION_SUPERSEDED` at both authenticators, the verifier and the refresh; sign-out frees the slot; client notice in 18 locales; the `tenant-config.ts` schema key and the OQ step follow the file's window | `P1-1-concurrency/` |
| P1-22 | DP-20 retention sweep unscheduled, no retention date ever set, no way to place or lift a legal hold, deletion audit in a file | commit of 2026-09-26 | yes but for per-organisation policies: sweep scheduled at boot, clock at admission, legal-hold routes, each disposition one transaction with its chained row | `P1-22/` |
| P1-13 (documents half) | INF-07 breach governance contradictory (DPA 48 h vs 72 h), no regulator timelines, no incident log, no access-review procedure, no training record | commit of 2026-09-26 | documents: IR-004 §3a matrix and the 72-hour tenant clock, `INCIDENT_LOG.md`, AC-002 §4a, `TRAINING_RECORD.md`, SIG G.4; the DPA figure (counsel), tabletop, first review and first completion are the founder's | — (policy files; no test) |
| P1-26 | DP-25 mixed clocks, no declared time source | commit of 2026-09-26 | VMP-001 v0.2 §7a: Amazon Time Sync declared (Fargate, RDS); every audit and signature timestamp is writer-generated UTC, never client-supplied; the chain is ordered by `chain_seq`, not by clock | `P1-26/` (writers.txt, the reading) |
| P1-2 (sign-up verification) | IAM-17: sign-up returned a day-long administrator session for any address, nothing showed the address was the holder's | commit of 2026-09-26 | the account is created `pending_verification`; a signed 24-hour link (URL fragment) moves it to `active` once; sign-in for a pending account is refused `AUTH_EMAIL_UNVERIFIED` at both doors; resend answers alike for every address; no mail service, no sign-up (503); client waiting step and verify page in 18 locales | `P1-2-signup/` |
| P1-2 (common passwords) | IAM-17: no check against commonly used passwords or the account's own words | commit of 2026-09-26 | a bundled list (11,197 entries, SecLists/NCSC) compared bare and behind decorations; the address, name, organisation and product refused inside a password; every caller passes its context | `P1-2-passwords/` |
| P1-8 | IAM-11: drafting tasks recorded against any project id and read by id alone, with a process-wide memory fallback; stability result PATCH/DELETE by id alone, no audit row | commit of 2026-09-26 | a drafting task is written and read only through a program the session's organisation owns (404 otherwise; a failed write or read is 503, never a memory copy); a stability result is changed or deleted only as the tenant's own row, in one transaction with its `stab_audit` record (`result_update` with previous values and the fields changed, `result_delete` with the deleted values); the unmounted duplicate `routes/templates.ts` and the unkeyed file audit logger deleted, unreferenced-modules baseline 88 → 87 | `P1-8/` (README; red: 14 of 14 cases failing before; green: 38 passing after, gates) |
| P1-17 (paydown) | IAM-18 (1): 145 baselined 5xx bodies still carried the caught error's text | commit of 2026-09-26 | the first 25 cold sites in 12 files answer through `serverError()` (static envelope, detail logged against the request id); the two coded 503s in `ana-features.ts` say a static sentence; baseline 145 → 120 sites / 89 → 77 files, shrunk by the gate's own documented path; the gate now runs in `.husky/pre-push`; 7 sites in 4 hot files and 113 cold sites remain | `P1-17-paydown/` (README; red: gate and list before, 5 containment cases failing; green: gate at 120/77, 13 files / 147 tests) |
| IAM-18 (8) enterprise door + P1-3 resend cap | the enterprise `verify-password` step answered an unknown e-mail, and both doors a null-hash account, without a bcrypt comparison; nothing counted the codes one challenge minted, and a cleared row refilled the five guesses | commit of 2026-09-26 | one shared pad `services/login-timing-pad.ts` at both doors (cost 12); `users.email_otp_resends`, `reissueEmailOtp` in one conditional UPDATE, at most 3 re-issued codes per challenge, `/mfa/resend` 429 `MFA_RESEND_LIMIT` with its audit row, a fresh challenge costs the password; the column added by amending `20260923_users_mfa_totp_last_step.sql` in place (Rule 1; `migration-set.mjs` hot); the column gates cannot see a drizzle-only column, the PGlite migration-contract case can; the client's `ApiClient` now reads a nested `{ error: { code, message } }` so the sentence is shown | `IAM-18-8/` (README; red: 0 bcrypt calls, no cap, 42703 without the amendment; green: 4 suites, neighbours 51/51, gates) |
| P1-34 (registry half) | DP-36: the propose-only partition covered the command bridge only; ~170 directly registered AnA write tools ran with no person confirming | commit of 2026-09-26 | `propose-only-tools.ts` (161 proposals, 22 refusals, 12 read-only exclusions) with the source-derived anti-drift test; `classifyDirectToolCall`; the MCP door refuses a proposal or a refused act and audits the denial; the wrapper gate, `/governed-action` tool branch and stream hold-and-ask wait for their windows (README §5 has the steps) | `P1-34/` (README; red: 18/25 with an empty registry, 7 gate cases, 6 MCP cases; green: 25/25, 22/22, 9/9, pins 16/16) |
| P1-7 (`x-client-id` half) | IAM-15: the `X-Client-ID` workspace header was trusted by the feature gate (a toggle enabled for one organisation's workspace opened for anyone naming that id) and written unverified into `documents` / `regulatory_submissions.client_workspace_id` by the cerv2 save and regulatory-submissions routes | commit of 2026-09-26 | one ownership predicate (`FeatureToggleService.workspaceInOrganization`, fail-closed) before the gate trusts the claim or a route writes it, 403 on a foreign workspace; `workspace-trust-header` in `check:security-patterns` (4 → 0); the `x-org-uuid` half by lane `…01W5zW66` (`b1618c69`); deferred `cortex-unified.ts:128` (hot until 2026-09-27 04:43 UTC); the two-tenant dbtest written, unexecuted here | `P1-7b/` (README; red: 15 of 20 cases and 4 gate violations before; green: 20 pass, gate 0, neighbours 61/61) |

## Not reached in this tranche, and why

- **Hot files (board §0).** `routes/authoring.router.ts` (until 23:24 UTC 09-26): its two upload sites.
  `.husky/pre-push` (07:23 UTC 09-26) and `package.json` (another lane's merge touched it at 02:20 UTC 09-26): wiring of
  `ci:upload-guards` and `ci:trivyignore-hygiene`. `routes/auth.ts` and `routes/audit-trail-routes.ts` left their
  windows and were done (P1-2 part 2, P1-20).
- **Larger items** left with their acceptance tests in the plan: P1-1's concurrent-session limit (the server and
  client halves and the OQ step landed), P1-22's per-organisation policies (the rest landed), P1-24/P1-25 (D5; P1-19's `audit_logs` half was closed by the D5 lane's VR-02 on 2026-09-25, and P1-19b waits on `environment.ts`), P1-7's remaining `x-client-id` half (in progress) and P1-8 (closed 2026-09-26), the Terraform and workflow items (W2), the founder's (P1-10, P1-11,
  P1-13, P1-15, P1-16, Redis).
- **Helper agents:** one completed the second upload sweep under the control tower; one dispatched for the P0-12 client
  half did not report back and wrote nothing, so that half is a hand-off with its contract in the P0-12 README.

## Trunk CI

Trunk CI was red before this lane's first push and after every one of them, on the same three jobs and steps, and this
lane added no failing step: Lint ("server SQL references a table nothing creates", "Proof tier", "requestDb adoption",
"ESLint warning ratchet" — the tree-wide ratchet, distinct from the per-push one), Security Scan (Trivy config,
`AWS-0011` CloudFront without a WAF, the founder's INF-05 decision) and Test ("Run tests"). Compared on `5f1e35b0`
(this lane) against `ba25be2f` (the neighbouring commit): identical failing steps, one fewer in this lane's run.

## How to read a folder

As in the P0 tranche: `README.md` states what was wrong (with `file:line`), what is true now, a red/green table naming
the tests and the HEAD the red ran against, and what is not done; `red/` and `green/` hold verbatim output ending in an
`exit=` line.
