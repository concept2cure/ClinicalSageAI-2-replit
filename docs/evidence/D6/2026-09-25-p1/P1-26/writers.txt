# Which clock stamps each audit and signature record, read at 173549d2 on 2026-09-26T04:39:07Z

## audit_logs.occurred_at (writeChainedAuditRow)
269:  const occurredAt = new Date().toISOString();
284:    occurred_at: occurredAt,
290:        actor_id, target, payload_hash, sha256_chain, occurred_at, hmac_seal,
304:      occurredAt,

## electronic_signatures.signed_at
270:  signedAt: Date;
287:): Promise<{ id: number; signedAt: Date }> {
324:       ip_address, device_info, signed_at, bound_payload_digest, organization_id
333:     ) RETURNING id, signed_at`,
358:      record.signedAt,
363:  return { id: result.rows[0].id as number, signedAt: result.rows[0].signed_at as Date };
705:): Promise<{ id: number; signedAt: Date }> {
733:  const signedAtIso = params.occurredAt.toISOString();

## audit.tamper_proof_log.event_timestamp
141:  event_timestamp: Date | string;
211:      timestamp: row.event_timestamp,
435:      const timestamp = new Date();
467:          id, event_type, event_timestamp, user_id, user_name, session_id,
635:      query += ` AND event_timestamp >= $${params.length}`;
639:      query += ` AND event_timestamp <= $${params.length}`;

## audit_events (client-recorded): timestamp and created_at are the database's NOW()
325:         VALUES ($1, $2, $3, $4, $5, $6, $7, $8, NOW(), $9, $10, $11, $12, NOW()) RETURNING id`,
397:             VALUES ($1, $2, $3, $4, $5, $6, $7, $8, NOW(), $9, $10, $11, $12, NOW()) RETURNING id`,
495:         VALUES ($1, 'signature.create', $2, $3, $4, $5, $6, NOW(), 'unverified', $7, NOW(), $8, $9, $10, true, true, NOW()) RETURNING id`,

## No audit or signature writer reads a time from a request body or header
(no matches)

## Chain order is chain_seq, not the clock
31: *   • reads that tenant's head by AUDIT_CHAIN_HEAD_ORDER_SQL — chain_seq, the
86:export const AUDIT_CHAIN_ORDER_ASC_SQL = 'chain_seq ASC NULLS FIRST, occurred_at ASC, id ASC';
87:export const AUDIT_CHAIN_HEAD_ORDER_SQL = 'chain_seq DESC NULLS LAST, occurred_at DESC, id DESC';

## Compute and database run on Fargate and RDS
125:  requires_compatibilities = ["FARGATE"]
199:  requires_compatibilities = ["FARGATE"]
239:  launch_type     = "FARGATE"
287:  launch_type     = "FARGATE"
13:  engine                = "postgres"
