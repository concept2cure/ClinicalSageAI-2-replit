# Mutation check of the D6 one-time-credential fixes against tests/db/one-time-credentials.dbtest.ts (real PostgreSQL)
# 2026-09-23T08:29:49Z. Each mutant is applied alone, the file run, and the source restored.

M1 same step accepted again (lt -> lt step+1): KILLED  [8 ⎯⎯⎯⎯⎯⎯⎯]
    × a code opens one session (POST /api/auth/mfa/verify) > refuses the same code the second time 120ms
    × a code opens one session (POST /api/auth/mfa/verify) > refuses a code from a step earlier than the last accepted, though that code was never used 34ms
    × a code opens one session (POST /api/auth/mfa/verify) > accepts the next code: a refusal is per step, not a lockout 31ms
    × concurrent verifications of one code > two sign-ins racing one code: exactly one session 73ms
    × concurrent verifications of one code > eight verifiers racing one code: exactly one accepts it 34ms
    × one record of used codes, whichever path verifies > the enterprise sign-in refuses a code it already accepted 91ms
    × one record of used codes, whichever path verifies > the password sign-in refuses a code the enterprise sign-in used 29ms
    × signing: the pre-check does not use the code, the signature does > a used code neither checks nor signs again 16ms
M2 read-then-write instead of compare-and-set: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × concurrent verifications of one code > eight verifiers racing one code: exactly one accepts it 46ms
M3 pre-check accepts the last used step: KILLED  [2 ⎯⎯⎯⎯⎯⎯⎯]
    × signing: the pre-check does not use the code, the signature does > a used code neither checks nor signs again 28ms
    × the email one-time code is used once, and its attempts are counted once each > eight verifiers racing one emailed code: exactly one accepts it 61ms
M4 pre-check consumes the code: KILLED  [2 ⎯⎯⎯⎯⎯⎯⎯]
    × signing: the pre-check does not use the code, the signature does > the modal can check a code, even twice, and it is still unused 92ms
    × signing: the pre-check does not use the code, the signature does > the signature then accepts that same code, and uses it 13ms
M5 matched step reported as the current step: KILLED  [9 ⎯⎯⎯⎯⎯⎯⎯]
    × a code opens one session (POST /api/auth/mfa/verify) > refuses the same code the second time 116ms
    × a code opens one session (POST /api/auth/mfa/verify) > refuses a code from a step earlier than the last accepted, though that code was never used 43ms
    × a code opens one session (POST /api/auth/mfa/verify) > accepts the next code: a refusal is per step, not a lockout 81ms
    × concurrent verifications of one code > two sign-ins racing one code: exactly one session 86ms
    × concurrent verifications of one code > eight verifiers racing one code: exactly one accepts it 48ms
    × one record of used codes, whichever path verifies > the enterprise sign-in refuses a code it already accepted 89ms
    × signing: the pre-check does not use the code, the signature does > the modal can check a code, even twice, and it is still unused 66ms
    × signing: the pre-check does not use the code, the signature does > the signature then accepts that same code, and uses it 12ms
    × signing: the pre-check does not use the code, the signature does > the pre-check is limited per signer, since it answers without using the code 65ms
M6 email consume not conditional on the stored code: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × the email one-time code is used once, and its attempts are counted once each > eight verifiers racing one emailed code: exactly one accepts it 34ms
M7 email attempts not bounded in the counting UPDATE: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × the email one-time code is used once, and its attempts are counted once each > twelve concurrent wrong guesses exhaust the five attempts, and the right code is then refused 39ms
M8 pre-check route not limited: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × signing: the pre-check does not use the code, the signature does > the pre-check is limited per signer, since it answers without using the code 57ms
M9 reset writes the password without re-checking the token: SURVIVED  [16 passed (16)]

## M2 repeated 5 times (-t racing, earlier run): the eight-way race killed it every time. The two-request
## HTTP race does not: HTTP serialises the pair enough for a read-then-write to pass, which is why
## the eight-way service-level race exists.
0 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']
1 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']
2 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']
3 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']
4 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']

M9b reset written by account id alone (the pre-fix write): KILLED
    × a password-reset token is used once > two resets racing one token: exactly one succeeds, and its password is the one set 809ms

## M9 survives, and is equivalent under every interleaving the suite can force. The first reset to
## complete clears reset_token_expires_at, so the expiry condition alone refuses the second. The
## token condition matters only when a NEW token is issued while an older request is mid-flight
## (the user asks for another link during the older request's bcrypt hash); then the expiry is set
## again, and only 'reset_token = this token' refuses the older request. No hook exists to pause a
## request between its read and its write, so that interleaving is argued here, not tested.

## Added after the adversarial review of the D6 commits (2026-09-23T10:04:34Z), each case shown killing its mutant
M10 a new secret keeps the old secret's last step: KILLED
    × replay state belongs to one secret > a new authenticator's first code is not refused for the old one's last step 19ms
M11 email attempts bounded at six, not five: KILLED
    × the email one-time code is used once, and its attempts are counted once each > after 5 wrong guesses the right code is accepted: false (the limit is exactly five) 16ms
