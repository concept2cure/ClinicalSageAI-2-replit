# Mutation check of the D6 one-time-credential fixes against tests/db/one-time-credentials.dbtest.ts (20 cases, real PostgreSQL)
# 2026-09-23T10:19:09Z, tree 902c2607d + working tree. Each mutant is applied alone, the file run, and the source restored.

M1 same step accepted again (lt -> lt step+1): KILLED  [8 ⎯⎯⎯⎯⎯⎯⎯]
    × a code opens one session (POST /api/auth/mfa/verify) > refuses the same code the second time 99ms
    × a code opens one session (POST /api/auth/mfa/verify) > refuses a code from a step earlier than the last accepted, though that code was never used 34ms
    × a code opens one session (POST /api/auth/mfa/verify) > accepts the next code: a refusal is per step, not a lockout 33ms
    × concurrent verifications of one code > two sign-ins racing one code: exactly one session 69ms
    × concurrent verifications of one code > eight verifiers racing one code: exactly one accepts it 29ms
    × one record of used codes, whichever path verifies > the enterprise sign-in refuses a code it already accepted 79ms
    × one record of used codes, whichever path verifies > the password sign-in refuses a code the enterprise sign-in used 25ms
    × signing: the pre-check does not use the code, the signature does > a used code neither checks nor signs again 16ms
M2 read-then-write instead of compare-and-set: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × concurrent verifications of one code > eight verifiers racing one code: exactly one accepts it 28ms
M3 pre-check accepts the last used step: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × signing: the pre-check does not use the code, the signature does > a used code neither checks nor signs again 10ms
M4 pre-check consumes the code: KILLED  [2 ⎯⎯⎯⎯⎯⎯⎯]
    × signing: the pre-check does not use the code, the signature does > the modal can check a code, even twice, and it is still unused 67ms
    × signing: the pre-check does not use the code, the signature does > the signature then accepts that same code, and uses it 11ms
M5 matched step reported as the current step: KILLED  [10 ⎯⎯⎯⎯⎯⎯⎯]
    × a code opens one session (POST /api/auth/mfa/verify) > refuses the same code the second time 93ms
    × a code opens one session (POST /api/auth/mfa/verify) > refuses a code from a step earlier than the last accepted, though that code was never used 29ms
    × a code opens one session (POST /api/auth/mfa/verify) > accepts the next code: a refusal is per step, not a lockout 30ms
    × concurrent verifications of one code > two sign-ins racing one code: exactly one session 57ms
    × concurrent verifications of one code > eight verifiers racing one code: exactly one accepts it 28ms
    × one record of used codes, whichever path verifies > the enterprise sign-in refuses a code it already accepted 43ms
    × signing: the pre-check does not use the code, the signature does > the modal can check a code, even twice, and it is still unused 46ms
    × signing: the pre-check does not use the code, the signature does > the signature then accepts that same code, and uses it 14ms
    × signing: the pre-check does not use the code, the signature does > the code pre-check is metered per signer, and a wrong code counts against the account 62ms
    × replay state belongs to one secret > a new authenticator's first code is not refused for the old one's last step 4ms
M6 email consume not conditional on the stored code: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × the email one-time code is used once, and its attempts are counted once each > five verifiers racing one emailed code: exactly one accepts it 24ms
M7 email attempts not bounded in the counting UPDATE: KILLED  [2 ⎯⎯⎯⎯⎯⎯⎯]
    × the email one-time code is used once, and its attempts are counted once each > after 5 wrong guesses the right code is accepted: false (the limit is exactly five) 20ms
    × the email one-time code is used once, and its attempts are counted once each > twelve concurrent wrong guesses exhaust the five attempts, and the right code is then refused 66ms
M8 pre-check route not limited: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × signing: the pre-check does not use the code, the signature does > the code pre-check is metered per signer, and a wrong code counts against the account 66ms
M8b password pre-check route not limited: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × signing: the pre-check does not use the code, the signature does > the password pre-check is metered per signer too, on its own budget, and a wrong password counts 77ms
M9b reset written by account id alone (the pre-fix write): KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × a password-reset token is used once > two resets racing one token: exactly one succeeds, and its password is the one set 646ms
M10 a new secret keeps the old secret's last step: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × replay state belongs to one secret > a new authenticator's first code is not refused for the old one's last step 24ms
M11 email attempts bounded at six, not five: KILLED  [1 ⎯⎯⎯⎯⎯⎯⎯]
    × the email one-time code is used once, and its attempts are counted once each > after 5 wrong guesses the right code is accepted: false (the limit is exactly five) 13ms
M9 reset writes the password without re-checking the token: SURVIVED  [20 passed (20)]

## M2 repeated 5 times (earlier run, -t racing): the eight-way TOTP race killed it every time; the two-request HTTP
## race does not, which is why the eight-way case exists.
0 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']
1 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']
2 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']
3 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']
4 ['✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > two sign-ins rac', '× tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > concurrent verifications of one code > eight verifiers ', '✓ tests/db/one-time-credentials.dbtest.ts (then named totp-replay) > the email one-time code is used once, and its attempts ']

## M9 survives, and is equivalent under every interleaving the suite can force. The first reset to
## complete clears reset_token_expires_at, so the expiry condition alone refuses the second. The
## token condition matters only when a NEW token is issued while an older request is mid-flight
## (the user asks for another link during the older request's bcrypt hash); then the expiry is set
## again, and only 'reset_token = this token' refuses the older request. No hook exists to pause a
## request between its read and its write, so that interleaving is argued here, not tested.

## Note: the two pre-check limiter cases were restated by F-27 (c3e891bba, another session) to meter
## both the per-signer rate limit and the account's lockout; M8 and M8b are killed by those cases.
