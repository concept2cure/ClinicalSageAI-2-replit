[
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "db-defaults",
  "file": "db/migrations/20260730_cmc_change_control_store.sql",
  "line": 29,
  "symbol": "cmc_change_controls (CREATE TABLE)",
  "votes": "3/3",
  "absentBecomes": "\"nobody has assessed whether this post-approval change touches a critical process step\" becomes \"the sponsor has recorded that it does not\". server/services/cmc/cmc-change-control-service.ts:89 forces it: `VALUES ($1,...,COALESCE($11,false),$12,COALESCE($13,false),$14,$15,$16)` while the route (server/routes/cmc-changes.routes.ts:90) deliberately passes `null` when the field is absent. server/services/cmc/cmc-change-view.ts:86 then writes `touchesCriticalStep: row.touches_critical_step ?? undefined` \u2014 dead code, because a NOT NULL column never yields null, so the classifier's optional `touchesCriticalStep?: boolean` can never receive \"unknown\".",
  "shouldBe": "`touches_critical_step BOOLEAN` \u2014 nullable, no default \u2014 so the `?? undefined` mapping in cmc-change-view.ts and the optional `touchesCriticalStep?: boolean` in supac-classifier.ts:80 mean what they were written to mean. `classifyFda` must then refuse to classify (or return the strictest category, PAS) when the flag is undefined, instead of falling through the `!input.touchesCriticalStep` branches.",
  "consequence": "A post-approval manufacturing change is shown to the regulatory lead, and stored as the platform's verdict, with the wrong FDA reporting category and a 'low' risk band. supac-classifier.ts:203 gates the PAS branch on `if (input.touchesCriticalStep && (changeCategory === 'manufacturing_process' | 'manufacturing_site' | 'components_composition'))`; with the default false that branch never fires and the change falls through to `cbe_30` or `annual_report`. Worse, lines 246-262 read `&& !input.touchesCriticalStep` affirmatively: a specifications change nobody assessed returns `cbe_0` \u2014 \"Specification tightening (non-critical) \u2014 CBE-0 supplement effective on submission\" \u2014 and cmc-change-view.ts:70 renders `risk: 'low'`. Implementing a change that required a Prior Approval Supplement without one is an unapproved change under 21 CFR 314.70(b); the platform told the sponsor it was effective on submission.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "db-defaults",
  "file": "migrations/20260705_export_control.sql",
  "line": 29,
  "symbol": "export_control_reviews (CREATE TABLE)",
  "votes": "3/3",
  "absentBecomes": "\"nobody has been asked whether the sponsor imposed publication or proprietary/access restrictions, or whether foreign nationals will have access\" becomes \"the institution has recorded that there are none\". server/services/export-control/export-control-service.ts:58 writes `input.hasPublicationRestrictions === true` on create (absent -> false), and export-control-logic.ts:55-56 reads it as a settled answer: `const restricted = input.hasPublicationRestrictions === true || input.hasProprietaryRestrictions === true;` / `const freApplies = !restricted && !(input.involvesPhysicalExport === true);`",
  "shouldBe": "All three restriction/access columns nullable with NO DEFAULT (the tri-state the sibling column `license_required text NOT NULL DEFAULT 'unknown'` already models correctly on this same table). `evaluateExportControl` must treat NULL as unknown and return `licenseRequired: 'unknown'` rather than 'no', and `evaluateExportReviewReadiness` must add a blocker: \"the publication/proprietary-restriction and foreign-national questions have not been answered\". Narrowing on absence is right here; granting an exclusion on absence is not.",
  "consequence": "An export-control officer, and later a federal auditor, is told the project qualifies for the Fundamental Research Exclusion and needs no export licence, on the basis of questions nobody answered. Concretely: create an EAR review with an ECCN of EAR99 and never touch the restriction fields; `evaluateExportReviewReadiness` raises no blocker (its blockers are only pending jurisdiction, missing classification, flagged-foreign-without-country, and licenseRequired==='unknown'), so `determineReviewTx` sets status='determined', `license_required='no'`, `fundamental_research_exclusion=true`, with the finding text \"EAR information qualifies for the Fundamental Research Exclusion \u2014 no license for the resulting information (15 CFR 734.8).\" A sponsor-imposed publication restriction is the single fact that defeats the FRE; a deemed export of controlled technology to a foreign national without a licence is a criminal EAR violation, and the record now says the institution cleared it.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "boolean-verdicts",
  "file": "server/services/capa-mdr/capaMdr.service.ts",
  "line": 299,
  "symbol": "createComplaint",
  "votes": "3/3",
  "absentBecomes": "`CreateComplaintInput` declares `patientHarm?: HarmLevel` and `severityAssessment?: SeverityLevel` (lines 284-285) as optional. A complaint intake that omits them is persisted as `patientHarm: 'none'` and `severityAssessment: 'minor'` (line 325-326), and `preliminaryClassification` (line 327) is stored as the not-reportable verdict computed from those invented values. The DB reinforces it: `patientHarm: varchar('patient_harm', {length: 24}).notNull().default('none')` and `severityAssessment: ... .notNull().default('minor')` (shared/schema/capa-mdr.ts:240-241) \u2014 a NOT NULL DEFAULT on a tri-state fact, with no column recording whether a human ever assessed harm.",
  "shouldBe": "Harm and severity should be nullable (or carry an explicit `unknown` enum member), and `createComplaint` should refuse to compute and persist a `preliminaryClassification` at all when harm has not been assessed \u2014 storing `null` with a triage state that demands assessment, rather than storing a computed \"not reportable\" verdict derived from a default.",
  "consequence": "A complaint taken from a call centre or an API integration that does not carry a harm field is filed with an audit-logged, machine-computed preliminary classification asserting no FDA MDR, no EU MDR vigilance report and no 21 CFR 806 notice \u2014 and, per the triage engine's own docstring (\"seed the reportability clocks so the UI can show aging immediately on intake\"), no reportability clock is started. The complaint sits in the queue showing zero days aging against a 30-day 21 CFR 803.50 obligation that may have started the day it was received. The record reads as a determination; nothing was determined.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "nullish-defaults",
  "file": "server/services/capa-mdr/capaMdr.service.ts",
  "line": 299,
  "symbol": "createComplaint",
  "votes": "3/3",
  "absentBecomes": "A device complaint whose patient-harm level has not yet been assessed (input.patientHarm undefined \u2014 server/routes/capa-mdr.ts:201 validates it ONLY when defined, and passes it through undefined at line 222) becomes patientHarm: 'none'. HARM_LEVELS (shared/schema/capa-mdr.ts:86) is ['none','malfunction','injury','serious_injury','death'] \u2014 it has no 'unknown' / 'not_assessed' member, and the column is varchar('patient_harm').notNull().default('none'). The same call also writes patientHarm: input.patientHarm ?? 'none' and severityAssessment: input.severityAssessment ?? 'minor' into the complaints row (lines 325-326).",
  "shouldBe": "HARM_LEVELS needs a 'not_assessed' member (and the column must allow it). classify() must return a 'cannot_classify' preliminary classification for it \u2014 fdaReportable/mdrEuReportable/requires806Notice as an undetermined state with a rationale of 'patient harm not yet assessed', with the complaint held in triageState 'new' and blocked from closure until a human records the harm level. Intake must never synthesise the harm level it exists to capture.",
  "consequence": "classify() falls to `case 'none'` in server/services/capa-mdr/triageEngine.ts:171-174 and returns fdaReportable: false, mdrEuReportable: false, requires806Notice: false, suggestedFdaReportType: null, rationale 'No harm reported \u2014 log under 820.198, no MDR', autoComputed: true. That verdict is persisted as the complaint's preliminaryClassification. A complaint that arrived by phone with the harm field left blank is recorded, and shown to an FDA or Notified Body inspector, as a device complaint affirmatively determined to be non-reportable under 21 CFR 803 and EU MDR Article 87, with the engine's own rationale asserting 'No harm reported'. Nobody reported 'no harm'; nobody reported anything. The 30-day / 5-day MDR clock and the 21 CFR 806 10-business-day correction notice never start. eventLocationCountry ?? null compounds it: isEUEvent(null) returns false, so an unrecorded event country also reads as 'not in the EU' and suppresses EU MDR reportability.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "boolean-verdicts",
  "file": "server/services/capa-mdr/triageEngine.ts",
  "line": 179,
  "symbol": "classify",
  "votes": "3/3",
  "absentBecomes": "An unrecorded / unknown `patientHarm` falls through the switch's `default:` arm together with the explicitly-recorded value `'none'`, producing `fdaReportable: false`, `mdrEuReportable: false`, `requires806Notice: false`, `suggestedFdaReportType: null` and the rationale string \"No harm reported\". `HARM_LEVELS = ['none','malfunction','injury','serious_injury','death']` (shared/schema/capa-mdr.ts:86) has no `unknown` / `not_assessed` member, so the verdict object cannot say \"harm was never assessed\".",
  "shouldBe": "`HarmLevel` needs an explicit `unknown` (or the triage input needs a `harmAssessed: boolean`), and `classify` needs a fourth reportability state \u2014 e.g. `fdaReportable: boolean | 'undetermined'` \u2014 so an unassessed complaint blocks and seeds the 30-day clock conservatively instead of resolving to \"no MDR\". The file's own header states the intended rule: \"Rules are intentionally conservative: when in doubt, flag reportable\" (line 16) \u2014 the `default:` arm does the opposite. Note the same collapse one case earlier: `isMalfunction = false` (line 97) makes a `patientHarm: 'malfunction'` complaint with no recurrence-harm assessment read \"Malfunction without harm-likelihood signal \u2014 review for trend only\", contradicting the comment three lines above it that says \"Default to flagging.\"",
  "consequence": "`AnaToolExecutor.ts:6172-6177` registers `assess_mdr_reportability`, which passes `patientHarm: input.patientHarm as any` and `isMalfunction: input.isMalfunction === true`, then instructs the model: \"Report the reportability determination verbatim: FDA MDR, EU MDR vigilance, 806 notice, suggested jurisdiction/report-type/severity, and the rationale.\" So asking AnA to assess an adverse device event without stating the harm level returns, verbatim to the user, a 21 CFR 803 determination of \"No harm reported \u2014 log under 820.198, no MDR\" for an event nobody has assessed. An FDA investigator auditing the complaint file is told the firm made a reportability determination of not-reportable; what actually happened is that the harm question was never answered.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "percentages",
  "file": "server/services/data-lineage-service.ts",
  "line": 418,
  "symbol": "getLineageCoverage",
  "votes": "3/3",
  "absentBecomes": "Sections with NO lineage record are absent from `fieldCoverage`, because `fieldCoverage` is built by iterating `records` (the lineage rows that exist) and keying on `rec.targetField`. So the denominator `totalSections` is the count of sections that HAVE lineage, and the numerator `sectionsWithLineage` is the same number. `coveragePercent` is arithmetically 100 for every object that has at least one lineage record, and `gaps` is hard-coded to `[]` unless there are zero records.",
  "shouldBe": "The denominator must be the object's actual section/field inventory (the sections the artifact contains), obtained independently of the lineage table \u2014 not the key count of the lineage rows that happened to be found. Sections in that inventory with no lineage row belong in `gaps`. If the section inventory cannot be resolved for this object type, `coveragePercent` must be null with an explicit `assessed: false`, not a number.",
  "consequence": "The route that serves this (`GET /api/data-lineage/coverage/:objectType/:objectId`, mounted at `/api/data-lineage` by server/bootstrap/register-advanced-platform-routes.ts:233) documents itself as answering \"What % of this artifact has source attribution? Identifies which sections/fields have lineage and which are gaps.\" A 40-section eCTD module with exactly one recorded lineage row is reported to an auditor as `totalSections: 1, sectionsWithLineage: 1, coveragePercent: 100, gaps: []` \u2014 \"100% of this document has traceable source attribution, no gaps.\" The correct answer is 2.5% with 39 gaps. Contrast server/services/sentenceTraceabilityService.ts:645 and server/services/clinical-regulatory-evidence/span-lineage.service.ts:1205, which both divide by the full population and are right.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "fixed 78420cb02",
  "severity": "critical",
  "angle": "error-swallowing",
  "file": "server/services/governance-boundary-service.ts",
  "line": 341,
  "symbol": "GovernanceBoundaryService.evaluateTransition (gate 3: contradiction gate; gate 4 at line 382)",
  "votes": "3/3",
  "absentBecomes": "An exception anywhere in the contradiction gate (the dynamic import, `checkPromotionBlocked`, or its query) contributes ZERO entries to `blockedReasons`. The identical pattern sits at line 382 for the fabric readiness gate: `} catch { // Fabric readiness gate unavailable \u2014 continue without (non-blocking degradation) }`. Line 396 then computes `let allowed = blockedReasons.length === 0;` \u2014 so two skipped gates are arithmetically identical to two passed gates.",
  "shouldBe": "Both catches must push a blocked reason, the way every other failure path in this same function already does \u2014 the rules loader at line 191 (\"Failing closed: transition to ${request.toBoundary} is blocked until the rule store is reachable\"), the confidence gate at line 315 (\"confidence gate could not be evaluated \u2026 Failing closed\"), and the audit-persistence catch at line 424 (\"a transition without a durable audit record is not allowed\"). e.g. `blockedReasons.push('Contradiction gate could not be evaluated (\u2026). Failing closed.')`.",
  "consequence": "A promotion to `approved`, `locked` or `submission_ready` \u2014 reached from the Authoring surface at server/routes/authoring-actions.ts:511, 816, 1023 and 1207 \u2014 succeeds while the two gates that check for unresolved blocking contradictions and fabric readiness never ran. The append-only audit row inserted at line 406 records `transitionAllowed: true` with `blockedReasons: []`. An auditor reading `governance_boundary_transitions` is told the artifact cleared every governance gate at the moment it was locked; in fact two of the five were never executed, and nothing anywhere records that.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "fixed 78420cb02",
  "severity": "critical",
  "angle": "error-swallowing",
  "file": "server/services/governance/separation-of-duties.ts",
  "line": 105,
  "symbol": "resolveTargetOwnerId / assertSignerIsNotAuthor",
  "votes": "3/3",
  "absentBecomes": "Any failure of the owner-resolution query \u2014 statement timeout, dropped connection, deadlock, permission denied (42501), an RLS refusal \u2014 becomes `null`, which is the SAME value the function returns for the documented, benign case \"this target type has no modelled owner column\". `assertSignerIsNotAuthor` (line 125) then reads `ownerId === null` and ALLOWS the signature: `if (ownerId !== null && ownerId === signerId) throw`. The module header calls this invariant \"the un-disableable Part 11 invariant\u2026 There is NO flag to turn this off\" \u2014 and a transient DB error turns it off.",
  "shouldBe": "The resolver must return a three-state result, e.g. `{ kind: 'owner', id } | { kind: 'not-modelled' } | { kind: 'unresolved', reason }`. Only 'not-modelled' (and the explicit 42P01/42703 un-migrated case) may degrade to allow; 'unresolved' must throw a SeparationOfDutiesError (\"authorship could not be verified \u2014 a different authorized user must sign, or retry\"), exactly as the sibling gate in governance-boundary-service.ts does with \"Failing closed\".",
  "consequence": "During any database blip, the author of an eCTD sequence, document, section, work item or program can execute `sign` or `lock` on their own record at POST /api/c2c/actions (server/routes/c2c/actions.ts:583). The Part 11 e-signature row is written with no marker that the four-eyes check was skipped. An auditor inspecting the signature manifest is told a two-person control was enforced on that sequence when it was not \u2014 and the only trace is a console.warn that says 'degraded', indistinguishable from the benign un-modelled-target case that the same line prints.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "percentages",
  "file": "server/services/innovation/submission-readiness-twin-service.ts",
  "line": 898,
  "symbol": "calculateDimensionScores",
  "votes": "2/3",
  "absentBecomes": "\"Consistency\" is defined as the spread of the criterion scores, so a dossier in which NOTHING is present scores maximally consistent. Every mandatory criterion with no content gets `score = 0` (line 836); if the program has no content at all, every score is 0, `avgScore` is 0, `variance` is 0, and `consistency` is 100. The same holds when `evaluations` is empty: `reduce` over `[]` is 0, divided by `Math.max(0,1)`, giving variance 0 and consistency 100.",
  "shouldBe": "Consistency must measure agreement between recorded facts across sections (dates, doses, N, endpoints), not the dispersion of a completeness score. A dimension with no basis to compute must be null / 'not_assessed', and the overall score must exclude it rather than absorb a 100.",
  "consequence": "An empty or wholly unstarted submission is written to `innovation.readiness_twin_assessments.consistency_score = 100` (INSERT at line ~700, `dimensionScores.consistency`). server/services/intelligence/readiness-scoring-engine.ts:450 then reads that column back and weights it at 20% of the unified readiness score (line 171), and AnaToolExecutor's `get_submission_readiness_twin` instructs the model to \"Lead with the overall score... Report per-module readiness.\" A program with zero authored content is reported to a customer as scoring 100/100 on cross-document consistency, and is handed 20 readiness points for it.",
  "note": "1 of 3 verifiers refuted on reachability: Innovation routes are not mounted (register-advanced-platform-routes.ts:65-82) and initializeInnovationRoutes has zero callers. The arithmetic is right, but no production path reaches it today. Treat as latent."
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "percentages",
  "file": "server/services/intelligence/readiness-scoring-engine.ts",
  "line": 454,
  "symbol": "gatherTwinAssessment",
  "votes": "2/3",
  "absentBecomes": "Every one of these columns is NULLABLE in the DDL (db/migrations/072_gcc_innovation_platform_core.sql:351-359 \u2014 `quality_score FLOAT CHECK (...)`, `predicted_review_time_days INT`, `predicted_deficiency_count INT`, all without NOT NULL), and the service that writes rows explicitly writes `predicted_approval_probability` as NULL by design (comment at line ~715: \"is now written NULL rather than the rescaled readiness score\"). A NULL column \u2014 'this was never predicted' \u2014 is coalesced to a number here.",
  "shouldBe": "`TwinSignal`'s fields must be `number | null` and pass NULL straight through, so that lines 245-247 (`twin?.approvalProbability ?? null`) actually yield null. The file's own contract at lines 66-80 says so: \"Null now means 'not computed' and must be rendered as such. It is never replaced with a default at the point of use \u2014 that just moves the invention.\"",
  "consequence": "This defeats the fix documented 20 lines above it. The header comment states the constant 180 was removed as fabrication \u2014 it is still here. Because `gatherTwinAssessment` never returns null for these fields, `predictions.basis` is set to `'twin_assessment'` (line 248) and server/services/ana-ri/context-enrichment.ts:466 passes the `p.estimatedReviewDays !== null` guard and renders \"**From the recorded readiness assessment:** ... 180-day agency review clock for this pathway\" into the assistant's prompt. A NULL column is presented to a customer as a recorded agency review clock. Likewise `predicted_deficiency_count` NULL renders as \"0 criteria not met or partially met\", and a NULL `compliance_score` becomes a 70/100 compliance grade carrying 20% of the readiness score.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "fixed 3495d6c4f",
  "severity": "critical",
  "angle": "nullish-defaults",
  "file": "server/services/protocol-amendments/protocol-amendments-service.ts",
  "line": 87,
  "symbol": "createAmendmentTx",
  "votes": "3/3",
  "absentBecomes": "An amendment created without a consent/risk declaration is stored as an affirmative declaration of 'does not affect consent' and 'does not affect risk'. Both fields are optional (AmendmentInput lines 53-54; server/routes/protocol-amendments.ts:93-94 `z.boolean().optional()`), and server/services/ana/AnaToolExecutor.ts:10709-10710 deliberately passes `undefined` when the model did not state a value (`typeof input.affects_consent === 'boolean' ? input.affects_consent : undefined`) \u2014 this line then destroys that distinction. migrations/20260629_protocol_amendments.sql:26-27 makes the tri-state unrepresentable: `affects_consent boolean NOT NULL DEFAULT false` / `affects_risk boolean NOT NULL DEFAULT false`.",
  "shouldBe": "The columns must be nullable (null = not declared) and the writer must pass `input.affectsConsent ?? null`. classifyAmendmentImpact must take `boolean | null` and return a fourth outcome \u2014 review path undetermined, reconsent undetermined \u2014 so an amendment cannot be routed until the sponsor declares. The reader already preserves the distinction (lines 275-276: `affectsConsent: row.affects_consent ?? null`); only the writer and the column destroy it.",
  "consequence": "classifyAmendmentImpact (server/services/protocol-amendments/protocol-amendments-logic.ts:42) computes `const substantive = input.affectsConsent || input.affectsRisk` as false, so a non-major amendment is routed to expedited IRB review with requiresReconsent: false, citing '45 CFR 46.110 / 21 CFR 56.110 \u2014 minor changes to previously approved research may be reviewed by expedited procedure'. An IRB is told the sponsor declared the amendment affects neither consent nor subject risk when the sponsor declared nothing at all, and enrolled subjects are not re-consented under 45 CFR 46.116. Worse, substantiality.ts:336 (`if (touchesSubject && declared.affectsRisk === false && declared.affectsConsent === false)`) is written to catch a sponsor whose declaration is contradicted by the design delta \u2014 it will now accuse a sponsor of a false declaration they never made.",
  "note": "Partly overstated: classifyAmendmentImpact has no production caller, so the \"routed to expedited review\" consequence was not reachable. The reachable harm was the false declaration-conflict accusation in substantiality.ts and the \"no re-consent\" display. Both are fixed."
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "nullish-defaults",
  "file": "server/services/protocol-deviations/protocol-deviations-service.ts",
  "line": 58,
  "symbol": "createDeviationTx",
  "votes": "3/3",
  "absentBecomes": "A protocol deviation recorded without a severity assessment becomes severity 'minor' and category 'other'; input.affectsSafety stays undefined so assessReportability's `input.affectsSafety === true` is false. All three are optional on the route (server/routes/protocol-deviations.ts:90 `severity: z.enum([...]).optional()`) and on the ANA tool path (server/services/ana/AnaToolExecutor.ts:10752). The DB reinforces it: migrations/20260629_protocol_deviations.sql:26 `severity text NOT NULL DEFAULT 'minor' CHECK (severity IN ('minor','major','critical'))` \u2014 no 'not_assessed' value exists.",
  "shouldBe": "Severity must be required at the boundary, or assessReportability must take `severity: DeviationSeverity | null` and return a third `reportable: 'undetermined'` state with timelinessDays null and a basis saying the severity was never assessed, so the deviation cannot be closed and shows as pending assessment. Defaulting to the least reportable severity is the widening direction; if a default is wanted at all it must be 'critical'.",
  "consequence": "assessReportability (server/services/protocol-deviations/protocol-deviations-logic.ts:52-56) returns { reportable: false, timelinessDays: 0, basis: 'ICH E6(R2) \u00a74.5.3 \u2014 minor deviations not affecting subject safety/rights or data integrity are logged and summarized rather than promptly reported' }, and `is_reportable` is written false to protocol_deviations. A sponsor's deviation log hands a GCP inspector a deviation classified minor and affirmatively non-reportable, citing ICH E6(R2) \u00a74.5.3, on a severity and safety-impact assessment that no human ever made. The prompt-reporting window (3 days critical / 10 days major) never opens, and the deviation is eligible for closure under the CAPA gate as a routine minor finding.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "error-swallowing",
  "file": "server/services/rbm/site-risk-engine.ts",
  "line": 87,
  "symbol": "readProgramSites (consumed by recomputeSiteRisk, line 99)",
  "votes": "3/3",
  "absentBecomes": "Any failure reading `site_intel.sites` becomes \"this program has no sites\". `recomputeSiteRisk` does not re-check: it takes the empty list, then unconditionally runs `DELETE FROM rbm_site_risk_scores WHERE organization_id = $1 AND program_id = $2` and inserts nothing. A failed read therefore does not merely report zero \u2014 it destroys the previously persisted risk snapshot.",
  "shouldBe": "Distinguish the documented benign case (schema absent, 42P01) from a real read failure, and let a real failure propagate. `recomputeSiteRisk` must not delete the existing snapshot on a source read it could not complete \u2014 an ICH E6(R3) risk-proportionate monitoring record must never be replaced by the product of a failed query.",
  "consequence": "POST /api/mdx/rbm-site-risk/recompute (server/routes/mdx-rbm-data.ts:74) returns 200 with `count: 0`, and the RBM Sites surface reports \"Rescored 0 sites from Site Intelligence.\" (client/src/concept2cure/v2/surfaces/RbmSurfacesB.tsx:333). Every site's composite risk and monitoring tier is gone from `rbm_site_risk_scores`. The monitoring-tier counts read reduced/standard/enhanced = 0, and central statistical monitoring \u2014 which the surface tells the user \"scans the site-risk snapshot\" \u2014 now scans an empty table and finds no outliers. A study team is told no site is at elevated risk on the strength of a query that died.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "error-swallowing",
  "file": "server/services/realTimeValidationService.ts",
  "line": 410,
  "symbol": "RealTimeValidationService.analyzeContentWithAI (second swallow at performValidation, line 299)",
  "votes": "3/3",
  "absentBecomes": "An AI-gateway outage, timeout or unparseable response becomes \"the regulatory-compliance analysis ran and found nothing\". The result is merged unconditionally at line 297 (`issues.push(...aiAnalysis.issues)`), so the failure is invisible downstream. `calculateComplianceScore` then hits `if (issues.length === 0) return 100;` (line 546) and the function returns `isValid: issues.filter(i => i.type === 'error').length === 0` (line 350) \u2014 true. The swallow is doubled: the caller at line 299 has its own bare `catch { console.error('AI analysis failed:', error); }`.",
  "shouldBe": "Return a third state \u2014 `{ issues: [], suggestions: [], analysisRan: false, reason }` \u2014 and have `performValidation` refuse to emit a `complianceScore` or an `isValid` verdict when a constituent analysis did not run; the report should carry `skippedRules` > 0 and `isValid: null`/'undetermined'.",
  "consequence": "The persisted row in `coauthor_validation_history` (line 207) records `complianceScore: 100`, `totalIssues: 0`, `failedRules: 0` and a hard-coded `skippedRules: 0` \u2014 an explicit, false assertion that no rule was skipped. Worse, the AI-action handler at server/services/ai-actions/handlers/run-validation.ts:118 reads `if (validationResult.isValid)` and offers the user the action labelled \"Promote to document\" with the description \"Validation passed \u2014 promote this artifact to a governed document\". That handler's own degraded flag (`validationResult._degraded = true`, line 86) is never set, because the service did not throw \u2014 it returned a clean 100.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "error-swallowing",
  "file": "server/services/regulator-overlay-engine.ts",
  "line": 293,
  "symbol": "RegulatorOverlayEngine.applyOverlays",
  "votes": "3/3",
  "absentBecomes": "A failed `UPDATE contradiction_findings SET severity = $1, authority_state = $2, consequence_type = $3, \u2026 ` becomes a success. Execution falls straight through to `log.info('Overlay applied', \u2026)` and `return { finding, overlayApplied: true, overlayConsequence }` at line 306 \u2014 an OverlayConsequence object asserting `overriddenAuthorityState: finding.authorityState` (e.g. `blocks_promotion`) that exists only in the in-memory object about to be garbage collected.",
  "shouldBe": "The catch must mark the overlay as not durable \u2014 `return { finding, overlayApplied: false, overlayConsequence: null, persistenceFailed: true }` or throw \u2014 and `applyOverlaysToFindings` must surface the count of non-persisted overlays so the caller cannot report a regulator escalation that no reader will ever see.",
  "consequence": "`checkPromotionBlocked` (contradiction-engine-service.ts:657) decides `blocked` purely from the persisted column \u2014 `WHERE \u2026 authority_state IN ('blocks_promotion','requires_approval')`. So a finding the scan report tells the user was escalated by an FDA/EMA overlay rule to BLOCKS PROMOTION sits in the database at its old, un-escalated authority state and does not block the promotion. The scan UI, `getOpenContradictionsForOrg`'s \"\u2014 BLOCKS PROMOTION\" line, and the overlayConsequences audit trail all say the regulator-specific escalation was applied; the gate that is supposed to act on it never sees it.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "critical",
  "angle": "error-swallowing",
  "file": "server/services/submission-twin-service.ts",
  "line": 251,
  "symbol": "SubmissionTwinService.assessEvidenceIntegrity",
  "votes": "3/3",
  "absentBecomes": "A batch of up to 10 claims whose evidence assessment threw contributes nothing to `supported`, `weak`, `unsupported` or `contradicted` \u2014 but those claims remain inside `totalClaims: claims.length` in the returned object (line 256). Unassessed claims are therefore silently reclassified as \"not unsupported\".",
  "shouldBe": "Track and return `unassessedClaims` (or throw once any batch fails), and refuse to persist a `status: 'completed'` assessment whose claim counts do not sum to `totalClaims`.",
  "consequence": "`runFullAssessment` writes the result straight into `submission_twin_assessments` with `status: 'completed'`, `claimCount: claims.length` and `unsupportedClaimCount: evidenceResult.unsupportedClaims` (line 989). A package where 40 of 60 claims were never assessed is persisted as a COMPLETED twin assessment reporting 0 unsupported claims, with a readiness score and an AI executive summary written on top of it. A reviewer preparing to file reads \"every claim in this submission is evidenced\"; in fact two thirds of them were never checked, and the only record is a console.error line.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "fixed 78420cb02",
  "severity": "critical",
  "angle": "nullish-defaults",
  "file": "server/src/control-plane/governed-document-evaluator.ts",
  "line": 227,
  "symbol": "evaluateGovernedDocument",
  "votes": "2/3",
  "absentBecomes": "A lane that calls the Governed Document Decision Fabric without an `exportState` gets aiGenerated: false \u2014 'this document was not AI-generated'. The publish path does the same thing through a nullish default at line 246 (`aiGenerated: input.exportState?.aiGenerated ?? false`). isStale ?? false (lines 230 and 249) likewise turns 'staleness never computed' into 'document is current'.",
  "shouldBe": "aiGenerated and isStale must be `boolean | null` on ExportGateInput/PublishGateInput. When either is null the gate must emit a FAILING required check ('AI-provenance of this content is not recorded' / 'document freshness not evaluated') and a blocker, not omit the check. The no-exportState branch must return outcome 'insufficient_context', which the type already supports and which the publish branch already does for a missing publishState (lines 240-247).",
  "consequence": "server/src/control-plane/export-publish-gates.ts:160 guards the whole AI-content control behind `if (input.aiGenerated) {`. With aiGenerated false, the 'export_human_review' check is never pushed into `gateChecks` and the critical 'AI-generated content requires human review before export' blocker is never raised \u2014 the check does not fail, it disappears. Every other required check then passes and outcome is 'eligible'. The persisted governed decision record shown to a Part 11 auditor lists the export gate's checks with no human-review-of-AI-content entry at all, which reads as 'this control did not apply to this document' rather than 'nobody recorded whether a model wrote it'. isStale ?? false similarly makes check 'export_not_stale' pass with detail 'Document is current' for a document whose staleness was never computed.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "percentages",
  "file": "server/routes/mission-control.ts",
  "line": 1050,
  "symbol": "GET /programs/:programId/readiness",
  "votes": "2/3",
  "absentBecomes": "Four of the nine readiness dimensions are penalty-from-100 formulas over collections that are empty when nothing has been recorded. A program with no `dependency` rows scores `consistencyIntegrity: 100`; a program with no `risk` rows scores `authorityConfidence: 100` and `timelineConfidence: 100`. `routeFit` is the literal constant 70 with no input at all. `complianceIntegrity` is 85 as soon as a single provenance row of any kind exists.",
  "shouldBe": "Each dimension needs an assessed/not-assessed distinction: no dependency graph recorded means consistency integrity is null (and must be reported as \"no dependencies recorded\"), not 100. `routeFit: 70` must be null with a stated reason until a route-fit assessment exists, and must be excluded from the weighted average rather than contributing 7 points to every program in the system.",
  "consequence": "This is served at `GET /api/mission-control/programs/:id/readiness` (mounted in server/bootstrap/register-advanced-platform-routes.ts:198) and rendered by the MissionControl surface. A program created five minutes ago, with no artifacts, no evidence, no reviews, no risks and no dependencies, returns `consistencyIntegrity: 100, authorityConfidence: 100, timelineConfidence: 100, routeFit: 70` and an `overallConfidence` of 39% \u2014 and its `blockers` array contains only the evidence/approval entries, never a \"nothing has been assessed\" blocker. A reviewer is told the program's consistency integrity and timeline confidence are perfect.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "error-swallowing",
  "file": "server/services/ai-actions/action-history.ts",
  "line": 125,
  "symbol": "getActionHistory",
  "votes": "3/3",
  "absentBecomes": "A failed read of `regulatoryAuditLogs` \u2014 the Part 11 audit trail for every executed AI action \u2014 becomes `total: 0, entries: [], hasMore: false`, byte-identical to an organization that has genuinely never run an AI action.",
  "shouldBe": "Let the error reach the route so GET /api/ai-actions/history returns a non-200, or carry an explicit `unavailable: <reason>` third state \u2014 precisely the fix already applied one directory over in server/services/export/docx-ledger-collector.ts:348, whose header states the rule: \"a query that FAILED \u2026 produced `<AuditLog count=\\\"0\\\">` \u2026 and a reviewer reads a count as 'this artifact has no history'. A count is a claim.\"",
  "consequence": "The route at server/routes/ai-actions.ts:448 spreads this straight into `res.json({ success: true, ...history })` \u2014 HTTP 200, `success: true`, `total: 0`. The AI-action audit surface shows an empty, successful-looking history. During a Part 11 inspection of what the model was permitted to change, the platform reports that no AI actions exist for the organization, when the audit query simply failed.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "percentages",
  "file": "server/services/ana/project-readiness-aggregator.ts",
  "line": 185,
  "symbol": "deriveProjectReadinessAggregate",
  "votes": "3/3",
  "absentBecomes": "Zero alerts scores 100. The summary this reads carries no \"was a scan ever run\" signal, so a project the cross-artifact consistency scanner has never touched is indistinguishable from one scanned clean. The identical shape is at line 200 for the Guidance-compliance component. Together they contribute 20 of the 100 readiness points (WEIGHTS.consistency 0.1 + WEIGHTS.guidance 0.1), and neither can set `filingBlocking`, because that is OR-ed only from `openCritical > 0`.",
  "shouldBe": "Score null / 'not_assessed' when no scan is on record for the project, and exclude the component from the weighted total rather than award it. The guidance summary already carries `recent.lastAlertAt`, and both summaries could carry a last-scan timestamp; the aggregator discards that information.",
  "consequence": "A project that has never been scanned is reported with `consistency: { score: 100 }`, `guidance: { score: 100 }` and the detail strings \"0 open consistency alerts (0 critical)\" / \"0 open guidance alerts (0 critical)\" \u2014 read by a reviewer as \"cross-artifact consistency and guidance compliance checked, clean.\" It also receives a flat 20-point uplift on the headline readiness score served to the UI and to AnA's authoring-plan generator.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "error-swallowing",
  "file": "server/services/auth-security-service.ts",
  "line": 122,
  "symbol": "checkPasswordHistory",
  "votes": "3/3",
  "absentBecomes": "A failed read of `users.passwordHistory`, or a bcrypt failure, becomes `true` \u2014 the function's own contract for \"this password has NOT been used recently\". The boolean return type has no room for \"could not check\", so the caller cannot tell the two apart.",
  "shouldBe": "Return `'ok' | 'reused' | 'unverified'` (or throw). On 'unverified' the password change must be refused with a retry message \u2014 narrowing on absence, not widening. A reuse control that cannot run has not passed.",
  "consequence": "At server/routes/auth.ts:2043 `const historyOk = await checkPasswordHistory(userData.id, newPassword);` \u2014 a `true` skips the 400 and the change proceeds. A user can re-set one of their last five passwords whenever the history read fails, and the platform's Part 11 \u00a711.300(b) password-aging/reuse control is bypassed with no record beyond one logger.error. Nothing on the resulting audit row distinguishes a change that passed the reuse check from one where the check never ran.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "nullish-defaults",
  "file": "server/services/benefit-risk/benefit-risk-knowledge.ts",
  "line": 529,
  "symbol": "structureBenefitRiskFramework",
  "votes": "2/3",
  "absentBecomes": "TreatmentOption.status is optional with members 'approved' | 'off_label' | 'supportive_care' | 'none' (lines 334-340) and no 'unknown'. A therapy listed in the current treatment landscape without a recorded regulatory status is labelled approved \u2014 twice: once as printed evidence text, once inside the unmet-need test. Note that the module's own helper `norm(t.name, 'unnamed option')` shows the correct habit for the adjacent field.",
  "shouldBe": "Add an 'unknown' member (or treat undefined as unknown) and print it as '(regulatory status not recorded)'. The unmet-need test must not resolve while any option's status is unknown \u2014 it should return an undetermined unmet-need conclusion naming the options whose status is missing, rather than silently resolving them into the 'an approved alternative exists' branch.",
  "consequence": "The FDA Benefit-Risk Framework Row 2 evidence text renders a named therapy as e.g. 'Drug X (approved).' \u2014 a factual claim about another product's regulatory status that nobody entered, printed into a document intended for submission or internal decision use. It also decides the row's conclusion: because 'approved' is neither 'none' nor 'supportive_care', `every(...)` is false and the framework concludes 'Effective alternatives exist; the product must show a favourable benefit-risk profile relative to, not merely in addition to, the current standard of care.' A reviewer is told an approved alternative exists, on the basis of a status field left blank.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "boolean-verdicts",
  "file": "server/services/citi/citi-logic.ts",
  "line": 60,
  "symbol": "trainingStatus",
  "votes": "3/3",
  "absentBecomes": "`CitiTrainingStatus = 'current' | 'expiring' | 'expired' | 'missing'` (line 34) has no member for \"completed, but we do not know when it lapses\". A record with a completion date and a null `expires_date` returns `'current'` \u2014 permanently. The column is nullable (`expires_date date`, migrations/20260611_research_compliance.sql:34) and the intake route makes it optional (`expiresDate: z.string().optional()`, server/routes/citi-training.ts:49), so an unrecorded expiry is ordinary, not exotic. The same collapse happens one line later for an unparseable clock: `const now = toDays(today); if (now == null) return 'current';` (line 62).",
  "shouldBe": "A fifth member \u2014 `'expiry_unknown'` \u2014 returned when a completion exists but no expiry was recorded, and a hard failure (not `'current'`) when `today` cannot be parsed. The comment \"// completed, no expiry tracked\" asserts a fact the data does not carry: the module's own header (lines 12-14) says CITI human-subjects and GCP completions are \"commonly carried on a recertification cycle (often ~3 years)\", so a blank expiry on those modules is a missing date, not the absence of a clock.",
  "consequence": "`assembleOrgResearchAdmin` (server/services/research-admin/research-admin-view-assembler.ts:137,152) derives the ResearchAdmin surface's CITI matrix cells from this function, and the surface renders `'current'` with the `ok` tone (ResearchAdmin.tsx:112). `buildTrainingMatrix` rolls the same value into `MatrixSummary.current`. An institutional official, or an OHRP/IRB reviewer checking that investigators hold current human-subjects training before approval under 45 CFR 46.111(a)(7), is shown a green \"current\" cell for a certificate whose expiry nobody entered \u2014 which reads identically to a certificate verified as in-date.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "nullish-defaults",
  "file": "server/services/clinical-regulatory-evidence/index.ts",
  "line": 710,
  "symbol": "getTrace",
  "votes": "3/3",
  "absentBecomes": "An evidence relationship whose is_inferred flag is false \u2014 which means only 'not machine-inferred' (server/services/clinical-regulatory-evidence/evidence-spine.service.ts:593: `const inferred = p.relationshipType === 'potentially_related' ? true : Boolean(p.isInferred);`) \u2014 is reported as having been reviewed by a human. Nothing in the relationship record stores a reviewer, a review timestamp, or a review decision. A seeded, imported or hand-loaded edge with the flag simply unset lands in the same bucket.",
  "shouldBe": "reviewState must have a third member, 'not_reviewed' / 'unknown', and must be derived from an actual review record (reviewer id + timestamp) on the relationship, not from the absence of an inference flag. Absent a review record the trace must say the review state is unrecorded.",
  "consequence": "The evidence trace presents 'human_reviewed' \u2014 a Part 11-relevant assertion that a qualified person examined and accepted this evidence chain \u2014 for every trace in which no edge happens to be flagged as inferred. An auditor asking 'was this evidence chain human-reviewed?' is answered yes by a field that has never once been fed by a human review.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "error-swallowing",
  "file": "server/services/contradiction-engine-service.ts",
  "line": 1392,
  "symbol": "detectBodySpecificExpectationConflicts (per-section loop)",
  "votes": "3/3",
  "absentBecomes": "A section whose `detectBodySpecificGaps` call threw becomes a section with no missing regulator requirements. The loop continues, the detector resolves normally, and `scanProjectFull` pushes `body_specific_expectations` into `detectionMethods` as though it had run over every section.",
  "shouldBe": "Rethrow, or accumulate the failed sections and surface them so the scan can report itself as incomplete. The enclosing catch at line 1396 already encodes the contract in its own comment \u2014 \"A contradiction scan that could not run its full detector set is unknown, not clean\" \u2014 and was changed on 2026-09-10 to `throw err` for exactly this reason. This inner catch defeats the same contract one level down, at section granularity.",
  "consequence": "A full contradiction scan reports `summary.total` short by every body-specific gap in the failed sections, while `detectionMethods` claims the FDA/EMA body-expectation detector ran. Because findings from this detector carry severity up to 'high' and can be escalated by overlay rules to `blocks_promotion`, a section missing four agency-expected requirements produces no finding at all \u2014 and the user is told the project was scanned against that agency's expectations and came back clean.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "boolean-verdicts",
  "file": "server/services/ectd/release-signature-status.ts",
  "line": 59,
  "symbol": "isReleaseSignatureRequired",
  "votes": "3/3",
  "absentBecomes": "An application type that cannot be read, or that is not spelled exactly as one of `{'IND','NDA','BLA','MAA'}`, returns `false` \u2014 \"no 21 CFR Part 11 \u00a711.70 release signature required\". Two absent-input paths reach it: (a) `loadApplicationType` returns `row?.applicationType ?? null` (assess-dispatch-readiness.ts:275), which is null whenever the submission row is not readable in this tenant; (b) the field is unconstrained free text \u2014 `applicationType: z.string().min(1).max(64)` on create (server/routes/submissions.ts:172) and `application_type TEXT NOT NULL` with no CHECK (migrations/20260604_submission_core_canonical.sql:27) \u2014 while `region` and `clientType` on the same schema are `z.enum`. So 'IND Original', 'NDA 505(b)(2)' or 'BLA-351(a)' all read as not-requiring a signature.",
  "shouldBe": "A three-valued return (`'required' | 'not_required' | 'undetermined'`) with `undetermined` blocking, exactly as the verdict side of this same module already does \u2014 `refusalToVerdict` maps `'run-not-found'` and `'signature-unverifiable'` to `'undetermined'` with the comment \"Not revoked: nothing is known about the signature's standing\" (lines 92-96). The requirement side needs the same treatment; an unrecognised application type is unknown, not exempt.",
  "consequence": "`assess-dispatch-readiness.ts:435-441` feeds this straight into the gate, and `evaluateReleaseSignatureGate` short-circuits on it: `if (!input.required) { return { cleared: true, blockers: [] }; }` (dispatch-gate.ts:165-167). The assessment then returns `releaseSignature: { required: false, cleared: true }` and the sequence dispatches to FDA/EMA having never been e-signed. The same file goes out of its way to refuse this shape one control earlier \u2014 an unrecognised region raises an `UNKNOWN_REGION_PROFILE` warning because \"the absence of MISSING_REQUIRED_SECTION findings does NOT mean the required sections are present\" (lines 339-343) \u2014 and the \u00a76c comment states the principle verbatim: \"an undetermined lookup is not an absent requirement\" (line 413). The requirement lookup is the one place it is not applied. An auditor reading the dispatch record is told this submission type does not cross the \u00a711.70 boundary; what actually happened is that nobody could tell what type it was.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "boolean-verdicts",
  "file": "server/services/grants/grants-logic.ts",
  "line": 329,
  "symbol": "costShareStatus",
  "votes": "2/3",
  "absentBecomes": "`committed: null` (the commitment was never entered) and `committed: 0` (the award genuinely carries no cost share) both collapse to `c = 0`, and `met: c === 0 || ...` then returns **true** with `shortfall: 0` and `metPct: 0`. `CostShareStatus.met` is a plain boolean, so \"no commitment is on file\" has nowhere to go but the reassuring value. The column is nullable and was added to an existing table \u2014 `ALTER TABLE grant_awards ADD COLUMN IF NOT EXISTS cost_share_committed numeric(14,2)` (migrations/20260612_grant_costshare_nce.sql:17) \u2014 so every award predating that migration reads as cost-share-met, and `grants-service.ts:380` preserves the null rather than defaulting it.",
  "shouldBe": "`met: boolean | 'unknown'` (or a `committedRecorded: boolean`), so a null commitment is reported as not-yet-recorded. There is no \"cost share not applicable\" flag on the award to distinguish the two facts, so absence cannot honestly be read as zero.",
  "consequence": "`assembleCloseoutReadiness` gates on this directly: `if (!i.costShare.met) blockers.push('Cost-share commitment not met \u2014 shortfall ... (2 CFR 200.306).')` (line 367). With `met: true` no blocker is raised, so `readyToClose` can be true. Its own docstring claims the verdict \"also wants ... the cost-share commitment met (200.306)\". A grants administrator closing out a federal award \u2014 and any subsequent single audit under 2 CFR 200 Subpart F \u2014 is told the committed match was met, when the commitment figure was never entered and the actual contributions total zero.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "boolean-verdicts",
  "file": "server/services/iacuc/iacuc-logic.ts",
  "line": 117,
  "symbol": "reviewStatus",
  "votes": "3/3",
  "absentBecomes": "An IACUC protocol whose `approval_date` was never recorded returns `expired: false` and `annualReviewDue: false` \u2014 the same two-booleans-cannot-say-unknown shape as `irb-logic.ts continuingReviewStatus`. The return type has no third state, so \"we have no approval date, therefore we cannot tell whether the 3-year de novo review or the annual continuing review has lapsed\" is rendered as \"not expired, no review due\".",
  "shouldBe": "Return a status union with an explicit undetermined member (e.g. `status: 'current' | 'annual_due' | 'expired' | 'undetermined'`), or a `approvalDateRecorded: boolean` alongside, so a caller can distinguish an approval that is current from one nobody dated. `protocol-portfolio-logic.ts` already proves the pattern is available in this codebase: its `ExpirationBucket` carries `'undated'` as a first-class bucket.",
  "consequence": "`GET /api/iacuc/protocols/:id/completeness` (server/routes/iacuc.ts:200-201) returns `reviewStatus: { expired: false, annualReviewDue: false }`, and AnA's `review_iacuc_protocol` tool (AnaToolExecutor.ts:10185-10190) returns the same object next to the message \"Protocol passes the deterministic gate (low risk).\" An IACUC administrator or an OLAW/USDA inspector is told an animal-use protocol is not expired and has no continuing review due, when the fact is that the approval date is blank \u2014 including for a protocol whose approval actually lapsed years ago.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "boolean-verdicts",
  "file": "server/services/ibc/ibc-logic.ts",
  "line": 104,
  "symbol": "registrationExpiration",
  "votes": "3/3",
  "absentBecomes": "An IBC registration with no recorded approval date returns `expired: false`. The docstring immediately above states the rule it is supposed to enforce \u2014 \"NIH Guidelines require at least annual review of active registrations; approval sets a 1-year expiration\" \u2014 and then the undated case is resolved to the value that means the annual review has not lapsed.",
  "shouldBe": "A third state (`expired: boolean | 'undetermined'`, or a status union with `'undated'`), so an unrecorded approval date blocks rather than reassures. Same fix as the IACUC and IRB helpers; the IRB package manifest in this same codebase already does it, with `requirement: 'undetermined'` and the comment \"An undetermined requirement blocks readiness by design: 'we could not tell' is not a finished package\" (server/services/irb/package-manifest.ts:251-255).",
  "consequence": "`GET /api/ibc/registrations/:id/containment` (server/routes/ibc.ts:181-182) returns `expiration: { expirationDate: null, expired: false }`, and AnA's `review_ibc_registration` tool (AnaToolExecutor.ts:10394-10399) returns it beside the message \"Registration passes the deterministic containment gate.\" A biosafety officer, or an NIH OSP reviewer asking whether recombinant-DNA work is covered by a current IBC registration, is told the registration is not expired when nobody ever recorded that it was approved.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "boolean-verdicts",
  "file": "server/services/ind-lifecycle/ind-sae-line-listing.ts",
  "line": 115,
  "symbol": "buildSaeLineListing",
  "votes": "3/3",
  "absentBecomes": "`SaeLineListingRow.expectedness` is typed `'unexpected' | 'expected'` (line 39) \u2014 two values, no third. `isUnexpected(e.expectedness)` is explicitly documented in the sibling module as returning false both for a recorded \"expected\" and for an absent field, and `mapRowToAdverseEvent` (server/services/compliance/pharmacovigilanceService.ts:1036) maps the nullable column straight through as `expectedness: row.expectedness ?? null`. So every serious case where no reviewer has assessed the event against the IB / RSI is written into the line listing as `expectedness: 'expected'`, and the tabulation counts it: `byExpectedness: { unexpected: unexpectedCount, expected: rows.length - unexpectedCount }` (line 159). The SUSAR-like cut `byReactionPt[].suspectedUnexpected` (line 147) silently undercounts for the same reason.",
  "shouldBe": "A third member \u2014 `'not_assessed'` \u2014 carried on the row and counted as its own bucket in `SaeSummaryTabulation`. The sibling module already models exactly this: `classifyIndSafetyReport` carries `expectednessRecorded` with the comment \"`unexpected` is false both when the reviewer marked the event expected and when no one has assessed it; only the first is a finding\" (ind-safety-report-service.ts:91-96). That distinction is dropped here, and the two modules import from each other.",
  "consequence": "The IND annual report / DSUR aggregate artefact this module exists to build (21 CFR 312.33(b) / ICH E2F Appendix) tells FDA that N serious cases in the reporting period were assessed as expected \u2014 i.e. listed in the Investigator's Brochure or consistent with the Reference Safety Information \u2014 when in fact no expectedness assessment exists for them. The same numbers go out in the CSV appendix (`saeLineListingToCsv`) and through AnA's `build_sae_line_listing` tool, which instructs the model to \"Report the listing and summary as recorded.\"",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "percentages",
  "file": "server/services/intelligence/cross-artifact-consistency-scanner.ts",
  "line": 536,
  "symbol": "getConsistencyAlertsSummary",
  "votes": "3/3",
  "absentBecomes": "Postgres 42P01 is undefined_table. On a deployment where `cross_artifact_consistency_alerts` was never created, the failure to read is converted into a fully-populated zero-valued summary: `bySeverity: {critical:0, major:0, minor:0, info:0}, totalOpen: 0, openCritical: 0, topProjects: [], topArtifacts: []`. The identical line is server/services/intelligence/guidance-impact-scanner.ts:795 for `guidance_alerts`.",
  "shouldBe": "Return a summary carrying `assessed: false` (or throw, as the non-42P01 path does). An unreadable table is an error, and per the repo's standing rule an error is never rendered as an empty result. The caller must be able to tell \"no alerts\" from \"alert store absent\".",
  "consequence": "This is the exact input to server/services/ana/project-readiness-aggregator.ts:185/200, which turns `totalOpen: 0` into a 100/100 component score. On an installation missing that migration, every project in every organization is scored 100 for consistency and 100 for guidance compliance, with the detail line \"0 open consistency alerts (0 critical)\", and `filingBlocking` never trips from either source. The absence of the alert store reads as a clean bill of health.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "percentages",
  "file": "server/services/intelligence/readiness-scoring-engine.ts",
  "line": 162,
  "symbol": "computeReadinessScore",
  "votes": "3/3",
  "absentBecomes": "A project with no twin assessment on record \u2014 nothing was ever scanned for cross-document consistency \u2014 is scored 70/100 on the Consistency dimension. 70 is not \"neutral\"; on this engine's own module-status bands (line 182: >=90 complete, >=50 in_progress) it is a mid-passing grade. `estimateCompliance` at line 483 has the same shape: a project with an empty `risks` array scores `80 - 0 - 0 = 80` on Compliance.",
  "shouldBe": "`dimensions.consistency` must be `number | null` with null meaning not assessed, and the weighted average must renormalise over the dimensions that were actually computed (or the overall score must itself be null), exactly as the adjacent `ReadinessPredictions` block already does for the prediction fields.",
  "consequence": "A project on which no consistency scan and no risk assessment has ever run does not score 0 and does not report 'not assessed' \u2014 it reports `dimensions: { completeness: 0, quality: 65, consistency: 70, compliance: 80 }` and an overallScore of 46/100. That number is rendered as a `| dimension | value/100 |` table into the assistant's context (server/services/ana-ri/context-enrichment.ts:440) and served from /api/intelligence and project-home-routes. An auditor is told the dossier was found 70% consistent and 80% compliant when neither was ever examined.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "boolean-verdicts",
  "file": "server/services/invention-disclosure/invention-disclosure-logic.ts",
  "line": 92,
  "symbol": "evaluateBayhDoleCompliance",
  "votes": "3/3",
  "absentBecomes": "When `disclosureDate` is absent (`discMs == null`), both the agency-disclosure obligation (line 92) and the election-of-title obligation (line 101, identical shape) are pushed with `status: 'on_track'`. `DeadlineStatus = 'on_track' | 'due_soon' | 'overdue' | 'satisfied' | 'not_applicable'` (line 24) has no undetermined member, and `hasOverdue: deadlines.some(d => d.status === 'overdue')` (line 113) is therefore false. The inventor-disclosure date is the event that starts both clocks, so its absence means the clocks cannot be evaluated at all \u2014 not that they are running comfortably.",
  "shouldBe": "An `'undetermined'` member on `DeadlineStatus` used for the no-start-date branch. The function already demonstrates it knows how to say \"this obligation cannot be judged yet\": the patent-filing branch pushes `status: 'not_applicable'` when no election date exists (line 110). The two disclosure-driven branches assert `'on_track'` instead.",
  "consequence": "`GET` of the Bayh-Dole compliance picture (`invention-disclosure-service.ts getCompliance`) reports a federally funded invention as on track for both the 2-month agency disclosure and the 2-year election of title under 37 CFR 401.14(c), with `hasOverdue: false`, when no inventor-disclosure date has been recorded. Blowing the Bayh-Dole election window forfeits title to the funding agency; a technology-transfer office watching this report sees green on the two obligations whose deadlines it cannot actually compute.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "boolean-verdicts",
  "file": "server/services/invention-disclosure/invention-disclosure-service.ts",
  "line": 103,
  "symbol": "getCompliance",
  "votes": "3/3",
  "absentBecomes": "`federal_funding boolean NOT NULL DEFAULT false` (migrations/20260705_invention_disclosure.sql:25) \u2014 a NOT NULL DEFAULT false on a fact that is genuinely tri-state (federally funded / not federally funded / funding source not yet determined). A disclosure created before anyone works out the funding source reads `federal_funding = false`, and `d.federal_funding === true` turns it into `federalFunding: false`, which drives the short-circuit at `invention-disclosure-logic.ts:80-81`: `if (!input.federalFunding) { ... return { applicable: false, deadlines: [na('agency_disclosure'), na('election_of_title'), na('patent_filing')], hasOverdue: false, ... } }`. The update path even preserves the intent to have a third state \u2014 `typeof input.federalFunding === 'boolean' ? input.federalFunding : null` (line 91) \u2014 but the column cannot hold it.",
  "shouldBe": "Make the column nullable (NULL = not yet determined) and give `BayhDoleComplianceResult.applicable` a third state, so an undetermined funding source reports \"Bayh-Dole applicability not determined\" rather than \"not applicable\". Per RULE 1 in CLAUDE.md the fix is an in-place amendment of the creating migration with a dated header note, not an appended ALTER.",
  "consequence": "Every newly created invention disclosure \u2014 and every disclosure whose funding source has not yet been researched \u2014 reports `applicable: false` with all three Bayh-Dole obligations marked `not_applicable` and `hasOverdue: false`. An institution reviewing its disclosure portfolio for Bayh-Dole exposure is shown \"not subject to 37 CFR 401.14\" for inventions whose federal funding simply has not been checked. The 2-month agency-disclosure clock runs regardless of what the record says.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "error-swallowing",
  "file": "server/services/licensing/eula-service.ts",
  "line": 234,
  "symbol": "getOutstandingAgreements (and getActiveAgreements, line 199)",
  "votes": "3/3",
  "absentBecomes": "Any failure reading `license_agreements` or `license_acceptances` becomes an EMPTY outstanding list, i.e. \"this user has accepted everything required\". `getActiveAgreements` has the same swallow at line 199, and line 214-215 compounds it \u2014 if the active-agreement read failed, `active.length === 0` short-circuits to `return []` too.",
  "shouldBe": "Return a three-state result (`{ outstanding, readOk }`) or let the error propagate; callers that publish an acceptance verdict must render \"acceptance status unavailable\", never \"accepted\".",
  "consequence": "`resolveEntitlements` (server/services/licensing/entitlements-service.ts:318) computes `licenseAccepted: outstanding.length === 0` and emits `outstandingAgreements: []`. The entitlements/compliance panel therefore states affirmatively that the organization has accepted every active, required license agreement \u2014 a legal acceptance claim manufactured from a failed database read. (The middleware at server/middleware/requireLicenseAcceptance.ts:49 is separately documented as fail-open, which is a deliberate availability trade; publishing `licenseAccepted: true` as a fact is not.)",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "error-swallowing",
  "file": "server/services/regulator-overlay-engine.ts",
  "line": 373,
  "symbol": "RegulatorOverlayEngine.getMatchingRules",
  "votes": "2/3",
  "absentBecomes": "A failed read of `contradiction_overlay_rules` becomes \"no regulator overlay rule applies to this finding\". `applyOverlays` then returns `overlayApplied: false`, indistinguishable from the genuine no-match case, and `applyOverlaysToFindings` reports `overlayCount: 0`.",
  "shouldBe": "Let the error propagate, or return `{ rules, readOk: false }` so the scan can report that regulator overlays were not evaluated rather than that none applied.",
  "consequence": "Overlay rules carry `severityOverride`, `authorityOverride` and `consequenceOverride` \u2014 they are how an FDA- or EMA-specific rule raises a medium finding to `blocks_promotion`. When the rule table cannot be read, every finding keeps its generic base severity and the scan result reports `overlayCount: 0` with an empty `overlayConsequences` array. The user is told no jurisdiction-specific escalation applies to any contradiction in the project, on the strength of a query that never returned.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "nullish-defaults",
  "file": "server/services/regulatory/spl-fhir.ts",
  "line": 126,
  "symbol": "validateFhirResource",
  "votes": "3/3",
  "absentBecomes": "FHIR_REQUIRED (lines 111-115) models exactly three resource types \u2014 Observation, DiagnosticReport, Device. Every other resourceType ('Patient', 'MedicinalProductDefinition', 'AdverseEvent', 'Substance', \u2026) gets `[]`, the loop body never runs, errors stays empty, and the function returns valid: true.",
  "shouldBe": "Return a third state for an unmodelled resource type \u2014 valid: null / 'not_validated' with an explicit note that no profile is modelled for that resourceType \u2014 the same way the function already fails closed on a missing resourceType at line 122-124. A validator must never report a pass on rules it does not have.",
  "consequence": "POST /fhir/validate (server/routes/spl-fhir.ts:63-69) returns { valid: true, errors: [], framework: 'HL7 FHIR' } for any resource outside those three types. A user submitting a FHIR MedicinalProductDefinition or AdverseEvent resource is told, under an HL7 FHIR banner, that it validated clean \u2014 when nothing was checked beyond the presence of the resourceType string itself. The empty errors array is the same shape a genuinely clean Observation produces, so there is no way for the caller to tell a pass from a no-op.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "nullish-defaults",
  "file": "server/services/regulatory/submissionPackageBuilder.ts",
  "line": 105,
  "symbol": "buildPackageManifest",
  "votes": "3/3",
  "absentBecomes": "A filing type that has no artifact matrix gets `[]` from getRequiredArtifacts (server/services/regulatory/requiredArtifactMatrix.ts:113, `return ARTIFACT_MATRICES[registryId] ?? [];`) \u2014 indistinguishable here from a filing type that genuinely requires no artifacts. ARTIFACT_MATRICES defines 9 entries (US_IND, US_IND_AMENDMENT, US_NDA, US_BLA, US_510K, US_PMA, US_DE_NOVO, EU_MAA, EU_CTA) against the whole application registry, so the undefined case is the normal case, not the edge.",
  "shouldBe": "Guard with hasArtifactMatrix(registryId) \u2014 the function that exists in the same module for precisely this, whose docstring names this exact failure ('otherwise an unmodelled filing type reports 100% complete with zero gaps'). The manifest must carry an artifactsAssessed flag, and packageComplete must be null/undetermined when it is false. The sibling consumer already does this correctly: server/services/regulatory/readinessEvaluator.ts:120, `const artifactsAssessed = hasArtifactMatrix(registryId);`.",
  "consequence": "With requiredArtifacts empty, `artifacts` is [], `requiredArtifactsList` is [], `presentArtifacts` is [], and the manifest metadata at lines 170-172 reports totalArtifacts: 0, requiredArtifacts: 0, presentArtifacts: 0 with `packageComplete: completedSections.length === requiredSectionsList.length && presentArtifacts.length === requiredArtifactsList.length` \u2014 the artifact half is `0 === 0`, vacuously true. For any filing type outside those 9 (a PMDA, Health Canada, MHRA or TGA application), the Submission Center tells the user and the submission record that the package is complete and that zero artifacts were required, when in truth no artifact requirement was ever evaluated.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "nullish-defaults",
  "file": "server/services/sap-generator-service.ts",
  "line": 570,
  "symbol": "SapGeneratorService.updateSap",
  "votes": "2/3",
  "absentBecomes": "A SAP regeneration that does not carry a sample size gets N = 100; a missing primary endpoint becomes the literal string 'treatment efficacy'; a missing study duration becomes 24 weeks; a missing dropout rate becomes 15%. `||` also swallows a legitimately supplied 0. The same fabrication exists as default parameter values inside generateSAP itself (lines 100-101: `duration_weeks = 24,` / `dropout_rate = 0.15,`), so a direct caller is affected too. The protocol row that was just loaded is consulted for indication and phase but never for sample size.",
  "shouldBe": "sample_size, primary_endpoint, duration_weeks and dropout_rate must be `number | null` / `string | null`, and the renderer must emit an explicit deferral for each absent value \u2014 exactly the pattern the same file already applies to target_power and minimum_detectable_effect (lines 128-139, cited to FORENSIC_CODE_AUDIT_2026-05-29.md HI-2), and to estimands via renderEstimandSection. The interface's own comment at lines 37-38 states the rule: 'Supplied by the study statistician \u2014 this service does not fabricate them when absent.'",
  "consequence": "The generated Statistical Analysis Plan text at line 159 reads '- Sample Size: 100 participants (85 effective after 15% dropout)' and '- Duration: 24 weeks', and the sample-size justification at line 139 asserts 'Sample size of 100 is recorded as provided; this draft does not assert a power figure that has not been calculated.' It was not recorded as provided \u2014 it was invented by this line. A statistician, and any regulator reading the SAP, is told the study enrols 100 subjects over 24 weeks with a 15% assumed dropout, none of which came from the protocol. The file's honesty machinery for power and effect size is intact, which makes the surrounding fabricated numbers read as equally sourced.",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "major",
  "angle": "percentages",
  "file": "server/services/validate-completeness-engine.ts",
  "line": 220,
  "symbol": "ValidateCompletenessEngine.buildChecklist",
  "votes": "2/3",
  "absentBecomes": "`sectionScores` is optional at the HTTP boundary (server/routes/validate-completeness.ts:28, `sectionScores: z.record(...).optional()`). A section with no quality score recorded yields `qualityScore === null`, so `needsReview` is false, so the section is classified `'complete'` \u2014 identical to a section that was scored and passed the 70 threshold. \"Never quality-reviewed\" and \"quality-reviewed and passed\" are the same value.",
  "shouldBe": "A third status, `present_not_assessed`, that is excluded from `completeSections` (or at minimum counted separately and named in the go/no-go rationale). An unscored section must not be able to reach `decision: 'go'`.",
  "consequence": "POST /api/validate-completeness/validate with only `submissionType` and a list of section names \u2014 no scores \u2014 returns `readinessPercentage: 100`, `rtfRisk: { rtfScore: 100, overallRTFRisk: 'low', weakSections: [] }` and `goNoGo: { decision: 'go', rationale: 'Submission is ready for filing. 100% completeness with 0 minor risk(s).' }`. Nothing about content was examined; `present` is a substring match on a section name (line 216). The same figures then become the `readinessPercentage` and `weakSectionCount` features fed to the CRL/RTF predictive model (server/services/intelligence/regulatory-intelligence.ts:198-201).",
  "note": ""
 },
 {
  "state": "confirmed",
  "status": "open",
  "severity": "minor",
  "angle": "error-swallowing",
  "file": "server/services/documentQuality/qualityLintService.ts",
  "line": 67,
  "symbol": "DocumentQualityLintService.runVale",
  "votes": "3/3",
  "absentBecomes": "Vale failing to run at all \u2014 binary absent, non-zero exit with empty stdout (the client rejects at server/integrations/vale/client.ts:36), a bad style config, an unwritable temp dir \u2014 becomes zero style findings. `lint` merges the empty array into `issues: [...concept2cureIssues, ...valeIssues, ...languageToolIssues]` (line 36) and the `DocumentQualityReport` contract has no per-checker status field to say otherwise.",
  "shouldBe": "Add a per-checker state to `DocumentQualityReport` (`checkers: [{ name: 'vale', ran: false, reason }]`) so a report where a checker did not run cannot be rendered as a report where it ran and was satisfied. `parseValeJson` has the same `catch { return []; }` at server/integrations/vale/client.ts:66 and needs the same treatment.",
  "consequence": "POST /api/conversation-os/conversations/:id/quality/lint returns HTTP 200 with `success: true` and a report whose only issues are the in-process Concept2Cure rules. A medical writer reviewing a regulatory draft is shown a clean house-style/readability result for a checker that never executed, with nothing in the response to distinguish it from a document Vale actually passed.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "critical",
  "angle": "claims-and-labels",
  "file": "client/src/concept2cure/v2/surfaces/DispatchReadiness.tsx",
  "line": 442,
  "symbol": "DispatchReadiness",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "`ev.cleared` is the external agency-grade eValidator gate. server/services/ectd/external-validator/gate.ts returns `cleared: blockers.length === 0`, and blockers are pushed ONLY when `enforced` (`requireEvalidator && environment === 'production'`). With ECTD_REQUIRE_EVALIDATOR unset \u2014 the documented default-off state \u2014 `cleared` is `true` whether the validator never ran, or ran and reported errors. The `!== false` form additionally turns an absent/undefined field into `ok`. The sub-gate then renders with `class=\"dr2-gate ok\"` and `I.check` (lines 547 and 551), i.e. a green tick, beside its own detail string `'Configured; not yet run for this sequence.'` or `'3 errors'`.",
  "shouldBe": "The tick must be driven by positive evidence that the external validation ran clean \u2014 `ev.ran === true && ev.errorCount === 0` \u2014 and a configured-but-unrun or errored validator must render as not-assessed / blocked, never as a pass. `!== false` should be `=== true` at minimum.",
  "consequence": "On the last screen before an eCTD sequence is transmitted to an agency, a user reading the three composed gates sees a green check against \"Agency-grade validator\" for a validator that never executed against this sequence, or that executed and returned error-severity findings the agency would reject the package for. They transmit on the strength of a tick nothing earned.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "critical",
  "angle": "db-defaults",
  "file": "migrations/20260504_capa_mdr.sql",
  "line": 60,
  "symbol": "complaints (CREATE TABLE)",
  "votes": "cast=1 refuted=0 failed=2",
  "absentBecomes": "\"harm has not been assessed yet on this freshly intaken complaint\" becomes \"no patient harm occurred\". The CHECK on line 87 admits only ('none','malfunction','injury','serious_injury','death') \u2014 the union has no not_assessed member. server/routes/capa-mdr.ts:201 validates patientHarm only `if (body.patientHarm !== undefined)`, and server/services/capa-mdr/capaMdr.service.ts:299 and :325 both collapse it: `patientHarm: input.patientHarm ?? 'none'`.",
  "shouldBe": "Add a `'not_assessed'` member to the CHECK, make it the column default (or drop the default and NOT NULL), and make it the triage engine's explicit branch: the classification returned must be \"harm not assessed \u2014 reportability undetermined, MDR clock not started\", never \"no MDR\". A complaint whose harm is unassessed must also block triage_state from leaving 'new'.",
  "consequence": "An MDR reportability determination is computed and PERSISTED at intake from a fact nobody supplied. server/services/capa-mdr/capaMdr.service.ts:296-303 feeds `patientHarm: input.patientHarm ?? 'none'` straight into `classify()`, whose `case 'none': default:` branch (triageEngine.ts:179-183) pushes the reason \"No harm reported \u2014 log under 820.198, no MDR\" and returns fdaReportable=false; that object is written to `preliminary_classification` and into the vigilance ledger via `recordVigilance`. A call-centre intake of a device death, posted without the optional patientHarm field, is on the record as non-reportable with an affirmative \"no harm reported\" rationale, and the 21 CFR 803.50 30-day (or 803.53 5-day) clock is never started. An FDA inspector reading the complaint file is told the firm assessed the event and found no harm.",
  "note": "Only completed vote said REAL; the other verifiers failed on the spend limit. Not refuted."
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "critical",
  "angle": "db-defaults",
  "file": "migrations/20260705_research_agreements.sql",
  "line": 30,
  "symbol": "research_agreements (CREATE TABLE)",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "\"nobody has been asked whether this data transfer carries PHI\" becomes \"the institution has recorded that it does not\". server/services/research-agreements/research-agreements-service.ts:69 writes `input.containsPhi === true` on create (the route field is `z.boolean().optional()`, routes/research-agreements.ts:71), and research-agreements-logic.ts:63 gates the entire HIPAA branch on `if (a.containsPhi === true) {`.",
  "shouldBe": "`contains_phi` and `contains_human_data` nullable with no default (is_deidentified / limited_data_set may stay false, since those narrow). `evaluateAgreementReadiness` must add a blocker when containsPhi is null \u2014 \"whether this transfer carries PHI has not been recorded\" \u2014 so an unanswered HIPAA question blocks execution instead of bypassing every 45 CFR 164.514 check.",
  "consequence": "A DUA moving a patient dataset executes with the platform reporting zero HIPAA findings. Because `containsPhi` is false-by-default, the three blockers inside that branch \u2014 PHI neither de-identified nor a limited data set; a limited data set moving under an MTA/CDA instead of a DUA; a DUA carrying PHI not marked as a limited data set \u2014 are all skipped, `readyToExecute` is true, and `executeAgreementTx` (research-agreements-service.ts:156-162) stamps status='executed' with an executor and timestamp. The portfolio roll-up then reports `phiCount` computed as `entries.filter(r => r.containsPhi === true)` \u2014 a compliance dashboard telling a privacy officer that none of the institution's agreements involve PHI.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "critical",
  "angle": "status-unions",
  "file": "server/services/compliance/pharmacovigilanceService.ts",
  "line": 621,
  "symbol": "generateICSR",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`SeriousnessCriteria` and `EventType` are closed unions with no 'unknown' / 'not_recorded' member, so an adverse event that could not be read has to be given a real E2B(R3) value. An AE id that returns no row (`if (result.rows.length > 0)` simply does not fire) or an adverse_events table that does not exist (error code 42P01, caught and logged as a warning) both leave `adverseEvent = null`, and the ICSR is built with reportType 'SAE', seriousness 'medically_important' and organizationId ''. The row is then INSERTed into `icsrs` and returned 201 by POST /api/pharmacovigilance/icsr/generate (server/routes/pharmacovigilance-routes.ts:368); the route's 404 branch only fires on an error whose message contains 'not found', and nothing here throws one.",
  "shouldBe": "A missing or unreadable source adverse event must throw (the route already has the 404 branch waiting for it) rather than minting an ICSR. If a partial ICSR record is genuinely wanted, seriousness and reportType must be nullable and the row must be flagged as unsourced \u2014 an empty organizationId additionally detaches the row from every tenant filter.",
  "consequence": "The icsrs table gains a row bearing a worldwide unique case identifier (C.1.8.1), e2b_version 'R3', report_type 'SAE' and seriousness 'medically_important' for a case whose source record was never read \u2014 and organization_id '' puts it outside tenant scoping. A regulator or an inspector auditing the ICSR log is shown a seriousness qualifier that no one ever assessed, on a case that may not exist.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "critical",
  "angle": "empty-set-vacuous",
  "file": "server/services/external-control-arm-service.ts",
  "line": 856,
  "symbol": "ExternalControlArmService.validateControl \u2192 overallQuality",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "When no covariate was assessed (the same reachable empty `covariateBalance` as above), `finiteSmds` is empty and `maxSmd` becomes 0 \u2014 the BEST possible standardized mean difference. `anySeparating` is likewise `false` on the empty array. `maxSmd < 0.1` is then trivially satisfied, so the verdict falls through to `'good'` whenever eValue > 2 and the overlap ratio holds.",
  "shouldBe": "`finiteSmds.length === 0` is 'no balance evidence', not 'perfect balance'. `overallQuality` needs an `'unassessed'` member (the union today is only good/moderate/poor), and the empty case must select it \u2014 or at minimum fall to `'poor'`, the same direction the line immediately below already takes for a perfectly-separating covariate.",
  "consequence": "The persisted `diagnostics.overallQuality` reads `good`, and `generateRegulatoryPackage` copies it into the executive summary verbatim: 'Diagnostics indicate good overall quality.' Because the caution at line 988 is gated on `overallQuality === 'poor'`, the limitations disclosure that would warn about covariate balance is also omitted. An agency reviewer is told the diagnostics are good and is shown no balance caveat, for an analysis in which balance was never computed.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "critical",
  "angle": "empty-set-vacuous",
  "file": "server/services/external-control-arm-service.ts",
  "line": 1027,
  "symbol": "ExternalControlArmService.generateRegulatoryPackage \u2192 diagnosticsSummary",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "An external control arm whose source arms carry NO numeric demographics (or fewer than two source arms) produces `covariateBalance: []`, because `assessCovariateBalanceOfArms` returns `[]` on `sourceArms.length < 2` (line 155) and on `allKeys.size === 0` (line 163), and `continue`s past any key missing from either group (line 182). `[].every(...)` is `true`, so the regulatory package asserts every covariate is balanced when zero covariates were ever assessed. The sibling fields agree with the lie: `maxStandardizedDifference` is `null` and `separatingCovariates` is `[]`.",
  "shouldBe": "The three balance fields must distinguish 'assessed and balanced' from 'not assessed'. Either `covariateBalance.length > 0 && covariateBalance.every(c => c.balanced)`, or better a tri-state (`'balanced' | 'imbalanced' | 'not_assessed'`) so the package says so in words, matching the pattern already used in this repo at server/services/ind-forms/ind-form-data-builders.ts:463 (`investigators.length > 0 && investigators.every(...)`) for the Form 3454 'NONE' certification.",
  "consequence": "`GET /api/biostat/external-control/:controlId/regulatory-package` (server/routes/biostatPlatform.ts:648) returns a document titled 'External Control Arm: \u2026' citing ICH E10 and the FDA 2023 externally-controlled-trials guidance. Its diagnostics section tells an FDA reviewer that every baseline covariate is balanced between the external control and the treated population. No covariate was compared. The one thing an externally-controlled trial lives or dies on \u2014 comparability of the borrowed control \u2014 is affirmed on an empty list.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "critical",
  "angle": "status-unions",
  "file": "server/services/regulatory/change-assessment.ts",
  "line": 96,
  "symbol": "assessFdaChange",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "`export type ChangeDecision = 'letter_to_file' | 'new_510k' | 'pma_supplement_or_de_novo';` (line 40) has no 'insufficient_information' member. Every one of the seven FdaChangeInput fields is a plain boolean, and every unanswered question is `undefined`, which is falsy, so it contributes no trigger. The route `router.post('/change/fda-510k', calcK(assessFdaChange, 'change.fda-510k'))` (server/routes/ivd-lifecycle.ts:462) passes the body through unvalidated \u2014 `const out = fn(req.body as TIn);` (server/routes/ivd-lifecycle.ts:146) \u2014 so POST with `{}` returns `decision: 'letter_to_file'`.",
  "shouldBe": "ChangeDecision needs an 'insufficient_information' member (the module already has the vocabulary: shared/regulatory/change-control-taxonomy.ts distinguishes the same three FDA outcomes and nothing more). The input fields should be `boolean | null` with null meaning \"not answered\", and the engine should return that member, naming the unanswered questions, whenever any flowchart input is unrecorded. Failing that, the route must reject a body that does not carry all seven answers.",
  "consequence": "An auditor or a reviewer reading the platform's change-control record is told, for a device change nobody has actually evaluated, that \"No flowchart trigger is met; document the change in a Letter-to-File\" \u2014 the determination that no new 510(k) is required. Shipping a modified device on an unevaluated Letter-to-File is precisely what FDA's \"Deciding When to Submit a 510(k) for a Change to an Existing Device\" exists to prevent, and the record shows a deterministic engine issued the clearance.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "claims-and-labels",
  "file": "client/src/concept2cure/v2/surfaces/ClientPortal.tsx",
  "line": 187,
  "symbol": "ClientPortal",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`p.blocker` is hard-coded null by the server on every row: server/routes/client-portal.ts:222 \u2014 `blocker: null as string | null, // no per-program blocker column \u2014 honest null`. The server's null is honest; the client turns it into the green `ok` tone. Every program bar on this surface is therefore green, for every client, always \u2014 the falsy branch is the only branch that can ever be taken.",
  "shouldBe": "An absent blocker field is not an absence of blockers. The tone should be neutral/unknown while the data model carries no blocker column, or the bar should not be tone-coded at all \u2014 the surface must not paint a clearance colour off a field the backend documents as unpopulated.",
  "consequence": "This is the read-only portal a CRO's CLIENT sees \u2014 the sponsor paying for the programme. They read a green readiness bar on every program and conclude their regulatory partner has flagged no blockers. No blocker assessment exists at all; the column does not exist in the data model.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "claims-and-labels",
  "file": "client/src/concept2cure/v2/surfaces/DispatchReadiness.tsx",
  "line": 491,
  "symbol": "DispatchReadiness (lead)",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "The clause \", external validator clean\" is selected by `ev.configured` alone \u2014 whether a licensed engine exists \u2014 not by whether it ran or what it found. A deployment with an eValidator configured but no report for this sequence (`ev.ran === false`) is told the external validator is clean. So is one where it ran and returned errors while ECTD_REQUIRE_EVALIDATOR is off.",
  "shouldBe": "Gate the clause on `ev.ran && ev.errorCount === 0`, and say so explicitly otherwise \u2014 e.g. \"external validator configured but not run for this sequence\". The surrounding sentence (\"Every hard gate is proven clear\", line 490) must not be printed over a gate that was never executed.",
  "consequence": "The sentence is the surface's answer-first verdict, and it explicitly claims to be \"computed from server state \u2014 not a model opinion\". A publisher reads \"external validator clean\" and files. Nothing validated the package against the agency's own engine.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "claims-and-labels",
  "file": "client/src/concept2cure/v2/surfaces/ReviewThreads.tsx",
  "line": 225,
  "symbol": "post",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "The toast asserts a notification unconditionally on `res.ok`. Server-side (server/routes/c2c/reviews.ts:936-938) the recipients are computed as `if (thread.assigneeId && thread.assigneeId !== userId) notifyTargets.add(thread.assigneeId); if (thread.createdById && thread.createdById !== userId) notifyTargets.add(thread.createdById);` \u2014 so when the commenter is the thread's own creator and the thread has no assignee (`assigneeId` is nullable, shared/schema.ts:6465), `notifyTargets` is empty and NO notification is created. That is the normal reviewer flow: open a thread against someone else's document, then post the change request on it. Separately, the document's AUTHOR is never in the target set under any configuration \u2014 the handler never resolves the artifact's author.",
  "shouldBe": "Report what the server actually did: have the route return the recipients it notified and have the toast name them, or say \"Change request recorded\" when none were notified. \"the author has been notified\" should not be said at all while the author is not a notification target.",
  "consequence": "A reviewer posts a formal change request, reads \"Changes requested \u2014 the author has been notified\", and stops chasing it. Nobody is notified, and the person who has to act on the change request is never told under any code path. The change request sits open on a document heading for review sign-off.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "db-defaults",
  "file": "migrations/20260610_ibc_biosafety.sql",
  "line": 27,
  "symbol": "ibc_registrations (CREATE TABLE)",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "\"this registration has not been classified under the NIH Guidelines yet\" becomes \"the NIH Guidelines do not apply to this research\" \u2014 an affirmative regulatory claim. The union has no not_assessed member, and server/services/ibc/ibc-service.ts:52 hard-codes the collapse: `input.nihGuidelinesSection ?? 'not_applicable'` (the route field is `SECTION.optional()`, routes/ibc.ts:89).",
  "shouldBe": "Add `'not_assessed'` to the CHECK and make it the default (or drop the default and NOT NULL), and have `requiresConvenedReview` / `evaluateContainment` return an undetermined verdict for it rather than false. 'not_applicable' and 'exempt' are both determinations somebody has to make; neither is the state of a row nobody has classified.",
  "consequence": "The API tells the submitter that a gene-therapy or viral-vector registration needs no convened IBC review. `requiresConvenedReview(section, involvesHumanGeneTransfer)` (ibc-logic.ts:95-97) returns `['III-A','III-B','III-C'].includes(section) || involvesHumanGeneTransfer`, so with the defaults it returns false, and POST /api/ibc/registrations responds `{ requiresConvenedReview: false }` (routes/ibc.ts:101-102) while `evaluateContainment` omits the \"requires review and approval by the convened IBC before initiation\" finding. Under the NIH Guidelines, III-A/B/C research and human gene transfer may not be initiated before convened-IBC approval; the registration record also reads, to an OBA or institutional auditor, as an assertion that the Guidelines do not apply. `biosafety_level NOT NULL DEFAULT 'BSL-1'` compounds it by making the lowest containment the value of an unfilled field.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "db-defaults",
  "file": "migrations/20260618_coverage_analysis.sql",
  "line": 65,
  "symbol": "coverage_analysis_items (CREATE TABLE)",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "\"nobody has yet answered whether the sponsor pays for this item under the CTA/informed consent\" becomes \"the sponsor does not pay for it\". server/services/coverage-analysis/coverage-service.ts:135 writes `!!input.isStandardOfCare, !!input.sponsorPaidInBudget` on insert from optional fields, and the AnA tool does the same (AnaToolExecutor.ts:11863-11864: `typeof input.sponsor_paid_in_budget === 'boolean' ? input.sponsor_paid_in_budget : false`).",
  "shouldBe": "Both columns nullable with no default, so an item carries \"not yet answered\" until a human answers \u2014 consistent with the row's own `classification text NOT NULL DEFAULT 'unclear'` / `billing_designation text NOT NULL DEFAULT 'hold'`, which do model the unanswered state correctly. `suggestItemClassifications` must then suggest 'hold' with the reason \"sponsor-payment / standard-of-care questions unanswered\" instead of running the classifier on invented answers.",
  "consequence": "The suggestion list a hospital biller works from recommends billing Medicare for items nobody has checked against the study budget. `suggestItemClassifications` (coverage-service.ts:300) calls `classifyCoverageItem({ trialQualifies, isStandardOfCare: row.is_standard_of_care, sponsorPaidInBudget: row.sponsor_paid_in_budget })` on exactly the items still on hold; with both stored as false-by-default the classifier skips its first two branches and returns `bill_medicare` with the rationale \"Routine cost of a qualifying clinical trial ... Billable to Medicare (NCD 310.1; 42 CFR 405.211).\" Billing Medicare for an item the sponsor already pays for is the double-billing the module's own header calls out as False Claims Act exposure \u2014 and the platform is the thing suggesting it.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "db-defaults",
  "file": "migrations/20260629_protocol_amendments.sql",
  "line": 26,
  "symbol": "protocol_amendments (CREATE TABLE)",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "\"the sponsor has not stated whether this amendment affects informed consent or subject risk\" becomes \"the sponsor has declared that it affects neither\". Both reader paths were written for a tri-state and are defeated by the column: server/services/protocol-amendments/protocol-amendments-service.ts:275-276 maps `affectsConsent: row.affects_consent ?? null` (never null \u2014 the column is NOT NULL), and substantiality.ts:336 tests `declared.affectsRisk === false && declared.affectsConsent === false`, which a NOT NULL default makes indistinguishable from silence. The write side collapses it at protocol-amendments-service.ts:87: `input.affectsConsent ?? false` (route field `z.boolean().optional()`, routes/protocol-amendments.ts:93).",
  "shouldBe": "Both columns nullable with no default, matching the `boolean | null` the service mapper and substantiality.ts:78 already declare. `classifyAmendmentImpact` must return an undetermined review path / re-consent verdict when either is null, and `reconcileDeclaration` must not attribute a declaration to a sponsor who made none.",
  "consequence": "Two false statements to an IRB. First, `classifyAmendmentImpact` (protocol-amendments-logic.ts:42) computes `const substantive = input.affectsConsent || input.affectsRisk`, so an amendment nobody assessed returns `requiresReconsent: false` and routes to 'expedited' (minor) or 'administrative' review \u2014 enrolled subjects are never re-consented on a change that may have altered their risk, and the pdev view renders `reconsent: false` (pdev-view-assembler.ts:167). Second, substantiality.ts:336-338 then tells the reviewer \"This amendment is declared to affect neither consent nor risk, but the comparison shows change to what a subject is exposed to or protected by\" \u2014 attributing to the sponsor a declaration the sponsor never made, in the one output the module's own comment calls \"the only place in the platform where a declaration can be contradicted by evidence\".",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "db-defaults",
  "file": "migrations/20260704_other_support.sql",
  "line": 49,
  "symbol": "other_support_entries (CREATE TABLE)",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "\"nobody asked this investigator whether the support or resource is foreign\" becomes \"the investigator has declared it domestic\". server/services/other-support/other-support-service.ts:98 writes `input.isForeign === true` (route field optional, routes/other-support.ts:144), and other-support-logic.ts:131 gates the NIH foreign-disclosure blocker on `e.isForeign === true`.",
  "shouldBe": "`is_foreign boolean` nullable with no default, and `evaluateOtherSupportReadiness` must add a blocker for any entry whose foreign status is unrecorded, in the same style as the four blockers already there for missing title / funding source / major goals / overlap statement. An unanswered question is not a negative declaration, least of all on a form signed under penalty of perjury.",
  "consequence": "A certified NIH Other Support document omits a foreign component and asserts the omission. `evaluateOtherSupportReadiness` only fires \"Foreign entry ... must disclose the country (NOT-OD-21-073)\" when is_foreign is true, so an entry created without the flag produces no blocker, `readyToCertify` is true, and `buildOtherSupportExport` emits the row as `isForeign: false, foreignCountry: null` while `computeOtherSupportSummary` reports `foreignCount: 0`. NIH Other Support is certified by the PI and the institution as true, complete and accurate; undisclosed foreign support is the exact failure NOT-OD-21-073 was issued about and the one NIH has pursued as fraud.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "claims-and-labels",
  "file": "server/routes/mdx-imports.ts",
  "line": 509,
  "symbol": "GET /api/mdx/onboarding/:tenantId",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "An `import_job_findings` row whose `severity` column is NULL is reported with severity `'ok'`. Seven lines later that value is the filter for the headline count: line 516 \u2014 `{ label: 'Open findings', metric: String(validations.filter((v) => v.severity !== 'ok').length), meta: '', tone: 'warn' }`. So an unclassified finding is silently subtracted from the \"Open findings\" number while still sitting in the validations list carrying a green-reading severity.",
  "shouldBe": "A finding with no recorded severity is unclassified, not clean. Emit `'unknown'` (or null) and count it as open \u2014 an unclassified finding is precisely the one a migration reviewer must look at.",
  "consequence": "A customer migrating a legacy submission archive reads \"Open findings: 0\" on the migration importer while findings exist in `import_job_findings` that nobody classified. They sign off the import as clean.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "claims-and-labels",
  "file": "server/services/ana-ri/context-enrichment.ts",
  "line": 474,
  "symbol": "enrichWithReadiness",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`score.gaps` is built (readiness-scoring-engine.ts:188-232) only from `profile.risks`, `docs.staleDrafts` and `milestones.overdue`. A program that has had no risk recorded, no stale draft and no overdue milestone \u2014 i.e. one nobody has populated \u2014 produces `gaps: []`, and the empty list renders as the literal `- None identified`. \"We found nothing\" and \"nothing has been entered to find\" are the same string.",
  "shouldBe": "Distinguish the two: when the gap sources carried no rows at all, say that no gap assessment has run and name what would populate it, rather than printing a clearance line. The same file already does exactly this for `predictions` at lines 466-472 (\"Do not state an approval probability \u2026 none has been computed\").",
  "consequence": "The prompt block ends with \"Present the scores above directly\". AnA therefore tells the customer, in its own voice, that their program has 0 gaps and none identified \u2014 over a program whose risk register, document store and milestone list are simply empty. The user stops looking for blockers that were never searched for.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "empty-set-vacuous",
  "file": "server/services/ana/AnaToolExecutor.ts",
  "line": 4154,
  "symbol": "registerToolHandler('check_regulatory_compliance')",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`checks` starts as `[]` (line 4117) and is only populated inside `if (framework.includes('510k') || framework.includes('fda'))` and `if (framework.includes('eu_mdr'))`. Any other `regulatory_framework` value \u2014 'ivdr', 'mdsap', 'pmda', 'iso_13485', a typo, an empty string \u2014 leaves `checks` empty, and `[].every(...)` is `true`. The tool then reports `overallStatus: 'compliant'` with `gapsCount: 0` and `complianceChecks: []`.",
  "shouldBe": "An unrecognised framework is 'not assessed', not 'compliant'. The handler should return an explicit error or `overallStatus: 'not_assessed'` naming the frameworks it can check, and must never emit `compliant` with an empty `complianceChecks` array.",
  "consequence": "AnA tells a user their section is 'compliant' with 0 gaps against a regulatory framework the tool has no rules for. The user reads a compliance verdict on a section that was never examined against anything, and the empty `complianceChecks` array that would have given it away is the least-read field in the JSON.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "empty-set-vacuous",
  "file": "server/services/authoring/revision-ledger.ts",
  "line": 151,
  "symbol": "verifyLedger",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`verifyLedger([])` returns `{ intact: true, chainedCount: 0, preLedgerCount: 0, breaks: [] }`. The loop never executes, so no hash is recomputed and no link is checked, yet the verdict is the affirmative one. The route feeding it (server/routes/authoring.router.ts:2098) selects `WHERE section_id = $1 AND tenant_id = $2` \u2014 a section with no revisions, or a tenant-scope mismatch that returns no rows, both land on the empty array.",
  "shouldBe": "`intact` is only meaningful once something was verified. The verdict needs a third state ('nothing to verify' / 'no chained revision on record') when `chainedCount === 0`, and `intact` should be false or undefined there \u2014 the same discipline this file's own sibling `dispatch-gate.ts` applies: 'a skipped check must never read as a passed one.'",
  "consequence": "client/src/concept2cure/v2/editor/DocumentWorkbench.tsx:4355 renders the verdict in the ok tone as 'Ledger intact \u2014 0 chained revisions recomputed and verified'. That is a 21 CFR Part 11 \u00a711.10(e) integrity assertion \u2014 issued about a section for which the server recomputed nothing. A reviewer clicking 'Verify ledger' on a section whose history could not be read for their tenant is told the record is verified. The test file server/services/authoring/__tests__/revision-ledger.test.ts covers five tamper classes and never calls verifyLedger([]).",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "empty-set-vacuous",
  "file": "server/services/cdisc/cdisc-conformance-service.ts",
  "line": 184,
  "symbol": "validateSdtmDataset",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "Rule 3 runs only `if (requiredVars)` \u2014 i.e. only for the 12 domains keyed in `SDTM_REQUIRED_VARIABLES` (DM, AE, CM, LB, VS, EX, DS, MH, SU, PE, EC, IE). `SDTM_KNOWN_DOMAINS` is far larger (it includes TR, IS, SS, AG, BE, BS, CP, CV, DD, FT, GF, HO, MK, OE, PR, RE, RP, SR, UR \u2026). For any of those, Rule 1 passes (the domain IS recognised), Rule 2 iterates an empty `variables` array, Rule 3 is skipped entirely, `findings` is `[]` and `valid` is `true`. `validateAdamDataset` (line 268) has the identical hole for ADPC, ADPP, ADEG, ADMH, ADDS and ADSAFETY, which are in `ADAM_KNOWN_DATASETS` but absent from `ADAM_REQUIRED_VARIABLES`.",
  "shouldBe": "`valid` must not be returned for a dataset against which no content rule could be applied. Either emit an explicit finding ('no required-variable profile is modelled for domain X \u2014 conformance was not assessed') so `valid` goes false, or return a tri-state (`'conformant' | 'nonconformant' | 'not_assessed'`) with `rulesApplied` alongside it. An empty `variables` array should in any case be a finding of its own: a dataset with zero variables is not a conformant dataset.",
  "consequence": "Reachable through AnA: server/services/ana/AnaToolExecutor.ts:17148 maps user-supplied datasets with `validateSdtmDataset({ domain: ds.name, variables: ds.variables || [] })`, so a dataset whose variables were never captured arrives as `[]`. A sponsor is told a Tumour Results (TR) or ADPC dataset is 'valid' against SDTM-IG v3.4 / ADaM IG v1.3 when not one IG rule was evaluated against it. That verdict goes into define.xml / submission-readiness conversations before the package reaches Pinnacle 21.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "status-unions",
  "file": "server/services/clinical-regulatory-evidence/index.ts",
  "line": 704,
  "symbol": "getTrace",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`EvidenceTrace.corpusSnapshot` is a bare `string` (types.ts:318) with no unknown value, so `freshness: string | null` collapses to the literal 'current'. This contradicts the documented rule in the sibling module, coverage-service.ts:74: \"Unknown freshness is NOT treated as fresh \u2014 an unanswerable question returns false only because we cannot assert staleness, and callers should show freshness as unknown rather than current.\" On the same object, `reviewState: 'draft' | 'extracted' | 'human_reviewed'` (types.ts:324) has no 'unreviewed' member, so relationships that merely lack an `isInferred` flag are labelled 'human_reviewed', and `recalculateRequired` is hard-coded `false` although its own doc comment reads \"True when the corpus snapshot advanced past this trace.\"",
  "shouldBe": "Carry `corpusSnapshot: string | null` (or add an explicit 'unknown') and render it as unknown, per coverage-service.ts's stated contract. `reviewState` needs an 'unreviewed' member and must be driven by a recorded review event, not by the absence of an inferred flag. `recalculateRequired` must be computed with `isStale(coverage, currentSnapshot)` rather than pinned to false.",
  "consequence": "The EvidenceTrace is described in types.ts:314 as \"what makes a recommendation auditable rather than merely plausible.\" It tells an auditor the recommendation was drawn from the *current* corpus snapshot when the corpus freshness is unknown, that the evidence links were *human reviewed* when no human review is recorded, and that no recalculation is required when staleness was never evaluated.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "status-unions",
  "file": "server/services/intelligence/judgment-framework.ts",
  "line": 175,
  "symbol": "evaluateEvidenceSufficiency",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`JudgmentVerdict` (line 64) is 'pass' | 'acceptable' | 'needs_attention' | 'at_risk' | 'fail' \u2014 no 'undetermined'. Four factors default an unmeasured readiness dimension to 50 at full weight: line 175 (completeness), line 208 (quality), line 272 (compliance, weight 0.30), line 542 (consistency, weight 0.50). Their enclosing functions then use a plain sum \u2014 `const totalScore = Math.round(factors.reduce((sum, f) => sum + f.weightedScore, 0));` \u2014 while the file's own `renormalisedScore` helper (line 131) exists for exactly this and is called at only one site, line 433. The helper's docstring states the rule being broken: \"When a factor is omitted because its input was never measured, a plain sum silently reports the missing weight as zero score \u2014 so 'we could not measure this' and 'this scored badly' produce the same number.\" A sibling model, evaluateReviewerSensitivity, was already corrected this way (dated note at line 357).",
  "shouldBe": "Omit the factor when its input was never measured and call `renormalisedScore(factors)`, as evaluateReviewerSensitivity does; add an 'undetermined' member to JudgmentVerdict (or return null with a basis) for a model whose surviving weight is too small to support any verdict, and propagate it through evaluateSubmissionRisk to `overallVerdict`.",
  "consequence": "With `readiness === null` and empty gaps/recommendations/chains \u2014 a project on which nothing has been assessed \u2014 evaluateDefensibility returns 75/100 with verdict 'acceptable', documented in the union as \"Minor concerns, not blocking\". generateJudgmentReport (line 700) surfaces that through `overallVerdict`. A regulatory lead asking \"can this submission withstand scrutiny?\" is told its defensibility is acceptable and not blocking, on a score three-quarters composed of two hard-coded 50s.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "claims-and-labels",
  "file": "server/services/regulatory-precedent-intelligence/confidence-calibration-service.ts",
  "line": 343,
  "symbol": "ConfidenceCalibrationService.getCalibrationReport",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "A Brier score of 0 is PERFECT calibration. When no prediction has ever been resolved (`brierCount === 0`) the report emits `averageBrierScore: 0`, which falls into the first branch of the very next ternary and produces the literal string `'Excellent calibration. Maintain current approach.'`. `calculateOverconfidenceBias` (line 405) does the same: `totalCount > 0 ? ... : 0`, and 0 bias means perfectly unbiased. Nothing in the returned CalibrationReport distinguishes \"measured and excellent\" from \"never measured\".",
  "shouldBe": "With `brierCount === 0` the score and the bias are not numbers the service has \u2014 they should be `null`, and `recommendation` should state that no prediction has been resolved so calibration is unknown. The CalibrationReport type (line 79) needs to permit that.",
  "consequence": "GET /api/regulatory-precedent-intelligence/confidence/calibration-report is the platform's own honesty report about how much weight to put on its predictions \u2014 the Filing Strategy surface introduces it as \"the honest basis for how much weight to put on any of the platform's predictions\". A brand-new tenant that has resolved zero predictions is told its prediction rules are excellently calibrated with zero overconfidence bias, and to maintain the current approach.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "status-unions",
  "file": "server/services/regulatory/change-assessment.ts",
  "line": 147,
  "symbol": "assessEuSignificantChange",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "`EuSignificantChangeResult.significant` is a plain `boolean` (line 129) \u2014 two-valued, so it cannot say \"not assessed\", exactly the shape irb-logic.ts was faulted for. All five EuSignificantChangeInput fields are booleans; an unanswered one is `undefined` and pushes no trigger, so an empty body yields `significant: false` with the rationale 'No significant-change criterion is met; the change is non-significant.' The route `router.post('/change/eu-significant', calcK(assessEuSignificantChange, 'change.eu-significant'))` (server/routes/ivd-lifecycle.ts:463) does not validate the body.",
  "shouldBe": "Return a three-valued verdict ('significant' | 'non_significant' | 'not_assessed'), or make the inputs `boolean | null` and refuse to conclude while any criterion is unanswered. shared/regulatory/change-control-taxonomy.ts already names the two decided outcomes as `EuDeviceChangeOutcome`; the third state belongs beside them.",
  "consequence": "An MDR/IVDR change record states the change is non-significant, which under MDCG 2020-3 is the finding that preserves transitional-provision eligibility and means no Notified Body assessment is needed \u2014 produced from an input where the five criteria were never answered. A manufacturer relying on that record keeps selling under a legacy certificate on a change nobody assessed.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "status-unions",
  "file": "server/services/regulatory/iec-62304-software.ts",
  "line": 46,
  "symbol": "classifySoftwareSafety",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`export type SoftwareSafetyClass = 'A' | 'B' | 'C';` (line 23) has no 'undetermined' member, and `canContributeToHazard` is a bare boolean. An unrecorded answer is `undefined` \u2192 falsy \u2192 Class A, the least-rigorous class, with a rationale asserting the software cannot contribute to a hazardous situation. Reached unvalidated via `router.post('/software/safety-class', calcK(classifySoftwareSafety, 'software.safety-class'))` (server/routes/ivd-lifecycle.ts:455).",
  "shouldBe": "`canContributeToHazard` and `worstCaseHarm` should be nullable with an explicit unknown, and the union should carry an 'undetermined' member returned whenever the hazard question is unanswered. IEC 62304 \u00a74.3 classification is a documented determination; the absence of an answer is not the answer 'no hazard'.",
  "consequence": "The safety class then drives `assessSdlcCompleteness` via REQUIRED_BY_CLASS, where Class A requires 6 lifecycle artifacts and Class C requires all 10 (no architecture, no detailed design, no unit verification, no integration testing). A software file that was never classified is reported as Class A with the rationale 'Software cannot contribute to a hazardous situation', and the SDLC completeness assessment built on it then tells a reviewer the file is complete while four IEC 62304 \u00a75 artifacts are absent by construction.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "major",
  "angle": "empty-set-vacuous",
  "file": "server/services/regulatory/registry/registryCoverage.ts",
  "line": 435,
  "symbol": "getDocumentCoverage",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`requiredFormCoverage` (line 345) builds its list by filtering `entry.requiredArtifacts` with `/^form[\\s_-]/i`. `entry()` defaults `requiredArtifacts: opts.requiredArtifacts || []` (shared/regulatory/global-document-registry.ts:66), and of the 234 `entry(...)` rows in that file only 8 declare `requiredArtifacts` at all and only 5 name a form. So for ~226 active registry entries `requiredForms` is `[]`, and BOTH booleans come back `true`. The regex is also anchored at the start of the string, so an artifact spelled 'FDA Form 3674' rather than 'form_3674' silently drops out of the list and produces the same `true`.",
  "shouldBe": "Both flags must be guarded on a non-empty list (`requiredForms.length > 0 && requiredForms.every(...)`), or the field must carry a third state ('no forms required' vs 'all required forms backed'). The file's own doc comment at line 253 calls `formsFullyBacked` 'a number that gates a filing' and says the report 'can no longer call a form backed that the renderer would refuse' \u2014 the empty case is the one path where it still can.",
  "consequence": "`buildCoverageReport().entries` and `getDocumentCoverage(id)` are the client-facing filing-readiness record (surfaced through server/services/regulatory/registry/index.ts and scripts/regulatory-coverage-report.ts). For EU_CTA, EU_MAA, US_510K, US_PMA and ~222 other filing types the report states that every required form is registered, has a full builder, has an integrity-verified official FDA edition installed, AND names a named human reviewer. None of those things was checked; no form was examined and no person reviewed anything. This is the same defect class the repo already fixed for Form 3454 and left unfixed one directory away.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "claims-and-labels",
  "file": "client/src/concept2cure/v2/surfaces/AuthoringCollab.tsx",
  "line": 184,
  "symbol": "takeover",
  "votes": "cast=0 refuted=0 failed=0",
  "absentBecomes": "Stated as accomplished fact on a 200. Server-side (server/routes/realtime-collab.ts:1016-1030) the notification is sent only when `Number(result.previousHolder.id)` is finite and > 0 \u2014 a non-numeric principal id skips it silently \u2014 and `sendDisplacedNotification` (line 1094) is fire-and-forget: `void (async () => { ... })().catch(() => {});`. Every failure, including the dynamic import and the createNotification write, is swallowed. The route has already returned success by then.",
  "shouldBe": "Say what the response proves: \"Lock taken over. The displaced author is notified in the app.\" only if the route reports the notification was created; otherwise \"Lock taken over \u2014 the takeover is recorded in the audit trail.\"",
  "consequence": "An author takes a colleague's section lock, reads that the colleague has been notified, and edits on. The displaced author was never told and loses work-in-progress context on a governed document. The takeover reason \u2014 which the backend correctly insists on because \"the displaced author is shown it\" (line 1017 comment) \u2014 is shown to nobody.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "claims-and-labels",
  "file": "client/src/concept2cure/v2/surfaces/ClientPortal.tsx",
  "line": 236,
  "symbol": "ClientPortal",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "Two governed claims with nothing behind them on this surface. The portal is read-only by construction (the file's own header: \"the read-only view a CRO's client sees\"); there is no share control and no request-a-change control, GET /api/client-portal/overview is the only endpoint, and server/routes/client-portal.ts contains no notification code at all (`grep notif` returns nothing). \"All exchanges are logged under 21 CFR Part 11\" is asserted over a surface that records no exchange.",
  "shouldBe": "Either wire a share/request control that demonstrably notifies and audits, or say what is true: how to reach the CRO today, and that this view is read-only. A Part 11 audit claim belongs only next to a path that writes an audit row.",
  "consequence": "A sponsor's client is told their CRO is notified instantly of any file or change request, and that every exchange is Part-11 logged. Nothing on this screen notifies anyone or writes an audit record. A client who believes they have raised a change request through the portal has raised nothing.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "claims-and-labels",
  "file": "client/src/concept2cure/v2/surfaces/CmcModule3Build.tsx",
  "line": 395,
  "symbol": "CmcModule3Build",
  "votes": "cast=0 refuted=0 failed=0",
  "absentBecomes": "`summary` is optional on the wire (`summary?: Module3BuildSummary`, line 75) and is read with no shape guard. A 200 that omits or renames the summary object renders \"Stale 0\" and \"Blocked 0\" with the green `ok` tone \u2014 a reported count of zero for a figure the server did not report. The neighbouring \"Approved\" KPI fails the other way (`>= 80 ? 'ok' : ... : 'err'`), which is correct; these two fail open.",
  "shouldBe": "Render `\u2014` with a neutral tone when `summary` is absent, as the surface already does for the artifact registry a few lines above (\"no governed artifacts are shown \u2014 not because there are none\"). A count that was not reported is not zero, and never green.",
  "consequence": "On the CMC Module 3 build board \u2014 whose stale and blocked counts are the ones the final-export gate refuses on \u2014 a payload skew paints two green zeros. A user reads \"0 stale, 0 blocked\" and compiles Module 3 against sections that went stale after approval.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "claims-and-labels",
  "file": "client/src/concept2cure/v2/surfaces/EctdCompile.tsx",
  "line": 482,
  "symbol": "doValidate",
  "votes": "cast=0 refuted=0 failed=0",
  "absentBecomes": "A 200 whose body carries no `results` array becomes an empty findings list, which selects the clearance panel at line 823: `<EmptyState icon={I.checkCircle} title=\"No findings\" hint=\"No blocking or advisory issues were raised for the selected region.\" />` \u2014 a green tick plus an explicit assertion that nothing was raised. This directly contradicts the comment four lines above it (line 477): \"Do NOT collapse a failed run into an empty findings list \u2014 that reads as a clean pass\", which the code correctly honours only for the `!ok || !body` branch.",
  "shouldBe": "`body.results` must be validated as an array before it is accepted as a result; a 200 missing it is a failed read, so `setValidationFailed(true)` \u2014 the same treatment the transport failure already gets, and the same shape-guard discipline the rest of this codebase applies to a 200 that lost its payload.",
  "consequence": "An eCTD pre-compile validation whose response loses its `results` field paints the green \"No findings \u2014 no blocking or advisory issues were raised for the selected region\" panel. The publisher takes that as a clean regional validation and proceeds to compile and transmit.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "empty-set-vacuous",
  "file": "server/config/FDAFormsRegistry.ts",
  "line": 722,
  "symbol": "checkFormDependencies",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "A `formId` that is not in `FDAFormsRegistry` \u2014 a typo, a retired form id, a form added to a submission-type list before it was registered \u2014 returns `true`, i.e. 'all dependencies met'. The unknown-form case is widened, not narrowed: the function cannot look up the form's prerequisites, so it declares there are none.",
  "shouldBe": "An unresolvable form id is a failure to determine, not a determination. `!form` should return `false` (or throw), and only a registered form with a genuinely absent `dependencies` field should return `true`. Narrowing on absence is the repo's stated rule; this widens.",
  "consequence": "Any caller gating 'this form may be generated / marked complete' on `checkFormDependencies` is told the prerequisites are satisfied for a form the registry has never heard of, so a form is cleared to fill with its predecessor forms unfilled. Reachability is limited today (the only consumer is the `FDAFormsRegistryClass` wrapper at line 762, which nothing else calls yet), which is why this is ranked below the others \u2014 but the direction is wrong and the next caller inherits it.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "claims-and-labels",
  "file": "server/routes/mdx-imports.ts",
  "line": 520,
  "symbol": "GET /api/mdx/onboarding/:tenantId",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "The first onboarding step is a constant `'complete'`. Nothing is read \u2014 not the org record, not the SSO configuration, not whether an admin exists. The three siblings immediately below it are evidence-driven (`artifacts.length > 0 ? 'complete' : 'pending'`, `mapped > 0 ? 'active' : 'pending'`, `validations.length > 0 ? 'active' : 'pending'`), which is exactly what the route's own doc comment at line 474 promises: \"steps is the onboarding workflow sequence, marked complete by evidence.\"",
  "shouldBe": "Derive it from the same evidence as its siblings \u2014 the org record, an SSO/domain configuration row and an admin user \u2014 or drop the step. A step labelled \"Org account \u00b7 SSO \u00b7 admin\" must not report complete without checking any of the three.",
  "consequence": "The migration onboarding tracker reports the Connect step complete, including SSO, on a tenant with no SSO configured. Anyone reading the onboarding state believes identity federation is done.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "claims-and-labels",
  "file": "server/services/clinical-regulatory-evidence/index.ts",
  "line": 704,
  "symbol": "getTrace",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "When the evidence corpus's freshness is not recorded, the trace declares the corpus snapshot to be `'current'`. Unknown provenance becomes the most favourable provenance.",
  "shouldBe": "`corpusSnapshot: coverage.freshness ?? 'unknown'` \u2014 an unrecorded refresh date is not a statement that the corpus is up to date.",
  "consequence": "The evidence trace is the provenance record behind a regulatory recommendation. A reviewer checking \"what corpus was this reasoned from, and how old is it?\" is told the snapshot is current when the platform does not know when the corpus was last refreshed.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "status-unions",
  "file": "server/services/pdev/pdev-workflow-bridge.ts",
  "line": 543,
  "symbol": "decideCheckpoint",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "A workflow run whose metadata does not carry `targetState` is treated as a promotion to 'approved'. `PdevActivityState` has no 'unknown' member, and 'approved' is one of the four COMPLETED_STATES ('approved','locked','submission_ready','submitted') in server/services/pdev/pdev-readiness-service.ts:37. The same default appears at line 449 in the read path. Creation does set it (line 296), so this fires on legacy or damaged metadata \u2014 but that is exactly the case where the target is genuinely unknown.",
  "shouldBe": "Throw when `meta.targetState` is absent or is not a member of PDEV_ACTIVITY_STATES. The bridge already refuses non-completed targets at creation time (line 243: \"Workflow approval chain is only used for promotions to completed states\"), so an unreadable target should be refused at decision time for the same reason.",
  "consequence": "On approval the activity row is written `state: targetState` (line 744) and the audit row records `newState: 'approved'` (line 785) \u2014 a 21 CFR Part 11 record asserting the approvers promoted the activity to a state the run never recorded. The PDEV readiness rollup then counts that activity as complete, inflating the IND-readiness percentage a founder or a sponsor reads.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "status-unions",
  "file": "server/services/projects/schedule-of-events/health.ts",
  "line": 132,
  "symbol": "assessScheduleHealth",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`OverallScheduleStatus = 'on_track' | 'at_risk' | 'off_track'` (line 40) has no 'not_scheduled' / 'unknown' member. An empty milestone list makes every count zero and the initialiser survives, so `assessScheduleHealth([])` returns overallStatus 'on_track' with summary \"0/0 milestones complete. Schedule is on track.\" Separately, a milestone with no target date returns unchanged at line 75 with reason 'No target date set.' and is then counted into `onTrack = results.length - completed - slipped - atRisk - blocked` \u2014 an undated milestone is arithmetically on track.",
  "shouldBe": "Add an 'unknown' (or 'not_scheduled') member returned when there are no milestones, and exclude undated milestones from the `onTrack` count by giving them their own bucket \u2014 a milestone with no target date cannot be on or off schedule.",
  "consequence": "A study whose schedule of events has not been populated, or whose milestones carry no dates, reports \"Schedule is on track\" through the read API and the proactive sweep that both call this (server/services/projects/schedule-of-events/service.ts:291). The reassurance is strongest exactly where the data is thinnest.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "status-unions",
  "file": "server/services/regulatory-programs.service.ts",
  "line": 699,
  "symbol": "getSafetySignals",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`SignalSeverity` is `['critical','serious','moderate','low']` (shared/constants/mdx.ts:164) with no 'unknown' member, and severity is derived from the recommended regulatory action via ACTION_TO_SEVERITY (withdrawal\u2192critical, rems\u2192serious, dear_doctor\u2192serious, label_update\u2192moderate, none\u2192low). A safety_signals row whose `action` column is NULL \u2014 no regulatory action decided yet \u2014 misses the map and becomes 'low'. The adjacent `status` default of 'review' is the honest one, which makes the severity default stand out.",
  "shouldBe": "Add an 'unknown' / 'not_assessed' member to SignalSeverity and map a null or unrecognised action onto it, so an undecided signal sorts and renders as undetermined rather than as the lowest of four tiers. Narrowing here would mean escalating, not demoting.",
  "consequence": "The CER safety-signal pane presents a signal for which no regulatory action has been decided at the same severity tier as one actively decided to need no action. In a Clinical Evaluation Report context this understates the safety picture: an undecided signal is invisible in any severity sort or filter.",
  "note": ""
 },
 {
  "state": "UNVERIFIED",
  "status": "",
  "severity": "minor",
  "angle": "empty-set-vacuous",
  "file": "server/services/regulatory/design-controls.ts",
  "line": 257,
  "symbol": "assessDhfCompleteness",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`changes` is optional on `DhfInput` (line 87: `changes?: DesignChange[]`). A DHF that records no design changes \u2014 or one assembled by a path that never populates the field \u2014 yields `[]`, `[].every(...)` is `true`, and `designChanges` is counted as a PRESENT element of 21 CFR 820.30(i). It then contributes to `completenessScore = present.length / DHF_ELEMENTS.length` and hence to `auditReady: blockers.length === 0 && completenessScore >= 1`.",
  "shouldBe": "Every other element in the same object requires positive evidence (`dhf.inputs.length > 0`, `dhf.reviews.length > 0 && dhf.reviews.some(...)`, `dhf.validations.length > 0 && dhf.validations.some(...)`). `designChanges` is the one that does not, and should read `(dhf.changes ?? []).length > 0 && (dhf.changes ?? []).every(...)` \u2014 or the field should be required so 'no changes' is a recorded fact rather than an unfilled one.",
  "consequence": "The DHF completeness record tells a design-control auditor that 820.30(i) change control is satisfied, and can reach `completenessScore: 1` / `auditReady: true`, for a device file containing zero design-change records. 'Nobody entered any changes' reads as 'every change was reviewed and verified'. Ranked minor only because `assessDhfCompleteness` has no production caller today (grep finds only its own test) \u2014 the defect is in a shipped engine, not yet on a served path.",
  "note": ""
 },
 {
  "state": "refuted",
  "status": "",
  "severity": "major",
  "angle": "nullish-defaults",
  "file": "server/services/clinical-regulatory-evidence/index.ts",
  "line": 704,
  "symbol": "getTrace",
  "votes": "cast=6 refuted=3 failed=0",
  "absentBecomes": "coverage.freshness is `string | null` (server/services/clinical-regulatory-evidence/types.ts:154) and holds an ISO timestamp \u2014 MAX(updated_at) over the corpus (index.ts:181, 205), null when the corpus has no recorded update time. This turns 'the corpus snapshot date is unknown' into the literal string 'current' in a field typed `corpusSnapshot: string` (types.ts:318), which has no null/unknown representation.",
  "shouldBe": "corpusSnapshot must be `string | null` and be rendered as 'snapshot date not recorded'. The rule is written down one module away, in the docstring of isStale (server/services/clinical-regulatory-evidence/coverage-service.ts:74-76): 'Unknown freshness is NOT treated as fresh \u2014 an unanswerable question returns false only because we cannot assert staleness, and callers should show freshness as unknown rather than current.' This caller does the one thing that comment forbids.",
  "consequence": "The evidence trace an auditor opens to see what a regulatory recommendation was built on states its corpus snapshot as 'current' \u2014 an affirmative claim that the evidence was drawn from an up-to-date corpus \u2014 when the corpus has no recorded update timestamp at all. Because the value is a literal word rather than the ISO date the field normally carries, it also silently defeats any downstream comparison against the live snapshot id. The enclosing function's own docstring promises 'Null when the ref is unparseable or has no edges \u2014 never a fabricated chain'; the chain is honest and the provenance stamp on it is not.",
  "note": ""
 },
 {
  "state": "refuted",
  "status": "",
  "severity": "major",
  "angle": "percentages",
  "file": "server/services/innovation/submission-readiness-twin-service.ts",
  "line": 841,
  "symbol": "evaluateCriterion",
  "votes": "cast=9 refuted=4 failed=0",
  "absentBecomes": "The branch is entered when `hasContent` is FALSE \u2014 i.e. no document content was found for the criterion. Absence of content is taken as proof that the conditional requirement does not apply. Nobody assessed the condition; nothing recorded an applicability determination. The criterion is stamped 'not_applicable' with a full 100 score purely because it is empty.",
  "shouldBe": "A conditional criterion with no recorded applicability determination is 'applicability_undetermined', must contribute no score (excluded from the denominator, not scored 100), and must appear in `gapsIdentified` asking someone to determine whether it applies. 'Not applicable' is a judgement a human or a rule makes, not a synonym for 'no content found'.",
  "consequence": "Each such criterion adds a 100 to `totalWeightedScore` (line ~659) and counts as `compliant` in `compliance = compliant.length / evaluations.length * 100` (line 902, which treats `not_applicable` as compliant). The more conditional requirements a program has failed to address, the higher its readiness and compliance percentages climb. An auditor reading the criterion evaluation table sees \"Not applicable \u2014 100\" against a requirement whose applicability was never determined.",
  "note": ""
 },
 {
  "state": "refuted",
  "status": "",
  "severity": "major",
  "angle": "percentages",
  "file": "server/services/innovation/submission-readiness-twin-service.ts",
  "line": 886,
  "symbol": "calculateDimensionScores",
  "votes": "cast=9 refuted=4 failed=0",
  "absentBecomes": "When the criteria set contains no mandatory criteria \u2014 which happens when `getCriteria` returns rows for a submission type/agency that has no seeded mandatory criteria, or falls back to `SubmissionReadinessTwinService.criteriaCache` (line 571) \u2014 the program is scored 100% complete. Nothing was required, so nothing was checked, and the result is indistinguishable from a fully complete dossier.",
  "shouldBe": "`completeness` must be null when `mandatory.length === 0`, and the assessment must record a blocker stating that no mandatory criteria are on file for this submission type/agency, so `overall_readiness_score` is not presented as a measurement. This is the same shape already fixed in server/services/ind-lifecycle/ind-readiness-service.ts:189 and server/services/protocol-development/protocol-development-logic.ts:123.",
  "consequence": "`completeness_score = 100` is persisted to `innovation.readiness_twin_assessments` and surfaced by the twin dashboard and by AnA's `get_submission_readiness_twin` tool, whose instruction block tells the model to lead with the score and the criteria met-vs-total. A submission type with no criteria on file is reported as 100% complete on mandatory requirements.",
  "note": ""
 },
 {
  "state": "refuted",
  "status": "",
  "severity": "major",
  "angle": "nullish-defaults",
  "file": "server/services/pdev/pdev-workflow-bridge.ts",
  "line": 543,
  "symbol": "recordDecision",
  "votes": "cast=6 refuted=5 failed=0",
  "absentBecomes": "A workflow run whose jsonb metadata does not carry targetState \u2014 an untyped blob, so any run created outside startApprovalChain, or one whose metadata was rewritten or migrated \u2014 yields targetState 'approved'. The start path validates the caller's targetState against COMPLETED_TARGET_STATES and rejects anything else (line 243-245); this read path substitutes one of those promoted states with no validation at all. The same default is applied on the read/status path at line 449.",
  "shouldBe": "Missing targetState must throw the same way a missing run or wrong workflowType does a few lines above ('Workflow run is not a PDEV approval chain.'). An approval chain whose destination state was not recorded is not decidable, and must not be resolved by guessing the most permissive of the four completed states.",
  "consequence": "When the final checkpoint clears, line 744 writes `state: targetState` onto the pdev_program_activities row, and lines 766/785/795 write finalState / newState / activityFinalState: 'approved' into the audit trail. A protocol-development activity is promoted to an approved state, and the Part 11 audit record says a human approved it to that state, on the strength of a metadata field that was absent. The audit trail is the artifact that would be produced to defend the transition, and it will name a target state nobody requested.",
  "note": ""
 },
 {
  "state": "refuted",
  "status": "",
  "severity": "major",
  "angle": "percentages",
  "file": "server/services/precedent-engine.ts",
  "line": 931,
  "symbol": "PrecedentEngine.recommendStrategy",
  "votes": "cast=3 refuted=2 failed=0",
  "absentBecomes": "`strategyPatterns` is built by looping over `approved` only (line 905), and the denominator here is `approved.length`. Every precedent with a REJECTED / CRL / REFUSE_TO_FILE outcome \u2014 collected two lines earlier into `rejected` and then never used in this calculation \u2014 is excluded from both numerator and denominator. A field named `successRate` is computed over the successes alone.",
  "shouldBe": "`successRate` must be `approvalsUsingStrategy / (approvalsUsingStrategy + rejectionsUsingStrategy)`, with a null rate and a stated N when the denominator is below a defensible minimum. server/services/corpus/precedent-benchmark.ts:192 is the model to copy: it carries `assessable`, `knownOutcomeN`, a Wilson interval, and refuses to emit a rate below MIN_TRIALS_FOR_SUCCESS_RATE.",
  "consequence": "A regulatory strategy used in 3 clearances and 30 refuse-to-files is returned as `successRate: 1.0` \u2014 100% \u2014 over /api/precedent-engine (server/routes/precedent-engine.ts:188), the precedent board (server/routes/precedent-engine-board.ts:338) and the public API (server/routes/public-api.ts:642). It is mathematically impossible for this field to report any strategy as having failed, because failures are not in the population. A sponsor choosing a 510(k) pathway is shown a 100% historical success rate for an approach that mostly fails.",
  "note": ""
 },
 {
  "state": "refuted",
  "status": "",
  "severity": "minor",
  "angle": "percentages",
  "file": "server/services/ana/citation-section-coverage.ts",
  "line": 243,
  "symbol": "deriveProjectSectionCoverage",
  "votes": "cast=3 refuted=2 failed=0",
  "absentBecomes": "A registry slice with no required sections \u2014 nothing was known to be required, so nothing was checked \u2014 yields 100% complete. `requiredMissing` is also 0 in that case, so `filingBlocking` stays false and every artifact is bucketed as 'extra'.",
  "shouldBe": "`completionPercent: number | null` with null on an empty required set, plus a rollup flag saying the requirement set was empty. This is the same defect already written up and fixed in server/services/protocol-development/protocol-development-logic.ts:123 and server/services/ind-lifecycle/ind-readiness-service.ts:189.",
  "consequence": "`deriveProjectSectionCoverage` is exported and consumed by server/services/ana/project-readiness-aggregator.ts:149, where `clamp(coverage.rollup.completionPercent)` becomes the Section Coverage component at 40% weight \u2014 the largest single weight in the readiness score. Today `getSectionsForSubmissionType` always returns the full IND_SECTIONS list, so the branch is not reachable through the live entry point and I have not seen it fail; it is reachable through the exported pure function and would survive any future registry that returns an empty or optional-only slice, at which point a project with zero required sections satisfied reports 100% coverage, 40 readiness points, and no filing block.",
  "note": ""
 },
 {
  "state": "refuted",
  "status": "",
  "severity": "minor",
  "angle": "nullish-defaults",
  "file": "server/services/pdev/pdev-workflow-bridge.ts",
  "line": 524,
  "symbol": "recordDecision",
  "votes": "cast=6 refuted=5 failed=0",
  "absentBecomes": "An approver whose role is not recorded becomes the empty string, and `&& role` short-circuits the entire required-roles comparison \u2014 so the approval is not merely allowed, it is not even noticed. The approval record itself then stores `approverRole: input.userRole ?? 'unspecified'` (line 615). Separately, the requiredApproverRoles jsonb defaulting to `[]` (shared/schema/orchestration.ts:205) means a checkpoint whose required roles were never configured also skips the check via `requiredRoles.length > 0`.",
  "shouldBe": "An unrecorded approver role on a checkpoint that declares required roles must be treated as not satisfying them \u2014 the branch should be `requiredRoles.length > 0 && !requiredRoles.includes(role)`, which catches the empty-role case. And the comment's promise must be kept: the audit row written at lines 660+ must actually carry a role_outside_required marker; today the only record of the mismatch is a server log line that no audit export reads.",
  "consequence": "A checkpoint that names required approver roles can be cleared by a user whose role is unknown, with nothing anywhere recording that the required-roles control was bypassed. An auditor reconstructing the approval sees an approval by 'unspecified' against a checkpoint listing required roles, and no finding, warning or audit field indicating the mismatch \u2014 the code comment says the audit is marked, and it is not.",
  "note": ""
 },
 {
  "state": "duplicate",
  "status": "see confirmed entry",
  "severity": "critical",
  "angle": "status-unions",
  "file": "server/services/capa-mdr/capaMdr.service.ts",
  "line": 299,
  "symbol": "createComplaint",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "A complaint filed without a recorded patient-harm level becomes HarmLevel 'none' \u2014 the least-reportable member of HARM_LEVELS = ['none','malfunction','injury','serious_injury','death'], which has no 'unknown' / 'not_assessed' member. The route (server/routes/capa-mdr.ts:201) validates patientHarm only when it is present, so omitting it is accepted. The same default is written to the row at line 325 (`patientHarm: input.patientHarm ?? 'none'`) alongside `severityAssessment: input.severityAssessment ?? 'minor'` at line 326, and the column itself is declared `varchar('patient_harm', { length: 24 }).notNull().default('none')` (shared/schema/capa-mdr.ts:240).",
  "shouldBe": "HarmLevel needs an explicit 'unknown' (or 'not_assessed') member, defaulted by the DB column and by intake, and triageEngine.classify must handle it as its own case \u2014 not fall through `case 'none': default:`. The engine's own header states the policy: \"Rules are intentionally conservative: when in doubt, flag reportable. The RA team can demote during triage with a recorded rationale.\" An unassessed harm level is the definition of doubt, so it should produce a reportable-pending-triage classification (or refuse to classify) rather than the not-reportable one.",
  "consequence": "server/services/capa-mdr/triageEngine.ts:180 reaches `case 'none': default:` and pushes the rationale 'No harm reported \u2014 log under 820.198, no MDR'. The persisted complaint row therefore carries preliminary_classification with fdaReportable=false, mdrEuReportable=false, requires806Notice=false, autoComputed=true and a timestamp \u2014 and patient_harm='none'. An FDA or Notified Body inspector reading the complaint file is told the device complaint was assessed and found to involve no patient harm and to require no 21 CFR 803 MDR and no EU MDR Article 87 report, when in fact nobody entered a harm level. The 803.50 30-day and Article 87 10/15-day clocks are never seeded, so the aging display the module exists to provide shows nothing to chase.",
  "note": ""
 },
 {
  "state": "duplicate",
  "status": "see confirmed entry",
  "severity": "critical",
  "angle": "claims-and-labels",
  "file": "server/services/clinical-regulatory-evidence/index.ts",
  "line": 710,
  "symbol": "getTrace",
  "votes": "cast=0 refuted=0 failed=6",
  "absentBecomes": "An evidence chain that no human has ever reviewed is reported as `reviewState: 'human_reviewed'`. The only input is the `is_inferred` flag on the relationship edges (db/migrations/20260724_clinical_regulatory_evidence_spine.sql:183 \u2014 `is_inferred BOOLEAN NOT NULL DEFAULT false`). No human-review record is consulted anywhere: there is no reviewer id, no review timestamp, no review table. Any edge written without explicitly setting the flag defaults to false, so a chain assembled entirely by automated extraction returns 'human_reviewed'. The union `'draft' | 'extracted' | 'human_reviewed'` (types.ts:324) has no unreviewed / unknown member, so the function cannot say \"nobody looked\".",
  "shouldBe": "'human_reviewed' must require positive evidence of a review \u2014 a reviewer identity and timestamp on the chain. With no such record the trace should report an explicit unreviewed/unknown state, which means adding that member to the union. `isInferred === false` proves only that the edge was not marked inferred; it is not a human attestation.",
  "consequence": "GET /api/clinical-regulatory-evidence/.../trace/:traceId (server/routes/clinical-regulatory-evidence-routes.ts:287) is the evidence chain behind a regulatory recommendation. It tells an auditor asking \"who checked this evidence?\" that the chain was human_reviewed. In a 21 CFR Part 11 product that is a governance attestation, and it is produced from the absence of a boolean flag rather than from any human act.",
  "note": ""
 },
 {
  "state": "duplicate",
  "status": "see confirmed entry",
  "severity": "major",
  "angle": "claims-and-labels",
  "file": "server/services/benefit-risk/benefit-risk-knowledge.ts",
  "line": 529,
  "symbol": "structureBenefitRiskFramework",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`TreatmentOption.status` is optional (`status?: 'approved' | 'off_label' | 'supportive_care' | 'none'`, line 337) with no unknown member. A comparator therapy whose regulatory status nobody supplied is written into the FDA Benefit-Risk Framework grid as `Name (approved).` \u2014 a positive regulatory-status assertion the caller never made. The same default is applied again at line 537 in the unmet-need test: `treatments.every((t: TreatmentOption) => (t.status ?? 'approved') === 'none' || ...)`.",
  "shouldBe": "An absent status is unknown. Render it as `(status not recorded)` and exclude the option from the unmet-need determination \u2014 or refuse to state the Current Treatment Options row until every option carries a status. The union needs an explicit 'unknown'.",
  "consequence": "This is an AnA tool (server/services/ana/AnaToolExecutor.ts:18701) whose output is the Current Treatment Options row of an FDA Benefit-Risk Framework, the material for CTD 2.5.6. It puts \"(approved)\" next to a named comparator drug on the strength of nobody having typed anything. That is a regulatory-status claim about a third-party product, in a document destined for FDA.",
  "note": ""
 },
 {
  "state": "duplicate",
  "status": "see confirmed entry",
  "severity": "major",
  "angle": "status-unions",
  "file": "server/services/intelligence/readiness-scoring-engine.ts",
  "line": 162,
  "symbol": "computeReadinessScore",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "`ReadinessDimensions` (line 37) declares all four dimensions as non-nullable `number`, so the type cannot express \"not assessed\". With no twin assessment, consistency becomes a hard-coded 70/100. With no project intelligence profile row, gatherProfileSignal returns `{ risks: [], openQuestionCount: 0, decisionCount: 0 }` (line 278), so estimateQuality returns 65 and estimateCompliance returns 80 \u2014 invented figures for a project with nothing recorded. The same file already fixed this class of defect for predictions (\"Null now means 'not computed' and must be rendered as such. It is never replaced with a default at the point of use\", line 76), and the sibling engine server/services/orchestration/readiness-engine.ts:18 was fixed the same way (\"WO-16C finding 58: both returned a bare `50`\"), reporting `unassessedDimensions` per shared/types/orchestration.ts:500.",
  "shouldBe": "Make the four dimensions `number | null` and carry an `unassessedDimensions: UnassessedReadinessDimension[]` exactly as shared/types/orchestration.ts:449 already defines for 'compliance' | 'consistency', and renormalise the weighted overall score over the dimensions actually measured (the pattern judgment-framework.ts:131 already implements).",
  "consequence": "server/services/ana-ri/context-enrichment.ts:437 renders these dimensions into the assistant's prompt as a `| Dimension | Score |` table and appends \"Present the scores above directly, and only those.\" Two lines above, the same block correctly says \"**No readiness assessment is on record for this program.**\" So on a project with no twin assessment, no profile and no documents, AnA is handed \u2014 and instructed to state to a customer \u2014 consistency 70/100, quality 65/100, compliance 80/100 and an overall 46/100, in the same message that says no assessment exists. Nothing was measured; three of the four numbers are constants.",
  "note": ""
 },
 {
  "state": "duplicate",
  "status": "see confirmed entry",
  "severity": "major",
  "angle": "claims-and-labels",
  "file": "server/services/intelligence/readiness-scoring-engine.ts",
  "line": 162,
  "symbol": "computeReadinessScore",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "A project with no submission-readiness twin assessment on record gets consistency = 70/100 outright, and compliance = `estimateCompliance(profile)`, which returns 80/100 for a profile with no risks recorded (line 488: `Math.max(30, Math.min(90, 80 - riskPenalty - criticalRisks * 10))`) and 50 for no profile at all. Quality does the same through `estimateQuality` (line 476). These are then folded into `overallScore` at weights 0.20/0.20/0.25 \u2014 a score presented as a measurement. Nothing in the returned ReadinessScore marks a dimension as estimated or unassessed.",
  "shouldBe": "An unmeasured dimension is not 70. It should be `null` and excluded from the weighted average (the same treatment `predictions` already gets in this file, and that `evaluateReadiness`/`hasArtifactMatrix` already gives the unmodelled-artifact case in server/services/regulatory/readinessEvaluator.ts:126-136), with the dimension table naming it not assessed.",
  "consequence": "server/services/ana-ri/context-enrichment.ts:474 renders these as a markdown table \u2014 `| consistency | 70/100 |`, `| compliance | 80/100 |` \u2014 under the heading \"## Live Readiness Assessment\" and then instructs the model: \"Present the scores above directly, and only those.\" A customer is told, as fact, that their program scores 70 on consistency and 80 on compliance when no consistency or compliance assessment has ever run against it.",
  "note": ""
 },
 {
  "state": "duplicate",
  "status": "see confirmed entry",
  "severity": "minor",
  "angle": "db-defaults",
  "file": "server/services/ibc/ibc-logic.ts",
  "line": 104,
  "symbol": "registrationExpiration",
  "votes": "cast=0 refuted=0 failed=3",
  "absentBecomes": "\"this registration has no approval date on record, so whether its annual review has lapsed is unknown\" becomes `expired: false` \u2014 indistinguishable from a registration approved yesterday. The return type is two fields, neither of which can say \"undetermined\".",
  "shouldBe": "Return a third state \u2014 e.g. `{ expirationDate: null, status: 'undetermined', reason: 'no approval date recorded' }` \u2014 and have GET /registrations/:id/containment surface it as undetermined. Fail closed: an approved registration with no approval date should read as \"annual review status unknown\", not as current.",
  "consequence": "An IBC registration whose status was moved straight to 'approved' through PATCH /api/ibc/registrations/:id/status (setRegistrationStatusTx, ibc-service.ts:66-73, writes status only and never touches approval_date) is reported by GET /registrations/:id/containment as `expiration: { expirationDate: null, expired: false }`. A biosafety officer reading that surface sees an approved, non-expired registration for work whose NIH-mandated annual review may have lapsed years ago \u2014 the same shape as the continuingReviewStatus defect, on a different committee.",
  "note": ""
 }
]