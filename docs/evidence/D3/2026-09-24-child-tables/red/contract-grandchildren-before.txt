
 RUN  v4.1.7 /home/user/ClinicalSageAI-2-replit

 × tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > every table named by the migration ends up policied, enabled and FORCEd 13ms
   → ai_claim_citations has no tenant_isolation_policy: expected false to be true // Object.is equality
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > chat_messages: each tenant sees its own row and not the other's 23ms
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > csr_details: each tenant sees its own row and not the other's 14ms
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > csr_sections: each tenant sees its own row and not the other's 13ms
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > rag_chunks: each tenant sees its own row and not the other's 18ms
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > complaints: each tenant sees its own row and not the other's 13ms
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > pdev_program_activities: each tenant sees its own row and not the other's 12ms
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > resolution_bundle_items: each tenant sees its own row and not the other's 12ms
 × tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > c2c_document_section_versions: each tenant sees its own row and not the other's 16ms
   → c2c_document_section_versions: tenant B can read tenant A's row: expected 1 to be +0 // Object.is equality
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > leaves the three chained tables deny-all, deliberately and visibly 2ms
 ✓ tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > the policy is inert when enforcement is off, exactly like its siblings 2ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > every table named by the migration ends up policied, enabled and FORCEd
AssertionError: ai_claim_citations has no tenant_isolation_policy: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ tests/db/child-table-parent-scoped-rls.dbtest.ts:538:80
    536|     // happen is a table that EXISTS and is left unprotected.
    537|     for (const row of rows) {
    538|       expect(row.policied, `${row.table_name} has no tenant_isolation_…
       |                                                                                ^
    539|       expect(row.relrowsecurity, `${row.table_name} has RLS disabled`)…
    540|       // Without FORCE the table OWNER skips the policy entirely.

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/db/child-table-parent-scoped-rls.dbtest.ts > child tables are scoped to their parent row’s tenant > c2c_document_section_versions: each tenant sees its own row and not the other's
AssertionError: c2c_document_section_versions: tenant B can read tenant A's row: expected 1 to be +0 // Object.is equality

- Expected
+ Received

- 0
+ 1

 ❯ tests/db/child-table-parent-scoped-rls.dbtest.ts:578:90
    576|
    577|       // NEGATIVE half — the isolation this migration exists for.
    578|       expect(await countFor(ORG_B, fkA), `${c.child}: tenant B can rea…
       |                                                                                          ^
    579|       expect(await countFor(ORG_A, fkB), `${c.child}: tenant A can rea…
    580|     });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


 Test Files  1 failed (1)
      Tests  2 failed | 9 passed (11)
   Start at  20:10:52
   Duration  1.22s (transform 114ms, setup 77ms, import 84ms, tests 754ms, environment 0ms)

