# scripts/ops/terraform-preflight-proof.mjs — deploy-aws.yml's own preflight shell run against the task definition the stack renders
## 1. as rendered
[terraform-preflight-proof] ok — preflight step found in job "migrate" (6208 bytes of shell)
[terraform-preflight-proof] ok — terraform test: 21 passed, 0 failed, 0 errored
[terraform-preflight-proof] ok — rendered family matches the pipeline's ECS_API_TASK_FAMILY (c2c-production-api)
Production boot contract satisfied by arn:aws:ecs:us-east-1:123456789012:task-definition/c2c-production-api:1.
[terraform-preflight-proof] ok — deploy-aws.yml's preflight accepted the task definition
[terraform-preflight-proof] ok — the preflight published the revision it checked (arn:aws:ecs:us-east-1:123456789012:task-definition/c2c-production-api:1)
[terraform-preflight-proof] ok — the revision the preflight checked is the one migrate and deploy-api derive from (ARN pinned end to end)
[terraform-preflight-proof] ok — provision-database.yml accepts the rendered task definition and passes the owner URL from the same secret
[terraform-preflight-proof] ok — every AWS call the deploy and provision workflows make is granted to the role its job can assume (38 job×action pairs)
[terraform-preflight-proof] ok — the preflight runs before the migration (job "migrate")
[terraform-preflight-proof] ok — health check runs and discriminates: live but not ready (/healthz 200, /readyz 503) → exit 0, /healthz 503 → exit 1, nothing listening → exit 1

[terraform-preflight-proof] every check holds.
exit=0

## 2. the same task definition with every SMTP_* entry removed
[terraform-preflight-proof] ok — preflight step found in job "migrate" (6208 bytes of shell)
[terraform-preflight-proof] ok — rendered family matches the pipeline's ECS_API_TASK_FAMILY (c2c-production-api)
::error::Task definition c2c-production-api (container api) is missing required production env: SMTP_HOST SMTP_USER SMTP_PASS
::error::The app refuses to boot without each of these, except SMTP_*: without those it boots and no one can sign in (login OTP is the mandatory second factor). Add them to the task definition (terraform/stack) before deploying.
[terraform-preflight-proof] FAIL — deploy-aws.yml's preflight refused the task definition (exit 1)

[terraform-preflight-proof] 1 check(s) failed.
exit=1
