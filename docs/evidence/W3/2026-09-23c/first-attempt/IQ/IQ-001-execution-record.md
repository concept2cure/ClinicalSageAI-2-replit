# IQ-001 — Installation Qualification: execution record

**Status:** DRAFT — UNSIGNED (automated execution; human review and signature outstanding)

> Generated by `scripts/validation/run-iq.mjs`. Do not edit by hand — re-run the protocol.

Executed 2026-09-23T16:25:50.202Z against http://localhost:5200; concept2cure-v2 @ bfdb0a08df1a6abf9b3fffd81db4b8a1b364e065; node v22.22.2.

**Counts:** 11 pass · 1 fail · 3 deviation · 0 not-executed

| Check | Title | Expected | Observed | Verdict | Evidence |
|---|---|---|---|---|---|
| IQ-01 | Node runtime matches the declared engine range | process.version satisfies package.json engines.node | node v22.22.2; engines.node = >=22.0.0 <23.0.0 | **pass** |  |
| IQ-02 | Deployment artefacts are present in the checkout | Dockerfile.optimized, docker-compose.yml, docker-compose.staging.yml, terraform production environment and modules, .github/workflows/deploy-aws.yml | all 11 present; terraform modules on disk: alb, cloudfront, compliance-evidence, ecr, ecs-fargate, rds, secrets, vpc-secure; production main.tf uses: vpc, ecr, secrets, rds, alb, ecs, evidence, cdn | **pass** |  |
| IQ-03 | Container image definition matches the runbook | node:22-slim base; non-root user; migrations and scripts/db copied; HEALTHCHECK probes /readyz; assets vendored | Dockerfile checks pass; deploy-aws.yml jobs: push → test → security-gate → build-push → migrate → deploy-api → deploy-frontend → smoke-test | **pass** |  |
| IQ-04 | Required configuration is declared and (locally) set | Every variable docker-compose.yml marks as required exists in .env.example; report which are set in the local env files, .env.local then .env (values never printed) | IQ-DEV-002: local environment is a development install, not the production boot contract — compose-required: DB_PASSWORD, APP_SERVICE_DB_PASSWORD, APP_DATABASE_URL, JWT_SECRET, SESSION_SECRET, REFRESH_TOKEN_SECRET, MFA_ENCRYPTION_KEY, AUDIT_HMAC_KEY, AUDIT_HMAC_SECRET, CONNECTOR_ENCRYPTION_KEY; not documented in .env.example: none; set in local .env.local/.env: APP_DATABASE_URL, JWT_SECRET; unset locally: DB_PASSWORD, APP_SERVICE_DB_PASSWORD, SESSION_SECRET, REFRESH_TOKEN_SECRET, MFA_ENCRYPTION_KEY, AUDIT_HMAC_KEY, AUDIT_HMAC_SECRET, CONNECTOR_ENCRYPTION_KEY; AI provider key present locally: no | **deviation** |  |
| IQ-05 | Database is reachable and carries the application schema | PostgreSQL ≥ 15 reachable on DATABASE_URL; pgvector installed; critical tables organizations/users present; migration journal present | database 127.0.0.1:5432/c2c_oq_w3_20260923c; PostgreSQL 16.13 (Ubuntu 16.13-0ubuntu0.24.04.1); extensions: pgcrypto@1.3, uuid-ossp@1.1, vector@0.6.0; public tables: 977; critical present: audit_logs, organization_users, organizations, platform_role_grants, projects, revoked_tokens, users; c2c_migration_journal rows: 307 | **pass** | `db-schema.json` |
| IQ-06 | Migration set files listed by scripts/db/migration-set.mjs exist on disk | Every .sql path named in C2C_MIGRATION_FILES resolves to a file | 307 migration files named in the set, all present on disk | **pass** |  |
| IQ-07 | Runtime database role can reach the tables the launch apps read and write | The role in DATABASE_URL (or APP_DATABASE_URL) holds SELECT/INSERT on every public table; RLS-relevant role attributes recorded; the role owns no table whose row-level security is not forced | role app_service (superuser=false, bypassrls=false); tables without SELECT: 0; launch-relevant among them: none; owns 0 RLS-enabled table(s) without FORCE | **pass** | `db-grants-before.txt`<br>`db-role-denied-tables.json` |
| IQ-08 | Tenant-isolation posture of this installation | RLS_ENFORCE recorded; APP_DATABASE_URL (non-superuser app_service role) recorded; production requires RLS_ENFORCE=on on a non-superuser role (D3) | RLS_ENFORCE=on; APP_DATABASE_URL set; APP_SERVICE_DB_PASSWORD unset | **pass** |  |
| IQ-09 | Application boots and reports readiness honestly | GET /healthz 200; GET /readyz JSON names every dependency; database and schema ok; an absent AI provider is reported as ana=down (503), never hidden | IQ-DEV-004: no AI provider configured — /healthz 200; /readyz 503: {"database":"ok","schema":"ok","ana":"down","redis":"skipped","worker":"skipped"}; schemaState=ready; anaState=no_provider; capabilityRegistry=pending. Readiness correctly fails closed (503); every AnA/model step in the OQs is a deviation for this reason. | **deviation** | `readyz.json` |
| IQ-10 | Development authentication policy is as configured | POST /api/auth/dev-login answers only when NODE_ENV=development and ALLOW_DEV_AUTH=1 (server/auth/dev-auth-policy.ts); anywhere else — production included — it must be 404 | dev-login → 404 (NODE_ENV=development, ALLOW_DEV_AUTH=0): refused, as a server without development authentication must | **pass** |  |
| IQ-11 | Launch scope is enforced by the server | LAUNCH_SCOPE_ENFORCE=on and GET /api/module-subscriptions/navigation reports launchScope.enforced=true | LAUNCH_SCOPE_ENFORCE=on; enforced=true; 102 verdicts, 82 locked by launch-scope, 20 entitled | **pass** | `navigation-summary.json` |
| IQ-12 | Security middleware is active on responses | Content-Security-Policy and X-Content-Type-Options present; API responses carry rate-limit headers | IQ-DEV-005: CSP report-only (development build: server/middleware/enterprise-security.ts:165); nosniff=nosniff; referrer-policy=no-referrer; HSTS=absent (plain http locally); api rate limit 600/min (remaining 599). Enforced CSP and HSTS are production-mode behaviour; verify on staging. | **deviation** | `security-headers.json` |
| IQ-13 | Vendored agency artefacts are present | assets/estar-templates with checksums.txt; assets/ectd-dtd; assets/ectd-schema; assets/fda-recognized-standards | assets/estar-templates: 4 entries; assets/ectd-dtd: 4 entries; assets/ectd-schema: 3 entries; assets/fda-recognized-standards: 2 entries; checksums.txt present | **pass** |  |
| IQ-14 | CI gates named by the launch definition are declared | package.json declares ci:migration-drop-safety, ci:migration-set-order, ci:launch-scope, ci:fixture-fallback, ci:no-mock-in-prod-routes | all present; 209 ci:* scripts declared | **pass** |  |
| IQ-15 | Validation toolchain is installed | playwright-core resolvable from tests/validation; Chromium binary present at CHROMIUM_PATH | playwright-core not installed under tests/validation (run npm install there) | **fail** |  |

## Signature block

| Role | Name | Signature | Date |
|---|---|---|---|
| Executed by (automation owner) | | *unsigned* | |
| Reviewed by (qualified validation contractor) | | *unsigned* | |
| Approved by (founder / system owner) | | *unsigned* | |
