# Explicit pins to unapproved models: classification, 2026-09-23 (three passes)

Found by a grep for `model: '<gpt-*|claude-sonnet*|claude-haiku*|claude-3*|kimi*|moonshot*>'` in non-test server code (40 lines across 16 files). The six lines in `server/services/submission-twin-service.ts` were excluded: the regulatory digital twin is a CLAUDE.md Rule 2 surface. Doc-comment matches were also excluded.

One agent classified each call site: is it reachable, and does its output become governed regulatory content? A second agent then tried to refute that classification. For a non-governed verdict, the refuter looked specifically for a path by which the output does become governed. The raw findings are in the session workflow journal.

| Site | Classifier | Verifier | Outcome |
|---|---|---|---|
| `server/services/ai/openai-orchestrator.ts:146` | dead-or-unreachable, unreachable | agrees: governed, unreachable | Dead: no importer reaches it (last route deleted in 3cc8547d2). Not changed; listed for deletion by an owner. |
| `server/services/ana/AnaDocumentDraftingService.ts:484` | non-governed-helper, reachable | agrees: not governed, reachable | Not governed (stateless). But the verifier found an image dropped on text-only fallback, now refused at the gateway (MediaNotCarriedError). |
| `server/services/ana/AnaDocumentDraftingService.ts:718 + server/routes/ana-intelligence.ts:` | non-governed-helper, reachable | agrees: not governed, reachable | Not governed. The sealed audit row recorded the literal claude-sonnet-4-6; it now records the model that served. |
| `server/services/estimand-engine-service.ts:356` | governed, reachable | agrees: governed, reachable | Governed. Now routed as regulatory_review, shape-checked and labelled model/deterministic. The fabricated precedent claim is removed. |
| `server/services/predictiveSectionService.ts:205` | non-governed-helper, reachable | agrees: not governed, reachable | Not governed (suggestions, not stored). Fail-open catch returns [] silently. Not changed. |
| `server/services/DocumentDataCenterService.ts:454` | non-governed-helper, reachable | agrees: not governed, reachable | Not governed, but the provenance was fabricated. It now records the served model and labels keyword tags; no invented confidence, category or IP. |
| `server/services/aiProviderRouter.ts:140-250` | governed, reachable | agrees: governed, reachable | Governed via the citation reranker. The judge is routed as regulatory_review for verdicts and fails closed. The router records the served model. |
| `server/api/cmc/blueprintRoutes.ts:308` | governed, reachable | agrees: governed, reachable | Governed. Now document_drafting, fails closed (503 NO_DRAFT_PRODUCED), and leaves no orphan project. |
| `server/api/cmc/playbookRoutes.ts:366` | governed, reachable | agrees: governed, reachable | Governed. Now document_drafting with the "Not supplied" instruction; a failure is recorded as failed and answered with 503. |
| `server/api/drafting/routes.ts:92 (CONFIG.model used at ~408 and ~446)` | governed, reachable | disagrees: governed, unreachable | Governed, but it cannot run: the verifier showed its embedding step always failed. The pin and provenance are fixed, and ai.embeddings now uses the real embedding provider. |
| `server/api/drafting/routes.ts:547` | non-governed-helper, reachable | agrees: not governed, reachable | Not governed (entity index; its store is not provisioned). Not changed. |
| `server/lib/unified-ai-client.ts:295-320` | dead-or-unreachable, unreachable | agrees: not governed, unreachable | Dead helpers (no callers). Not changed. |
| `server/routes/agent-swarm.ts:200-360` | config-or-comment, reachable | agrees: not governed, reachable | Display metadata only; routing is by task type. Not changed; the literal display model is noted. |
| `server/routes/c2c/context-intelligence.ts:165 (/summarize) and :604 (/extract-decisions)` | non-governed-helper, reachable | agrees: not governed, reachable | Not governed (working memory). Fail-open summaries noted. Not changed. |
| `server/api/cmc/audit-risk-monitor.js (10 calls pinning gpt-4o: lines 142,165,196,228,342,3` (pass 2 (.js files the .ts-only grep missed)) | dead-or-unreachable, reachable | agrees: not governed, reachable | Reachable, but every POST handler throws after its first call (it reads .choices), so nothing is stored or returned. The first calls still send documents to gpt-4o. Baselined; listed for retirement. |
| `server/api/cmc/change-impact-simulator.js (lines 108, 200 pin gpt-4o)` (pass 2 (.js files the .ts-only grep missed)) | governed, reachable | agrees: governed, reachable | Governed. Both calls are now regulatory_review. The filing category remains a model verdict (Rule 2); the deterministic classifier is /api/cmc/variations/classify. |
| `server/api/cmc/cmc-copilot.js (lines 112,215,421,503,579 pin gpt-4o)` (pass 2 (.js files the .ts-only grep missed)) | non-governed-helper, reachable | agrees: not governed, reachable | Not governed (chat answers written to a local log). Baselined. |
| `server/api/cmc/global-compliance.js (lines 144,170,200,293,383,406 pin gpt-4o)` (pass 2 (.js files the .ts-only grep missed)) | governed, reachable | disagrees: not governed, reachable | Governed by intent. Its later calls threw on .choices, so no output was returned. Now fixed to .content, and every call declares its task (drafting/review). |
| `server/api/cmc/manufacturing-tuner.js (lines 128,168,202,302,466 pin gpt-4o)` (pass 2 (.js files the .ts-only grep missed)) | non-governed-helper, reachable | agrees: not governed, reachable | Not governed (suggestions written to a local file). Baselined. |
| `server/api/cmc/preclinical-translator.js (lines 147,168,204,234,334,445 pin gpt-4o)` (pass 2 (.js files the .ts-only grep missed)) | governed, reachable | agrees: governed, reachable | Governed. Five drafting calls are now document_drafting and the extraction call is document_analysis. Its .choices reads are fixed. |
| `server/services/indCopilot.js (lines 552,608,957 gpt-4o; 1164 gpt-4o-mini)` (pass 2 (.js files the .ts-only grep missed)) | governed, reachable | disagrees: governed, unreachable | Governed, but every path fails before its model call (Supabase is unconfigured). All calls now declare tasks, the served model is recorded, and the undefined aiResult in checkFDACompliance is fixed. |
| `server/services/unifiedDocumentIngestion.js:853 (gpt-4o)` (pass 2 (.js files the .ts-only grep missed)) | dead-or-unreachable, unreachable | agrees: governed, unreachable | Unreachable (no production caller). Baselined; its designed sink is governed. |
| `server/utils/document-generator.js:68 (gpt-4o)` (pass 2 (.js files the .ts-only grep missed)) | governed, reachable | agrees: governed, reachable | Governed. Now document_drafting with an empty-draft check, and the OpenAI-key gate is removed. |
| `server/services/sentenceTraceabilityService.ts:447 (model: process.env.OPENAI_MODEL || 'gp` (pass 3 (`x || 'model'` defaults the first regex missed)) | governed, reachable | agrees: governed, reachable | Governed (evidence_links and the Part 11 report). Now regulatory_review. The excerpt is kept only if verbatim in the source, link types are validated, skipped sentences are reported unsupported, and keyword fallback is never "supported". |
| `server/services/confidenceScoringEngine.ts:462 (model: process.env.OPENAI_MODEL || 'gpt-4o` (pass 3 (`x || 'model'` defaults the first regex missed)) | governed, reachable | agrees: governed, reachable | Governed (the critical check in claim verification). Now regulatory_review. An unreadable reply is a skipped check, and a skipped check can no longer yield "verified". |
| `server/services/autoExtractionPipeline.ts:443 and :485 (model: process.env.OPENAI_MODEL ||` (pass 3 (`x || 'model'` defaults the first regex missed)) | governed, reachable | agrees: governed, reachable | Governed (extracted evidence and classification stored as artifacts). Tables are document_drafting and classification is regulatory_review. A failure now fails the job instead of recording "0 tables" or "Other". |
| `server/services/figureGenerationService.ts:282 and :375 (const model = process.env.OPENAI_` (pass 3 (`x || 'model'` defaults the first regex missed)) | governed, reachable | agrees: governed, reachable | Governed (figures stored as artifacts and included in export). Refuses without source data, is document_drafting, records the served model, and the placeholder figure is removed. |
| `server/services/keywordExtractionService.ts:168 (model: process.env.OPENAI_MODEL || 'gpt-4` (pass 3 (`x || 'model'` defaults the first regex missed)) | non-governed-helper, reachable | agrees: not governed, reachable | Not governed (search keywords; regex overrides the model). Baselined. |
| `server/services/cognitive-ecosystem/agent-runtime.service.ts:61 (defaultModel: config.defa` (pass 3 (`x || 'model'` defaults the first regex missed)) | dead-or-unreachable, unreachable | agrees: not governed, unreachable | Dead config value, never read. Baselined. |

The first grep searched .ts files only. The CI gate (`scripts/ci/check-unapproved-model-pins.mjs`) scans .js as well; on its first run against the repo it failed on 43 unclassified pins (`after/pin-gate-first-run.txt`), which became pass 2. Extending its pattern to `x || 'model'` defaults then found 8 more, which became pass 3. Every governed site was fixed rather than baselined. The 54 remaining pins are listed in `scripts/ci/unapproved-model-pins-baseline.json`, each with its reason.
