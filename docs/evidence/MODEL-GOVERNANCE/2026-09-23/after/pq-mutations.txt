Mutation testing of server/eval/pq/pq-verdict.ts, 2026-09-23.
Each mutant is applied, `vitest run server/eval/pq` is executed, and the file restored.

P1  a DRAFT protocol may produce PASS            Tests  2 failed | 32 passed (34)
P2  tasks answered by another model count        Tests  2 failed | 32 passed (34)
P3  bare prefix match (gpt-4o-mini = gpt-4o)     Tests  1 failed | 33 passed (34)
P4  a "passed" registry claim is not checked     Tests  2 failed | 32 passed (34)
--  original                                     Tests  34 passed (34)

Caller gate (scripts/ci/check-pq-evaluation-callers.mjs --self-test):
  control — the PQ runner calls it; control — a test calls it; control — a doc
  comment names it; a route handler calls it (caught); a service calls it across
  a line break (caught). On first run against the repo it flagged its own
  self-test fixtures, which carry the call inside string literals; the gate now
  excludes its own file by path.

Surrounding suites after the change:
  vitest server/services/ai-gateway server/services/ai-governance server/eval
    Test Files 39 passed (39) · Tests 450 passed (450)
  tsc --noEmit -p tsconfig.json            exit 0, 0 errors
  ci:eslint-ratchet:since origin           no file changed its warning count
    (it first refused +2 in pq-verdict.ts — computeVerdict complexity 23 and a
     dead assignment; both fixed by splitting the rules into named helpers, not
     by moving the baseline)
