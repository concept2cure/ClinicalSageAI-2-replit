# Accessibility lens: launch catalog, 2026-09-24 (run 2026-09-25 01:10 UTC)

Auditor: `a11y-auditor` (`.claude/agents/a11y-auditor.md`), read-only. Head reviewed:
`f14f5510` (`concept2cure-v2`). Its report is condensed here with every finding kept.

## Scope

Resolved via `client/src/concept2cure/v2/LaunchScopeGate.tsx`, `shared/constants/launch-scope.ts`
and `client/src/concept2cure/v2/surfaceViews.ts`; the per-app file table in
`../2026-09-22/README.md`. Read: the launch surfaces under `v2/surfaces`, `v2/editor/*`,
`quality/*`, `_shared/components/*`. Gates: `ci:token-contrast` PASS (67 pairs),
`ci:check-chip-tones` PASS (138 uses).

Excluded by instruction (fixed earlier this week at the CSS level, commits `868736dd`…`dbb9d726`):
focus-ring contrast, title-only icon buttons in Shell/AnaRail/DocumentsPanel, hover-only reveal of
`.pj-tool-go`/`.fc-item-go`/`.estar-open`, white-on-accent fills, disabled opacity, sub-10px type,
reduced-motion overrides, phantom `--danger`/`--ok-600` tokens.

## Prior state the auditor recorded

The shared `EsignModal`, `GovernedConfirmDialog` and `useDialog` hook (focus trap, Escape,
focus-return) are used consistently across the dialogs checked (`TaskBoard`, `FilingsCatalog`,
`Review`, `Orchestration`, `AuthoringPlaceIntoFiling`, `VaultPlaceIntoSubmission`,
`AssignReviewDialog`, `FileToVaultDialog`, `DocumentWorkbench` unsaved-changes guard). Clickable
`role="button"` divs found carry `tabIndex`, Enter/Space handling and `aria-label`
(`ProjectHome.tsx:390-404`, `ProtocolDevRegisters.tsx:91-102`, `TaskBoard.tsx:861-873,912-927`).
Tables have `<th>` headers. Colour-coded chips sampled carry a text label beside the tone class.

## Findings

| # | Sev | Where | Finding | Criterion | Status |
|---|---|---|---|---|---|
| A1 | **blocker** | `v2/surfaces/SubmissionCenter.tsx:1049-1057` | The submissions-portfolio row is a bare `<tr onClick>` — no role, no tab stop, no key handling. A keyboard or screen-reader user cannot open a submission from the table at all. Same defect class `TaskBoard.tsx:855-860` fixed for its cards. | 2.1.1, 4.1.2 | **fixed** `4f0becb6`: the title is a real `<button>` styled as its text; its click (mouse or Enter/Space) bubbles to the row's handler, so the `<tr>` keeps its table semantics and there is one open path |
| A2 | advisory | `v2/surfaces/AdminSurfaces.tsx` (ArtifactsCenter, ~2540 and 1393, 1475) | E-signed indicator is an icon-only `<span title=…>` (one with no title at all); not text-equivalent, invisible on touch/keyboard. | 1.1.1 | **fixed** `4f0becb6`: `role="img"` + `aria-label="E-signed (21 CFR Part 11)"` where the icon stands alone; `aria-hidden` where visible text sits beside it |
| A3 | advisory | `quality/App.tsx:279-290` | `role="tablist"`/`role="tab"` on plain buttons with no roving tabindex or arrow keys — operable via Tab/Enter, but not the APG tabs pattern a screen-reader user expects. | ARIA APG | **fixed** `4f0becb6`: one tab stop; Arrow / Home / End move and select |

No other blockers were found by code reading in scope.

## Not checked statically (needs a browser + assistive-technology pass)

- Announcement behaviour of `role="status"`/`aria-live` regions, `role="alert"` banners, and the
  `EsignModal`/`GovernedConfirmDialog` focus trap and Escape.
- Rendered focus-ring visibility (fixed at CSS level this week; not re-verified visually).
- Contrast of dynamically computed inline styles (for example `FilingsCatalog.tsx:571`); the token
  gate checks pairs, not every inline site.
- `v2/editor/DocumentWorkbench.tsx` (~5,100 lines) and `RichSectionEditor.tsx` (~2,700) were
  sampled (modal, guard and toolbar sections), not read line by line.
- `ProtocolDevDesign/Compliance/Projections/Reviews/Shared`, `ProtocolGov`, `ProtocolRegisterForms`
  were grepped for the target patterns, not read line by line.
- Touch-target sizing and motion were outside this lens.
