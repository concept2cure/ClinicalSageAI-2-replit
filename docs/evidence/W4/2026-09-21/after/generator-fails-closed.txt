# Proving the generator fails closed, on the real code path

## 1. No registry — no documents
```

wrote 4 document(s) to docs/validation/launch/
  NOT EXECUTED: 2
```
exit 1; docs/validation/launch/ gained nothing.

## 2. A citation that does not resolve — no documents
A registry citing `tests/e2e/a-spec-that-was-deleted.spec.ts`:
```
✗ the registry is not structurally valid; no documents were written.

  [dangling-ref] URS-VAULT-01: cited path does not exist: server/routes/project-vault.ts
  [dangling-ref] URS-VAULT-01: cited path does not exist: tests/e2e/a-spec-that-was-deleted.spec.ts
```
exit 1. Both dangling citations named; zero documents rendered, because a
rendered matrix is indistinguishable from a correct one.

## 3. A suite that executes nothing is ERROR, never a pass
A requirement citing a file that exists and contains no tests:
```
  executing 2 vitest file(s) in one run…

wrote 4 document(s) to docs/validation/launch/
  PASS: 1
  ERROR: 1

Verdict: INCOMPLETE
```
and the matrix row it produced:
```
| URS-QMS-02 | QMS controlled documents | medium | unit | `server/services/auditService.ts` | NOT EXECUTED |  |
```

## 4. The same two requirements with real passing tests
```
  executing 2 vitest file(s) in one run…

wrote 4 document(s) to docs/validation/launch/
  PASS: 2

Verdict: COMPLETE
```
