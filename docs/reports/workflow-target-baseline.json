{
  "_comment": "Workflow steps that invoke a repo path which does not exist, per scripts/ci/check-workflow-targets.mjs. Every entry needs a REASON naming why the step is still here. These are not safe — each one fails the moment its workflow runs. This list may shrink, never grow.",
  "entries": {
    "cerv2-staging-deploy.yml -> scripts/cerv2_staging_verify.mjs": {
      "kind": "script",
      "reason": "BROKEN, KEPT DELIBERATELY. The CERV2 API smoke test that gates the staging deploy. The script was removed by b79f020e1 (dead-code purge); the workflow is workflow_dispatch-only, so the step has never been observed to fail. Left in place rather than deleted because a deploy whose verification step vanished would deploy silently — this one at least stops. Restore the script or replace the step with a verification that exists."
    },
    "ci-staging-integration.yml -> requirements-staging.txt": {
      "kind": "pip -r",
      "reason": "BROKEN, KEPT DELIBERATELY. The Part 11 staging smoke installs from a root requirements-staging.txt that no longer exists. workflow_dispatch-only, so never observed. The smoke test it installs for (scripts/e2e_staging_smoke.py) DOES still exist, so the step is worth repairing rather than deleting."
    },
    "ci-staging-integration.yml -> scripts/generate_part11_report.py": {
      "kind": "script",
      "reason": "BROKEN, KEPT DELIBERATELY. Generates the ALCOA+ compliance report from the evidence directory; removed by b79f020e1. Runs `if: always()`, so it fails the run even when the smoke test passed. Kept visible because a Part 11 evidence run that quietly stops producing its report is the worse failure of the two."
    },
    "test-assembly.yml -> scripts/e2e_smoke_assembly.mjs": {
      "kind": "script",
      "reason": "BROKEN, KEPT DELIBERATELY. Optional assembly e2e, gated on the TEST_DATABASE_URL secret; the script was removed by b79f020e1. The sibling step in the same job (scripts/check_assembly_docs.mjs) still exists and still runs, so the job is live and only this step is dead."
    }
  }
}
