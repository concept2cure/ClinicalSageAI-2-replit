version: '3.9'

# Canonical self-host / local install stack.
#
# Usage:
#   docker compose up --build                 # app + db + redis (the install SKU)
#   docker compose --profile ingestion up     # + Tika / GROBID / OpenSearch
#   docker compose --profile workflow up      # + Temporal
#   docker compose --profile llm up           # + LiteLLM gateway / OPA
#
# Required .env: DB_PASSWORD, JWT_SECRET, and at least one of ANTHROPIC_API_KEY / OPENAI_API_KEY.
# This stack is single-tenant by default (SEED_DEMO_USER=false): it seeds one
# organization and no publicly-known demo admin. Create your admin user via the
# app's first-run flow or the db:seed:admin script.

services:
  # ---------- Database ----------
  db:
    # pgvector image, not stock postgres: the schema's CSR knowledge tables
    # need `CREATE EXTENSION vector` (migrations/0005), which stock postgres lacks.
    image: pgvector/pgvector:pg15
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-concept2cure}
    ports:
      - '${DB_PORT:-5432}:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-postgres}']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - c2c

  # ---------- Redis (queues + caching) ----------
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - '${REDIS_PORT:-6379}:6379'
    volumes:
      - redisdata:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - c2c

  # ---------- Application ----------
  app:
    build:
      context: .
      dockerfile: Dockerfile.optimized
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - '${APP_PORT:-5000}:5000'
    environment:
      NODE_ENV: production
      PORT: 5000
      # sslmode=prefer: encrypt when the server offers TLS, otherwise plaintext.
      # The stock image does not ship TLS-on by default; bind-mount certs and a
      # postgres `command:` to enable ssl=on, then upgrade this to `require`.
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD}@db:5432/${DB_NAME:-concept2cure}?sslmode=prefer
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      SENTRY_DSN: ${SENTRY_DSN:-}
      VITE_SENTRY_DSN: ${VITE_SENTRY_DSN:-}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Single-tenant install: do not seed the publicly-known demo admin.
      SEED_DEMO_USER: ${SEED_DEMO_USER:-false}
    logging:
      driver: 'json-file'
      options:
        max-size: '50m'
        max-file: '5'
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '0.5'
          memory: 1G
    networks:
      - c2c

  # ---------- Optional: document ingestion (profile: ingestion) ----------
  tika:
    image: apache/tika:2.9.2.1
    ports:
      - '9998:9998'
    restart: unless-stopped
    profiles: ['ingestion']
    networks:
      - c2c

  grobid:
    image: lfoppiano/grobid:0.8.0
    ports:
      - '8070:8070'
    restart: unless-stopped
    profiles: ['ingestion']
    networks:
      - c2c

  opensearch:
    image: opensearchproject/opensearch:2.19.0
    environment:
      - discovery.type=single-node
      - plugins.security.disabled=true
      - OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m
    ports:
      - '9200:9200'
    restart: unless-stopped
    profiles: ['ingestion']
    networks:
      - c2c

  # ---------- Optional: long-running workflows (profile: workflow) ----------
  temporal-postgres:
    image: postgres:15
    environment:
      - POSTGRES_USER=temporal
      - POSTGRES_PASSWORD=temporal
    ports:
      - '5434:5432'
    restart: unless-stopped
    profiles: ['workflow']
    networks:
      - c2c

  temporal:
    image: temporalio/auto-setup:1.28.1
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - POSTGRES_USER=temporal
      - POSTGRES_PWD=temporal
      - POSTGRES_SEEDS=temporal-postgres
      - DYNAMIC_CONFIG_FILE_PATH=config/dynamicconfig/development-sql.yaml
    ports:
      - '7233:7233'
    depends_on:
      - temporal-postgres
    restart: unless-stopped
    profiles: ['workflow']
    networks:
      - c2c

  # ---------- Optional: LLM gateway + policy engine (profile: llm) ----------
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - LITELLM_MASTER_KEY=${LITELLM_API_KEY:-}
    ports:
      - '4000:4000'
    command: ['--port', '4000', '--host', '0.0.0.0']
    profiles: ['llm']
    networks:
      - c2c

  opa:
    image: openpolicyagent/opa:latest
    command:
      [
        'run',
        '--server',
        '--set=decision_logs.console=true',
        '/policies',
      ]
    volumes:
      - ./server/policies:/policies:ro
    ports:
      - '8181:8181'
    restart: unless-stopped
    profiles: ['llm']
    networks:
      - c2c

volumes:
  pgdata:
    driver: local
  redisdata:
    driver: local

networks:
  c2c:
    driver: bridge
