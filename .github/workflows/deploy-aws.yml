name: Deploy to AWS (Production)

on:
  workflow_dispatch:
    inputs:
      deploy_api:
        description: 'Deploy API service'
        type: boolean
        default: true
      deploy_worker:
        description: 'Deploy Worker service'
        type: boolean
        default: true
      deploy_frontend:
        description: 'Deploy frontend to S3/CloudFront'
        type: boolean
        default: true

  push:
    tags:
      - 'v*'

concurrency:
  group: deploy-production
  cancel-in-progress: false # Never cancel production deploys

permissions:
  id-token: write # OIDC for AWS
  contents: read

env:
  AWS_REGION: us-east-1
  ECR_API_REPO: c2c-prod-api
  ECR_WORKER_REPO: c2c-prod-worker
  ECS_CLUSTER: c2c-production
  ECS_API_SERVICE: c2c-production-api
  ECS_WORKER_SERVICE: c2c-production-worker
  # Task definition families and container names for the digest-pinned
  # register-then-deploy path in deploy-api / deploy-worker. The container
  # names must match the names in the existing task definitions so the
  # patch step modifies the right container's image and not a sibling
  # (e.g. a fluent-bit log router or a datadog sidecar).
  ECS_API_TASK_FAMILY: c2c-production-api
  ECS_WORKER_TASK_FAMILY: c2c-production-worker
  ECS_API_CONTAINER_NAME: api
  ECS_WORKER_CONTAINER_NAME: worker
  # Family for the one-off schema-migration task (see the `migrate` job). It is
  # registered fresh on every deploy by deriving it from the API task def, so it
  # inherits the same DATABASE_URL secret, execution role and log config without
  # a second place to configure them. ECS creates the family on first register,
  # so this needs no pre-existing infrastructure.
  ECS_MIGRATE_TASK_FAMILY: c2c-production-migrate
  FRONTEND_BUCKET: c2c-prod-frontend
  CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}

jobs:
  # ── Gate: Run tests before deploying ──────────────────────────────────────
  test:
    name: Pre-deploy Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm test

  # ── Gate: Security / SAST / governance must pass before any prod deploy ────
  # Production deploys must not bypass the same security gates enforced on CI.
  # ci.yml runs these on PRs/pushes, but deploy-aws.yml is triggered by tags /
  # workflow_dispatch, so the relevant gates are re-run here and made a hard
  # `needs:` dependency of the build/deploy jobs.
  security-gate:
    name: Pre-deploy Security Gate (SAST + Trivy + governance)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Guardrails — SAML SSO fail-closed (no auth bypass)
        run: npm run ci:saml-fail-closed

      - name: Guardrails — no leaky dev-auth gates in prod paths
        run: npm run ci:no-dev-auth-in-prod

      - name: Guardrails — password hygiene
        run: npm run ci:password-hygiene

      - name: Run security contract tests (tenant-isolation + prompt-injection)
        run: npm run test:security
        env:
          CI: true

      - name: Run npm audit (with documented false-positive allowlist)
        run: node scripts/ci/audit-with-allowlist.mjs

      - name: Trivy filesystem scan (deps + secrets)
        # Blocking: HIGH/CRITICAL findings fail the deploy gate.
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: CRITICAL,HIGH
          exit-code: '1'
          format: table

      - name: Trivy config scan (Dockerfile/IaC)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: .
          severity: CRITICAL,HIGH
          exit-code: '1'
          format: table
          skip-dirs: node_modules

  # ── Build & Push container images ─────────────────────────────────────────
  build-push:
    name: Build & Push to ECR
    needs: [test, security-gate]
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.deploy_api == 'true' || github.event.inputs.deploy_worker == 'true' || startsWith(github.ref, 'refs/tags/v') }}
    outputs:
      image_tag: ${{ steps.meta.outputs.tag }}
      # Image content digests captured at push time so deploy-api / deploy-worker
      # can register task-def revisions pinned to @sha256:<digest>. Without this
      # the deploys would --force-new-deployment whatever the current task def
      # references and rollback would not be deterministic. See deploy-api.
      api_digest: ${{ steps.api-push.outputs.digest }}
      worker_digest: ${{ steps.worker-push.outputs.digest }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Determine image tag
        id: meta
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            TAG="${GITHUB_REF#refs/tags/}"
          else
            TAG="sha-${GITHUB_SHA::8}"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Build & push API image
        id: api-push
        if: ${{ github.event.inputs.deploy_api != 'false' }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ steps.meta.outputs.tag }}
        # Push the immutable, SHA/tag-pinned image AND capture the content
        # digest. The digest is the only identifier that survives tag
        # mutation / ECR garbage-collection, so deploy-api pins the ECS
        # task-def to <repo>@sha256:<digest> rather than the mutable tag.
        # Mutable `:latest` was removed so prod can never resolve a floating
        # image. Rollback redeploys a prior task-def revision (which carries
        # its own pinned digest), which is the deterministic shape ECR /
        # ECS support.
        run: |
          docker build -f Dockerfile.optimized \
            -t "$ECR_REGISTRY/$ECR_API_REPO:$IMAGE_TAG" \
            .
          docker push "$ECR_REGISTRY/$ECR_API_REPO:$IMAGE_TAG"
          # `docker inspect` on the local tag returns RepoDigests like
          # "<registry>/<repo>@sha256:<hex>"; take the digest portion.
          DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' \
            "$ECR_REGISTRY/$ECR_API_REPO:$IMAGE_TAG" | cut -d'@' -f2)
          if [[ -z "$DIGEST" || "$DIGEST" != sha256:* ]]; then
            echo "::error::Could not capture sha256 digest for pushed API image"
            exit 1
          fi
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"
          echo "API image pushed: $ECR_REGISTRY/$ECR_API_REPO:$IMAGE_TAG @ $DIGEST"

      - name: Build & push Worker image
        id: worker-push
        if: ${{ github.event.inputs.deploy_worker != 'false' }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ steps.meta.outputs.tag }}
        # See API push step. Same digest-capture pattern so deploy-worker
        # pins by @sha256:<digest>.
        run: |
          docker build -f worker/Dockerfile \
            -t "$ECR_REGISTRY/$ECR_WORKER_REPO:$IMAGE_TAG" \
            .
          docker push "$ECR_REGISTRY/$ECR_WORKER_REPO:$IMAGE_TAG"
          DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' \
            "$ECR_REGISTRY/$ECR_WORKER_REPO:$IMAGE_TAG" | cut -d'@' -f2)
          if [[ -z "$DIGEST" || "$DIGEST" != sha256:* ]]; then
            echo "::error::Could not capture sha256 digest for pushed Worker image"
            exit 1
          fi
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"
          echo "Worker image pushed: $ECR_REGISTRY/$ECR_WORKER_REPO:$IMAGE_TAG @ $DIGEST"

  # ── Migrate the database BEFORE any service rolls ─────────────────────────
  #
  # C2C-DB-001 / REM-7. Until this job existed there was no production migration
  # mechanism at all: the container CMD is `npm run start` (which applies
  # nothing), this workflow contained zero migration references, and the runtime
  # image did not even carry db/migrations or scripts/db. Schema reached real
  # databases only when a human remembered to run an applier by hand — which is
  # the mechanism behind "merged ≠ applied": code that reads a table ships on a
  # deploy, the table does not.
  #
  # Shape: run scripts/db/deploy-migrate.mjs as a one-off Fargate task built from
  # the SAME digest-pinned image the services are about to run, inside the same
  # VPC/subnets/security groups as the API service — the database is on private
  # RDS subnets (terraform/modules/rds), so a GitHub-hosted runner cannot reach
  # it and running the migration from the runner is not an option.
  #
  # deploy-api and deploy-worker both `needs: migrate`, so a failed or halted
  # migration blocks the rollout instead of shipping code onto a schema that
  # cannot serve it.
  migrate:
    name: Migrate Database (one-off ECS task)
    needs: build-push
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Register migration task definition (pinned to the deploying image)
        id: register-migrate-td
        env:
          IMAGE_DIGEST: ${{ needs.build-push.outputs.api_digest }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        # Derived from the API task def so the migration runs with the identical
        # DATABASE_URL secret, execution role and log configuration the app uses.
        # Deliberate differences from the API definition:
        #   • only the app container is kept — log routers / APM sidecars would
        #     keep a batch task alive after the migration exits;
        #   • portMappings + healthCheck are dropped — a one-off task registers
        #     with no load balancer and must not be killed by an HTTP probe;
        #   • command is the migration entrypoint.
        run: |
          set -euo pipefail

          if [[ -z "$IMAGE_DIGEST" || "$IMAGE_DIGEST" != sha256:* ]]; then
            echo "::error::No API image digest available, so there is no image to migrate from."
            echo "::error::A deploy that skips the API image cannot migrate the schema. Re-run with deploy_api enabled,"
            echo "::error::or run 'npm run db:migrate:deploy' out of band before deploying."
            exit 1
          fi

          PINNED_IMAGE="$ECR_REGISTRY/$ECR_API_REPO@$IMAGE_DIGEST"
          echo "Migration task image: $PINNED_IMAGE"

          aws ecs describe-task-definition \
            --task-definition "$ECS_API_TASK_FAMILY" \
            --no-cli-pager \
            --query 'taskDefinition' > /tmp/api-td.json

          jq --arg img "$PINNED_IMAGE" \
             --arg api_container "$ECS_API_CONTAINER_NAME" \
             --arg family "$ECS_MIGRATE_TASK_FAMILY" \
             '
             {taskRoleArn, executionRoleArn, networkMode, containerDefinitions, volumes, placementConstraints, requiresCompatibilities, cpu, memory, runtimePlatform, ipcMode, pidMode, proxyConfiguration, ephemeralStorage}
             | with_entries(select(.value != null))
             | .family = $family
             | .containerDefinitions = (
                 .containerDefinitions
                 | map(select(.name == $api_container))
                 | map(
                     .image = $img
                     | .command = ["npm", "run", "db:migrate:deploy"]
                     | .essential = true
                     | del(.portMappings, .healthCheck)
                     # Inter-container wiring that can only refer to the sidecars
                     # dropped above. Left in place, register-task-definition
                     # rejects the whole revision for naming a container that is
                     # no longer in the definition.
                     | del(.dependsOn, .links, .volumesFrom)
                   )
               )
             ' /tmp/api-td.json > /tmp/migrate-td.json

          if [[ "$(jq '.containerDefinitions | length' /tmp/migrate-td.json)" != "1" ]]; then
            echo "::error::Container '$ECS_API_CONTAINER_NAME' not found in task def '$ECS_API_TASK_FAMILY'."
            exit 1
          fi

          TD_ARN=$(aws ecs register-task-definition \
            --cli-input-json file:///tmp/migrate-td.json \
            --no-cli-pager \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)
          # `--output text` yields the literal "None" when the query misses,
          # which is a string the next step would happily pass to run-task.
          # Catch it here, where the cause is still visible.
          if [[ -z "$TD_ARN" || "$TD_ARN" == "None" ]]; then
            echo "::error::register-task-definition returned no ARN for family '$ECS_MIGRATE_TASK_FAMILY'."
            exit 1
          fi
          echo "Registered migration task def: $TD_ARN"
          echo "task_def_arn=$TD_ARN" >> "$GITHUB_OUTPUT"

          # Log location for the failure tail below — read off the definition so
          # it stays correct if the API's log config changes.
          jq -r '.containerDefinitions[0].logConfiguration.options["awslogs-group"] // ""' \
            /tmp/migrate-td.json > /tmp/log-group
          jq -r '.containerDefinitions[0].logConfiguration.options["awslogs-stream-prefix"] // ""' \
            /tmp/migrate-td.json > /tmp/log-prefix

      - name: Run migration task and wait for it to finish
        env:
          TD_ARN: ${{ steps.register-migrate-td.outputs.task_def_arn }}
        # Network configuration is read off the running API service rather than
        # hard-coded: the migration must land on the same subnets and security
        # groups that are already allowed through to RDS, and duplicating those
        # IDs here would be a second place to get them wrong.
        run: |
          set -euo pipefail

          NETWORK_CONFIG=$(aws ecs describe-services \
            --cluster "$ECS_CLUSTER" \
            --services "$ECS_API_SERVICE" \
            --no-cli-pager \
            --query 'services[0].networkConfiguration' \
            --output json)

          if [[ -z "$NETWORK_CONFIG" || "$NETWORK_CONFIG" == "null" ]]; then
            echo "::error::Could not read networkConfiguration from service '$ECS_API_SERVICE'."
            echo "::error::Without it the migration task would launch outside the VPC that can reach the database."
            exit 1
          fi
          # The service runs tasks with a public IP disabled/enabled per its own
          # config; reuse it verbatim so the migration has the same egress path
          # (NAT / VPC endpoints) the app has for Secrets Manager and ECR.
          echo "$NETWORK_CONFIG" | jq '{awsvpcConfiguration: .awsvpcConfiguration}' > /tmp/netcfg.json

          TASK_ARN=$(aws ecs run-task \
            --cluster "$ECS_CLUSTER" \
            --task-definition "$TD_ARN" \
            --launch-type FARGATE \
            --network-configuration file:///tmp/netcfg.json \
            --started-by "deploy-${GITHUB_RUN_ID}" \
            --no-cli-pager \
            --query 'tasks[0].taskArn' \
            --output text)

          if [[ -z "$TASK_ARN" || "$TASK_ARN" == "None" ]]; then
            echo "::error::run-task returned no task ARN — the migration never started."
            aws ecs run-task \
              --cluster "$ECS_CLUSTER" \
              --task-definition "$TD_ARN" \
              --launch-type FARGATE \
              --network-configuration file:///tmp/netcfg.json \
              --no-cli-pager \
              --query 'failures' --output json || true
            exit 1
          fi
          echo "Migration task: $TASK_ARN"
          echo "$TASK_ARN" > /tmp/task-arn

          # `wait tasks-stopped` polls for up to 10 minutes. A migration that runs
          # longer than that is itself a problem worth failing the deploy on.
          aws ecs wait tasks-stopped --cluster "$ECS_CLUSTER" --tasks "$TASK_ARN"

          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster "$ECS_CLUSTER" --tasks "$TASK_ARN" --no-cli-pager \
            --query "tasks[0].containers[?name=='${ECS_API_CONTAINER_NAME}'].exitCode | [0]" \
            --output text)
          STOPPED_REASON=$(aws ecs describe-tasks \
            --cluster "$ECS_CLUSTER" --tasks "$TASK_ARN" --no-cli-pager \
            --query 'tasks[0].stoppedReason' --output text)

          echo "Migration task exit code: $EXIT_CODE (stoppedReason: $STOPPED_REASON)"

          # Always surface the migration's own output — it is the only place the
          # per-file apply log and the readiness-contract verification appear.
          LOG_GROUP=$(cat /tmp/log-group)
          LOG_PREFIX=$(cat /tmp/log-prefix)
          if [[ -n "$LOG_GROUP" && -n "$LOG_PREFIX" ]]; then
            TASK_ID="${TASK_ARN##*/}"
            echo "── migration log ──────────────────────────────────────────────"
            aws logs get-log-events \
              --log-group-name "$LOG_GROUP" \
              --log-stream-name "${LOG_PREFIX}/${ECS_API_CONTAINER_NAME}/${TASK_ID}" \
              --no-cli-pager --query 'events[].message' --output text || \
              echo "(log stream not available)"
            echo "───────────────────────────────────────────────────────────────"
          fi

          case "$EXIT_CODE" in
            0)
              echo "Schema migration succeeded — services may roll."
              ;;
            3)
              echo "::error::The database has never been provisioned (deploy-migrate.mjs exit 3)."
              echo "::error::Run the one-time from-scratch installer against it before deploying:"
              echo "::error::  DATABASE_URL='…' node scripts/db/install-fresh.mjs"
              exit 1
              ;;
            *)
              echo "::error::Schema migration failed (exit ${EXIT_CODE}). Services were NOT rolled."
              exit 1
              ;;
          esac

  # ── Deploy API to ECS ────────────────────────────────────────────────────
  deploy-api:
    name: Deploy API → ECS
    # `migrate` is a hard gate: rolling the API onto a schema whose migration
    # failed is exactly the outcome this deploy path is meant to prevent.
    needs: [build-push, migrate]
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.deploy_api != 'false' }}
    environment: production

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Register pinned task definition revision (API)
        id: register-api-td
        env:
          # Immutable image tag built+pushed by build-push (e.g. sha-<gitsha> or
          # the release tag). Combined with the captured digest so the new task
          # def is pinned to a specific image, not a mutable tag.
          IMAGE_TAG: ${{ needs.build-push.outputs.image_tag }}
          IMAGE_DIGEST: ${{ needs.build-push.outputs.api_digest }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        # The deterministic shape ECS supports: pull the current task def,
        # patch the API container image to <repo>@sha256:<digest> (the
        # digest the build-push job captured), register it as a new
        # revision, and deploy that revision by ARN. Rollback is a one-line
        # `aws ecs update-service --task-definition <prior-arn>` against
        # a prior revision. --force-new-deployment is gone — it could not
        # guarantee the right image was used because it only re-pulls
        # whatever the existing task def already references.
        run: |
          if [[ -z "$IMAGE_DIGEST" || "$IMAGE_DIGEST" != sha256:* ]]; then
            echo "::error::API image digest missing or malformed: '$IMAGE_DIGEST'. Refusing to deploy without a pinned digest."
            exit 1
          fi

          PINNED_IMAGE="$ECR_REGISTRY/$ECR_API_REPO@$IMAGE_DIGEST"
          echo "Pinning API task def to: $PINNED_IMAGE"

          # Get current task def, strip metadata that's not allowed on
          # register-task-definition input, patch the API container image,
          # register the new revision.
          aws ecs describe-task-definition \
            --task-definition "$ECS_API_TASK_FAMILY" \
            --no-cli-pager \
            --query 'taskDefinition' > /tmp/api-td.json

          jq --arg img "$PINNED_IMAGE" \
             --arg api_container "$ECS_API_CONTAINER_NAME" \
             '
             {family, taskRoleArn, executionRoleArn, networkMode, containerDefinitions, volumes, placementConstraints, requiresCompatibilities, cpu, memory, runtimePlatform, ipcMode, pidMode, proxyConfiguration, inferenceAccelerators, ephemeralStorage}
             | with_entries(select(.value != null))
             | .containerDefinitions = (.containerDefinitions | map(if .name == $api_container then .image = $img else . end))
             ' /tmp/api-td.json > /tmp/api-td-patched.json

          NEW_TD_ARN=$(aws ecs register-task-definition \
            --cli-input-json file:///tmp/api-td-patched.json \
            --no-cli-pager \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)

          echo "Registered API task def: $NEW_TD_ARN"
          echo "task_def_arn=$NEW_TD_ARN" >> "$GITHUB_OUTPUT"

      - name: Deploy pinned task def revision (API)
        env:
          NEW_TD_ARN: ${{ steps.register-api-td.outputs.task_def_arn }}
        # Deploy by ARN of the just-registered revision. ECS pulls the
        # image that revision references (which is @sha256:<digest>) and
        # rolls the service to new tasks. Rollback = update-service with a
        # prior revision ARN.
        run: |
          aws ecs update-service \
            --cluster "$ECS_CLUSTER" \
            --service "$ECS_API_SERVICE" \
            --task-definition "$NEW_TD_ARN" \
            --no-cli-pager

      - name: Wait for API deployment stability
        run: |
          aws ecs wait services-stable \
            --cluster "$ECS_CLUSTER" \
            --services "$ECS_API_SERVICE"

  # ── Deploy Worker to ECS ──────────────────────────────────────────────────
  deploy-worker:
    name: Deploy Worker → ECS
    # Same hard gate as deploy-api. The worker reads the same schema, so a failed
    # migration must block it too.
    needs: [build-push, migrate]
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.deploy_worker != 'false' }}
    environment: production

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Register pinned task definition revision (Worker)
        id: register-worker-td
        env:
          IMAGE_TAG: ${{ needs.build-push.outputs.image_tag }}
          IMAGE_DIGEST: ${{ needs.build-push.outputs.worker_digest }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        # Mirrors deploy-api: describe current TD → patch worker container
        # image to <repo>@sha256:<digest> → register new revision →
        # update-service by the new revision ARN. --force-new-deployment is
        # gone for the same reason. See deploy-api step.
        run: |
          if [[ -z "$IMAGE_DIGEST" || "$IMAGE_DIGEST" != sha256:* ]]; then
            echo "::error::Worker image digest missing or malformed: '$IMAGE_DIGEST'. Refusing to deploy without a pinned digest."
            exit 1
          fi

          PINNED_IMAGE="$ECR_REGISTRY/$ECR_WORKER_REPO@$IMAGE_DIGEST"
          echo "Pinning Worker task def to: $PINNED_IMAGE"

          aws ecs describe-task-definition \
            --task-definition "$ECS_WORKER_TASK_FAMILY" \
            --no-cli-pager \
            --query 'taskDefinition' > /tmp/worker-td.json

          jq --arg img "$PINNED_IMAGE" \
             --arg worker_container "$ECS_WORKER_CONTAINER_NAME" \
             '
             {family, taskRoleArn, executionRoleArn, networkMode, containerDefinitions, volumes, placementConstraints, requiresCompatibilities, cpu, memory, runtimePlatform, ipcMode, pidMode, proxyConfiguration, inferenceAccelerators, ephemeralStorage}
             | with_entries(select(.value != null))
             | .containerDefinitions = (.containerDefinitions | map(if .name == $worker_container then .image = $img else . end))
             ' /tmp/worker-td.json > /tmp/worker-td-patched.json

          NEW_TD_ARN=$(aws ecs register-task-definition \
            --cli-input-json file:///tmp/worker-td-patched.json \
            --no-cli-pager \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)

          echo "Registered Worker task def: $NEW_TD_ARN"
          echo "task_def_arn=$NEW_TD_ARN" >> "$GITHUB_OUTPUT"

      - name: Deploy pinned task def revision (Worker)
        env:
          NEW_TD_ARN: ${{ steps.register-worker-td.outputs.task_def_arn }}
        run: |
          aws ecs update-service \
            --cluster "$ECS_CLUSTER" \
            --service "$ECS_WORKER_SERVICE" \
            --task-definition "$NEW_TD_ARN" \
            --no-cli-pager

      - name: Wait for Worker deployment stability
        run: |
          aws ecs wait services-stable \
            --cluster "$ECS_CLUSTER" \
            --services "$ECS_WORKER_SERVICE"

  # ── Deploy frontend to S3 + CloudFront ────────────────────────────────────
  deploy-frontend:
    name: Deploy Frontend → S3/CloudFront
    needs: [test, security-gate]
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.deploy_frontend != 'false' }}
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build
        env:
          NODE_ENV: production

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync to S3
        run: |
          aws s3 sync dist/public/ "s3://$FRONTEND_BUCKET/" \
            --delete \
            --cache-control "public,max-age=31536000,immutable" \
            --exclude "index.html" \
            --exclude "*.json"

          # index.html and manifests should not be cached aggressively
          aws s3 cp dist/public/index.html "s3://$FRONTEND_BUCKET/index.html" \
            --cache-control "public,max-age=0,must-revalidate"

      - name: Invalidate CloudFront cache
        if: ${{ env.CLOUDFRONT_DISTRIBUTION_ID != '' }}
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "$CLOUDFRONT_DISTRIBUTION_ID" \
            --paths "/*"

  # ── Post-deploy smoke test ────────────────────────────────────────────────
  smoke-test:
    name: Post-deploy Smoke Test
    needs: [deploy-api, deploy-frontend]
    runs-on: ubuntu-latest
    if: always() && (needs.deploy-api.result == 'success' || needs.deploy-frontend.result == 'success')

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Health check
        run: |
          ALB_DNS=$(aws elbv2 describe-load-balancers \
            --names c2c-prod \
            --query 'LoadBalancers[0].DNSName' \
            --output text 2>/dev/null || echo "")

          if [ -n "$ALB_DNS" ]; then
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://$ALB_DNS/api/health" --insecure || true)
            if [ "$STATUS" = "200" ]; then
              echo "✅ API health check passed"
            else
              echo "⚠️ API health check returned $STATUS"
              exit 1
            fi
          else
            echo "⚠️ Could not resolve ALB DNS — skipping health check"
          fi
