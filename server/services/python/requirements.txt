# Python Backend Service - Unified dependencies for all Python services
# FastAPI and web framework dependencies
fastapi==0.136.3
uvicorn[standard]==0.47.0
pydantic[email]==2.13.4
pydantic-settings==2.14.1
python-multipart==0.0.30
starlette==1.3.1
websockets==16.0

# Database dependencies
psycopg2-binary==2.9.12
sqlalchemy==2.0.50
alembic==1.18.4

# Machine Learning dependencies
scikit-learn==1.3.0
pandas==3.0.3
numpy==2.4.6
matplotlib==3.10.9
seaborn==0.13.2

# Document processing
PyPDF2==3.0.1
python-docx==1.2.0           # Aligned with root requirements.txt; 0.8.11 fails to build on Python 3.11 and conflicts with docxtpl 0.20.2 (needs python-docx 1.x)
docxtpl==0.20.2
openpyxl==3.1.5

# AI/NLP dependencies
# AI/NLP dependencies
openai==2.37.0
# CVE-2026-9856 (HIGH): save_pretrained accepted a chat-template name that
# traversed out of the target directory, so a template could write an arbitrary
# file. Fixed in 5.10.0 — the Trivy filesystem scan fails the build below it.
transformers==5.10.0
torch==2.12.0
sentence-transformers==5.5.1

# Vector database
# Chroma was a leftover from the deleted Python backend and has no importer,
# runtime entrypoint, container, or workflow in this repository. Keeping the
# package nevertheless shipped its remotely exploitable server components (and
# their cross-tenant/RCE advisories) in dependency scans. PostgreSQL + pgvector
# is the platform's active vector store; do not re-add Chroma without a deployed
# architecture, threat model, and an advisory-free supported release.
pinecone-client==2.2.4

# API and HTTP
requests==2.34.2
httpx==0.28.1
# 3.14.3 fixes CVE-2026-69244 (HIGH) — out-of-bounds heap read in the C HTTP
# response parser's error path on a malformed response. No Python source in this
# repo imports aiohttp directly, so this is a transitive pin and the bump is
# confined to the dependency graph.
aiohttp==3.14.3

# Utilities
python-dotenv==1.2.2
click==8.4.1
email-validator==2.3.0

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0

# Regulatory and compliance specific
lxml==6.1.1
beautifulsoup4==4.15.0
jinja2==3.1.6

# Data processing
xmltodict==1.0.4
jsonschema==4.26.0
pyyaml==6.0.3

# Image processing (for document analysis)
# 12.3.0 fixes ten HIGH advisories present in 12.2.0 (CVE-2026-54058/54059/54060,
# CVE-2026-55379/55380, CVE-2026-59197/59199/59200/59204/59205) — memory
# disclosure, heap out-of-bounds writes and DoS via crafted McIdas/PCF/BDF/GD/
# JPEG2000/PDF inputs. This service OCRs untrusted uploaded documents, so those
# code paths are directly reachable. The repo's root requirements.txt was
# already on 12.3.0; this pin had simply drifted behind it.
Pillow==12.3.0
pytesseract==0.3.13

# Async and concurrency
celery==5.6.3
redis==7.4.0

# Cryptography and security
# Cryptography and security
# 50.0.0 fixes two HIGH advisories present in 48.0.1: CVE-2026-69247 (PKCS#7
# EnvelopedData decryption exposes a Bleichenbacher oracle through
# distinguishable errors) and CVE-2026-69249 (duplicate self-signed
# intermediates cause exponential path-building — a DoS on certificate
# verification). 50.0.0 is the lower bound that covers BOTH; 49.0.0 clears only
# the second. No Python source in this repo imports cryptography directly, so
# despite spanning two majors this is a transitive pin with no call sites here
# to break.
cryptography==50.0.0
bcrypt==5.0.0

# Monitoring and logging
structlog==25.5.0
prometheus-client==0.25.0
prometheus-fastapi-instrumentator==7.1.0
