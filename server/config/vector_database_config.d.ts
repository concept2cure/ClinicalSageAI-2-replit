/**
 * Type declarations for vector_database_config.js
 *
 * Hand-written to give the TypeScript barrel (config/index.ts) a typed view
 * of the runtime JS config. The shape is intentionally permissive — the JS
 * file is the source of truth for the concrete values.
 */

export declare const VECTOR_DATABASE_CONFIG: Record<string, any>;

declare const _default: typeof VECTOR_DATABASE_CONFIG;
export default _default;
