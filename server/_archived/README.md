# ⚠️ ARCHIVED - DO NOT USE

**Archived:** 2025-01-24

These server entry points were archived during simplification. The canonical entry points are:
- `server/index.ts` - Production server
- `server/vite.ts` - Development server with HMR

## Archived Files
- `server.js` - Duplicate of index.ts
- `direct-server.js` - Standalone variant
- `simple-api.js` - Minimal API server
- `startup-fix.js` - Startup workaround
- `advisor-standalone.js` - Standalone advisor
- `swagger-standalone.js` - Standalone Swagger UI
- `vault-server.js` - Standalone vault server

## Recovery
If you need functionality from these files, integrate it into `server/index.ts`.

## Delete After
These files can be permanently deleted after 2025-02-24 if no issues arise.
