// Stub file for @langchain/core - replace with actual implementation when needed
export const OpenAIChat = () => {
  console.warn('[stub] LangChain called – replace later');
  return {};
};

// Add more LangChain stubs as needed
export const LLMChain = () => {
  console.warn('[stub] LangChain LLMChain called – replace later');
  return {};
};

export const BasePromptTemplate = () => {
  console.warn('[stub] LangChain BasePromptTemplate called – replace later');
  return {};
};

// Common classes/types often used
export class BaseChain {
  constructor() {
    console.warn('[stub] LangChain BaseChain instantiated – replace later');
  }
}

export class BaseLLM {
  constructor() {
    console.warn('[stub] LangChain BaseLLM instantiated – replace later');
  }
}

// Default export for wildcard imports
export default {
  OpenAIChat,
  LLMChain,
  BasePromptTemplate,
  BaseChain,
  BaseLLM,
};
