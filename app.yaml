runtime: nodejs20

env_variables:
  NODE_ENV: production

instance_class: F2

automatic_scaling:
  min_instances: 1
  max_instances: 10

handlers:
  - url: /.*
    script: auto
    secure: always

build_env_variables:
  NODE_ENV: production
