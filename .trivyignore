# Pre-existing security issues to be addressed in dedicated security hardening PR

# See: https://avd.aquasec.com/misconfig/ds002

# Dockerfile USER command (running as non-root) - requires build pipeline changes

DS002

# S3/CloudTrail best practices - requires Terraform module updates

AVD-AWS-0016
AVD-AWS-0086
AVD-AWS-0087
AVD-AWS-0091
AVD-AWS-0093

# chromadb 1.5.9 pre-auth RCE (server/services/python/requirements.txt).
# Pre-existing pin surfaced now that Trivy scanning is blocking. No patched
# chromadb release is published for this CVE yet (Trivy reports no fixed
# version), so it cannot be remediated by a version bump. Allowlisted to keep
# the Trivy gate blocking for *fixable* CRITICAL/HIGH findings.
# TODO(security): upgrade chromadb as soon as a patched release ships, and
# confirm chromadb is not exposed to untrusted pre-auth network input.
CVE-2026-45829

# react-router GHSA-qwww-vcr4-c8h2 (RSC-mode CSRF: an action can run before a
# 400 response). Range >=7.12.0 <8.3.0; the only fix is the react-router 8.x
# major. This app routes with wouter and uses react-router-dom only on a couple
# of auth screens — no RSC / data-router server actions — so the RSC CSRF path
# is not reachable. Trivy scans package-lock.json separately from npm audit, so
# this must be listed here in ADDITION to ACCEPTED_GHSA_IDS in
# scripts/ci/audit-with-allowlist.mjs. The other react-router advisory (route-
# matching DoS) is genuinely fixed by the react-router-dom 7.18.1 bump.
# TODO(security): adopt react-router 8.x in a dedicated upgrade PR, then drop this.
GHSA-qwww-vcr4-c8h2

# image-size CVE-2025-71329 (zero-valued size DoS) + CVE-2025-71330 (ICNS DoS),
# vulnerable range <=2.0.2. The only installed copy is image-size@1.2.1, pulled
# solely by pptxgenjs@4.0.1 (which pins "image-size": "^1.2.1"). NO fixed version
# exists — the latest published image-size is 2.0.2, still in-range, and 2.x is
# outside pptxgenjs's range — so this cannot be remediated by a bump/override
# (Trivy reports no fixed version). Accepted as a low, non-request-path DoS:
# image-size reads dimensions of images embedded into generated PPTX decks
# (first-party export content), not an endpoint parsing attacker-supplied
# ICNS/JXL/HEIF. Trivy scans package-lock.json separately from npm audit, so
# these must be listed here IN ADDITION to the GHSA ids in ACCEPTED_GHSA_IDS
# (scripts/ci/audit-with-allowlist.mjs).
# TODO(security): drop when image-size ships a fix >2.0.2 and pptxgenjs widens
# its range (or pptxgenjs is replaced). Re-check with `npm ls image-size --all`.
CVE-2025-71329
CVE-2025-71330
