# ICH Ingest Service - Service-specific dependencies  
# Note: Core dependencies (FastAPI, uvicorn, openai, pinecone-client, pydantic, PyPDF2, python-multipart, httpx, pytest, structlog) are in server/services/python/requirements.txt
# Only service-specific dependencies listed here to avoid duplicates
pydantic-settings>=2.0.0
prometheus-client>=0.16.0
prometheus-fastapi-instrumentator>=6.0.0
websockets>=11.0.0
starlette>=0.26.1