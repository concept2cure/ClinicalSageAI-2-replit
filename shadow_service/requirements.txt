# Shadow Interrogation Service - Python Dependencies
# Install: pip install -r requirements.txt

# =============================================================================
# Core Framework
# =============================================================================
fastapi>=0.110
uvicorn[standard]>=0.42
pydantic>=2.6
pydantic-settings>=2.2

# =============================================================================
# Database
# =============================================================================
asyncpg>=0.29

# =============================================================================
# HTTP Client (for embeddings, external APIs)
# =============================================================================
httpx>=0.27

# =============================================================================
# Configuration
# =============================================================================
python-dotenv>=1.0

# =============================================================================
# Document Generation
# =============================================================================
python-docx>=1.2.0
fpdf2>=2.8.0

# =============================================================================
# PDF + Document text extraction (Knowledge Base layer)
# =============================================================================
PyMuPDF>=1.27.0              # Strategy 1: fastest, best layout preservation
PyPDF2>=3.0.0                # Strategy 2: native PDF fallback
pdfminer.six>=20260107       # Strategy 3: layout-aware extraction
pdf2image>=1.17.0            # Strategy 6: pdf→image for OCR via poppler
pytesseract>=0.3.13          # Tesseract OCR bindings (tesseract-ocr must be installed)
opencv-python-headless>=4.8.0  # Image preprocessing for enhanced OCR
Pillow>=10.0.0               # Image I/O for OCR
mammoth>=1.6.0               # DOCX → plain-text extraction
openpyxl>=3.1.0              # XLSX extraction
docx2txt>=0.8                # DOCX fallback extractor
tiktoken>=0.5.0              # Token counting for context trimming
numpy>=1.24.0                # Required by OpenCV + tiktoken

# =============================================================================
# AI / LLM (section generation)
# =============================================================================
openai>=1.0.0
httpx>=0.27                  # Already listed above, repeated for clarity

# =============================================================================
# Testing
# =============================================================================
pytest>=8.0
pytest-asyncio>=0.23

# =============================================================================
# Optional: Production
# =============================================================================
# gunicorn>=21.0
# structlog>=24.0
