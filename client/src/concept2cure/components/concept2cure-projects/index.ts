/**
 * Phase 3 Projects — public barrel.
 * Mounted from Concept2CureHome when the rail's `projects` item is
 * active. The 17-component prototype lives one level down; consumers
 * should import only ProjectsScreen unless they're testing.
 */
export { ProjectsScreen } from './ProjectsScreen';
export type {
  Project, ProjectChat, ProjectFile, ProjectPhase, ProjectStatus,
  SubmissionType, Agency, ProjectsFilters, FilterKey,
} from './types';
