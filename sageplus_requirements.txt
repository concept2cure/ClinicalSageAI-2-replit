# SagePlus Pipeline Requirements

# PDF Processing
pymupdf==1.22.1  # PyMuPDF for PDF extraction

# API & HTTP Requests
requests==2.31.0
python-dotenv==1.0.0

# Database Integration (Optional)
pymongo==4.5.0

# Vector Embedding & Search
# Note: We're using the Hugging Face API instead of local models to reduce dependencies